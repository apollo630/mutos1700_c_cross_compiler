.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_mpxip,2
.globl	_execnt
.data
_execnt:/0
.globl	_exec
.text
.even
_exec:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L1
L2:mov	di,122.+_u
mov	*4.(di),*0.
call	_exece
L3:|RTYP 0
jmp	cret
L1:jmp	L2
.globl	_exece
.text
.even
_exece:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L4
L5:|NREG 2
| _nc=di
|NREG 1
| _cp=si
| _addr=-6.
| _bp=-8.
| _bp1=-10.
| _uap=-12.
| _na=-14.
| _ne=-16.
| _bno=-18.
| _ucp=-20.
| _ap=-22.
| _c=-24.
| _nec=-26.
| _arg2=-28.
| _arg2p=-30.
| _getxret=-32.
| _argseg=-34.
| _ip=-36.
mov	dx,*0.
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	*-36.(bp),ax
or	ax,ax
beq	L6
mov	*-18.(bp),*0.
mov	*-8.(bp),*0.
L7:cmp	_execnt,*2.
blt	L8
mov	dx,*-1.
push	dx
mov	dx,#_execnt
push	dx
call	_sleep
add	sp,*4.
jmp	L7
L8:inc	_execnt
mov	*-10.(bp),*0.
mov	dx,*64.
push	dx
push	*-36.(bp)
call	_access
add	sp,*4.
or	ax,ax
bne	L9
mov	dx,*-36.(bp)
mov	bx,dx
mov	dx,*6.(bx)
and	dx,#-4096.
cmp	dx,#-32768.
bne	L10000
mov	dx,*-36.(bp)
mov	bx,dx
test	*6.(bx),*73.
bne	L10
L10000:movb	111.+_u,*13.
jmp	L9
L10:mov	*-14.(bp),*0.
mov	*-16.(bp),*0.
mov	di,*0.
mov	dx,122.+_u
mov	*-12.(bp),dx
mov	dx,*-12.(bp)
mov	bx,dx
mov	dx,*2.(bx)
or	dx,dx
beq	L11
L12:mov	*-22.(bp),*0.
mov	dx,*-12.(bp)
mov	bx,dx
mov	dx,*2.(bx)
or	dx,dx
beq	L14
mov	dx,*-12.(bp)
mov	bx,dx
mov	dx,*2.(bx)
push	dx
call	_fuword
add	sp,*2.
mov	*-22.(bp),ax
mov	dx,*-12.(bp)
mov	bx,dx
add	*2.(bx),*2.
L14:cmp	*-22.(bp),*0
bne	L15
mov	dx,*-12.(bp)
mov	bx,dx
mov	dx,*4.(bx)
or	dx,dx
beq	L15
cmp	*-16.(bp),*0
bne	L16
mov	*-26.(bp),*0.
L16:mov	dx,*-12.(bp)
mov	bx,dx
mov	*2.(bx),*0.
mov	dx,*-12.(bp)
mov	bx,dx
mov	dx,*4.(bx)
push	dx
call	_fuword
add	sp,*2.
mov	*-22.(bp),ax
or	ax,ax
beq	L13
mov	dx,*-12.(bp)
mov	bx,dx
add	*4.(bx),*2.
inc	*-16.(bp)
L15:cmp	*-22.(bp),*0
beq	L13
inc	*-14.(bp)
cmp	*-22.(bp),*-1.
bne	L17
movb	111.+_u,*14.
L17:L20:cmp	di,#5119.
blt	L21
movb	111.+_u,*7.
L21:push	*-22.(bp)
inc	*-22.(bp)
call	_fubyte
add	sp,*2.
mov	*-24.(bp),ax
or	ax,ax
bge	L22
movb	111.+_u,*14.
L22:cmpb	111.+_u,*0
bne	L9
test	di,#1023.
bne	L23
cmp	di,*0
bne	L24
call	_geteblk
mov	*-10.(bp),ax
mov	*-8.(bp),ax
jmp	L25
L24:push	*-8.(bp)
call	_geteblk
pop	bx
mov	*6.(bx),ax
mov	*-8.(bp),ax
L25:mov	dx,*-8.(bp)
mov	bx,dx
mov	*6.(bx),*0.
mov	dx,*-8.(bp)
mov	bx,dx
mov	si,*14.(bx)
L23:inc	*-26.(bp)
inc	di
mov	dx,*-24.(bp)
movb	(si),dx
inc	si
L18:cmp	*-24.(bp),*0
bgt	L20
L19:jmp	L12
L13:L11:inc	di
and	di,*-2.
push	di
push	*-36.(bp)
call	_getxfil
add	sp,*4.
mov	*-32.(bp),ax
or	ax,ax
bne	L9
cmpb	111.+_u,*0
bne	L9
mov	dx,di
neg	dx
add	dx,*-2.
mov	*-20.(bp),dx
mov	dx,*-14.(bp)
sal	dx,*1
push	dx
mov	dx,*-20.(bp)
pop	bx
sub	dx,bx
add	dx,*-6.
mov	*-22.(bp),dx
push	322.+_u
mov	dx,*-22.(bp)
add	dx,*-14.
pop	bx
mov	*16.(bx),dx
mov	dx,*-14.(bp)
sub	dx,*-16.(bp)
push	dx
push	*-22.(bp)
call	_suword
add	sp,*4.
mov	di,*0.
L26:add	*-22.(bp),*2.
cmp	*-28.(bp),*-1.
beq	L28
inc	*-28.(bp)
L28:mov	dx,*-16.(bp)
cmp	*-14.(bp),dx
bne	L29
mov	dx,*0.
push	dx
push	*-22.(bp)
call	_suword
add	sp,*4.
add	*-22.(bp),*2.
L29:dec	*-14.(bp)
blt	L27
push	*-20.(bp)
push	*-22.(bp)
call	_suword
add	sp,*4.
L32:test	di,#1023.
bne	L33
cmp	di,*0
bne	L34
mov	dx,*-10.(bp)
mov	*-8.(bp),dx
jmp	L35
L34:mov	dx,*-8.(bp)
mov	bx,dx
mov	dx,*6.(bx)
mov	*-8.(bp),dx
L35:mov	dx,*-8.(bp)
mov	bx,dx
mov	si,*14.(bx)
L33:movb	ax,(si)
cbw
inc	si
mov	*-24.(bp),ax
push	ax
push	*-20.(bp)
inc	*-20.(bp)
call	_subyte
add	sp,*4.
inc	di
L30:test	*-24.(bp),#255.
bne	L32
L31:jmp	L26
L27:mov	dx,*0.
push	dx
push	*-22.(bp)
call	_suword
add	sp,*4.
mov	dx,*0.
push	dx
push	*-20.(bp)
call	_suword
add	sp,*4.
call	_setregs
L9:push	*-36.(bp)
call	_iput
add	sp,*2.
L36:mov	dx,*-10.(bp)
mov	*-8.(bp),dx
or	dx,dx
beq	L37
mov	dx,*-8.(bp)
mov	bx,dx
mov	dx,*6.(bx)
mov	*-10.(bp),dx
push	*-8.(bp)
call	_brelse
add	sp,*2.
jmp	L36
L37:mov	dx,_execnt
dec	_execnt
cmp	dx,*2.
blt	L38
mov	dx,#_execnt
push	dx
call	_wakeup
add	sp,*2.
L38:cmp	*-32.(bp),*-1.
bne	L39
mov	dx,*9.
push	dx
call	_exit
add	sp,*2.
L39:|NREG 3
L6:|RTYP 0
jmp	cret
L4:sub	sp,*32.
jmp	L5
.globl	_getxfil
.text
.even
_getxfil:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
|NREG 2
mov	di,*4.(bp)
| _ip=di
| _nargc=6.
jmp	L40
L41:|NREG 1
| _ds=si
| _ts=-6.
| _ss=-8.
| _sep=-10.
| _overlay=-12.
| _msdos=-14.
| _lsize=-18.
| _uhdr=-48.
| _psp=-50.
| _xp=-52.
| _n=-54.
| _b=-56.
mov	128.+_u,#340.+_u
mov	dx,*32.
mov	372.+_u,dx
mov	130.+_u,dx
mov	ax,*0.
cwd
mov	134.+_u,ax
mov	132.+_u,dx
movb	110.+_u,*1.
push	di
call	_readi
add	sp,*2.
movb	110.+_u,*0.
cmpb	111.+_u,*0
bne	L43
cmp	130.+_u,*0
bne	L44
mov	dx,*0.
mov	*-14.(bp),dx
mov	*-12.(bp),dx
mov	*-10.(bp),dx
mov	dx,*30.
push	dx
lea	dx,*-48.(bp)
push	dx
mov	dx,#340.+_u
push	dx
call	_copy
add	sp,*6.
mov	dx,340.+_u
and	dx,*-64.
cmp	dx,#320.
beq	L45
mov	372.+_u,*16.
mov	ax,*-46.(bp)
sub	dx,dx
mov	346.+_u,ax
mov	344.+_u,dx
mov	ax,*-44.(bp)
sub	dx,dx
mov	350.+_u,ax
mov	348.+_u,dx
mov	ax,*-42.(bp)
sub	dx,dx
mov	354.+_u,ax
mov	352.+_u,dx
mov	ax,*-40.(bp)
sub	dx,dx
mov	358.+_u,ax
mov	356.+_u,dx
mov	dx,*-38.(bp)
mov	360.+_u,dx
mov	362.+_u,*0.
mov	ax,*0.
cwd
mov	366.+_u,ax
mov	364.+_u,dx
movb	342.+_u,*0.
mov	dx,346.+_u
mov	400.+_u,dx
cmp	340.+_u,#263.
bne	L46
movb	343.+_u,*32.
jmp	L47
L46:cmp	340.+_u,#265.
beq	L10001
cmp	340.+_u,#261.
bne	L48
L10001:movb	343.+_u,*38.
jmp	L49
L48:jmp	L44
L49:L47:cmp	340.+_u,#261.
beq	L10002
mov	dx,#321.
jmp	L10003
L10002:mov	dx,#322.
L10003:mov	340.+_u,dx
L45:cmp	340.+_u,#321.
beq	L10004
cmp	340.+_u,#322.
bne	L44
L10004:movb	dx,343.+_u
and	dx,*24.
bgt	L44
cmp	340.+_u,#322.
bne	L10005
mov	dx,*1.
jmp	L10006
L10005:mov	dx,*0.
L10006:mov	*-12.(bp),dx
movb	dx,343.+_u
and	dx,*4.
beq	L10007
movb	dx,343.+_u
and	dx,*2.
beq	L44
L10007:movb	dx,343.+_u
and	dx,*4.
mov	*-10.(bp),dx
or	dx,dx
bne	L50
mov	ax,346.+_u
mov	dx,344.+_u
add	350.+_u,ax
adc	348.+_u,dx
mov	ax,350.+_u
sub	dx,dx
push	ax
push	dx
mov	ax,350.+_u
mov	dx,348.+_u
pop	cx
pop	bx
cmp	dx,cx
bne	L10008
cmp	ax,bx
beq	L51
L10008:movb	111.+_u,*12.
jmp	L43
jmp	L52
L51:mov	ax,*0.
cwd
mov	346.+_u,ax
mov	344.+_u,dx
L52:L50:cmp	344.+_u,*0
bne	L10009
cmp	346.+_u,*0.
beq	L53
L10009:movb	dx,(di)
and	dx,*32.
bne	L53
cmpb	*1.(di),*1.
beq	L53
movb	111.+_u,*26.
jmp	L43
L53:mov	ax,#2047.
cwd
push	ax
push	dx
mov	ax,346.+_u
mov	dx,344.+_u
pop	bx
pop cx
add	ax,cx
adc	dx,bx
mov	cx,*11.
sar	dx,*1
rcr	ax,*1
loop	.-4
mov	*-6.(bp),ax
mov	ax,350.+_u
mov	dx,348.+_u
add	ax,354.+_u
adc	dx,352.+_u
mov	*-16.(bp),ax
mov	*-18.(bp),dx
mov	ax,*-16.(bp)
sub	dx,dx
push	ax
push	dx
mov	ax,*-16.(bp)
mov	dx,*-18.(bp)
pop	cx
pop	bx
cmp	dx,cx
bne	L10010
cmp	ax,bx
beq	L54
L10010:movb	111.+_u,*12.
jmp	L43
L54:mov	dx,*-16.(bp)
add	dx,#2047.
mov	cx,*11.
shr	dx,cl
mov	si,dx
cmp	si,*0
bne	L55
mov	ax,*-16.(bp)
mov	dx,*-18.(bp)
cmp	dx,*0
bne	L10011
cmp	ax,*0
beq	L55
L10011:mov	si,*32.
L55:mov	dx,*6.(bp)
add	dx,#3071.
mov	cx,*11.
shr	dx,cl
mov	*-8.(bp),dx
cmp	*-12.(bp),*0
beq	L56
cmp	*-10.(bp),*0
beq	L10012
cmpb	333.+_u,*0
beq	L10012
cmp	*6.(bp),*0
beq	L57
L10012:movb	111.+_u,*12.
jmp	L43
L57:mov	si,246.+_u
mov	dx,248.+_u
mov	*-8.(bp),dx
call	_xfree
push	di
call	_xalloc
add	sp,*2.
mov	dx,322.+_u
mov	bx,dx
mov	dx,*16.(bx)
mov	*-50.(bp),dx
mov	dx,360.+_u
and	dx,*1.
push	dx
mov	dx,*-50.(bp)
add	dx,*8.
push	dx
call	_suword
add	sp,*4.
mov	dx,120.+_u
mov	bx,dx
mov	dx,*26.(bx)
mov	*-52.(bp),dx
mov	dx,*-52.(bp)
mov	bx,dx
mov	dx,*2.(bx)
mov	cx,*7.
sal	dx,cl
add	dx,362.+_u
push	dx
mov	dx,*-50.(bp)
add	dx,*10.
push	dx
call	_suword
add	sp,*4.
jmp	L58
L56:mov	330.+_u,*0.
call	_xfree
mov	dx,si
add	dx,*-8.(bp)
inc	dx
push	dx
call	_expand
add	sp,*2.
push	120.+_u
mov	dx,si
inc	dx
pop	bx
mov	*22.(bx),dx
mov	246.+_u,si
mov	dx,*-8.(bp)
mov	248.+_u,dx
mov	dx,*-10.(bp)
movb	333.+_u,dx
movb	410.+_u,*0.
mov	*-54.(bp),si
mov	dx,120.+_u
mov	bx,dx
mov	dx,*16.(bx)
inc	dx
mov	*-56.(bp),dx
L59:mov	dx,*-54.(bp)
dec	*-54.(bp)
or	dx,dx
beq	L60
push	*-56.(bp)
inc	*-56.(bp)
call	_clearse
add	sp,*2.
jmp	L59
L60:push	di
call	_xalloc
add	sp,*2.
mov	244.+_u,*0.
mov	246.+_u,si
mov	248.+_u,*0.
movb	333.+_u,*0.
mov	128.+_u,*0.
mov	ax,372.+_u
cwd
add	ax,346.+_u
adc	dx,344.+_u
mov	134.+_u,ax
mov	132.+_u,dx
mov	dx,350.+_u
mov	130.+_u,dx
push	di
call	_readi
add	sp,*2.
cmp	*-6.(bp),*0
bne	L61
mov	dx,*0.
push	dx
call	_ucodech
add	sp,*2.
L61:cmpb	111.+_u,*0
bne	L43
mov	dx,120.+_u
mov	bx,dx
movb	dx,*1.(bx)
and	dx,*16.
bne	L62
test	*6.(di),#2048.
beq	L63
cmp	112.+_u,*0
beq	L64
mov	dx,*10.(di)
mov	112.+_u,dx
push	120.+_u
mov	dx,*10.(di)
pop	bx
mov	*8.(bx),dx
L64:L63:test	*6.(di),#1024.
beq	L65
mov	dx,*12.(di)
mov	114.+_u,dx
L65:jmp	L66
L62:mov	dx,*5.
push	dx
push	120.+_u
call	_psignal
add	sp,*4.
L66:L58:mov	dx,*-6.(bp)
mov	244.+_u,dx
mov	246.+_u,si
mov	dx,*-8.(bp)
mov	248.+_u,dx
mov	dx,*-10.(bp)
movb	333.+_u,dx
jmp	L43
L44:movb	111.+_u,*8.
L43:mov	dx,*-12.(bp)
mov	ax,dx
jmp	L42
|NREG 2
L42:|RTYP 0
jmp	cret
L40:sub	sp,*52.
jmp	L41
.globl	_setregs
.text
.even
_setregs:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L67
L68:|NREG 2
| _rp=di
|NREG 1
| _cp=si
| _i=-6.
| _psp=-8.
| _pp=-10.
| _xp=-12.
| _pn=-14.
mov	dx,120.+_u
mov	*-10.(bp),dx
mov	dx,*-10.(bp)
mov	bx,dx
mov	dx,*26.(bx)
mov	*-12.(bp),dx
mov	di,#272.+_u
L70:cmp	di,#306.+_u
bhis	L71
test	(di),*1.
bne	L73
mov	(di),*0.
L73:L72:add	di,*2.
jmp	L70
L71:mov	si,#_regloc
L74:mov	dx,_reglocc
add	dx,#_regloc
cmp	dx,si
blos	L75
movb	ax,(si)
cbw
inc	si
sal	ax,*1
add	ax,322.+_u
mov	bx,ax
mov	(bx),*0.
jmp	L74
L75:mov	dx,322.+_u
mov	bx,dx
mov	dx,*16.(bx)
mov	*-8.(bp),dx
mov	dx,*-10.(bp)
mov	bx,dx
mov	dx,*16.(bx)
inc	dx
mov	cx,*7.
sal	dx,cl
mov	*-14.(bp),dx
push	322.+_u
mov	dx,*-14.(bp)
pop	bx
mov	*10.(bx),dx
push	322.+_u
mov	dx,*-14.(bp)
pop	bx
mov	*14.(bx),dx
push	*-14.(bp)
mov	dx,*-8.(bp)
add	dx,*2.
push	dx
call	_suword
add	sp,*4.
cmpb	333.+_u,*0
beq	L76
mov	dx,*-12.(bp)
mov	bx,dx
mov	dx,*2.(bx)
mov	cx,*7.
sal	dx,cl
mov	*-14.(bp),dx
L76:mov	dx,362.+_u
add	dx,*-14.(bp)
push	dx
mov	dx,*-8.(bp)
add	dx,*10.
push	dx
call	_suword
add	sp,*4.
mov	dx,360.+_u
and	dx,*-2.
push	dx
mov	dx,*-8.(bp)
add	dx,*8.
push	dx
call	_suword
add	sp,*4.
mov	dx,#512.
push	dx
mov	dx,*-8.(bp)
add	dx,*12.
push	dx
call	_suword
add	sp,*4.
mov	12.+_u,*0.
mov	dx,*96.
push	dx
mov	dx,#14.+_u
push	dx
call	_zeromem
add	sp,*4.
mov	*-6.(bp),*0.
L77:cmp	*-6.(bp),*20.
bge	L78
mov	dx,*-6.(bp)
mov	bx,dx
movb	dx,#214.+_u(bx)
and	dx,*1.
beq	L80
mov	dx,*-6.(bp)
sal	dx,*1
mov	bx,dx
mov	dx,#174.+_u(bx)
push	dx
call	_closef
add	sp,*2.
mov	dx,*-6.(bp)
sal	dx,*1
mov	bx,dx
mov	174.+_u(bx),*0.
mov	dx,*-6.(bp)
mov	bx,dx
andb	214.+_u(bx),*-2.
L80:L79:inc	*-6.(bp)
jmp	L77
L78:mov	dx,*14.
push	dx
mov	dx,#374.+_u
push	dx
mov	dx,#140.+_u
push	dx
call	_bcopy
add	sp,*6.
|NREG 3
L69:|RTYP 0
jmp	cret
L67:sub	sp,*10.
jmp	L68
.globl	_rexit
.text
.even
_rexit:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L81
L82:|NREG 2
| _uap=di
mov	di,122.+_u
mov	si,(di)
and	si,#255.
mov	cx,*8.
sal	si,cl
push	si
call	_exit
add	sp,*2.
|NREG 3
L83:|RTYP 0
jmp	cret
L81:jmp	L82
.globl	_exit
.text
.even
_exit:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
| _rv=4.
jmp	L84
L85:|NREG 2
| _i=di
|NREG 1
| _p=si
| _q=-6.
| _f=-8.
mov	si,120.+_u
andb	*1.(si),*-81.
mov	*30.(si),*0.
mov	di,*0.
L87:cmp	di,*17.
bge	L88
mov	dx,di
sal	dx,*1
mov	bx,dx
mov	272.+_u(bx),*1.
L89:inc	di
jmp	L87
L88:mov	di,*0.
L90:cmp	di,*20.
bge	L91
mov	dx,di
sal	dx,*1
mov	bx,dx
mov	dx,#174.+_u(bx)
mov	*-8.(bp),dx
mov	dx,di
sal	dx,*1
mov	bx,dx
mov	174.+_u(bx),*0.
push	*-8.(bp)
call	_closef
add	sp,*2.
L92:inc	di
jmp	L90
L91:push	136.+_u
call	_plock
add	sp,*2.
push	136.+_u
call	_iput
add	sp,*2.
cmp	138.+_u,*0
beq	L93
push	138.+_u
call	_plock
add	sp,*2.
push	138.+_u
call	_iput
add	sp,*2.
L93:call	_xfree
push	*16.(si)
mov	dx,*33.
push	dx
push	*18.(si)
call	_max
add	sp,*4.
push	ax
mov	dx,#_coremap
push	dx
call	_mfree
add	sp,*6.
movb	(si),*5.
mov	dx,*4.(bp)
mov	*16.(si),dx
mov	ax,316.+_u
mov	dx,314.+_u
add	ax,308.+_u
adc	dx,306.+_u
mov	*20.(si),ax
mov	*18.(si),dx
mov	ax,320.+_u
mov	dx,318.+_u
add	ax,312.+_u
adc	dx,310.+_u
mov	*24.(si),ax
mov	*22.(si),dx
mov	di,*0.
mov	*-6.(bp),#_proc
L94:mov	dx,_Nproca
cmp	*-6.(bp),dx
bhis	L95
mov	dx,*-6.(bp)
mov	bx,dx
mov	dx,*14.(bx)
cmp	dx,*12.(si)
bne	L97
mov	dx,*-6.(bp)
mov	bx,dx
mov	*14.(bx),*1.
mov	dx,*-6.(bp)
mov	bx,dx
cmpb	(bx),*5.
bne	L98
inc	di
jmp	L99
L98:mov	dx,*-6.(bp)
mov	bx,dx
cmpb	(bx),*6.
bne	L100
push	*-6.(bp)
call	_setrun
add	sp,*2.
L100:L99:jmp	L101
L97:mov	dx,*-6.(bp)
mov	bx,dx
mov	dx,*12.(bx)
cmp	dx,*14.(si)
bne	L102
push	*-6.(bp)
call	_wakeup
add	sp,*2.
L102:L101:L96:add	*-6.(bp),*32.
jmp	L94
L95:cmp	di,*0
beq	L103
mov	dx,#32.+_proc
push	dx
call	_wakeup
add	sp,*2.
L103:call	_swtch
|NREG 3
L86:|RTYP 0
jmp	cret
L84:sub	sp,*4.
jmp	L85
.globl	_wait
.text
.even
_wait:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L104
L105:|NREG 2
| _f=di
|NREG 1
| _p=si
mov	di,*0.
L107:mov	si,#_proc
L108:cmp	si,_Nproca
bhis	L109
mov	dx,120.+_u
mov	bx,dx
mov	dx,*12.(bx)
cmp	dx,*14.(si)
bne	L111
inc	di
cmpb	(si),*5.
bne	L112
mov	dx,*12.(si)
mov	124.+_u,dx
mov	dx,*16.(si)
mov	126.+_u,dx
mov	ax,*20.(si)
mov	dx,*18.(si)
add	316.+_u,ax
adc	314.+_u,dx
mov	ax,*24.(si)
mov	dx,*22.(si)
add	320.+_u,ax
adc	318.+_u,dx
mov	*12.(si),*0.
mov	*14.(si),*0.
mov	*10.(si),*0.
mov	*6.(si),*0.
movb	*1.(si),*0.
mov	*24.(si),*0.
call	_spl7
mov	di,ax
movb	(si),*0.
mov	dx,_Nproc
mov	cx,*5.
sal	dx,cl
add	dx,#-32.+_proc
cmp	dx,si
bne	L113
L114:cmp	si,#_proc
blos	L115
cmpb	(si),*0
bne	L115
dec	_Nproc
sub	si,*32.
jmp	L114
L115:mov	dx,_Nproc
mov	cx,*5.
sal	dx,cl
add	dx,#_proc
mov	_Nproca,dx
L113:push	di
call	_splx
add	sp,*2.
jmp	L106
L112:cmpb	(si),*6.
bne	L116
movb	dx,*1.(si)
and	dx,*32.
bne	L117
orb	*1.(si),*32.
mov	dx,*12.(si)
mov	124.+_u,dx
push	si
call	_fsig
add	sp,*2.
mov	cx,*8.
sal	ax,cl
or	ax,*127.
mov	126.+_u,ax
jmp	L106
L117:jmp	L110
L116:L111:L110:add	si,*32.
jmp	L108
L109:cmp	di,*0
beq	L118
mov	dx,*30.
push	dx
push	120.+_u
call	_sleep
add	sp,*4.
jmp	L107
L118:movb	111.+_u,*10.
|NREG 3
L106:|RTYP 0
jmp	cret
L104:jmp	L105
.globl	_fork
.text
.even
_fork:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L119
L120:|NREG 2
| _p1=di
|NREG 1
| _p2=si
| _a=-6.
| _psp=-8.
| _pn=-10.
cmp	_Bigswap,*0
beq	L122
mov	dx,#128.
push	dx
mov	dx,#_swapmap
push	dx
call	_malloc
add	sp,*4.
mov	*-6.(bp),ax
or	ax,ax
bne	L123
movb	111.+_u,*12.
jmp	L124
L123:push	*-6.(bp)
mov	dx,#128.
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
L122:mov	*-6.(bp),*0.
mov	si,*0.
mov	di,#_proc
L125:cmp	di,_Nproca
bhi	L126
cmpb	(di),*0
bne	L128
cmp	si,*0
bne	L128
mov	si,di
jmp	L129
L128:mov	dx,112.+_u
cmp	*8.(di),dx
bne	L130
cmpb	(di),*0
beq	L130
inc	*-6.(bp)
L130:L129:L127:add	di,*32.
jmp	L125
L126:cmp	si,*0
beq	L10013
cmp	112.+_u,*0
beq	L131
mov	dx,16.+_v
mov	cx,*5.
sal	dx,cl
add	dx,#-32.+_proc
cmp	dx,si
beq	L10014
mov	dx,26.+_v
cmp	*-6.(bp),dx
ble	L131
L10014:L10013:movb	111.+_u,*11.
jmp	L121
L131:mov	di,120.+_u
call	_newproc
or	ax,ax
beq	L132
mov	dx,*12.(di)
mov	124.+_u,dx
mov	126.+_u,*0.
mov	ax,2.+_time
mov	dx,_time
mov	390.+_u,ax
mov	388.+_u,dx
mov	ax,*0.
cwd
mov	320.+_u,ax
mov	318.+_u,dx
mov	ax,*0.
cwd
mov	312.+_u,ax
mov	310.+_u,dx
mov	ax,*0.
cwd
mov	316.+_u,ax
mov	314.+_u,dx
mov	ax,*0.
cwd
mov	308.+_u,ax
mov	306.+_u,dx
call	_sureg
jmp	L121
L132:mov	dx,*12.(si)
mov	124.+_u,dx
L124:mov	126.+_u,*1.
|NREG 3
L121:|RTYP 0
jmp	cret
L119:sub	sp,*6.
jmp	L120
.data
