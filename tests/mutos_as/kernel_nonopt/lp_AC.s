.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_lpcfg,10
.comm	_lps,24
.comm	_lplbs,16
.comm	_lplbd,16
.globl	_lpinit
.text
.even
_lpinit:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L1
L2:|NREG 2
| _cfg=di
|NREG 1
| _lp=si
| _i=-6.
| _alive=-8.
mov	*-6.(bp),*0.
L4:cmp	*-6.(bp),*1.
bge	L5
mov	ax,*-6.(bp)
mov	cx,*24.
imul	cx
mov	si,ax
add	si,#_lps
mov	ax,*-6.(bp)
mov	cx,*10.
imul	cx
mov	di,ax
add	di,#_lpcfg
mov	dx,#148.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*9.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*15.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*5.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,#170.
push	dx
push	*4.(di)
call	_outb
add	sp,*4.
push	*4.(di)
call	_inb
add	sp,*2.
cmp	ax,#170.
beq	L7
mov	*-8.(bp),*0.
jmp	L8
L7:mov	*-8.(bp),*8.
L8:cmp	_Stand,*0
bne	L9
cmp	*-8.(bp),*0
beq	L10000
mov	dx,#L11
push	dx
jmp	L10001
L10000:mov	dx,#L12
push	dx
L10001:push	(di)
push	*2.(di)
mov	dx,#L10
push	dx
call	_printf
add	sp,*8.
L9:mov	dx,*-8.(bp)
or	*14.(si),dx
mov	dx,(di)
sal	dx,*1
mov	bx,dx
mov	_lplbs(bx),si
mov	dx,(di)
sal	dx,*1
mov	bx,dx
mov	_lplbd(bx),di
L6:inc	*-6.(bp)
jmp	L4
L5:|NREG 3
L3:|RTYP 0
jmp	cret
L1:sub	sp,*4.
jmp	L2
.globl	_lpopen
.text
.even
_lpopen:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
| _dev=4.
jmp	L13
L14:|NREG 2
| _unit=di
|NREG 1
| _lp=si
mov	di,*4.(bp)
and	di,#255.
mov	cx,*3.
sar	di,cl
mov	ax,di
mov	cx,*24.
imul	cx
mov	si,ax
add	si,#_lps
cmp	di,*1.
bge	L10002
test	*14.(si),*8.
bne	L16
L10002:movb	111.+_u,*6.
jmp	L15
L16:mov	ax,di
mov	cx,*10.
imul	cx
add	ax,#_lpcfg
mov	*18.(si),ax
test	*14.(si),*1.
beq	L17
movb	111.+_u,*16.
jmp	L15
L17:or	*14.(si),*1.
call	_geteblk
mov	*22.(si),ax
mov	dx,*4.(bp)
and	dx,#255.
mov	*16.(si),dx
mov	ax,di
mov	cx,*10.
imul	cx
mov	bx,ax
mov	ax,#_lpcfg(bx)
push	ax
call	_lpintr
add	sp,*2.
cmp	*16.(si),*0
bne	L18
mov	dx,*12.
push	dx
push	*4.(bp)
call	_lp_cano
add	sp,*4.
L18:|NREG 3
L15:|RTYP 0
jmp	cret
L13:jmp	L14
.globl	_lpwrite
.text
.even
_lpwrite:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
| _dev=4.
jmp	L19
L20:|NREG 2
| _lp=di
|NREG 1
| _unit=si
| _a=-6.
| _c=-8.
| _i=-10.
mov	si,*4.(bp)
and	si,#255.
mov	cx,*3.
sar	si,cl
mov	ax,si
mov	cx,*24.
imul	cx
mov	di,ax
add	di,#_lps
L22:push	130.+_u
mov	dx,#1024.
push	dx
call	_min
add	sp,*4.
mov	si,ax
or	ax,ax
beq	L21
mov	dx,*22.(di)
mov	bx,dx
mov	dx,*14.(bx)
mov	*-6.(bp),dx
mov	dx,*0.
push	dx
push	si
push	*-6.(bp)
call	_iomove
add	sp,*6.
L23:mov	dx,si
dec	si
or	dx,dx
beq	L22
cmp	*16.(di),*4.
bne	L24
mov	dx,*-6.(bp)
mov	bx,dx
movb	ax,(bx)
cbw
push	ax
inc	*-6.(bp)
push	di
call	_lp_outc
add	sp,*4.
jmp	L23
L24:cmp	*16.(di),*2.
beq	L10003
cmp	*16.(di),*3.
bne	L25
L10003:mov	dx,*-6.(bp)
mov	bx,dx
movb	ax,(bx)
cbw
push	ax
inc	*-6.(bp)
push	*4.(bp)
call	_lp_cano
add	sp,*4.
jmp	L26
L25:mov	dx,*-6.(bp)
mov	bx,dx
movb	dx,(bx)
movb	*-8.(bp),dx
inc	*-6.(bp)
mov	ax,dx
cbw
jmp	L28
L29:mov	dx,*13.
push	dx
push	di
call	_lp_outc
add	sp,*4.
mov	dx,*10.
push	dx
push	di
call	_lp_outc
add	sp,*4.
mov	*8.(di),*0.
jmp	L27
L30:mov	dx,*8.
push	dx
push	di
call	_lp_outc
add	sp,*4.
dec	*8.(di)
jmp	L27
L31:mov	ax,*8.(di)
cwd
mov	cx,*8.
idiv	cx
push	dx
mov	dx,*8.
pop	bx
sub	dx,bx
mov	*-10.(bp),dx
L32:mov	dx,*-10.(bp)
dec	*-10.(bp)
or	dx,dx
beq	L33
mov	dx,*32.
push	dx
push	di
call	_lp_outc
add	sp,*4.
inc	*8.(di)
jmp	L32
L33:jmp	L27
L34:cmp	*16.(di),*0
bne	L35
movb	ax,*-8.(bp)
cbw
push	ax
push	di
call	_lp_outc
add	sp,*4.
inc	*8.(di)
jmp	L36
L35:cmpb	*-8.(bp),*97.
blt	L37
cmpb	*-8.(bp),*122.
bgt	L37
addb	*-8.(bp),*-32.
movb	ax,*-8.(bp)
cbw
push	ax
push	di
call	_lp_outc
add	sp,*4.
inc	*8.(di)
jmp	L38
L37:movb	dx,*-8.(bp)
mov	ax,dx
cbw
jmp	L40
L41:mov	dx,*40.
push	dx
push	di
call	_lp_outc
add	sp,*4.
jmp	L42
L43:mov	dx,*41.
push	dx
push	di
call	_lp_outc
add	sp,*4.
jmp	L42
L44:mov	dx,*39.
push	dx
push	di
call	_lp_outc
add	sp,*4.
jmp	L42
L45:mov	dx,*33.
push	dx
push	di
call	_lp_outc
add	sp,*4.
jmp	L42
L46:mov	dx,*94.
push	dx
push	di
call	_lp_outc
add	sp,*4.
L42:mov	dx,*8.
push	dx
push	di
call	_lp_outc
add	sp,*4.
mov	dx,*45.
push	dx
push	di
call	_lp_outc
add	sp,*4.
inc	*8.(di)
jmp	L39
L47:movb	ax,*-8.(bp)
cbw
push	ax
push	di
call	_lp_outc
add	sp,*4.
inc	*8.(di)
jmp	L39
L40:cmp	ax,*96.
beq	L44
cmp	ax,*123.
beq	L41
cmp	ax,*124.
beq	L45
cmp	ax,*125.
beq	L43
cmp	ax,*126.
beq	L46
br	L47
L39:L38:L36:jmp	L27
L28:sub	ax,#/8
cmp	ax,*2.
bhi	L34
shl	ax,#1
xchg	bx,ax
seg	cs
jmp	@L10006(bx)
L10006:L30
L31
L29
L27:L26:jmp	L23
|NREG 3
L21:|RTYP 0
jmp	cret
L19:sub	sp,*6.
jmp	L20
.globl	_lpclose
.text
.even
_lpclose:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
| _dev=4.
jmp	L48
L49:|NREG 2
| _lp=di
mov	si,*4.(bp)
and	si,#255.
mov	cx,*3.
sar	si,cl
mov	ax,si
mov	cx,*24.
imul	cx
mov	di,ax
add	di,#_lps
cmp	*16.(di),*2.
beq	L10007
cmp	*16.(di),*3.
bne	L51
L10007:mov	si,*12.
push	si
push	*4.(bp)
call	_lp_cano
add	sp,*4.
L51:push	*22.(di)
call	_brelse
add	sp,*2.
and	*14.(di),*-2.
|NREG 3
L50:|RTYP 0
jmp	cret
L48:jmp	L49
.globl	_lp_cano
.text
.even
_lp_cano:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
| _dev=4.
|NREG 2
mov	di,*6.(bp)
| _c=di
jmp	L52
L53:|NREG 1
| _a=si
| _lp=-6.
| _b=-8.
| _x=-10.
| _ch=-12.
mov	dx,*4.(bp)
and	dx,#255.
mov	cx,*3.
sar	dx,cl
mov	ax,dx
mov	cx,*24.
imul	cx
add	ax,#_lps
mov	*-6.(bp),ax
mov	dx,*-6.(bp)
mov	bx,dx
cmp	*16.(bx),*3.
bne	L55
cmp	di,*97.
blt	L56
cmp	di,*122.
bgt	L56
add	di,*-32.
jmp	L57
L56:mov	dx,di
mov	ax,dx
jmp	L59
L60:mov	*-12.(bp),*40.
jmp	L61
L62:mov	*-12.(bp),*41.
jmp	L61
L63:mov	*-12.(bp),*39.
jmp	L61
L64:mov	*-12.(bp),*33.
jmp	L61
L65:mov	*-12.(bp),*94.
L61:push	*-12.(bp)
push	*4.(bp)
call	_lp_cano
add	sp,*4.
mov	dx,*-6.(bp)
mov	bx,dx
dec	*8.(bx)
mov	di,*45.
jmp	L58
L59:cmp	ax,*96.
beq	L63
cmp	ax,*123.
beq	L60
cmp	ax,*124.
beq	L64
cmp	ax,*125.
beq	L62
cmp	ax,*126.
beq	L65
br	L58
L58:L57:L55:mov	dx,*-6.(bp)
mov	bx,dx
mov	si,*8.(bx)
mov	dx,*-6.(bp)
mov	bx,dx
mov	dx,*6.(bx)
mov	*-8.(bp),dx
mov	dx,*-6.(bp)
mov	bx,dx
mov	dx,*10.(bx)
mov	*-10.(bp),dx
cmp	di,*32.
beq	L66
mov	dx,di
mov	ax,dx
jmp	L68
L69:add	si,*8.
and	si,*-8.
jmp	L67
L70:mov	dx,*-6.(bp)
mov	bx,dx
mov	dx,*10.(bx)
or	dx,dx
bne	L10009
cmp	*-8.(bp),*0
beq	L67
L10009:L71:cmp	di,*12.
bne	L72
mov	dx,*13.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
mov	dx,*-6.(bp)
mov	bx,dx
test	*16.(bx),*1.
bne	L73
mov	dx,*72.
sub	dx,*-10.(bp)
dec	dx
mov	*-10.(bp),dx
mov	di,*10.
L74:mov	dx,*-10.(bp)
dec	*-10.(bp)
or	dx,dx
ble	L75
push	di
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
jmp	L74
L75:jmp	L76
L73:mov	dx,*10.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
L76:mov	dx,*-6.(bp)
mov	bx,dx
mov	*10.(bx),*0.
jmp	L77
L72:cmp	si,*0
ble	L78
mov	dx,*13.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
L78:mov	dx,*-6.(bp)
mov	bx,dx
inc	*10.(bx)
L77:mov	*-8.(bp),*0.
push	di
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
L79:mov	si,*0.
jmp	L67
L80:cmp	si,*0
ble	L67
dec	si
jmp	L67
L81:cmp	si,*-8.(bp)
bge	L82
mov	dx,*13.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
mov	*-8.(bp),*0.
L82:cmp	si,#132.
bge	L83
L84:cmp	si,*-8.(bp)
ble	L85
mov	dx,*32.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
inc	*-8.(bp)
jmp	L84
L85:push	di
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
inc	*-8.(bp)
L83:inc	si
jmp	L67
L68:sub	ax,#/8
cmp	ax,*5.
bhi	L81
shl	ax,#1
xchg	bx,ax
seg	cs
jmp	@L10011(bx)
L10011:L80
L69
L71
L81
L70
L79
L67:jmp	L86
L66:inc	si
L86:cmp	si,#132.
ble	L87
mov	si,#132.
L87:mov	dx,*-6.(bp)
mov	bx,dx
cmp	*10.(bx),*71.
ble	L88
mov	dx,*-6.(bp)
mov	bx,dx
mov	*10.(bx),*0.
L88:mov	dx,*-6.(bp)
mov	bx,dx
mov	*8.(bx),si
push	*-6.(bp)
mov	dx,*-8.(bp)
pop	bx
mov	*6.(bx),dx
|NREG 2
L54:|RTYP 0
jmp	cret
L52:sub	sp,*8.
jmp	L53
.globl	_lp_outc
.text
.even
_lp_outc:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
|NREG 2
mov	di,*4.(bp)
| _lp=di
| _c=6.
jmp	L89
L90:| _s=-6.
call	_spl5
mov	*-6.(bp),ax
L92:cmp	(di),*75.
blt	L93
or	*14.(di),*2.
mov	si,*26.
push	si
push	di
call	_sleep
add	sp,*4.
jmp	L92
L93:cmp	(di),*0
bgt	L94
test	*14.(di),*4.
bne	L94
push	*6.(bp)
push	di
push	*18.(di)
call	_lpoutpu
add	sp,*6.
jmp	L95
L94:L96:push	di
push	*6.(bp)
call	_putc
add	sp,*4.
or	ax,ax
beq	L97
mov	si,*26.
push	si
mov	si,#_lbolt
push	si
call	_sleep
add	sp,*4.
jmp	L96
L97:L95:push	*-6.(bp)
call	_splx
add	sp,*2.
jmp	L91
L91:|RTYP 0
jmp	cret
L89:sub	sp,*2.
jmp	L90
.globl	_lpintr
.text
.even
_lpintr:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
| _level=4.
jmp	L98
L99:|NREG 2
| _lp=di
|NREG 1
| _cfg=si
| _a=-6.
mov	dx,*4.(bp)
sal	dx,*1
mov	bx,dx
mov	di,_lplbs(bx)
mov	dx,*4.(bp)
sal	dx,*1
mov	bx,dx
mov	si,_lplbd(bx)
push	*2.(si)
call	_inb
add	sp,*2.
mov	*-6.(bp),ax
test	*-6.(bp),*1.
bne	L101
and	*14.(di),*-5.
jmp	L102
L101:or	*14.(di),*4.
L102:L103:test	*14.(di),*4.
bne	L104
cmp	(di),*0
ble	L105
push	di
call	_getc
add	sp,*2.
push	ax
push	di
push	si
call	_lpoutpu
add	sp,*6.
jmp	L106
L105:jmp	L104
L106:jmp	L103
L104:test	*14.(di),*2.
beq	L100
cmp	(di),*50.
bge	L100
and	*14.(di),*-3.
push	di
call	_wakeup
add	sp,*2.
|NREG 3
L100:|RTYP 0
jmp	cret
L98:sub	sp,*2.
jmp	L99
.globl	_lpoutpu
.text
.even
_lpoutpu:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
|NREG 2
mov	di,*4.(bp)
| _cfg=di
|NREG 1
mov	si,*6.(bp)
| _lp=si
| _c=8.
jmp	L107
L108:or	*14.(si),*4.
push	*8.(bp)
push	*4.(di)
call	_outb
add	sp,*4.
mov	dx,*7.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*6.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
push	*2.(di)
call	_inb
add	sp,*2.
and	ax,*9.
bne	L110
and	*14.(si),*-5.
L110:L109:|RTYP 0
jmp	cret
L107:jmp	L108
.data
L10:.byte	/4c,/70,/20,/20,/20,/20,/20,/20,/20
.byte	/42,/61,/73,/65,/64
.byte	/20,/25,/78,/20,/6c,/65,/76,/65,/6c
.byte	/20,/25,/64,/20,/25,/73
.byte	/2e,/a,/0
L11:.byte	/66,/6f,/75,/6e,/64,/0
L12:.byte	/4e,/4f,/54,/20,/66,/6f,/75,/6e,/64
.byte	/0
