.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_panicst,2
.globl	_printf
.text
.even
_printf:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_fmt=di
|	_x1=6.
sub	sp,*4.
|NREG	1
|	_c=si
|	_adx=-6.
|	_s=-8.
lea	dx,*6.(bp)
mov	*-6.(bp),dx
j	L5
L20001:cmp	si,*0
beq	L3
push	si
call	_putch
add	sp,*2.
L5:movb	ax,(di)
cbw
inc	di
mov	si,ax
cmp	si,*37.
jne	L20001
movb	ax,(di)
cbw
inc	di
mov	si,ax
cmp	si,*120.
jne	L7
mov	dx,*4.
push	dx
mov	bx,*-6.(bp)
push	(bx)
j	L20003
L20005:mov	dx,*2.
push	dx
mov	bx,*-6.(bp)
push	(bx)
L20003:call	_printx
add	sp,*4.
br	L8
L7:cmp	si,*98.
je	L20005
cmp	si,*100.
je	L10000
cmp	si,*117.
je	L10000
cmp	si,*111.
jne	L11
L10000:cmp	si,*111.
jne	L10001
mov	dx,*8.
push	dx
j	L10002
L10001:mov	dx,*10.
push	dx
L10002:mov	bx,*-6.(bp)
mov	ax,(bx)
sub	dx,dx
push	ax
push	dx
call	_printn
add	sp,*6.
j	L8
L11:cmp	si,*115.
jne	L13
mov	bx,*-6.(bp)
mov	dx,(bx)
mov	*-8.(bp),dx
L14:mov	bx,*-8.(bp)
movb	ax,(bx)
cbw
inc	*-8.(bp)
mov	si,ax
test	ax,ax
je	L8
push	si
call	_putch
add	sp,*2.
j	L14
L13:cmp	si,*68.
jne	L8
mov	dx,*10.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
call	_printn
add	sp,*6.
add	*-6.(bp),*2.
L8:add	*-6.(bp),*2.
br	L5
L3:|RTYP	0
br	cret
.globl	_chr
.data
_chr:L18
.globl	_printn
.text
.even
_printn:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_n=4.
|	_b=8.
sub	sp,*4.
|	_a=-8.
cmp	*4.(bp),*0
jg	L22
jl	L10003
cmp	*6.(bp),*0.
jae	L22
L10003:mov	di,*45.
push	di
call	_putch
add	sp,*2.
mov	si,*6.(bp)
mov	di,*4.(bp)
neg	di
neg	si
sbb	di,*0
mov	*6.(bp),si
mov	*4.(bp),di
L22:mov	ax,*8.(bp)
cwd
push	ax
push	dx
push	*6.(bp)
push	*4.(bp)
call	ldiv
add	sp,*8.
mov	*-6.(bp),ax
mov	*-8.(bp),dx
test	dx,dx
jne	L10004
test	ax,ax
je	L23
L10004:push	*8.(bp)
push	*-6.(bp)
push	*-8.(bp)
call	_printn
add	sp,*6.
L23:mov	ax,*8.(bp)
cwd
push	ax
push	dx
push	*6.(bp)
push	*4.(bp)
call	lrem
add	sp,*8.
add	ax,_chr
mov	di,ax
movb	ax,(di)
cbw
push	ax
call	_putch
add	sp,*2.
|RTYP	0
br	cret
.globl	_printx
.even
_printx:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_n=di
|NREG	1
mov	si,*6.(bp)
|	_b=si
cmp	si,*4.
jl	L27
mov	bx,di
mov	cx,*12.
shr	bx,cl
and	bx,*15.
add	bx,_chr
movb	ax,(bx)
cbw
push	ax
call	_putch
add	sp,*2.
L27:cmp	si,*3.
jl	L28
mov	bx,di
mov	cx,*8.
shr	bx,cl
and	bx,*15.
add	bx,_chr
movb	ax,(bx)
cbw
push	ax
call	_putch
add	sp,*2.
L28:cmp	si,*2.
jl	L29
mov	bx,di
mov	cx,*4.
shr	bx,cl
and	bx,*15.
add	bx,_chr
movb	ax,(bx)
cbw
push	ax
call	_putch
add	sp,*2.
L29:cmp	si,*1.
jl	L26
mov	bx,di
and	bx,*15.
add	bx,_chr
movb	ax,(bx)
cbw
push	ax
call	_putch
add	sp,*2.
L26:|RTYP	0
br	cret
.bss
_panicp:.blkb	2.
.globl	_panic
.text
.even
_panic:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_s=4.
mov	di,*4.(bp)
mov	_panicst,di
push	*4.(bp)
mov	di,#L34
push	di
call	_printf
add	sp,*4.
call	_update
sub	di,di
push	di
call	_haltcpu
add	sp,*2.
L35:call	_idle
j	L35
.globl	_prdev
.even
_prdev:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_str=4.
|	_dev=6.
mov	di,*6.(bp)
and	di,#255.
push	di
mov	di,*6.(bp)
mov	cx,*8.
sar	di,cl
push	di
push	*4.(bp)
mov	di,#L40
push	di
call	_printf
add	sp,*8.
|RTYP	0
br	cret
.globl	_deverro
.even
_deverro:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
|	_o1=6.
|	_o2=8.
cmp	_Stand,*0
jne	L43
push	*10.(di)
mov	si,#L45
push	si
call	_prdev
add	sp,*4.
push	*8.(bp)
push	*6.(bp)
push	*18.(di)
push	*16.(di)
mov	si,#L46
push	si
call	_printf
add	sp,*10.
L43:|RTYP	0
br	cret
.data
L18:.byte	/30,/31,/32,/33,/34,/35,/36,/37,/38
.byte	/39,/41,/42,/43,/44
.byte	/45,/46,/0
L34:.byte	/70,/61,/6e,/69,/63,/3a,/20,/25,/73
.byte	/a,/0
L40:.byte	/25,/73,/20,/6f,/6e,/20,/64,/65,/76
.byte	/20,/25,/75,/2f,/25
.byte	/75,/a,/0
L45:.byte	/65,/72,/72,/0
L46:.byte	/62,/6e,/3d,/25,/44,/20,/63,/6d,/64
.byte	/3d,/25,/78,/20,/73
.byte	/74,/73,/3d,/25,/78,/a,/0
