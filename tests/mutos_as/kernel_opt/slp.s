.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_io_info,222
.comm	_slpque,128
.globl	_sleep
.text
.even
_sleep:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_chan=4.
|	_pri=6.
sub	sp,*2.
|NREG	2
|	_rp=di
|NREG	1
|	_s=si
|	_h=-6.
mov	di,120.+_u
call	_spl6
mov	si,ax
cmp	*4.(bp),*0
jne	L4
mov	dx,#L5
push	dx
call	_panic
add	sp,*2.
L4:movb	(di),*1.
mov	dx,*4.(bp)
mov	*24.(di),dx
cmp	*4.(bp),*0
jne	L6
mov	dx,#L7
push	dx
call	_panic
add	sp,*2.
L6:mov	dx,*6.(bp)
movb	*2.(di),dx
mov	bx,*4.(bp)
mov	cx,*5.
sar	bx,cl
and	bx,*63.
mov	*-6.(bp),bx
sal	bx,*1
mov	dx,#_slpque(bx)
mov	*28.(di),dx
mov	bx,*-6.(bp)
sal	bx,*1
mov	_slpque(bx),di
cmp	*6.(bp),*25.
jle	L8
call	_issig
test	ax,ax
je	L9
mov	*24.(di),*0.
movb	(di),*3.
mov	bx,*-6.(bp)
sal	bx,*1
mov	dx,*28.(di)
mov	_slpque(bx),dx
call	_spl0
L10:mov	dx,#252.+_u
push	dx
mov	bx,120.+_u
push	*16.(bx)
call	_resume
add	sp,*4.
|NREG	3
L3:|RTYP	0
br	cret
L9:call	_spl0
cmpb	_runin,*0
je	L11
movb	_runin,*0.
mov	dx,#_runin
push	dx
call	_wakeup
add	sp,*2.
L11:call	_swtch
call	_issig
test	ax,ax
je	L12
j	L10
L8:call	_spl0
call	_swtch
L12:push	si
call	_splx
add	sp,*2.
j	L3
.globl	_wakeup
.even
_wakeup:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_chan=di
sub	sp,*8.
|NREG	1
|	_p=si
|	_q=-6.
|	_i=-8.
|	_s=-10.
call	_spl6
mov	*-10.(bp),ax
mov	bx,di
mov	cx,*5.
sar	bx,cl
and	bx,*63.
mov	*-8.(bp),bx
sal	bx,*1
mov	si,_slpque(bx)
mov	*-6.(bp),*0.
j	L16
L20002:cmpb	(si),*5.
je	L18
|	_sp=-12.
cmp	*-6.(bp),*0
jne	L19
mov	bx,*-8.(bp)
sal	bx,*1
mov	dx,*28.(si)
mov	_slpque(bx),dx
j	L20000
L19:mov	bx,*-6.(bp)
mov	dx,*28.(si)
mov	*28.(bx),dx
L20000:mov	*-12.(bp),dx
mov	*24.(si),*0.
push	si
call	_setrun
add	sp,*2.
mov	si,*-12.(bp)
j	L16
L20004:cmp	*24.(si),di
je	L20002
L18:mov	*-6.(bp),si
mov	si,*28.(si)
L16:cmp	si,*0
jne	L20004
push	*-10.(bp)
call	_splx
add	sp,*2.
|NREG	2
|RTYP	0
br	cret
.globl	_setrq
.even
_setrq:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_p=4.
|NREG	2
|	_q=di
|NREG	1
|	_s=si
call	_spl6
mov	si,ax
mov	di,_runq
j	L24
L20006:cmp	di,*4.(bp)
jne	L26
mov	dx,#L28
push	dx
call	_printf
add	sp,*2.
j	L29
L26:mov	di,*28.(di)
L24:cmp	di,*0
jne	L20006
mov	bx,*4.(bp)
mov	dx,_runq
mov	*28.(bx),dx
mov	dx,*4.(bp)
mov	_runq,dx
L29:push	si
call	_splx
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_setrun
.even
_setrun:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_p=di
|NREG	1
|	_w=si
cmpb	(di),*0
je	L10000
cmpb	(di),*5.
jne	L33
L10000:mov	dx,#L34
push	dx
call	_panic
add	sp,*2.
L33:mov	dx,*24.(di)
mov	si,dx
test	dx,dx
je	L35
push	si
call	_wakeup
add	sp,*2.
j	L32
L35:movb	(di),*3.
push	di
call	_setrq
add	sp,*2.
movb	dx,_curpri
cmpb	*2.(di),dx
jge	L36
incb	_runrun
L36:cmpb	_runout,*0
je	L37
movb	dx,*1.(di)
and	dx,*1.
jne	L37
movb	_runout,*0.
mov	dx,#_runout
push	dx
call	_wakeup
add	sp,*2.
L37:|NREG	2
L32:|RTYP	0
br	cret
.globl	_setpri
.even
_setpri:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_pp=di
|NREG	1
|	_p=si
movb	ax,*4.(di)
subb	ah,ah
cwd
mov	cx,*5.
idiv	cx
mov	si,ax
movb	ax,*5.(di)
cbw
add	ax,*30.
add	si,ax
cmp	si,*127.
jle	L41
mov	si,*127.
L41:movb	ax,_curpri
cbw
cmp	ax,si
jle	L42
incb	_runrun
L42:mov	ax,si
movb	*2.(di),ax
mov	ax,si
|RTYP	0
br	cret
.globl	_sched
.even
_sched:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*6.
|NREG	2
|	_rp=di
|NREG	1
|	_p=si
|	_outage=-6.
|	_inage=-8.
|	_maxsize=-10.
L46:call	_spl6
mov	*-6.(bp),#-20000.
movb	_runtxt,*0.
mov	di,#_proc
j	L47
L20008:cmpb	(di),*3.
jne	L49
movb	dx,*1.(di)
and	dx,*1.
jne	L49
movb	ax,*5.(di)
cbw
add	ax,*-20.
mov	cx,*3.
sal	ax,cl
mov	bx,ax
movb	ax,*3.(di)
cbw
sub	ax,bx
cmp	ax,*-6.(bp)
jle	L49
cmp	*26.(di),*0
je	L51
mov	bx,*26.(di)
movb	dx,*10.(bx)
and	dx,*8.
je	L51
movb	_runtxt,*1.
j	L49
L51:mov	si,di
movb	ax,*5.(di)
cbw
add	ax,*-20.
mov	cx,*3.
sal	ax,cl
mov	bx,ax
movb	ax,*3.(di)
cbw
sub	ax,bx
mov	*-6.(bp),ax
L49:add	di,*32.
L47:cmp	di,_Nproca
jb	L20008
cmp	*-6.(bp),#-20000.
je	L10001
cmp	_Hogproc,*0
je	L53
L10001:incb	_runout
sub	dx,dx
push	dx
mov	dx,#_runout
push	dx
L20012:call	_sleep
add	sp,*4.
br	L46
L53:call	_spl0
push	si
call	_swapin
add	sp,*2.
test	ax,ax
bne	L46
call	_spl6
sub	si,si
mov	*-10.(bp),*-1.
mov	*-8.(bp),*-1.
mov	di,#_proc
br	L54
L20010:cmpb	(di),*5.
beq	L56
movb	dx,*1.(di)
and	dx,*71.
cmp	dx,*1.
jne	L56
cmp	*26.(di),*0
je	L10002
mov	bx,*26.(di)
movb	dx,*10.(bx)
and	dx,*8.
jne	L56
L10002:cmpb	(di),*1.
jne	L10004
cmpb	*2.(di),*25.
jge	L10003
L10004:cmpb	(di),*6.
jne	L57
L10003:mov	dx,*18.(di)
cmp	*-10.(bp),dx
jge	L56
mov	si,di
mov	dx,*18.(di)
mov	*-10.(bp),dx
j	L56
L57:cmp	*-10.(bp),*0
jge	L56
cmpb	(di),*3.
je	L10005
cmpb	(di),*1.
jne	L56
L10005:movb	ax,*5.(di)
cbw
mov	bx,ax
movb	ax,*3.(di)
cbw
add	ax,bx
add	ax,*-20.
cmp	ax,*-8.(bp)
jle	L56
mov	si,di
movb	ax,*5.(di)
cbw
mov	bx,ax
movb	ax,*3.(di)
cbw
add	ax,bx
add	ax,*-20.
mov	*-8.(bp),ax
L56:add	di,*32.
L54:cmp	di,_Nproca
blo	L20010
call	_spl0
cmp	*-10.(bp),*0
jge	L10006
cmp	*-6.(bp),*3.
jl	L62
cmp	*-8.(bp),*2.
jl	L62
L10006:andb	*1.(si),*-2.
sub	dx,dx
push	dx
mov	dx,*1.
push	dx
push	si
call	_xswap
add	sp,*6.
br	L46
L62:call	_spl6
incb	_runin
sub	dx,dx
push	dx
mov	dx,#_runin
push	dx
br	L20012
.globl	_qswtch
.even
_qswtch:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
push	120.+_u
call	_setrq
add	sp,*2.
call	_swtch
|RTYP	0
br	cret
.globl	_swtch
.even
_swtch:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*8.
|NREG	2
|	_n=di
|NREG	1
|	_p=si
|	_q=-6.
|	_pp=-8.
|	_pq=-10.
|	_z=-12.
cmp	120.+_u,#_proc
je	L69
mov	dx,#_u
push	dx
call	_save
add	sp,*2.
test	ax,ax
je	L70
mov	bx,120.+_u
movb	dx,*1.(bx)
and	dx,#128.
beq	L68
call	_sureg
mov	bx,120.+_u
andb	*1.(bx),#-129.
br	L68
L70:cmp	12.+_u,*0
jne	L72
mov	dx,#14.+_u
push	dx
call	_savfp
add	sp,*2.
mov	12.+_u,*1.
L72:mov	dx,#252.+_u
push	dx
push	16.+_proc
call	_resume
add	sp,*4.
L69:mov	dx,#252.+_u
push	dx
call	_save
add	sp,*2.
test	ax,ax
jne	L73
mov	dx,#_u
push	dx
call	_save
add	sp,*2.
test	ax,ax
bne	L68
L73:call	_spl6
movb	_runrun,*0.
mov	*-8.(bp),*0.
mov	*-6.(bp),*0.
mov	di,#128.
mov	si,_runq
j	L74
L20020:cmpb	(si),*3.
jne	L77
movb	dx,*1.(si)
and	dx,*1.
je	L77
cmp	_Hogproc,*0
jne	L78
movb	ax,*2.(si)
cbw
cmp	ax,di
jl	L20018
j	L77
L78:cmp	si,_Hogproc
jne	L77
L20018:mov	*-8.(bp),si
mov	dx,*-6.(bp)
mov	*-10.(bp),dx
movb	ax,*2.(si)
cbw
mov	di,ax
L77:mov	*-6.(bp),si
mov	si,*28.(si)
L74:cmp	si,*0
jne	L20020
mov	si,*-8.(bp)
cmp	si,*0
jne	L82
call	_idle
j	L73
L82:mov	dx,*-10.(bp)
mov	*-6.(bp),dx
cmp	dx,*0
jne	L83
mov	dx,*28.(si)
mov	_runq,dx
j	L84
L83:mov	bx,*-6.(bp)
mov	dx,*28.(si)
mov	*28.(bx),dx
L84:mov	ax,di
movb	_curpri,ax
call	_spl0
movb	dx,*1.(si)
mov	di,dx
and	di,*8.
andb	*1.(si),*-9.
cmp	di,*0
je	L10008
mov	dx,#262.+_u
push	dx
j	L10009
L10008:mov	dx,#_u
push	dx
L10009:push	*16.(si)
call	_resume
add	sp,*4.
|NREG	3
L68:|RTYP	0
br	cret
.globl	_newproc
.even
_newproc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*16.
|	_p=-6.
|	_up=-8.
|NREG	2
|	_rpp=di
|NREG	1
|	_rip=si
|	_n=-10.
|	_x=-12.
|	_a1=-14.
|	_a2=-16.
|	_a3=-18.
|	_a4=-20.
mov	*-6.(bp),*0.
j	L88
L20022:mov	_mpid,*0.
L88:inc	_mpid
cmp	_mpid,#30000.
jge	L20022
mov	di,#_proc
j	L90
L20024:cmpb	(di),*0
jne	L93
cmp	*-6.(bp),*0
jne	L93
mov	*-6.(bp),di
L93:mov	dx,_mpid
cmp	*12.(di),dx
je	L88
cmp	*10.(di),dx
je	L88
add	di,*32.
L90:cmp	di,_Nproca
jbe	L20024
mov	dx,_Nproca
cmp	*-6.(bp),dx
jne	L94
mov	dx,16.+_v
cmp	_Nproc,dx
jge	L95
inc	_Nproc
L95:mov	dx,_Nproc
mov	cx,*5.
sal	dx,cl
add	dx,#_proc
mov	_Nproca,dx
L94:mov	dx,*-6.(bp)
mov	di,dx
test	dx,dx
jne	L96
mov	dx,#L97
push	dx
call	_panic
add	sp,*2.
L96:mov	si,120.+_u
mov	*-8.(bp),si
movb	(di),*3.
mov	*30.(di),*0.
movb	*1.(di),*1.
mov	dx,*8.(si)
mov	*8.(di),dx
mov	dx,*10.(si)
mov	*10.(di),dx
movb	dx,*5.(si)
movb	*5.(di),dx
mov	dx,*26.(si)
mov	*26.(di),dx
mov	dx,_mpid
mov	*12.(di),dx
mov	dx,*12.(si)
mov	*14.(di),dx
movb	*3.(di),*0.
movb	*4.(di),*0.
movb	*20.(di),*0.
movb	*21.(di),*0.
mov	*-10.(bp),*0.
L20026:mov	bx,*-10.(bp)
sal	bx,*1
mov	dx,#174.+_u(bx)
test	dx,dx
je	L100
mov	bx,*-10.(bp)
sal	bx,*1
mov	bx,#174.+_u(bx)
inc	*2.(bx)
L100:inc	*-10.(bp)
cmp	*-10.(bp),*20.
jl	L20026
mov	bx,*-8.(bp)
mov	dx,*26.(bx)
test	dx,dx
je	L102
mov	bx,*26.(bx)
incb	*8.(bx)
mov	bx,*-8.(bp)
mov	bx,*26.(bx)
incb	*9.(bx)
L102:mov	bx,136.+_u
incb	*1.(bx)
cmp	138.+_u,*0
je	L103
mov	bx,138.+_u
incb	*1.(bx)
L103:mov	di,*-6.(bp)
mov	120.+_u,di
mov	si,*-8.(bp)
mov	dx,*18.(si)
mov	*18.(di),dx
mov	dx,*22.(si)
mov	*-10.(bp),dx
mov	*22.(di),dx
mov	dx,*16.(si)
mov	*-14.(bp),dx
mov	dx,#262.+_u
push	dx
call	_save
add	sp,*2.
test	ax,ax
je	L104
mov	dx,*1.
br	L20031
L104:cmp	si,#_proc
jne	L105
push	*16.(si)
mov	dx,*31.
push	dx
call	_copyseg
add	sp,*4.
L105:mov	dx,*33.
push	dx
push	*18.(di)
call	_max
add	sp,*4.
push	ax
mov	dx,#_coremap
push	dx
call	_malloc
add	sp,*4.
mov	*-16.(bp),ax
cmp	ax,*0
jne	L106
movb	(si),*4.
orb	*1.(si),*4.
mov	dx,*16.(si)
mov	*16.(di),dx
push	*16.(si)
mov	dx,*31.
push	dx
call	_copyseg
add	sp,*4.
sub	dx,dx
push	dx
push	dx
push	di
call	_xswap
add	sp,*6.
movb	(si),*3.
andb	*1.(si),*-5.
br	L110
L106:mov	dx,*-16.(bp)
mov	*16.(di),dx
push	*-16.(bp)
inc	*-16.(bp)
mov	dx,*31.
push	dx
call	_copyseg
add	sp,*4.
dec	*-10.(bp)
inc	*-14.(bp)
mov	dx,*-14.(bp)
mov	*-18.(bp),dx
mov	dx,*-16.(bp)
mov	*-20.(bp),dx
j	L108
L20028:push	*-16.(bp)
inc	*-16.(bp)
push	*-14.(bp)
inc	*-14.(bp)
call	_copyseg
add	sp,*4.
L108:mov	dx,*-10.(bp)
dec	*-10.(bp)
test	dx,dx
jne	L20028
cmp	si,#_proc
je	L110
mov	bx,322.+_u
mov	dx,*16.(bx)
mov	cx,*11.
shr	dx,cl
mov	*-10.(bp),dx
add	*-18.(bp),dx
add	*-20.(bp),dx
mov	dx,*32.
sub	dx,*-10.(bp)
mov	*-10.(bp),dx
j	L111
L20030:push	*-20.(bp)
inc	*-20.(bp)
push	*-18.(bp)
inc	*-18.(bp)
call	_copyseg
add	sp,*4.
L111:mov	dx,*-10.(bp)
dec	*-10.(bp)
test	dx,dx
jne	L20030
L110:mov	120.+_u,si
orb	*1.(di),*8.
push	di
call	_setrq
add	sp,*2.
sub	dx,dx
L20031:mov	ax,dx
|RTYP	0
br	cret
.data
L5:.byte	/7a,/65,/72,/6f,/20,/77,/63,/68,/61
.byte	/6e,/0
L7:.byte	/53,/6c,/65,/65,/70,/69,/6e,/67,/20
.byte	/6f,/6e,/20,/77,/63
.byte	/68,/61,/6e,/20,/30,/0
L28:.byte	/70,/72,/6f,/63,/20,/6f,/6e,/20,/71
.byte	/a,/0
L34:.byte	/52,/75,/6e,/6e,/69,/6e,/67,/20,/61
.byte	/20,/64,/65,/61,/64
.byte	/20,/70,/72,/6f,/63,/0
L97:.byte	/6e,/6f,/20,/70,/72,/6f,/63,/73,/0
