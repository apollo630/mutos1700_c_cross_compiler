.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.globl	_read
.text
.even
_read:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,*1.
push	di
call	_rdwr
add	sp,*2.
|RTYP	0
br	cret
.globl	_write
.even
_write:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,*2.
push	di
call	_rdwr
add	sp,*2.
|RTYP	0
br	cret
.globl	_rdwr
.even
_rdwr:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_mode=di
sub	sp,*6.
|NREG	1
|	_fp=si
|	_ip=-6.
|	_uap=-8.
|	_p=-10.
mov	bx,122.+_u
mov	*-8.(bp),bx
push	(bx)
call	_getf
add	sp,*2.
mov	si,ax
cmp	si,*0
beq	L9
movb	ax,(si)
cbw
and	ax,di
jne	L10
movb	111.+_u,*9.
br	L9
L10:mov	bx,*-8.(bp)
mov	dx,*2.(bx)
mov	128.+_u,dx
mov	dx,*4.(bx)
mov	130.+_u,dx
movb	110.+_u,*0.
movb	dx,(si)
and	dx,*4.
je	L11
cmp	di,*1.
jne	L12
push	si
call	_readp
L20003:add	sp,*2.
br	L14
L12:push	si
call	_writep
j	L20003
L11:mov	dx,*4.(si)
mov	*-6.(bp),dx
movb	dx,(si)
and	dx,*24.
je	L15
sub	ax,ax
cwd
j	L20001
L15:mov	ax,*8.(si)
mov	dx,*6.(si)
L20001:mov	134.+_u,ax
mov	132.+_u,dx
mov	bx,*-6.(bp)
test	*6.(bx),#8192.
jne	L17
test	di,*1.
je	L10000
mov	dx,*1.
j	L10001
L10000:sub	dx,dx
L10001:mov	*-10.(bp),dx
mov	bx,*-6.(bp)
mov	dx,*18.(bx)
test	dx,dx
je	L18
mov	cx,130.+_u
sub	bx,bx
mov	ax,134.+_u
mov	dx,132.+_u
add	ax,cx
adc	dx,bx
push	ax
push	dx
push	134.+_u
push	132.+_u
push	*-6.(bp)
push	*-10.(bp)
call	_locked
add	sp,*12.
test	ax,ax
jne	L9
L18:push	*-6.(bp)
call	_plock
add	sp,*2.
L17:cmp	di,*1.
jne	L19
push	*-6.(bp)
call	_readi
j	L20002
L19:push	*-6.(bp)
call	_writei
L20002:add	sp,*2.
mov	dx,*-6.(bp)
mov	bx,dx
test	*6.(bx),#8192.
jne	L21
push	dx
call	_prele
add	sp,*2.
L21:movb	dx,(si)
and	dx,*24.
jne	L14
mov	bx,*-8.(bp)
mov	ax,*4.(bx)
sub	ax,130.+_u
sub	dx,dx
add	*8.(si),ax
adc	*6.(si),dx
L14:mov	bx,*-8.(bp)
mov	dx,*4.(bx)
sub	dx,130.+_u
mov	124.+_u,dx
|NREG	2
L9:|RTYP	0
br	cret
.globl	_rdchk
.even
_rdchk:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*6.
|NREG	2
|	_uap=di
|	_fp=-6.
|	_ip=-8.
|	_dev=-10.
mov	di,122.+_u
push	(di)
call	_getf
add	sp,*2.
mov	*-6.(bp),ax
cmp	ax,*0
je	L25
mov	si,ax
mov	si,*4.(si)
mov	*-8.(bp),si
mov	bx,ax
movb	dx,(bx)
and	dx,*4.
je	L26
mov	si,*-8.(bp)
cmp	*14.(si),*0
jg	L27
jl	L10002
cmp	*14.+2(si),*0
ja	L27
L10002:mov	bx,*-8.(bp)
cmpb	*1.(bx),*2.
jge	L25
L27:mov	124.+_u,*1.
|NREG	3
L25:|RTYP	0
br	cret
L26:mov	si,*-8.(bp)
mov	si,*6.(si)
and	si,#-4096.
cmp	si,#8192.
jne	L27
mov	si,*-8.(bp)
mov	si,*20.+2(si)
mov	*-10.(bp),si
sub	si,si
push	si
push	si
mov	si,#26115.
push	si
push	*-10.(bp)
mov	ax,*-10.(bp)
mov	cx,*8.
sar	ax,cl
mov	cx,*14.
imul	cx
mov	si,ax
call	@8.+_cdevsw(si)
add	sp,*8.
j	L25
.globl	_open
.even
_open:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
|NREG	1
|	_uap=si
mov	si,122.+_u
sub	dx,dx
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
je	L31
sub	dx,dx
push	dx
inc	*2.(si)
push	*2.(si)
push	di
call	_open1
add	sp,*6.
|NREG	3
L31:|RTYP	0
br	cret
.globl	_creat
.even
_creat:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
|NREG	1
|	_uap=si
mov	si,122.+_u
mov	dx,*1.
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
jne	L35
cmpb	111.+_u,*0
jne	L34
mov	dx,*2.(si)
and	dx,#3583.
push	dx
call	_maknode
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L34
mov	dx,*2.
push	dx
push	dx
push	di
j	L20005
L35:cmp	*18.(di),*0
je	L37
movb	ax,(di)
cbw
and	ax,#-4096.
cmp	ax,#-32768.
jne	L37
sub	dx,dx
push	dx
mov	dx,#16384.
push	dx
sub	ax,ax
cwd
push	ax
push	dx
push	di
mov	dx,*2.
push	dx
call	_locked
add	sp,*12.
test	ax,ax
je	L37
push	di
call	_iput
add	sp,*2.
j	L34
L37:mov	dx,*1.
push	dx
mov	dx,*2.
push	dx
push	di
L20005:call	_open1
add	sp,*6.
|NREG	3
L34:|RTYP	0
br	cret
.globl	_open1
.even
_open1:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|NREG	1
mov	si,*6.(bp)
|	_mode=si
|	_trf=8.
sub	sp,*4.
|	_fp=-6.
|	_i=-8.
cmp	*8.(bp),*2.
je	L43
test	si,*1.
je	L44
mov	dx,#256.
push	dx
push	di
call	_access
add	sp,*4.
L44:test	si,*2.
je	L43
mov	dx,#128.
push	dx
push	di
call	_access
add	sp,*4.
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#16384.
jne	L43
movb	111.+_u,*21.
L43:cmpb	111.+_u,*0
jne	L47
cmp	*8.(bp),*1.
jne	L48
push	di
call	_itrunc
add	sp,*2.
L48:push	di
call	_prele
add	sp,*2.
call	_falloc
mov	*-6.(bp),ax
test	ax,ax
je	L47
mov	bx,ax
mov	dx,si
and	dx,*3.
movb	(bx),dx
mov	bx,*-6.(bp)
mov	*4.(bx),di
mov	dx,124.+_u
mov	*-8.(bp),dx
mov	dx,si
and	dx,*2.
push	dx
push	di
call	_openi
add	sp,*4.
cmpb	111.+_u,*0
je	L42
mov	bx,*-8.(bp)
sal	bx,*1
mov	174.+_u(bx),*0.
mov	bx,*-6.(bp)
dec	*2.(bx)
L47:push	di
call	_iput
add	sp,*2.
L42:|RTYP	0
br	cret
.globl	_close
.even
_close:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_fp=di
|NREG	1
|	_uap=si
mov	si,122.+_u
push	(si)
call	_getf
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L51
mov	bx,(si)
sal	bx,*1
mov	174.+_u(bx),*0.
push	di
call	_closef
add	sp,*2.
|NREG	3
L51:|RTYP	0
br	cret
.globl	_seek
.even
_seek:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_fp=di
|NREG	1
|	_uap=si
mov	si,122.+_u
push	(si)
call	_getf
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L54
movb	dx,(di)
and	dx,*28.
je	L55
movb	111.+_u,*29.
j	L54
L55:cmp	*6.(si),*1.
jne	L56
mov	ax,*8.(di)
mov	dx,*6.(di)
j	L20007
L56:cmp	*6.(si),*2.
jne	L57
mov	bx,*4.(di)
mov	ax,*14. +2(bx)
mov	dx,*14.(bx)
L20007:add	*4.(si),ax
adc	*2.(si),dx
L57:cmp	*2.(si),*0
jg	L59
jl	L10003
cmp	*4.(si),*0.
jae	L59
L10003:movb	111.+_u,*22.
j	L54
L59:mov	ax,*4.(si)
mov	dx,*2.(si)
mov	*8.(di),ax
mov	*6.(di),dx
mov	ax,*4.(si)
mov	dx,*2.(si)
mov	126.+_u,ax
mov	124.+_u,dx
|NREG	3
L54:|RTYP	0
br	cret
.globl	_link
.even
_link:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_ip=di
|NREG	1
|	_xp=si
|	_uap=-6.
mov	dx,122.+_u
mov	*-6.(bp),dx
sub	dx,dx
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
beq	L62
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#16384.
jne	L10004
call	_suser
test	ax,ax
je	L63
L10004:push	di
call	_prele
add	sp,*2.
mov	bx,*-6.(bp)
mov	dx,*2.(bx)
mov	154.+_u,dx
mov	dx,*1.
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	si,ax
cmp	si,*0
je	L64
movb	111.+_u,*17.
push	si
call	_iput
add	sp,*2.
j	L63
L64:cmpb	111.+_u,*0
jne	L63
mov	bx,172.+_u
mov	dx,*2.(bx)
cmp	dx,*2.(di)
je	L65
push	bx
call	_iput
add	sp,*2.
movb	111.+_u,*18.
j	L63
L65:push	di
call	_wdir
add	sp,*2.
cmpb	111.+_u,*0
jne	L63
inc	*8.(di)
orb	(di),*64.
L63:push	di
call	_iput
add	sp,*2.
|NREG	3
L62:|RTYP	0
br	cret
.globl	_mknod
.even
_mknod:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
|NREG	1
|	_uap=si
mov	si,122.+_u
call	_suser
test	ax,ax
je	L70
mov	dx,*1.
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
je	L70
movb	111.+_u,*17.
j	L72
L70:cmpb	111.+_u,*0
jne	L69
push	*2.(si)
call	_maknode
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L69
mov	ax,*4.(si)
cwd
mov	*22.(di),ax
mov	*20.(di),dx
L72:push	di
call	_iput
add	sp,*2.
|NREG	3
L69:|RTYP	0
br	cret
.globl	_saccess
.even
_saccess:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*4.
|NREG	2
|	_svuid=di
|NREG	1
|	_svgid=si
|	_ip=-6.
|	_uap=-8.
mov	dx,122.+_u
mov	*-8.(bp),dx
mov	di,112.+_u
mov	si,114.+_u
mov	dx,116.+_u
mov	112.+_u,dx
mov	dx,118.+_u
mov	114.+_u,dx
sub	dx,dx
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	*-6.(bp),ax
cmp	ax,*0
je	L76
mov	bx,*-8.(bp)
test	*2.(bx),*4.
je	L77
mov	dx,#256.
push	dx
push	ax
call	_access
add	sp,*4.
L77:mov	bx,*-8.(bp)
test	*2.(bx),*2.
je	L78
mov	dx,#128.
push	dx
push	*-6.(bp)
call	_access
add	sp,*4.
L78:mov	bx,*-8.(bp)
test	*2.(bx),*1.
je	L79
mov	dx,*64.
push	dx
push	*-6.(bp)
call	_access
add	sp,*4.
L79:push	*-6.(bp)
call	_iput
add	sp,*2.
L76:mov	112.+_u,di
mov	114.+_u,si
|NREG	3
|RTYP	0
br	cret
.data
