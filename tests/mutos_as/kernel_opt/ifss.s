.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_ifsstty,68
.globl	_nifss
.data
_nifss:/1
.comm	_ifssend,2
.comm	_maptab,0
.globl	_ifssdev
.data
.data
_ifssdev:.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.byte /0
.globl	_ifssini
.text
.even
_ifssini:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*4.
|NREG	2
|	_pasp=di
|	_a=-6.
|	_b=-8.
cmp	_aspalig,*0
bne	L4
mov	di,#_aspcfg
mov	*-6.(bp),*2.
mov	si,*15.
push	si
push	*14.(di)
call	_outb
add	sp,*4.
mov	si,*85.
push	si
push	*10.(di)
call	_outb
add	sp,*4.
push	*10.(di)
call	_inb
add	sp,*2.
movb	*-8.(bp),ax
sub	si,si
push	si
push	*10.(di)
call	_outb
add	sp,*4.
cmpb	*-8.(bp),*85.
je	L5
mov	*-6.(bp),*1.
L5:cmp	_Stand,*0
jne	L6
cmp	*-6.(bp),*2.
jne	L10000
mov	si,#L8
push	si
j	L10001
L10000:mov	si,#L9
push	si
L10001:push	(di)
push	*2.(di)
mov	si,#L7
push	si
call	_printf
add	sp,*8.
L6:mov	si,*-6.(bp)
mov	_aspalig,si
cmp	*-6.(bp),*2.
jne	L4
sub	si,si
push	si
push	*2.(di)
call	_outb
add	sp,*4.
L4:|NREG	3
|RTYP	0
br	cret
.globl	_ifssope
.even
_ifssope:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_flag=6.
sub	sp,*4.
|NREG	2
|	_tp=di
|NREG	1
|	_pasp=si
|	_d=-6.
|	_s=-8.
mov	dx,*4.(bp)
subb	dh,dh
mov	*-6.(bp),dx
cmp	dx,*1.
jge	L10002
cmp	_aspalig,*2.
jge	L14
L10002:movb	111.+_u,*6.
br	L13
L14:mov	ax,*-6.(bp)
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_ifsstty
mov	si,*-6.(bp)
mov	cx,*5.
sal	si,cl
add	si,#_aspcfg
mov	*12.(di),#_ifsssta
test	*40.(di),*4.
bne	L15
cmpb	*22.(di),*0
jne	L16
mov	dx,*13.
movb	*23.(di),dx
movb	*22.(di),dx
mov	*26.(di),#1104.
push	di
push	*4.(bp)
call	_chanini
add	sp,*4.
L16:or	*40.(di),*20.
push	di
call	_ttychar
add	sp,*2.
L17:call	_spl3
mov	*-8.(bp),ax
push	*30.(si)
call	_inb
add	sp,*2.
mov	dx,*49.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*30.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*64.
push	dx
push	*4.(si)
call	_outb
add	sp,*4.
push	*-8.(bp)
call	_splx
add	sp,*2.
push	di
push	*4.(bp)
call	_ttyopen
add	sp,*4.
|NREG	3
L13:|RTYP	0
br	cret
L15:test	*40.(di),#128.
je	L17
cmp	112.+_u,*0
je	L17
movb	111.+_u,*16.
j	L13
.globl	_ifssclo
.even
_ifssclo:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_flag=6.
sub	sp,*4.
|NREG	2
|	_tp=di
|NREG	1
|	_pasp=si
|	_d=-6.
|	_s=-8.
mov	ax,*4.(bp)
subb	ah,ah
mov	*-6.(bp),ax
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_ifsstty
mov	si,*-6.(bp)
mov	cx,*5.
sal	si,cl
add	si,#_aspcfg
test	*40.(di),*4.
je	L21
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_ifsstty
push	di
call	_ttyclos
add	sp,*2.
call	_spl3
mov	*-8.(bp),ax
push	*30.(si)
call	_inb
add	sp,*2.
mov	dx,*49.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
sub	dx,dx
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
push	*-8.(bp)
call	_splx
add	sp,*2.
|NREG	3
L21:|RTYP	0
br	cret
.globl	_ifssrea
.even
_ifssrea:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*68.
imul	cx
add	ax,#_ifsstty
push	ax
call	_ttread
add	sp,*2.
|RTYP	0
br	cret
.globl	_ifsswri
.even
_ifsswri:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*68.
imul	cx
add	ax,#_ifsstty
push	ax
call	_ttwrite
add	sp,*2.
|RTYP	0
br	cret
.globl	_ifssint
.even
_ifssint:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_level=4.
sub	sp,*6.
|NREG	2
|	_tp=di
|NREG	1
|	_pasp=si
|	_c=-6.
|	_d=-8.
|	_temp=-10.
mov	bx,*4.(bp)
movb	ax,_ifssdev(bx)
cbw
mov	*-8.(bp),ax
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_ifsstty
mov	si,*-8.(bp)
mov	cx,*5.
sal	si,cl
add	si,#_aspcfg
cmp	*18.(di),*0
jne	L31
mov	*12.(di),#_ifsssta
L31:push	*30.(si)
call	_inb
add	sp,*2.
L32:push	*30.(si)
call	_inb
add	sp,*2.
mov	*-10.(bp),ax
and	ax,*1.
je	L33
mov	_ifssend,*1.
push	*26.(si)
call	_inb
add	sp,*2.
mov	*-6.(bp),ax
mov	dx,*1.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
push	*30.(si)
call	_inb
add	sp,*2.
and	ax,*112.
je	L34
mov	dx,*48.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
L34:test	*26.(di),#2048.
beq	L35
mov	dx,*-6.(bp)
and	dx,*127.
cmp	dx,*20.
bne	L35
or	*42.(di),*32.
or	*40.(di),#256.
L33:mov	_ifssend,*0.
test	*-10.(bp),*4.
je	L37
mov	dx,*40.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*40.(di)
and	dx,*20.
cmp	dx,*20.
jne	L37
push	di
call	_ttstart
add	sp,*2.
cmp	*6.(di),*0
je	L10003
cmp	*6.(di),*30.
jne	L37
L10003:mov	dx,di
add	dx,*6.
push	dx
call	_wakeup
add	sp,*2.
L37:cmp	_ifssend,*0
bne	L32
L41:mov	dx,di
add	dx,*62.
push	dx
call	_getc
add	sp,*2.
mov	*-6.(bp),ax
test	ax,ax
jl	L42
push	di
push	ax
call	_ttyinpu
add	sp,*4.
push	*30.(si)
call	_inb
add	sp,*2.
and	ax,*1.
je	L41
br	L32
L35:mov	dx,di
add	dx,*62.
push	dx
push	*-6.(bp)
call	_putc
add	sp,*4.
inc	_ifssend
br	L32
L42:push	*30.(si)
call	_inb
add	sp,*2.
and	ax,*1.
bne	L32
mov	dx,*64.
push	dx
push	*4.(si)
call	_outb
add	sp,*4.
test	*42.(di),*32.
je	L43
L44:mov	dx,di
add	dx,*6.
push	dx
call	_getc
add	sp,*2.
test	ax,ax
jge	L44
mov	dx,di
add	dx,*6.
push	dx
call	_wakeup
add	sp,*2.
L43:|NREG	3
|RTYP	0
br	cret
.globl	_ifssioc
.even
_ifssioc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_cmd=6.
|	_addr=8.
|	_flag=10.
sub	sp,*4.
|NREG	2
|	_tp=di
|	_speed=-6.
|	_flags=-8.
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_ifsstty
movb	ax,*22.(di)
cbw
mov	*-6.(bp),ax
mov	si,*26.(di)
mov	*-8.(bp),si
push	*4.(bp)
push	*8.(bp)
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*68.
imul	cx
add	ax,#_ifsstty
push	ax
push	*6.(bp)
call	_ttiocco
add	sp,*8.
test	ax,ax
jne	L49
movb	111.+_u,*25.
j	L48
L49:movb	ax,*22.(di)
cbw
cmp	ax,*-6.(bp)
jne	L10004
mov	si,*-8.(bp)
and	si,#8384.
mov	dx,*26.(di)
and	dx,#8384.
cmp	si,dx
je	L50
L10004:push	di
push	*4.(bp)
call	_chanini
add	sp,*4.
L50:|NREG	3
L48:|RTYP	0
br	cret
.globl	_ifsssta
.even
_ifsssta:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_tp=di
sub	sp,*4.
|NREG	1
|	_pasp=si
|	_c=-6.
|	_d=-8.
mov	si,*20.(di)
and	si,#255.
mov	*-8.(bp),si
mov	cx,*5.
sal	si,cl
add	si,#_aspcfg
push	*30.(si)
call	_inb
add	sp,*2.
push	*30.(si)
call	_inb
add	sp,*2.
and	ax,*4.
beq	L54
test	*40.(di),#256.
bne	L54
test	*42.(di),*2.
bne	L54
mov	dx,di
add	dx,*6.
push	dx
call	_getc
add	sp,*2.
mov	*-6.(bp),ax
test	ax,ax
jl	L55
inc	_ifssend
test	*26.(di),*32.
je	L56
push	ax
push	*26.(si)
j	L20001
L20003:push	*-6.(bp)
push	*26.(si)
L20001:call	_outb
add	sp,*4.
j	L54
L56:cmp	*-6.(bp),*127.
jle	L20003
cmp	*-6.(bp),#128.
jne	L60
or	*42.(di),*2.
j	L54
L60:mov	dx,*-6.(bp)
and	dx,*127.
add	dx,*4.
push	dx
push	di
mov	dx,#_ttrstrt
push	dx
call	_timeout
add	sp,*6.
or	*40.(di),*1.
j	L54
L55:test	*40.(di),*64.
je	L54
and	*40.(di),*-65.
mov	dx,di
add	dx,*6.
push	dx
call	_wakeup
add	sp,*2.
L54:mov	dx,*64.
push	dx
push	*4.(si)
call	_outb
add	sp,*4.
|RTYP	0
br	cret
.globl	_chanini
.even
_chanini:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
mov	di,*6.(bp)
|	_tp=di
sub	sp,*8.
|NREG	1
|	_pasp=si
|	_d=-6.
|	_s=-8.
|	_o=-10.
|	_zk=-12.
call	_spl3
mov	*-8.(bp),ax
mov	si,*4.(bp)
and	si,#255.
mov	*-6.(bp),si
mov	cx,*5.
sal	si,cl
add	si,#_aspcfg
movb	ax,*22.(di)
cbw
sub	ax,#/1
cmp	ax,*12.
ja	L79
sal	ax,#1
xchg	bx,ax
seg	cs
br	@L10006(bx)
L72:mov	*-10.(bp),*64.
j	L67
L75:mov	*-10.(bp),#128.
j	L67
L78:mov	*-10.(bp),#192.
j	L67
L79:movb	111.+_u,*22.
br	L66
L10006:L72
L75
L79
L79
L72
L78
L78
L75
L72
L79
L78
L75
L72
L67:movb	ax,*22.(di)
cbw
sub	ax,#/1
cmp	ax,*12.
ja	L85
sal	ax,#1
xchg	bx,ax
seg	cs
br	@L10008(bx)
L85:mov	*-12.(bp),*1.
j	L80
L88:mov	*-12.(bp),*8.
j	L80
L89:mov	*-12.(bp),*12.
j	L80
L91:mov	*-12.(bp),*64.
j	L80
L92:mov	*-12.(bp),#196.
j	L80
L10008:L92
L91
L85
L85
L91
L89
L88
L88
L88
L85
L85
L85
L85
L80:push	*30.(si)
call	_inb
add	sp,*2.
mov	dx,*24.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*4.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*3.
push	dx
push	*20.(si)
call	_outb
add	sp,*4.
mov	dx,*7.
push	dx
push	*20.(si)
call	_outb
add	sp,*4.
push	*-12.(bp)
push	*20.(si)
call	_outb
add	sp,*4.
test	*26.(di),#192.
je	L93
mov	dx,*26.(di)
and	dx,#192.
cmp	dx,#192.
je	L93
test	*26.(di),*64.
je	L10009
mov	dx,*1.
j	L10010
L10009:mov	dx,*3.
L10010:or	*-10.(bp),dx
L93:or	*-10.(bp),*4.
push	*-10.(bp)
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*5.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	*-10.(bp),*40.
test	*26.(di),#8192.
je	L94
or	*-10.(bp),*64.
L94:push	*-10.(bp)
push	*30.(si)
call	_outb
add	sp,*4.
mov	*-10.(bp),*64.
mov	dx,*3.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
test	*26.(di),#8192.
je	L95
or	*-10.(bp),#128.
L95:push	*-10.(bp)
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*3.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*-10.(bp)
or	dx,*1.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
push	*30.(si)
call	_inb
add	sp,*2.
mov	dx,*2.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*112.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*1.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*30.
push	dx
push	*30.(si)
call	_outb
add	sp,*4.
mov	dx,*64.
push	dx
push	*4.(si)
call	_outb
add	sp,*4.
push	*-8.(bp)
call	_splx
add	sp,*2.
|NREG	2
L66:|RTYP	0
br	cret
.data
L7:.byte	/41,/53,/50,/20,/20,/20,/20,/20,/20
.byte	/42,/61,/73,/65,/64
.byte	/20,/25,/78,/20,/6c,/65,/76,/65,/6c
.byte	/20,/25,/64,/20,/25,/73
.byte	/2e,/a,/0
L8:.byte	/66,/6f,/75,/6e,/64,/0
L9:.byte	/4e,/4f,/54,/20,/66,/6f,/75,/6e,/64
.byte	/0
