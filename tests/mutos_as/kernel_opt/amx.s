.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.globl	_amxdebu
.data
_amxdebu:/0
.comm	_amxslee,2
.comm	_amxwake,2
.globl	_amxinit
.text
.even
_amxinit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_board=di
cmp	_amxdebu,*3.
jl	L4
mov	si,#L5
push	si
call	_printf
add	sp,*2.
L4:call	_amxprob
sub	di,di
j	L6
L20001:mov	si,di
sal	si,*1
mov	si,#_amxaliv(si)
test	si,si
je	L8
push	di
call	_amxcal
add	sp,*2.
L8:inc	di
L6:cmp	di,_namx
jl	L20001
|NREG	3
|RTYP	0
br	cret
.globl	_amxprob
.even
_amxprob:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*6.
|NREG	2
|	_cf=di
|NREG	1
|	_Fbase=si
|	_alive=-6.
|	_board=-8.
|	_s=-10.
cmp	_amxdebu,*3.
jl	L13
mov	dx,#L14
push	dx
call	_printf
add	sp,*2.
L13:mov	*-8.(bp),*0.
j	L15
L20003:mov	di,*-8.(bp)
sal	di,*1
sal	di,*1
add	di,#_amxcfg
cmp	(di),*0
jne	L10000
cmp	*2.(di),*0.
je	L17
L10000:mov	ax,*-8.(bp)
mov	cx,#142.
imul	cx
mov	si,ax
add	si,#_amxfirm
call	_spl7
mov	*-10.(bp),ax
movb	*1.(si),*0.
movb	(si),*1.
push	si
mov	dx,*2.
push	dx
push	*2.(di)
push	(di)
sub	dx,dx
push	dx
call	_amxpoke
add	sp,*10.
push	*-10.(bp)
call	_splx
add	sp,*2.
L17:inc	*-8.(bp)
L15:mov	dx,_namx
cmp	*-8.(bp),dx
jl	L20003
mov	dx,#500.
push	dx
call	_delay
add	sp,*2.
mov	*-8.(bp),*0.
br	L18
L20005:mov	di,*-8.(bp)
sal	di,*1
sal	di,*1
add	di,#_amxcfg
cmp	(di),*0
jne	L10001
cmp	*2.(di),*0.
je	L20
L10001:mov	*-6.(bp),*0.
mov	ax,*-8.(bp)
mov	cx,#142.
imul	cx
mov	si,ax
add	si,#_amxfirm
mov	dx,si
inc	dx
push	dx
mov	dx,*1.
push	dx
push	*2.(di)
push	(di)
push	dx
call	_amxpeek
add	sp,*10.
cmpb	*1.(si),*1.
jne	L21
mov	*-6.(bp),*1.
L21:cmp	*-6.(bp),*0
je	L10002
mov	dx,#L23
push	dx
j	L10003
L10002:mov	dx,#L24
push	dx
L10003:push	_amxleve
mov	ax,*16.
cwd
push	ax
push	dx
push	*2.(di)
push	(di)
call	ldiv
add	sp,*8.
push	ax
mov	dx,#L22
push	dx
call	_printf
add	sp,*8.
mov	bx,*-8.(bp)
sal	bx,*1
mov	dx,*-6.(bp)
mov	_amxaliv(bx),dx
L20:inc	*-8.(bp)
L18:mov	dx,_namx
cmp	*-8.(bp),dx
blt	L20005
|NREG	3
|RTYP	0
br	cret
.globl	_amxcal
.even
_amxcal:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_board=4.
sub	sp,*12.
|NREG	2
|	_Fbase=di
|NREG	1
|	_cf=si
|	_of=-6.
|	_bnum=-8.
|	_i=-10.
|	_j=-12.
|	_s=-14.
|	_temp_off=-16.
cmp	_amxdebu,*3.
jl	L28
mov	dx,#L29
push	dx
call	_printf
add	sp,*2.
L28:mov	ax,*4.(bp)
mov	cx,#142.
imul	cx
mov	di,ax
add	di,#_amxfirm
mov	si,*4.(bp)
sal	si,*1
sal	si,*1
add	si,#_amxcfg
mov	dx,di
add	dx,*8.
sub	dx,di
mov	*-16.(bp),dx
mov	dx,di
add	dx,*8.
push	dx
mov	dx,*4.
push	dx
push	*2.(si)
push	(si)
push	*-16.(bp)
call	_amxpeek
add	sp,*10.
mov	dx,*4.(bp)
sal	dx,*1
sal	dx,*1
mov	*-8.(bp),dx
mov	*-10.(bp),dx
mov	*-12.(bp),*0.
br	L30
L20007:mov	ax,*10.(di)
imul	*-12.(bp)
add	ax,*8.(di)
cwd
add	ax,*2.(si)
adc	dx,(si)
mov	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	_amxladd+2(bx),ax
mov	_amxladd(bx),dx
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,cx
sub	ax,bx
mov	*-16.(bp),ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,cx
push	ax
mov	dx,*14.
push	dx
mov	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
push	*-16.(bp)
call	_amxpeek
add	sp,*10.
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
mov	bx,ax
mov	ax,*30.(bx)
cwd
add	ax,*2.(si)
adc	dx,(si)
push	ax
push	dx
mov	ax,*-10.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
pop	dx
pop	ax
mov	_amxibuf+2(bx),ax
mov	_amxibuf(bx),dx
mov	ax,*-10.(bp)
mov	cx,*6.
imul	cx
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
mov	bx,ax
mov	ax,*32.(bx)
pop	bx
mov	4.+_amxibuf(bx),ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
mov	bx,ax
mov	ax,*40.(bx)
cwd
add	ax,*2.(si)
adc	dx,(si)
push	ax
push	dx
mov	ax,*-10.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
pop	dx
pop	ax
mov	_amxobuf+2(bx),ax
mov	_amxobuf(bx),dx
mov	ax,*-10.(bp)
mov	cx,*6.
imul	cx
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
mov	bx,ax
mov	ax,*42.(bx)
pop	bx
mov	4.+_amxobuf(bx),ax
inc	*-10.(bp)
inc	*-12.(bp)
L30:mov	dx,*-8.(bp)
add	dx,*4.
cmp	dx,*-10.(bp)
bgt	L20007
mov	ax,*4.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-6.(bp),ax
mov	bx,ax
mov	dx,di
sub	dx,di
mov	(bx),dx
mov	bx,*-6.(bp)
mov	dx,di
inc	dx
sub	dx,di
mov	*2.(bx),dx
mov	bx,*-6.(bp)
mov	dx,di
add	dx,*4.
sub	dx,di
mov	*4.(bx),dx
mov	bx,*-6.(bp)
mov	dx,di
add	dx,*5.
sub	dx,di
mov	*6.(bx),dx
mov	*-12.(bp),*0.
L20009:mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*22.
sub	ax,bx
pop	bx
mov	*8.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*23.
sub	ax,bx
pop	bx
mov	*10.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*26.
sub	ax,bx
pop	bx
mov	*12.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*34.
sub	ax,bx
pop	bx
mov	*14.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*36.
sub	ax,bx
pop	bx
mov	*16.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*38.
sub	ax,bx
pop	bx
mov	*18.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*44.
sub	ax,bx
pop	bx
mov	*20.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*46.
sub	ax,bx
pop	bx
mov	*22.(bx),ax
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
push	ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*22.
mov	bx,ax
mov	ax,*-12.(bp)
imul	cx
add	ax,di
add	ax,*48.
sub	ax,bx
pop	bx
mov	*24.(bx),ax
inc	*-12.(bp)
cmp	*-12.(bp),*4.
blt	L20009
call	_spl7
mov	*-14.(bp),ax
movb	*1.(di),*0.
mov	dx,di
inc	dx
push	dx
mov	dx,*1.
push	dx
push	*2.(si)
push	(si)
push	dx
call	_amxpoke
add	sp,*10.
movb	*13.(di),*1.
mov	dx,di
add	dx,*13.
sub	dx,di
mov	*-16.(bp),dx
mov	dx,di
add	dx,*13.
push	dx
mov	dx,*1.
push	dx
push	*2.(si)
push	(si)
push	*-16.(bp)
call	_amxpoke
add	sp,*10.
push	*-14.(bp)
call	_splx
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_amxbaud
.data
_amxbaud:/0
/0
/0
/6e
/0
/96
/0
/12c
/258
/4b0
/0
/960
/12c0
/2580
/0
/0
.globl	_amxpara
.text
.even
_amxpara:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*18.
|NREG	2
|	_tp=di
|NREG	1
|	_Fbase=si
|	_cf=-6.
|	_of=-8.
|	_board=-10.
|	_l_num=-12.
|	_s=-14.
|	_speed=-16.
|	_unit=-18.
|	_x=-20.
|	_modem=-22.
mov	dx,*4.(bp)
and	dx,*31.
mov	*-18.(bp),dx
mov	dx,*4.(bp)
and	dx,#192.
mov	*-22.(bp),dx
cmp	_amxdebu,*2.
jl	L39
push	*-18.(bp)
mov	dx,#L40
push	dx
call	_printf
add	sp,*4.
L39:mov	dx,*-18.(bp)
sar	dx,*1
sar	dx,*1
mov	*-10.(bp),dx
mov	ax,*-18.(bp)
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_amxtty
mov	ax,*-10.(bp)
mov	cx,#142.
imul	cx
mov	si,ax
add	si,#_amxfirm
mov	dx,*-18.(bp)
and	dx,*3.
mov	*-12.(bp),dx
mov	dx,*-10.(bp)
sal	dx,*1
sal	dx,*1
add	dx,#_amxcfg
mov	*-6.(bp),dx
mov	ax,*-10.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-8.(bp),ax
movb	ax,*23.(di)
cbw
mov	*-14.(bp),ax
cmp	ax,*0
je	L42
mov	bx,ax
sal	bx,*1
mov	dx,#_amxbaud(bx)
mov	*-16.(bp),dx
cmp	*-14.(bp),*15.
jg	L10004
cmp	*-14.(bp),*0
beq	L48
cmp	dx,*0
bne	L48
L10004:movb	111.+_u,*22.
br	L38
L20011:cmp	_amxdebu,*2.
jl	L44
push	*-18.(bp)
mov	dx,#L45
push	dx
call	_printf
add	sp,*4.
L44:mov	bx,*-18.(bp)
orb	_amxscd(bx),*4.
inc	_amxslee
mov	dx,*28.
push	dx
mov	dx,*-18.(bp)
add	dx,#_amxscd
push	dx
call	_sleep
add	sp,*4.
L42:mov	bx,*-10.(bp)
movb	dx,#_amxcmd(bx)
and	dx,*1.
jne	L20011
cmp	_amxdebu,*2.
jl	L46
push	*-18.(bp)
mov	dx,#L47
push	dx
call	_printf
add	sp,*4.
L46:call	_spl7
mov	*-20.(bp),ax
mov	dx,*-12.(bp)
movb	*4.(si),dx
movb	(si),*9.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
push	ax
mov	ax,*-12.(bp)
imul	cx
add	ax,si
mov	bx,ax
movb	ax,*23.(bx)
cbw
and	ax,*-2.
or	ax,*16.
pop	bx
movb	*23.(bx),ax
movb	(si),*4.
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*23.
push	ax
mov	dx,*1.
push	dx
mov	bx,*-18.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*10.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
mov	bx,*-10.(bp)
orb	_amxcmd(bx),*1.
push	*-20.(bp)
call	_splx
add	sp,*2.
br	L38
L48:mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	ax,*26.(bx)
cmp	ax,*-16.(bp)
jne	L50
br	L56
L20013:cmp	_amxdebu,*2.
jl	L52
push	*-18.(bp)
mov	dx,#L53
push	dx
call	_printf
add	sp,*4.
L52:mov	bx,*-18.(bp)
orb	_amxscd(bx),*4.
inc	_amxslee
mov	dx,*28.
push	dx
mov	dx,*-18.(bp)
add	dx,#_amxscd
push	dx
call	_sleep
add	sp,*4.
L50:mov	bx,*-10.(bp)
movb	dx,#_amxcmd(bx)
and	dx,*1.
jne	L20013
cmp	_amxdebu,*2.
jl	L54
push	*-18.(bp)
mov	dx,#L55
push	dx
call	_printf
add	sp,*4.
L54:call	_spl7
mov	*-20.(bp),ax
mov	dx,*-12.(bp)
movb	*4.(si),dx
movb	(si),*9.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
mov	bx,*-10.(bp)
orb	_amxcmd(bx),*1.
push	*-20.(bp)
call	_splx
add	sp,*2.
j	L56
L20015:cmp	_amxdebu,*2.
jl	L58
push	*-18.(bp)
mov	dx,#L59
push	dx
call	_printf
add	sp,*4.
L58:mov	bx,*-18.(bp)
orb	_amxscd(bx),*4.
inc	_amxslee
mov	dx,*28.
push	dx
mov	dx,*-18.(bp)
add	dx,#_amxscd
push	dx
call	_sleep
add	sp,*4.
L56:mov	bx,*-10.(bp)
movb	dx,#_amxcmd(bx)
and	dx,*1.
jne	L20015
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
andb	*23.(bx),*-7.
test	*26.(di),#128.
je	L10006
test	*26.(di),*64.
jne	L61
L10006:test	*26.(di),#128.
jne	L60
test	*26.(di),*64.
je	L61
L60:test	*26.(di),#128.
je	L10007
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
orb	*23.(bx),*4.
j	L61
L10007:mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
orb	*23.(bx),*2.
L61:test	*26.(di),#8224.
je	L62
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
orb	*23.(bx),#16384.
j	L63
L62:mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
andb	*23.(bx),#-16385.
L63:call	_spl7
mov	*-20.(bp),ax
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	dx,*-16.(bp)
mov	*26.(bx),dx
mov	ax,*-12.(bp)
imul	cx
add	ax,si
mov	bx,ax
mov	*28.(bx),*0.
mov	ax,*-12.(bp)
imul	cx
add	ax,si
mov	bx,ax
orb	*23.(bx),*1.
cmp	*-22.(bp),*0
jne	L64
mov	ax,*-12.(bp)
imul	cx
add	ax,si
mov	bx,ax
orb	*23.(bx),*8.
j	L65
L64:mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
andb	*23.(bx),*-9.
L65:mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
andb	*23.(bx),*-17.
movb	(si),*4.
mov	dx,*-12.(bp)
movb	*4.(si),dx
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*26.
push	ax
mov	dx,*4.
push	dx
mov	bx,*-18.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*12.(bx)
call	_amxpoke
add	sp,*10.
mov	ax,*-12.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*23.
push	ax
mov	dx,*1.
push	dx
mov	bx,*-18.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-12.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*10.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
mov	bx,*-10.(bp)
orb	_amxcmd(bx),*1.
push	*-20.(bp)
call	_splx
add	sp,*2.
|NREG	3
L38:|RTYP	0
br	cret
.globl	_amxopen
.even
_amxopen:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*18.
|NREG	2
|	_tp=di
|NREG	1
|	_Fbase=si
|	_cf=-6.
|	_of=-8.
|	_unit=-10.
|	_board=-12.
|	_i=-14.
|	_l_num=-16.
|	_modem=-18.
|	_s=-20.
|	_temp_off=-22.
mov	dx,*4.(bp)
and	dx,*31.
mov	*-10.(bp),dx
cmp	_amxdebu,*2.
jl	L69
push	dx
mov	dx,#L70
push	dx
call	_printf
add	sp,*4.
L69:mov	dx,_namx
sal	dx,*1
sal	dx,*1
cmp	dx,*-10.(bp)
jg	L71
L20018:movb	111.+_u,*6.
br	L68
L71:mov	ax,*-10.(bp)
cwd
mov	cx,*4.
idiv	cx
sal	ax,*1
mov	bx,ax
mov	ax,#_amxaliv(bx)
test	ax,ax
je	L20018
mov	ax,*-10.(bp)
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_amxtty
mov	ax,*-10.(bp)
sar	ax,*1
sar	ax,*1
mov	*-12.(bp),ax
mov	cx,#142.
imul	cx
mov	si,ax
add	si,#_amxfirm
mov	dx,*-10.(bp)
and	dx,*3.
mov	*-16.(bp),dx
movb	*4.(si),dx
mov	dx,*-12.(bp)
sal	dx,*1
sal	dx,*1
add	dx,#_amxcfg
mov	*-6.(bp),dx
mov	ax,*-12.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-8.(bp),ax
mov	dx,*4.(bp)
and	dx,#192.
mov	*-18.(bp),dx
mov	dx,*-10.(bp)
mov	*18.(di),dx
mov	*12.(di),#_amxstar
test	*40.(di),*4.
bne	L73
push	di
call	_ttychar
add	sp,*2.
cmpb	*22.(di),*0
jne	L74
mov	dx,*13.
movb	*23.(di),dx
movb	*22.(di),dx
mov	*26.(di),#216.
L74:push	*4.(bp)
call	_amxpara
add	sp,*2.
call	_spl7
mov	*-20.(bp),ax
mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	*50.(bx),#256.
mov	ax,*-16.(bp)
imul	cx
add	ax,si
mov	bx,ax
movb	*22.(bx),*3.
mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*22.
mov	bx,ax
mov	ax,*-16.(bp)
imul	cx
add	ax,si
add	ax,*50.
sub	ax,bx
mov	*-22.(bp),ax
mov	ax,*-16.(bp)
imul	cx
add	ax,si
add	ax,*50.
push	ax
mov	dx,*2.
push	dx
mov	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
push	*-22.(bp)
call	_amxpoke
add	sp,*10.
mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*22.
push	ax
mov	dx,*1.
push	dx
mov	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-16.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*8.(bx)
call	_amxpoke
add	sp,*10.
push	*-20.(bp)
call	_splx
add	sp,*2.
test	*-18.(bp),*64.
je	L73
mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*22.
mov	bx,ax
mov	ax,*-16.(bp)
imul	cx
add	ax,si
add	ax,*24.
sub	ax,bx
mov	*-22.(bp),ax
mov	*-14.(bp),*0.
mov	ax,*-16.(bp)
imul	cx
add	ax,si
add	ax,*24.
push	ax
mov	dx,*2.
push	dx
mov	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
push	*-22.(bp)
call	_amxpeek
add	sp,*10.
sub	dx,dx
mov	*-14.(bp),*1.
L73:test	*40.(di),#128.
je	L80
cmp	112.+_u,*0
je	L80
movb	111.+_u,*16.
j	L68
L80:or	*40.(di),*16.
push	di
push	*4.(bp)
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@_linesw(bx)
add	sp,*4.
|NREG	3
L68:|RTYP	0
br	cret
.globl	_amxclos
.even
_amxclos:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*14.
|NREG	2
|	_tp=di
|NREG	1
|	_Fbase=si
|	_cf=-6.
|	_of=-8.
|	_unit=-10.
|	_board=-12.
|	_i=-14.
|	_l_num=-16.
|	_s=-18.
mov	dx,*4.(bp)
and	dx,*31.
mov	*-10.(bp),dx
cmp	_amxdebu,*2.
jl	L84
push	dx
mov	dx,#L85
push	dx
call	_printf
add	sp,*4.
L84:mov	ax,*-10.(bp)
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_amxtty
mov	ax,*-10.(bp)
sar	ax,*1
sar	ax,*1
mov	*-12.(bp),ax
mov	cx,#142.
imul	cx
mov	si,ax
add	si,#_amxfirm
mov	dx,*-10.(bp)
and	dx,*3.
mov	*-16.(bp),dx
mov	dx,*-12.(bp)
sal	dx,*1
sal	dx,*1
add	dx,#_amxcfg
mov	*-6.(bp),dx
mov	ax,*-12.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-8.(bp),ax
mov	dx,_namx
sal	dx,*1
sal	dx,*1
cmp	dx,*-10.(bp)
ble	L86
mov	*-14.(bp),*0.
L20020:mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*46.
push	ax
mov	dx,*2.
push	dx
mov	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-16.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*22.(bx)
call	_amxpeek
add	sp,*10.
mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
push	ax
mov	ax,*-10.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#4.+_amxobuf(bx)
dec	ax
pop	bx
cmp	*46.(bx),ax
jne	L10009
mov	bx,*-12.(bp)
movb	dx,#_amxcmd(bx)
and	dx,*1.
je	L89
L10009:cmp	_amxdebu,*2.
jl	L90
push	*-10.(bp)
mov	dx,#L91
push	dx
call	_printf
add	sp,*4.
L90:mov	bx,*-10.(bp)
orb	_amxscd(bx),*8.
inc	_amxslee
mov	dx,*28.
push	dx
mov	dx,*-10.(bp)
add	dx,#_amxscd
push	dx
call	_sleep
add	sp,*4.
j	L87
L89:mov	*-14.(bp),*1.
L87:cmp	*-14.(bp),*0
beq	L20020
test	*40.(di),#512.
je	L93
and	*40.(di),*-17.
L93:call	_spl7
mov	*-18.(bp),ax
mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
push	ax
mov	ax,*-16.(bp)
imul	cx
add	ax,si
mov	bx,ax
movb	ax,*23.(bx)
cbw
and	ax,*-2.
or	ax,*16.
pop	bx
movb	*23.(bx),ax
movb	(si),*4.
mov	dx,*-16.(bp)
movb	*4.(si),dx
mov	ax,*-16.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*23.
push	ax
mov	dx,*1.
push	dx
mov	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-16.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*10.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
mov	bx,*-12.(bp)
orb	_amxcmd(bx),*1.
push	*-18.(bp)
call	_splx
add	sp,*2.
push	di
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@2.+_linesw(bx)
add	sp,*2.
push	di
call	_ttyclos
add	sp,*2.
L86:|NREG	3
|RTYP	0
br	cret
.globl	_amxread
.even
_amxread:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
|	_tp=di
|NREG	1
|	_unit=si
mov	si,*4.(bp)
and	si,*31.
cmp	_amxdebu,*3.
jl	L97
push	si
mov	dx,#L98
push	dx
call	_printf
add	sp,*4.
L97:mov	ax,si
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_amxtty
push	di
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@4.+_linesw(bx)
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_amxwrit
.even
_amxwrit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
|	_tp=di
|NREG	1
|	_unit=si
mov	si,*4.(bp)
and	si,*31.
cmp	_amxdebu,*3.
jl	L102
push	si
mov	dx,#L103
push	dx
call	_printf
add	sp,*4.
L102:mov	ax,si
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_amxtty
push	di
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@6.+_linesw(bx)
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_st_inp
.even
_st_inp:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_tp=4.
sub	sp,*10.
|NREG	2
|	_Fbase=di
|NREG	1
|	_cf=si
|	_of=-6.
|	_board=-8.
|	_l_num=-10.
|	_s=-12.
|	_unit=-14.
mov	bx,*4.(bp)
mov	dx,*18.(bx)
mov	*-14.(bp),dx
sar	dx,*1
sar	dx,*1
mov	*-8.(bp),dx
mov	ax,*-14.(bp)
cwd
mov	cx,*3.
idiv	cx
mov	*-10.(bp),dx
mov	ax,*-8.(bp)
mov	cx,#142.
imul	cx
mov	di,ax
add	di,#_amxfirm
mov	si,*-8.(bp)
sal	si,*1
sal	si,*1
add	si,#_amxcfg
mov	ax,*-8.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-6.(bp),ax
L109:push	di
mov	dx,*1.
push	dx
push	*2.(si)
push	(si)
mov	bx,*-6.(bp)
push	(bx)
call	_amxpeek
add	sp,*10.
cmpb	(di),*0
jne	L109
call	_spl7
mov	*-12.(bp),ax
mov	ax,*-10.(bp)
mov	cx,*30.
imul	cx
add	ax,di
mov	bx,ax
mov	*38.(bx),*0.
mov	dx,*-10.(bp)
movb	*4.(di),dx
movb	(di),*2.
mov	ax,*-10.(bp)
mov	cx,*30.
imul	cx
add	ax,di
add	ax,*38.
push	ax
mov	dx,*2.
push	dx
mov	bx,*-14.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-10.(bp)
mov	cx,*18.
imul	cx
add	ax,*-6.(bp)
mov	bx,ax
push	*18.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,di
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
push	*2.(si)
push	(si)
mov	bx,*-6.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	di
mov	dx,*1.
push	dx
push	*2.(si)
push	(si)
mov	bx,*-6.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
push	*-12.(bp)
call	_splx
add	sp,*2.
mov	bx,*-8.(bp)
orb	_amxcmd(bx),*1.
|NREG	3
|RTYP	0
br	cret
.globl	_amxintr
.even
_amxintr:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_level=4.
sub	sp,*30.
|NREG	2
|	_tp=di
|NREG	1
|	_Fbase=si
|	_cf=-6.
|	_of=-8.
|	_c=-10.
|	_board=-12.
|	_count=-14.
|	_gotone=-16.
|	_i=-18.
|	_l_num=-20.
|	_libc=-22.
|	_libp=-24.
|	_nch=-26.
|	_s=-28.
|	_scd=-30.
|	_status=-32.
|	_unit=-34.
cmp	_amxdebu,*2.
jl	L117
mov	dx,#L114
push	dx
call	_printf
add	sp,*2.
L117:mov	*-16.(bp),*0.
mov	*-12.(bp),*0.
br	L118
L20022:mov	bx,*-12.(bp)
sal	bx,*1
mov	dx,#_amxaliv(bx)
test	dx,dx
beq	L120
mov	ax,*-12.(bp)
mov	cx,#142.
imul	cx
mov	si,ax
add	si,#_amxfirm
mov	dx,*-12.(bp)
sal	dx,*1
sal	dx,*1
add	dx,#_amxcfg
mov	*-6.(bp),dx
mov	ax,*-12.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-8.(bp),ax
mov	dx,si
inc	dx
push	dx
mov	dx,*6.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*2.(bx)
call	_amxpeek
add	sp,*10.
cmpb	*1.(si),*0
beq	L120
movb	ax,*1.(si)
cbw
mov	*-32.(bp),ax
movb	ax,*5.(si)
cbw
mov	bx,ax
mov	ax,*-12.(bp)
sal	ax,*1
sal	ax,*1
add	ax,bx
mov	*-34.(bp),ax
mov	cx,*68.
imul	cx
mov	di,ax
add	di,#_amxtty
movb	ax,*5.(si)
cbw
mov	*-20.(bp),ax
inc	*-16.(bp)
cmp	_amxdebu,*2.
jl	L121
push	*-34.(bp)
movb	ax,*1.(si)
cbw
push	ax
mov	dx,#L122
push	dx
call	_printf
add	sp,*6.
L121:call	_spl7
mov	*-28.(bp),ax
movb	*1.(si),*0.
mov	dx,si
inc	dx
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*2.(bx)
call	_amxpoke
add	sp,*10.
push	*-28.(bp)
call	_splx
add	sp,*2.
mov	ax,*-32.(bp)
sub	ax,#/1
cmp	ax,*6.
bhi	L120
sal	ax,#1
xchg	bx,ax
seg	cs
br	@L10011(bx)
L125:mov	bx,*-34.(bp)
movb	dx,#_amxscd(bx)
and	dx,*1.
je	L126
mov	bx,*-34.(bp)
andb	_amxscd(bx),*-2.
mov	bx,*-34.(bp)
movb	dx,#_amxi_bu(bx)
movb	*-10.(bp),dx
push	di
movb	ax,*-10.(bp)
cbw
push	ax
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@10.+_linesw(bx)
add	sp,*4.
L126:mov	bx,*-12.(bp)
andb	_amxcmd(bx),*-2.
br	L120
L128:cmp	(di),#255.
jl	L129
mov	dx,*2.
push	dx
push	di
mov	dx,#_st_inp
push	dx
call	_timeout
add	sp,*6.
push	di
L20028:call	_wakeup
add	sp,*2.
br	L120
L129:mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*34.
push	ax
mov	dx,*4.
push	dx
mov	bx,*-34.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-20.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*14.(bx)
call	_amxpeek
add	sp,*10.
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	ax,*34.(bx)
mov	*-24.(bp),ax
mov	ax,*-20.(bp)
imul	cx
add	ax,si
mov	bx,ax
mov	ax,*36.(bx)
mov	*-22.(bp),ax
mov	bx,*-34.(bp)
orb	_amxscd(bx),*1.
mov	dx,*-34.(bp)
add	dx,#_amxi_bu
push	dx
mov	dx,*1.
push	dx
mov	ax,*-34.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#_amxibuf +2(bx)
mov	dx,#_amxibuf(bx)
push	ax
push	dx
push	*-24.(bp)
call	_amxpeek
add	sp,*10.
L132:push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpeek
add	sp,*10.
cmpb	(si),*0
jne	L132
call	_spl7
mov	*-28.(bp),ax
mov	dx,*-20.(bp)
movb	*4.(si),dx
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	*38.(bx),*1.
movb	(si),*2.
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*38.
push	ax
mov	dx,*2.
push	dx
mov	bx,*-34.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-20.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*18.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
push	*-28.(bp)
L20033:call	_splx
add	sp,*2.
mov	bx,*-12.(bp)
orb	_amxcmd(bx),*1.
br	L120
L133:test	*40.(di),*1.
je	L134
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
push	ax
mov	ax,*-34.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#4.+_amxobuf(bx)
dec	ax
pop	bx
cmp	*46.(bx),ax
bne	L120
mov	ax,*-34.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
movb	ax,4.+_amxtout(bx)
cbw
push	ax
push	di
mov	dx,#_ttrstrt
push	dx
call	_timeout
add	sp,*6.
L134:and	*40.(di),*-33.
push	di
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@16.+_linesw(bx)
add	sp,*2.
test	*40.(di),*64.
beq	L120
cmp	*6.(di),*30.
bgt	L120
test	*40.(di),*1.
bne	L120
and	*40.(di),*-65.
mov	dx,di
add	dx,*6.
push	dx
br	L20028
L136:mov	dx,#_amxwake
push	dx
call	_wakeup
add	sp,*2.
call	_spl7
mov	*-28.(bp),ax
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
orb	*23.(bx),*1.
mov	ax,*-20.(bp)
imul	cx
add	ax,si
mov	bx,ax
andb	*23.(bx),*-17.
mov	dx,*-20.(bp)
movb	*4.(si),dx
movb	(si),*4.
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*23.
push	ax
mov	dx,*1.
push	dx
mov	bx,*-34.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-20.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*10.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
push	*-28.(bp)
br	L20033
L137:mov	dx,*40.(di)
and	dx,*20.
cmp	dx,*20.
jne	L138
mov	dx,*1.
push	dx
push	*16.(di)
call	_signal
add	sp,*4.
L138:and	*40.(di),*-17.
call	_spl7
mov	*-28.(bp),ax
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
push	ax
mov	ax,*-20.(bp)
imul	cx
add	ax,si
mov	bx,ax
movb	ax,*23.(bx)
cbw
and	ax,*-2.
or	ax,*16.
pop	bx
movb	*23.(bx),ax
mov	dx,*-20.(bp)
movb	*4.(si),dx
movb	(si),*4.
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*23.
push	ax
mov	dx,*1.
push	dx
mov	bx,*-34.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-20.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*10.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
push	*-28.(bp)
br	L20033
L10011:L125
L126
L128
L133
L136
L137
L120
L120:inc	*-12.(bp)
L118:mov	dx,_namx
cmp	*-12.(bp),dx
blt	L20022
cmp	*-16.(bp),*0
bne	L117
cmp	_amxslee,*0
jle	L140
mov	*-34.(bp),*0.
j	L141
L20024:mov	bx,*-34.(bp)
movb	dx,#_amxscd(bx)
and	dx,*12.
je	L143
dec	_amxslee
mov	bx,*-34.(bp)
movb	ax,_amxscd(bx)
cbw
mov	*-30.(bp),ax
test	ax,*8.
je	L145
and	*-30.(bp),*-9.
j	L146
L145:and	*-30.(bp),*-5.
L146:mov	bx,*-34.(bp)
mov	dx,*-30.(bp)
movb	_amxscd(bx),dx
mov	dx,*-34.(bp)
add	dx,#_amxscd
push	dx
call	_wakeup
add	sp,*2.
L143:inc	*-34.(bp)
L141:mov	dx,_namx
sal	dx,*1
sal	dx,*1
cmp	dx,*-34.(bp)
jg	L20024
L140:|NREG	3
|RTYP	0
br	cret
.globl	_partab
.data
_partab:.byte /1
.byte /ff81
.byte /ff81
.byte /1
.byte /ff82
.byte /3
.byte /5
.byte /ff81
.byte /ff81
.byte /1
.byte /1
.byte /ff81
.byte /1
.byte /ff81
.byte /ff81
.byte /1
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.byte /0
.byte /ff80
.byte /0
.byte /ff80
.byte /ff80
.byte /0
.globl
.data
.globl	_amxstar
.text
.even
_amxstar:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_tp=4.
sub	sp,*22.
|NREG	2
|	_bufp=di
|NREG	1
|	_Fbase=si
|	_cf=-6.
|	_of=-8.
|	_buff=-10.
|	_bnum=-12.
|	_cntl=-14.
|	_i=-16.
|	_io_count=-18.
|	_l_num=-20.
|	_limit=-22.
|	_s=-24.
|	_unit=-26.
call	_spl5
mov	*-24.(bp),ax
mov	bx,*4.(bp)
test	*40.(bx),*33.
je	L150
push	ax
L20041:call	_splx
add	sp,*2.
br	L149
L150:mov	bx,*4.(bp)
or	*40.(bx),*32.
push	*-24.(bp)
call	_splx
add	sp,*2.
cmp	_amxdebu,*2.
jl	L151
mov	bx,*4.(bp)
push	*18.(bx)
mov	dx,#L152
push	dx
call	_printf
add	sp,*4.
L151:mov	bx,*4.(bp)
mov	dx,*18.(bx)
mov	*-26.(bp),dx
sar	dx,*1
sar	dx,*1
mov	*-12.(bp),dx
mov	dx,*-26.(bp)
and	dx,*3.
mov	*-20.(bp),dx
mov	ax,*-12.(bp)
mov	cx,#142.
imul	cx
mov	si,ax
add	si,#_amxfirm
mov	ax,*-12.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-8.(bp),ax
mov	dx,*-12.(bp)
sal	dx,*1
sal	dx,*1
add	dx,#_amxcfg
mov	*-6.(bp),dx
mov	bx,*-26.(bp)
sal	bx,*1
mov	dx,#_iobuffe(bx)
mov	*-10.(bp),dx
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*44.
push	ax
mov	dx,*4.
push	dx
mov	bx,*-26.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-20.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*20.(bx)
call	_amxpeek
add	sp,*10.
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
cmp	*46.(bx),#256.
jle	L153
mov	*-22.(bp),#256.
j	L154
L153:mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	ax,*46.(bx)
mov	*-22.(bp),ax
L154:mov	bx,*-26.(bp)
movb	dx,#_amxscd(bx)
and	dx,*2.
cmp	dx,*2.
beq	L155
cmp	_amxdebu,*3.
jl	L156
mov	dx,#L157
push	dx
call	_printf
add	sp,*2.
L156:push	*-22.(bp)
push	*-10.(bp)
mov	dx,*4.(bp)
add	dx,*6.
push	dx
call	_q_to_b
add	sp,*6.
mov	*-18.(bp),ax
cmp	ax,*0
jne	L158
L20044:mov	bx,*4.(bp)
and	*40.(bx),*-33.
br	L149
L158:mov	bx,*4.(bp)
test	*26.(bx),*32.
beq	L159
cmp	_amxdebu,*3.
jl	L160
mov	dx,#L161
push	dx
call	_printf
add	sp,*2.
L160:push	*-10.(bp)
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
push	*44.(bx)
push	*-18.(bp)
sub	dx,dx
push	dx
push	*-26.(bp)
call	_amxbpok
add	sp,*10.
L164:push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpeek
add	sp,*10.
cmpb	(si),*0
jne	L164
mov	bx,*-12.(bp)
movb	dx,#_amxcmd(bx)
and	dx,*1.
je	L165
mov	dx,*3.
push	dx
call	_amxintr
add	sp,*2.
L165:call	_spl7
mov	*-24.(bp),ax
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	dx,*-18.(bp)
mov	*48.(bx),dx
mov	dx,*-20.(bp)
movb	*4.(si),dx
movb	(si),*3.
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*48.
push	ax
mov	dx,*2.
push	dx
mov	bx,*-26.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-20.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*24.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
mov	bx,*-12.(bp)
orb	_amxcmd(bx),*1.
push	*-24.(bp)
br	L20041
L159:mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	_amxtout(bx),*0.
mov	ax,*-26.(bp)
imul	cx
mov	bx,ax
mov	dx,*-18.(bp)
mov	2.+_amxtout(bx),dx
L155:mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
cmp	_amxtout(bx),#256.
jne	L166
push	*-22.(bp)
push	*-10.(bp)
mov	dx,*4.(bp)
add	dx,cx
push	dx
call	_q_to_b
add	sp,*6.
mov	*-18.(bp),ax
cmp	ax,*0
beq	L20044
mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	_amxtout(bx),*0.
mov	ax,*-26.(bp)
imul	cx
mov	bx,ax
mov	dx,*-18.(bp)
mov	2.+_amxtout(bx),dx
L166:cmp	_amxdebu,*3.
jl	L168
mov	dx,#L169
push	dx
call	_printf
add	sp,*2.
L168:mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	di,_amxtout(bx)
add	di,*-10.(bp)
mov	ax,*-26.(bp)
imul	cx
mov	bx,ax
mov	ax,#_amxtout(bx)
mov	*-16.(bp),ax
j	L170
L20039:inc	di
inc	*-16.(bp)
L170:mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#2.+_amxtout(bx)
cmp	ax,*-16.(bp)
jle	L171
cmpb	(di),*127.
jle	L20039
mov	ax,*-26.(bp)
imul	cx
mov	bx,ax
mov	dx,*-16.(bp)
mov	_amxtout(bx),dx
L171:push	*-10.(bp)
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
push	*44.(bx)
push	*-16.(bp)
mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
push	#_amxtout(bx)
push	*-26.(bp)
call	_amxbpok
add	sp,*10.
L177:push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpeek
add	sp,*10.
cmpb	(si),*0
jne	L177
mov	bx,*-12.(bp)
movb	dx,#_amxcmd(bx)
and	dx,*1.
je	L178
mov	dx,*3.
push	dx
call	_amxintr
add	sp,*2.
L178:call	_spl7
mov	*-24.(bp),ax
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
mov	bx,ax
mov	dx,*-16.(bp)
mov	*48.(bx),dx
mov	dx,*-20.(bp)
movb	*4.(si),dx
movb	(si),*3.
mov	ax,*-20.(bp)
mov	cx,*30.
imul	cx
add	ax,si
add	ax,*48.
push	ax
mov	dx,*2.
push	dx
mov	bx,*-26.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,#_amxladd +2(bx)
mov	dx,#_amxladd(bx)
push	ax
push	dx
mov	ax,*-20.(bp)
mov	cx,*18.
imul	cx
add	ax,*-8.(bp)
mov	bx,ax
push	*24.(bx)
call	_amxpoke
add	sp,*10.
mov	dx,si
add	dx,*4.
push	dx
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	*4.(bx)
call	_amxpoke
add	sp,*10.
push	si
mov	dx,*1.
push	dx
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
push	ax
push	dx
mov	bx,*-8.(bp)
push	(bx)
call	_amxpoke
add	sp,*10.
mov	bx,*-12.(bp)
orb	_amxcmd(bx),*1.
push	*-24.(bp)
call	_splx
add	sp,*2.
mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#2.+_amxtout(bx)
cmp	ax,*-16.(bp)
jne	L179
mov	bx,*-26.(bp)
andb	_amxscd(bx),*-3.
j	L180
L179:mov	bx,*-26.(bp)
orb	_amxscd(bx),*2.
mov	bx,*4.(bp)
or	*40.(bx),*1.
mov	bx,*4.(bp)
and	*40.(bx),*-33.
mov	ax,*-26.(bp)
mov	cx,*6.
imul	cx
push	ax
mov	bx,*-10.(bp)
movb	dx,(bx)
and	dx,*127.
pop	bx
movb	4.+_amxtout(bx),dx
L180:|NREG	3
L149:|RTYP	0
br	cret
.globl	_amxioct
.even
_amxioct:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_comd=6.
|	_addr=8.
|	_flag=10.
sub	sp,*14.
|	_tp=-6.
|	_Fbase=-8.
|	_of=-10.
|	_board=-12.
|	_l_num=-14.
|	_s=-16.
|	_unit=-18.
mov	di,*4.(bp)
and	di,*31.
mov	*-18.(bp),di
cmp	_amxdebu,*2.
jl	L184
push	di
mov	di,#L185
push	di
call	_printf
add	sp,*4.
L184:mov	ax,*-18.(bp)
mov	cx,*68.
imul	cx
add	ax,#_amxtty
mov	*-6.(bp),ax
push	*4.(bp)
push	*8.(bp)
push	ax
push	*6.(bp)
call	_ttiocco
add	sp,*8.
test	ax,ax
beq	L186
mov	di,*-18.(bp)
sar	di,*1
sar	di,*1
mov	*-12.(bp),di
mov	di,*-18.(bp)
and	di,*3.
mov	*-14.(bp),di
mov	ax,*-12.(bp)
mov	cx,#142.
imul	cx
add	ax,#_amxfirm
mov	*-8.(bp),ax
mov	ax,*-12.(bp)
mov	cx,*80.
imul	cx
add	ax,#_amxoff
mov	*-10.(bp),ax
cmp	*6.(bp),#29707.
je	L10012
cmp	*6.(bp),#29713.
jne	L187
L10012:push	*4.(bp)
call	_amxpara
add	sp,*2.
L187:mov	di,*-6.(bp)
test	*26.(di),*32.
je	L188
call	_spl7
mov	*-16.(bp),ax
mov	ax,*-14.(bp)
mov	cx,*30.
imul	cx
add	ax,*-8.(bp)
mov	di,ax
movb	*22.(di),*1.
mov	ax,*-14.(bp)
mov	cx,*30.
imul	cx
add	ax,*-8.(bp)
add	ax,*22.
push	ax
mov	di,*1.
push	di
mov	di,*-18.(bp)
sal	di,*1
sal	di,*1
mov	si,#_amxladd +2(di)
mov	di,#_amxladd(di)
push	si
push	di
mov	ax,*-14.(bp)
mov	cx,*18.
imul	cx
add	ax,*-10.(bp)
mov	bx,ax
push	*8.(bx)
call	_amxpoke
add	sp,*10.
push	*-16.(bp)
L20046:call	_splx
add	sp,*2.
j	L183
L188:call	_spl7
mov	*-16.(bp),ax
mov	ax,*-14.(bp)
mov	cx,*30.
imul	cx
add	ax,*-8.(bp)
mov	di,ax
movb	*22.(di),*3.
mov	ax,*-14.(bp)
mov	cx,*30.
imul	cx
add	ax,*-8.(bp)
add	ax,*22.
push	ax
mov	di,*1.
push	di
mov	di,*-18.(bp)
sal	di,*1
sal	di,*1
mov	si,#_amxladd +2(di)
mov	di,#_amxladd(di)
push	si
push	di
mov	ax,*-14.(bp)
mov	cx,*18.
imul	cx
add	ax,*-10.(bp)
mov	bx,ax
push	*8.(bx)
call	_amxpoke
add	sp,*10.
push	*-16.(bp)
j	L20046
L186:movb	111.+_u,*25.
L183:|RTYP	0
br	cret
.globl	_amxbpok
.even
_amxbpok:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_unit=4.
|	_index=6.
|	_cnt=8.
|	_oset=10.
|	_bufp=12.
sub	sp,*8.
|	_byte_rem=-6.
|	_cntl=-8.
|	_s=-10.
|	_temp_cnt=-12.
cmp	_amxdebu,*2.
jl	L194
push	*4.(bp)
mov	di,#L195
push	di
call	_printf
add	sp,*4.
L194:mov	ax,*4.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	di,#4.+_amxobuf(bx)
sub	di,*10.(bp)
mov	*-6.(bp),di
mov	*-8.(bp),*0.
cmp	*8.(bp),di
jle	L196
mov	*-12.(bp),di
mov	*-8.(bp),*1.
j	L197
L196:mov	di,*8.(bp)
mov	*-12.(bp),di
L197:call	_spl7
mov	*-10.(bp),ax
mov	di,*12.(bp)
add	di,*6.(bp)
push	di
push	*-12.(bp)
mov	ax,*4.(bp)
mov	cx,*6.
imul	cx
mov	di,ax
mov	si,#_amxobuf +2(di)
mov	di,#_amxobuf(di)
push	si
push	di
push	*10.(bp)
call	_amxpoke
add	sp,*10.
cmp	*-8.(bp),*0
je	L198
mov	di,*8.(bp)
sub	di,*-12.(bp)
mov	*-6.(bp),di
mov	di,*12.(bp)
add	di,*6.(bp)
add	di,*-12.(bp)
push	di
push	*-6.(bp)
mov	ax,*4.(bp)
mov	cx,*6.
imul	cx
mov	di,ax
mov	si,#_amxobuf +2(di)
mov	di,#_amxobuf(di)
push	si
push	di
sub	di,di
push	di
call	_amxpoke
add	sp,*10.
L198:push	*-10.(bp)
call	_splx
add	sp,*2.
|RTYP	0
br	cret
.globl	_amxpoke
.even
_amxpoke:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_offset=4.
|	_selector=6.
|	_count=10.
|NREG	2
mov	di,*12.(bp)
|	_src_off=di
sub	sp,*18.
|NREG	1
|	_p=si
|	_x=-6.
|	_ds=-8.
|	_c=-10.
|	_i=-12.
|	_s=-14.
|	_temp=-18.
|	_pgnum=-20.
|	_p_offset=-22.
cmp	_amxdebu,*2.
jl	L202
mov	dx,#L203
push	dx
call	_printf
add	sp,*2.
L202:mov	ax,*4.(bp)
cwd
add	ax,*8.(bp)
adc	dx,*6.(bp)
mov	*-16.(bp),ax
mov	*-18.(bp),dx
mov	*-8.(bp),*0.
and	ax,*0.
and	dx,*-1.
test	dx,dx
jne	L10013
test	ax,ax
je	L204
L10013:mov	ax,#2048.
cwd
push	ax
push	dx
push	*-16.(bp)
push	*-18.(bp)
call	lrem
add	sp,*8.
mov	*-6.(bp),ax
mov	ax,#2048.
cwd
push	ax
push	dx
push	*-16.(bp)
push	*-18.(bp)
call	ldiv
add	sp,*8.
mov	*-8.(bp),ax
mov	dx,ax
mov	cx,*7.
sal	dx,cl
mov	*-8.(bp),dx
mov	ax,*-6.(bp)
cwd
mov	*-16.(bp),ax
mov	*-18.(bp),dx
L204:cmp	*-8.(bp),*0
je	L205
mov	*-12.(bp),*0.
L206:mov	dx,*10.(bp)
cmp	*-12.(bp),dx
jge	L211
call	_spl7
mov	*-10.(bp),ax
movb	ax,(di)
cbw
push	ax
inc	di
push	*-16.(bp)
inc	*-16.(bp)
push	*-8.(bp)
call	_setbyte
add	sp,*6.
cmp	_amxdebu,*3.
jl	L209
movb	ax,*-1.(di)
cbw
push	ax
mov	dx,*-16.(bp)
dec	dx
push	dx
push	*-8.(bp)
mov	dx,#L210
push	dx
call	_printf
add	sp,*8.
L209:push	*-10.(bp)
call	_splx
add	sp,*2.
inc	*-12.(bp)
j	L206
L205:mov	si,*-16.(bp)
mov	*-12.(bp),*0.
j	L212
L20048:movb	dx,(di)
movb	(si),dx
inc	si
inc	di
inc	*-12.(bp)
L212:mov	dx,*10.(bp)
cmp	*-12.(bp),dx
jl	L20048
L211:cmp	_amxdebu,*4.
jl	L215
call	_monitor
L215:|NREG	2
|RTYP	0
br	cret
.globl	_amxpeek
.even
_amxpeek:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_offset=4.
|	_selector=6.
|	_count=10.
|NREG	2
mov	di,*12.(bp)
|	_dest_off=di
sub	sp,*18.
|NREG	1
|	_p=si
|	_x=-6.
|	_ds=-8.
|	_c=-10.
|	_i=-12.
|	_s=-14.
|	_temp=-18.
|	_pgnum=-20.
|	_p_offset=-22.
cmp	_amxdebu,*2.
jl	L219
mov	dx,#L220
push	dx
call	_printf
add	sp,*2.
L219:mov	ax,*4.(bp)
cwd
add	ax,*8.(bp)
adc	dx,*6.(bp)
mov	*-16.(bp),ax
mov	*-18.(bp),dx
mov	*-8.(bp),*0.
and	ax,*0.
and	dx,*-1.
test	dx,dx
jne	L10014
test	ax,ax
je	L221
L10014:mov	ax,#2048.
cwd
push	ax
push	dx
push	*-16.(bp)
push	*-18.(bp)
call	lrem
add	sp,*8.
mov	*-6.(bp),ax
mov	ax,#2048.
cwd
push	ax
push	dx
push	*-16.(bp)
push	*-18.(bp)
call	ldiv
add	sp,*8.
mov	*-8.(bp),ax
mov	dx,ax
mov	cx,*7.
sal	dx,cl
mov	*-8.(bp),dx
mov	ax,*-6.(bp)
cwd
mov	*-16.(bp),ax
mov	*-18.(bp),dx
L221:cmp	*-8.(bp),*0
je	L222
mov	*-12.(bp),*0.
L223:mov	dx,*10.(bp)
cmp	*-12.(bp),dx
jge	L228
call	_spl7
mov	*-10.(bp),ax
push	*-16.(bp)
inc	*-16.(bp)
push	*-8.(bp)
call	_getbyte
add	sp,*4.
mov	*-6.(bp),ax
cmp	_amxdebu,*3.
jl	L226
push	ax
mov	dx,*-16.(bp)
dec	dx
push	dx
push	*-8.(bp)
mov	dx,#L227
push	dx
call	_printf
add	sp,*8.
L226:push	*-10.(bp)
call	_splx
add	sp,*2.
mov	dx,*-6.(bp)
movb	(di),dx
inc	di
inc	*-12.(bp)
j	L223
L222:mov	si,*-16.(bp)
mov	*-12.(bp),*0.
j	L229
L20050:movb	dx,(si)
movb	(di),dx
inc	di
inc	si
inc	*-12.(bp)
L229:mov	dx,*10.(bp)
cmp	*-12.(bp),dx
jl	L20050
L228:cmp	_amxdebu,*4.
jl	L232
call	_monitor
L232:|NREG	2
|RTYP	0
br	cret
.comm	_cfreeli,2
.globl	_q_to_b
.even
_q_to_b:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_q=di
|NREG	1
mov	si,*6.(bp)
|	_cp=si
|	_cc=8.
sub	sp,*6.
|	_bp=-6.
|	_s=-8.
|	_acp=-10.
cmp	*8.(bp),*0
jg	L236
L20052:sub	dx,dx
j	L20055
L20054:mov	(di),*0.
sub	dx,dx
mov	*4.(di),dx
mov	*2.(di),dx
push	*-8.(bp)
call	_splx
add	sp,*2.
j	L20052
L236:call	_spl6
mov	*-8.(bp),ax
cmp	(di),*0
jle	L20054
mov	*-10.(bp),si
inc	*8.(bp)
L238:dec	*8.(bp)
je	L239
mov	bx,*2.(di)
movb	dx,(bx)
movb	(si),dx
inc	si
inc	*2.(di)
dec	(di)
jg	L240
mov	dx,*2.(di)
dec	dx
mov	*-6.(bp),dx
and	dx,*-8.
mov	*-6.(bp),dx
sub	dx,dx
mov	*4.(di),dx
mov	*2.(di),dx
mov	bx,*-6.(bp)
mov	dx,_cfreeli
mov	(bx),dx
mov	dx,*-6.(bp)
mov	_cfreeli,dx
L239:push	*-8.(bp)
call	_splx
add	sp,*2.
mov	dx,si
sub	dx,*-10.(bp)
L20055:mov	ax,dx
|RTYP	0
br	cret
L240:test	*2.(di),*7.
jne	L238
mov	dx,*2.(di)
mov	*-6.(bp),dx
sub	*-6.(bp),*8.
mov	bx,*-6.(bp)
mov	dx,(bx)
add	dx,*2.
mov	*2.(di),dx
mov	bx,*-6.(bp)
mov	dx,_cfreeli
mov	(bx),dx
mov	dx,*-6.(bp)
mov	_cfreeli,dx
br	L238
.data
L5:.byte	/61,/6d,/78,/2d,/69,/6e,/69,/74,/20
.byte	/0
L14:.byte	/61,/6d,/78,/2d,/70,/72,/6f,/62,/65
.byte	/20,/0
L22:.byte	/41,/4d,/58,/20,/20,/20,/20,/20,/20
.byte	/42,/61,/73,/65,/64
.byte	/20,/25,/78,/20,/6c,/65,/76,/65,/6c
.byte	/20,/25,/64,/20,/25,/73
.byte	/a,/0
L23:.byte	/66,/6f,/75,/6e,/64,/0
L24:.byte	/4e,/4f,/54,/20,/66,/6f,/75,/6e,/64
.byte	/0
L29:.byte	/61,/6d,/78,/2d,/63,/61,/6c,/20,/0
L40:.byte	/61,/6d,/78,/2d,/70,/61,/72,/61,/6d
.byte	/20,/75,/6e,/69,/74
.byte	/20,/25,/64,/20,/0
L45:.byte	/70,/61,/72,/61,/6d,/20,/77,/61,/69
.byte	/74,/20,/6f,/6e,/20
.byte	/68,/61,/6e,/67,/75,/70,/20,/75,/6e
.byte	/69,/74,/20,/25,/64,/20,/0
L47:.byte	/61,/6d,/78,/2d,/70,/61,/72,/61,/6d
.byte	/20,/68,/61,/6e,/67
.byte	/75,/70,/20,/73,/69,/67,/6e,/61,/6c
.byte	/20,/74,/6f,/20,/75,/6e
.byte	/69,/74,/20,/25,/64,/20,/0
L53:.byte	/70,/61,/72,/61,/6d,/20,/77,/61,/69
.byte	/74,/20,/6f,/6e,/20
.byte	/62,/61,/75,/64,/20,/63,/68,/61,/6e
.byte	/67,/65,/20,/75,/6e,/69
.byte	/74,/20,/25,/64,/20,/0
L55:.byte	/61,/6d,/78,/2d,/70,/61,/72,/61,/6d
.byte	/20,/62,/61,/75,/64
.byte	/20,/72,/61,/74,/65,/20,/63,/68,/61
.byte	/6e,/67,/65,/20,/75,/6e
.byte	/69,/74,/20,/25,/64,/20,/0
L59:.byte	/70,/61,/72,/61,/6d,/20,/77,/61,/69
.byte	/74,/20,/6f,/6e,/20
.byte	/75,/6e,/69,/74,/20,/25,/64,/20,/0
L70:.byte	/61,/6d,/78,/2d,/6f,/70,/65,/6e,/20
.byte	/75,/6e,/69,/74,/20
.byte	/25,/64,/20,/0
L85:.byte	/61,/6d,/78,/2d,/63,/6c,/6f,/73,/65
.byte	/20,/75,/6e,/69,/74
.byte	/20,/25,/64,/20,/0
L91:.byte	/63,/6c,/6f,/73,/65,/20,/77,/61,/69
.byte	/74,/20,/6f,/6e,/20
.byte	/75,/6e,/69,/74,/20,/25,/64,/20,/0
L98:.byte	/61,/6d,/78,/2d,/72,/65,/61,/64,/20
.byte	/6f,/6e,/20,/75,/6e
.byte	/69,/74,/20,/25,/64,/20,/0
L103:.byte	/61,/6d,/78,/2d,/77,/72,/69,/74,/65
.byte	/20,/6f,/6e,/20,/75
.byte	/6e,/69,/74,/20,/25,/64,/20,/0
L114:.byte	/61,/6d,/78,/2d,/69,/6e,/74,/72,/20
.byte	/0
L122:.byte	/73,/74,/61,/74,/75,/73,/20,/25,/64
.byte	/20,/6f,/6e,/20,/75
.byte	/6e,/69,/74,/20,/25,/64,/a,/0
L152:.byte	/61,/6d,/78,/73,/74,/61,/72,/74,/3a
.byte	/20,/63,/61,/6c,/6c
.byte	/65,/64,/20,/6f,/6e,/20,/75,/6e,/69
.byte	/74,/20,/25,/64,/a,/0
L157:.byte	/61,/6d,/78,/2d,/73,/74,/61,/72,/74
.byte	/2c,/20,/6e,/6f,/20
.byte	/74,/69,/6d,/65,/6f,/75,/74,/20,/70
.byte	/65,/6e,/64,/69,/6e,/67
.byte	/a,/0
L161:.byte	/61,/6d,/78,/2d,/73,/74,/61,/72,/74
.byte	/2c,/20,/52,/41,/57
.byte	/20,/6d,/6f,/64,/65,/a,/0
L169:.byte	/61,/6d,/78,/73,/74,/61,/72,/74,/3a
.byte	/20,/73,/63,/61,/6e
.byte	/20,/74,/69,/6d,/65,/6f,/75,/74,/a
.byte	/0
L185:.byte	/61,/6d,/78,/2d,/69,/6f,/63,/74,/6c
.byte	/20,/75,/6e,/69,/74
.byte	/20,/25,/64,/20,/0
L195:.byte	/61,/6d,/78,/2d,/62,/70,/6f,/6b,/65
.byte	/20,/75,/6e,/69,/74
.byte	/20,/25,/64,/20,/0
L203:.byte	/61,/6d,/78,/20,/70,/6f,/6b,/65,/20
.byte	/0
L210:.byte	/a,/64,/73,/3d,/25,/78,/20,/6f,/66
.byte	/66,/73,/65,/74,/3d
.byte	/25,/64,/20,/77,/65,/72,/74,/3d,/25
.byte	/78,/a,/0
L220:.byte	/61,/6d,/78,/20,/70,/65,/65,/6b,/20
.byte	/0
L227:.byte	/a,/64,/73,/3d,/25,/78,/20,/6f,/66
.byte	/66,/73,/65,/74,/3d
.byte	/25,/64,/20,/77,/65,/72,/74,/3d,/25
.byte	/78,/a,/0
