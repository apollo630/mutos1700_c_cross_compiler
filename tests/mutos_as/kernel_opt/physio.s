.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.globl	_physio
.text
.even
_physio:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_strat=4.
|NREG	2
mov	di,*6.(bp)
|	_bp=di
|	_dev=8.
|	_rw=10.
sub	sp,*12.
|NREG	1
|	_base=si
|	_nb=-6.
|	_p=-8.
|	_ts=-10.
|	_a=-12.
|	_b=-16.
mov	si,128.+_u
mov	dx,si
add	dx,130.+_u
cmp	dx,si
blos	L4
mov	dx,si
mov	cx,*11.
shr	dx,cl
mov	*-6.(bp),dx
mov	dx,244.+_u
mov	*-10.(bp),dx
cmpb	333.+_u,*0
je	L5
mov	*-10.(bp),*0.
L5:mov	bx,*-10.(bp)
cmp	*-6.(bp),bx
blt	L4
add	bx,246.+_u
mov	dx,si
add	dx,130.+_u
dec	dx
mov	cx,*11.
shr	dx,cl
cmp	dx,bx
jb	L10000
mov	dx,*32.
sub	dx,248.+_u
cmp	dx,*-6.(bp)
bgt	L4
L10000:mov	dx,246.+_u
add	dx,*-10.(bp)
cmp	dx,*-6.(bp)
jle	L10001
sub	dx,dx
j	L10002
L10001:mov	dx,*1.
L10002:mov	*-12.(bp),dx
mov	bx,120.+_u
mov	*-8.(bp),bx
orb	*21.(bx),*4.
call	_spl6
j	L6
L20001:or	(di),*64.
mov	dx,*21.
push	dx
push	di
call	_sleep
add	sp,*4.
L6:test	(di),*8.
jne	L20001
mov	dx,*10.(bp)
or	dx,*24.
mov	(di),dx
mov	dx,*8.(bp)
mov	*10.(di),dx
mov	bx,*-8.(bp)
mov	dx,*16.(bx)
inc	dx
mov	*-6.(bp),dx
mov	ax,si
sub	dx,dx
push	ax
push	dx
mov	ax,*-6.(bp)
mov	cx,*11.
sal	ax,*1
rcl	dx,*1
loop	.-4
pop	bx
pop	cx
add	ax,cx
adc	dx,bx
mov	*-14.(bp),ax
mov	*-16.(bp),dx
mov	*14.(di),ax
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
mov	cx,*16.
sar	dx,*1
rcr	ax,*1
loop	.-4
cbw
movb	*20.(di),ax
mov	ax,134.+_u
mov	dx,132.+_u
mov	*18.(di),ax
mov	*16.(di),dx
mov	dx,130.+_u
mov	*12.(di),dx
movb	*21.(di),*0.
mov	bx,120.+_u
orb	*1.(bx),*4.
push	di
call	@*4.(bp)
add	sp,*2.
call	_spl6
j	L8
L20003:mov	dx,*20.
push	dx
push	di
call	_sleep
add	sp,*4.
L8:test	(di),*2.
je	L20003
mov	bx,120.+_u
andb	*1.(bx),*-5.
test	(di),*64.
je	L10
push	di
call	_wakeup
add	sp,*2.
L10:call	_spl0
and	(di),*-73.
mov	dx,*22.(di)
mov	130.+_u,dx
push	di
call	_geterro
add	sp,*2.
j	L3
L4:movb	111.+_u,*14.
|NREG	2
L3:|RTYP	0
br	cret
.comm	_swbuf1,26
.comm	_swbuf2,26
.globl	_swbf1
.data
_swbf1:/0
.globl	_swbf2
.data
.data
_swbf2:/0
.globl	_swbwant
.data
.data
_swbwant:/0
.globl	_swap
.text
.even
_swap:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_blkno=4.
|NREG	2
mov	di,*6.(bp)
|	_coreaddr=di
|	_count=8.
|	_rdflg=10.
sub	sp,*2.
|NREG	1
|	_bp=si
|	_tcount=-6.
call	_spl6
j	L14
L20005:cmp	_swbf2,*0
je	L15
mov	_swbwant,*1.
mov	dx,*1.
push	dx
mov	dx,#_swbwant
push	dx
call	_sleep
add	sp,*4.
L14:cmp	_swbf1,*0
jne	L20005
L15:cmp	_swbf1,*0
jne	L18
mov	si,#_swbuf1
inc	_swbf1
j	L19
L18:mov	si,#_swbuf2
inc	_swbf2
L19:mov	ax,*8.(bp)
sal	ax,*1
cwd
add	220.+_io_info,ax
adc	218.+_io_info,dx
mov	dx,di
mov	cx,*11.
sal	dx,cl
mov	*14.(si),dx
mov	ax,di
cwd
mov	cx,*11.
sal	ax,*1
rcl	dx,*1
loop	.-4
mov	cx,*16.
sar	dx,*1
rcr	ax,*1
loop	.-4
movb	*20.(si),ax
mov	dx,*8.(bp)
mov	cx,*11.
sal	dx,cl
mov	*12.(si),dx
mov	ax,#1024.
cwd
push	ax
push	dx
mov	ax,*4.(bp)
cwd
add	ax,2.+_swplo
adc	dx,_swplo
push	ax
push	dx
call	lmul
add	sp,*8.
mov	*18.(si),ax
mov	*16.(si),dx
mov	dx,*10.(bp)
or	dx,*24.
mov	(si),dx
mov	dx,_swapdev
mov	*10.(si),dx
push	si
mov	bx,_swapdev
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@4.+_bdevsw(bx)
add	sp,*2.
call	_spl6
j	L20
L20007:sub	dx,dx
push	dx
push	si
call	_sleep
add	sp,*4.
L20:test	(si),*2.
je	L20007
test	(si),*4.
je	L22
mov	dx,#L23
push	dx
call	_panic
add	sp,*2.
L22:cmp	si,#_swbuf1
jne	L24
mov	_swbf1,*0.
j	L25
L24:mov	_swbf2,*0.
L25:cmp	_swbwant,*0
je	L26
mov	_swbwant,*0.
mov	dx,#_swbwant
push	dx
call	_wakeup
add	sp,*2.
L26:and	(si),*-73.
call	_spl0
|NREG	2
|RTYP	0
br	cret
.globl	_swapin
.even
_swapin:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_p=di
sub	sp,*16.
|NREG	1
|	_xp=si
|	_x=-6.
|	_a=-8.
|	_pp=-10.
|	_s=-12.
|	_ssize=-14.
|	_mw=-16.
|	_a1=-18.
|	_a2=-20.
mov	dx,*33.
push	dx
push	*18.(di)
call	_max
add	sp,*4.
push	ax
mov	dx,#_coremap
push	dx
call	_malloc
add	sp,*4.
mov	*-8.(bp),ax
test	ax,ax
jne	L30
L20009:sub	dx,dx
br	L20008
L30:mov	dx,*26.(di)
mov	si,dx
test	dx,dx
beq	L31
push	si
call	_xlock
add	sp,*2.
cmpb	*9.(si),*0
bne	L32
push	*4.(si)
mov	dx,#_coremap
push	dx
call	_malloc
add	sp,*4.
mov	*-6.(bp),ax
test	ax,ax
jne	L33
push	si
call	_xunlock
add	sp,*2.
push	*-8.(bp)
mov	dx,*33.
push	dx
push	*18.(di)
call	_max
add	sp,*4.
push	ax
mov	dx,#_coremap
push	dx
call	_mfree
add	sp,*6.
j	L20009
L33:mov	dx,*-6.(bp)
mov	*2.(si),dx
movb	dx,*10.(si)
and	dx,*4.
jne	L32
mov	dx,*1.
push	dx
push	*4.(si)
push	*2.(si)
push	(si)
call	_swap
add	sp,*8.
cmp	_Bigswap,*0
jne	L32
push	(si)
mov	dx,*4.(si)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
mov	(si),*0.
L32:incb	*9.(si)
push	si
call	_xunlock
add	sp,*2.
L31:mov	dx,*1.
push	dx
push	*22.(di)
push	*-8.(bp)
push	*16.(di)
call	_swap
add	sp,*8.
cmp	*18.(di),*33.
jg	L36
mov	dx,*18.(di)
sub	dx,*22.(di)
mov	*-14.(bp),dx
mov	dx,*1.
push	dx
push	*-14.(bp)
mov	dx,*-8.(bp)
add	dx,*33.
sub	dx,*-14.(bp)
push	dx
mov	dx,*22.(di)
sal	dx,*1
add	dx,*16.(di)
push	dx
call	_swap
add	sp,*8.
L36:push	*16.(di)
mov	dx,*18.(di)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
mov	dx,*-8.(bp)
mov	*16.(di),dx
orb	*1.(di),#129.
movb	*3.(di),*0.
mov	dx,*1.
L20008:mov	ax,dx
|RTYP	0
br	cret
.globl	_xswap
.even
_xswap:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_p=di
|	_ff=6.
|	_os=8.
sub	sp,*4.
|NREG	1
|	_a=si
|	_size=-6.
|	_ssize=-8.
cmp	*8.(bp),*0
jne	L40
mov	dx,*18.(di)
mov	*8.(bp),dx
L40:mov	dx,*18.(di)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_malloc
add	sp,*4.
mov	si,ax
cmp	si,*0
jne	L41
mov	dx,#L42
push	dx
call	_panic
add	sp,*2.
L41:orb	*1.(di),*4.
push	*26.(di)
call	_xccdec
add	sp,*2.
sub	dx,dx
push	dx
push	*22.(di)
push	*16.(di)
push	si
call	_swap
add	sp,*8.
cmp	*18.(di),*33.
jg	L43
mov	dx,*18.(di)
sub	dx,*22.(di)
mov	*-8.(bp),dx
sub	dx,dx
push	dx
push	*-8.(bp)
mov	dx,*16.(di)
add	dx,*33.
sub	dx,*-8.(bp)
push	dx
mov	dx,*22.(di)
sal	dx,*1
add	dx,si
push	dx
call	_swap
add	sp,*8.
L43:cmp	*6.(bp),*0
je	L44
push	*16.(di)
mov	dx,*33.
push	dx
push	*8.(bp)
call	_max
add	sp,*4.
push	ax
mov	dx,#_coremap
push	dx
call	_mfree
add	sp,*6.
L44:mov	*16.(di),si
andb	*1.(di),*-6.
movb	*3.(di),*0.
cmpb	_runout,*0
je	L45
movb	_runout,*0.
mov	dx,#_runout
push	dx
call	_wakeup
add	sp,*2.
L45:|NREG	2
|RTYP	0
br	cret
.data
L23:.byte	/49,/4f,/20,/65,/72,/72,/20,/69,/6e
.byte	/20,/73,/77,/61,/70,/0
L42:.byte	/6f,/75,/74,/20,/6f,/66,/20,/73,/77
.byte	/61,/70,/20,/73,/70
.byte	/61,/63,/65,/0
