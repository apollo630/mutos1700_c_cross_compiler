.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.globl	_pipe
.text
.even
_pipe:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*4.
|NREG	2
|	_ip=di
|NREG	1
|	_rf=si
|	_wf=-6.
|	_r=-8.
push	_pipedev
call	_ialloc
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L3
call	_falloc
mov	si,ax
cmp	si,*0
jne	L4
push	di
j	L20001
L20003:mov	*2.(si),*0.
mov	bx,*-8.(bp)
sal	bx,*1
mov	174.+_u(bx),*0.
push	di
L20001:call	_iput
add	sp,*2.
j	L3
L4:mov	dx,124.+_u
mov	*-8.(bp),dx
call	_falloc
mov	*-6.(bp),ax
cmp	ax,*0
je	L20003
mov	dx,124.+_u
mov	126.+_u,dx
mov	dx,*-8.(bp)
mov	124.+_u,dx
mov	bx,ax
movb	(bx),*6.
mov	bx,*-6.(bp)
mov	*4.(bx),di
movb	(si),*5.
mov	*4.(si),di
movb	*1.(di),*2.
mov	*6.(di),#-32768.
movb	(di),*70.
|NREG	3
L3:|RTYP	0
br	cret
.globl	_readp
.even
_readp:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_fp=di
|NREG	1
|	_ip=si
mov	si,*4.(di)
j	L9
L20005:cmp	*16.(si),*0.
jne	L10
push	si
call	_prele
add	sp,*2.
cmpb	*1.(si),*2.
blt	L8
or	*6.(si),#256.
mov	dx,*26.
push	dx
mov	dx,si
add	dx,*2.
push	dx
call	_sleep
add	sp,*4.
L9:push	si
call	_plock
add	sp,*2.
cmp	*14.(si),*0
je	L20005
L10:mov	ax,*8.(di)
mov	dx,*6.(di)
mov	134.+_u,ax
mov	132.+_u,dx
push	si
call	_readi
add	sp,*2.
mov	ax,134.+_u
mov	dx,132.+_u
mov	*8.(di),ax
mov	*6.(di),dx
mov	ax,*8.(di)
mov	dx,*6.(di)
cmp	dx,*14.(si)
jne	L11
cmp	ax,*16.(si)
jne	L11
sub	ax,ax
cwd
mov	*8.(di),ax
mov	*6.(di),dx
cwd
mov	*16.(si),ax
mov	*14.(si),dx
test	*6.(si),#128.
je	L11
and	*6.(si),#-129.
mov	dx,si
inc	dx
push	dx
call	_wakeup
add	sp,*2.
L11:push	si
call	_prele
add	sp,*2.
|NREG	2
L8:|RTYP	0
br	cret
.globl	_writep
.even
_writep:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_fp=di
sub	sp,*2.
|NREG	1
|	_c=si
|	_ip=-6.
mov	dx,*4.(di)
mov	*-6.(bp),dx
mov	si,130.+_u
L16:push	*-6.(bp)
call	_plock
add	sp,*2.
cmp	si,*0
jne	L17
push	*-6.(bp)
call	_prele
add	sp,*2.
mov	130.+_u,*0.
j	L15
L20007:push	*-6.(bp)
call	_prele
add	sp,*2.
movb	111.+_u,*32.
mov	dx,*13.
push	dx
push	120.+_u
call	_psignal
add	sp,*4.
L15:|RTYP	0
br	cret
L17:mov	dx,*-6.(bp)
mov	bx,dx
cmpb	*1.(bx),*2.
jl	L20007
mov	bx,dx
cmp	*14.(bx),*0
jl	L19
jg	L10000
cmp	*14.+2(bx),#4096.
jb	L19
L10000:mov	bx,*-6.(bp)
or	*6.(bx),#128.
push	*-6.(bp)
call	_prele
add	sp,*2.
mov	dx,*26.
push	dx
mov	dx,*-6.(bp)
inc	dx
push	dx
call	_sleep
add	sp,*4.
br	L16
L19:mov	bx,*-6.(bp)
mov	ax,*14. +2(bx)
mov	dx,*14.(bx)
mov	134.+_u,ax
mov	132.+_u,dx
mov	dx,#4096.
push	dx
push	si
call	_min
add	sp,*4.
mov	130.+_u,ax
sub	si,ax
push	*-6.(bp)
call	_writei
add	sp,*2.
push	*-6.(bp)
call	_prele
add	sp,*2.
mov	dx,*-6.(bp)
mov	bx,dx
test	*6.(bx),#256.
beq	L16
mov	bx,dx
and	*6.(bx),#-257.
mov	dx,*-6.(bp)
add	dx,*2.
push	dx
call	_wakeup
add	sp,*2.
br	L16
.globl	_plock
.even
_plock:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
j	L24
L20009:orb	(di),*16.
mov	si,*10.
push	si
push	di
call	_sleep
add	sp,*4.
L24:movb	dx,(di)
and	dx,*1.
jne	L20009
orb	(di),*1.
|RTYP	0
br	cret
.globl	_prele
.even
_prele:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
andb	(di),*-2.
movb	dx,(di)
and	dx,*16.
je	L28
andb	(di),*-17.
push	di
call	_wakeup
add	sp,*2.
L28:|RTYP	0
br	cret
.data
