.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_io_info,222
.globl	_fpp
.data
_fpp:/0
.globl	_main
.text
.even
_main:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_p=4.
cmp	_Stand,*0
jne	L4
mov	di,#L5
push	di
call	_printf
add	sp,*2.
L4:cmp	_fpp,*0
je	L6
mov	di,#L7
push	di
call	_printf
add	sp,*2.
L6:call	_initpic
push	*4.(bp)
call	_startup
add	sp,*2.
push	*4.(bp)
call	_generic
add	sp,*2.
cmp	_Stand,*0
jne	L8
mov	ax,#1024.
cwd
push	ax
push	dx
mov	ax,_maxmem
cwd
mov	di,dx
mov	si,ax
mov	cx,*11.
sal	si,*1
rcl	di,*1
loop	.-4
push	si
push	di
call	ldiv
add	sp,*8.
push	ax
push	dx
mov	di,#_Mmsg
push	di
mov	di,#L9
push	di
call	_printf
add	sp,*8.
L8:mov	di,*1.
push	di
push	_nswap
mov	di,#_swapmap
push	di
call	_mfree
add	sp,*6.
sub	2.+_swplo,*1.
sbb	_swplo,*0
movb	_proc,*3.
orb	1.+_proc,*3.
movb	5.+_proc,*20.
mov	di,*1.
push	di
mov	di,#_coremap
push	di
call	_malloc
add	sp,*4.
mov	16.+_proc,ax
mov	18.+_proc,*1.
mov	22.+_proc,*1.
mov	120.+_u,#_proc
mov	di,_Cmask
mov	396.+_u,di
mov	di,*2.
mov	_Nproc,di
mov	cx,*5.
sal	di,cl
add	di,#_proc
mov	_Nproca,di
call	_cinit
call	_binit
call	_spl7
call	_initpic
call	_clkstar
sub	di,di
push	di
call	_tasktim
add	sp,*2.
call	_iinit
mov	di,*1.
push	di
push	_swapdev
mov	di,_swapdev
mov	cx,*8.
sar	di,cl
mov	cx,*3.
sal	di,cl
call	@_bdevsw(di)
add	sp,*4.
mov	di,*2.
push	di
push	_rootdev
call	_iget
add	sp,*4.
mov	_rootdir,ax
mov	bx,ax
andb	(bx),*-2.
mov	di,*2.
push	di
push	_rootdev
call	_iget
add	sp,*4.
mov	136.+_u,ax
mov	bx,ax
andb	(bx),*-2.
mov	138.+_u,*0.
call	_update
call	_newproc
test	ax,ax
je	L10
mov	di,_szicode
add	di,#2047.
mov	cx,*11.
sar	di,cl
inc	di
push	di
call	_expand
add	sp,*2.
mov	244.+_u,*0.
mov	di,_szicode
add	di,#2047.
mov	cx,*11.
shr	di,cl
mov	246.+_u,di
mov	248.+_u,*1.
mov	di,_szicode
sal	di,*1
push	di
sub	di,di
push	di
mov	di,#_icode
push	di
call	_copyout
add	sp,*6.
mov	ax,48.+_proc
inc	ax
mov	cx,*7.
sal	ax,cl
j	L3
L10:call	_sched
L3:|RTYP	0
br	cret
.comm	_bootime,4
.globl	_iinit
.even
_iinit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_cp=di
|NREG	1
|	_bp=si
|	_fp=-6.
L14:movb	111.+_u,*0.
mov	dx,*1.
push	dx
push	_rootdev
mov	bx,_rootdev
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@_bdevsw(bx)
add	sp,*4.
mov	ax,*1.
cwd
push	ax
push	dx
push	_rootdev
call	_bread
add	sp,*6.
mov	si,ax
call	_geteblk
mov	di,ax
cmpb	111.+_u,*0
je	L15
cmp	_Stand,*0
je	L16
mov	dx,*45.
mov	_pipedev,dx
mov	_rootdev,dx
j	L14
L16:mov	dx,#L18
push	dx
call	_panic
add	sp,*2.
L15:mov	dx,#642.
push	dx
push	*14.(di)
push	*14.(si)
call	_bcopy
add	sp,*6.
push	si
call	_brelse
add	sp,*2.
mov	2.+_mount,di
mov	dx,_rootdev
mov	_mount,dx
mov	bx,*14.(di)
mov	*-6.(bp),bx
movb	*610.(bx),*0.
mov	bx,*-6.(bp)
movb	*611.(bx),*0.
mov	bx,*-6.(bp)
mov	dx,_ROroot
movb	*613.(bx),dx
mov	bx,*-6.(bp)
cmpb	*640.(bx),*70.
je	L19
call	_icodech
mov	bx,*-6.(bp)
movb	*640.(bx),*-71.
j	L20
L19:mov	bx,*-6.(bp)
movb	*640.(bx),*0.
L20:mov	bx,*-6.(bp)
incb	*612.(bx)
mov	bx,*-6.(bp)
mov	ax,#614. +2(bx)
mov	dx,#614.(bx)
mov	2.+_time,ax
mov	_time,dx
mov	2.+_bootime,ax
mov	_bootime,dx
|NREG	3
|RTYP	0
br	cret
.globl	_binit
.even
_binit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*4.
|NREG	2
|	_bp=di
|NREG	1
|	_dp=si
|	_i=-6.
|	_bdp=-8.
mov	dx,#_bfreeli
mov	8.+_bfreeli,dx
mov	6.+_bfreeli,dx
mov	4.+_bfreeli,dx
mov	2.+_bfreeli,dx
mov	*-6.(bp),*0.
j	L24
L20001:mov	ax,*-6.(bp)
mov	cx,*26.
imul	cx
mov	di,ax
add	di,#_buf
mov	*10.(di),*-1.
mov	dx,*-6.(bp)
mov	cx,*10.
sal	dx,cl
add	dx,#_buffers
mov	*14.(di),dx
mov	*4.(di),#_bfreeli
mov	dx,2.+_bfreeli
mov	*2.(di),dx
mov	bx,2.+_bfreeli
mov	*4.(bx),di
mov	2.+_bfreeli,di
mov	(di),*8.
push	di
call	_brelse
add	sp,*2.
inc	*-6.(bp)
L24:mov	dx,_v
cmp	*-6.(bp),dx
jl	L20001
mov	*-8.(bp),#_bdevsw
j	L27
L20003:mov	bx,*-8.(bp)
mov	si,*6.(bx)
cmp	si,*0
je	L29
mov	*2.(si),si
mov	*4.(si),si
L29:add	*-8.(bp),*8.
L27:mov	dx,_nblkdev
mov	cx,*3.
sal	dx,cl
add	dx,#_bdevsw
cmp	dx,*-8.(bp)
ja	L20003
|NREG	3
|RTYP	0
br	cret
.globl	_copyseg
.even
_copyseg:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_from=4.
|	_to=6.
mov	di,*2.
push	di
mov	di,*6.(bp)
sal	di,*1
push	di
mov	di,*4.(bp)
sal	di,*1
push	di
call	_copyblo
add	sp,*6.
|RTYP	0
br	cret
.data
L5:.byte	/a,/44,/65,/76,/69,/63,/65,/20,/69
.byte	/6e,/69,/74,/69,/61
.byte	/6c,/69,/7a,/61,/74,/69,/6f,/6e,/3a
.byte	/a,/a,/0
L7:.byte	/4e,/44,/50,/20,/20,/20,/20,/20,/20
.byte	/66,/6f,/75,/6e,/64
.byte	/a,/0
L9:.byte	/25,/73,/20,/6d,/65,/6d,/20,/3d,/20
.byte	/25,/44,/6b,/a,/0
L18:.byte	/69,/69,/6e,/69,/74,/0
