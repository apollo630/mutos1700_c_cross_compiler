.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_ipc,8
.globl	_signal
.text
.even
_signal:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_pgrp=di
|	_sig=6.
|NREG	1
|	_p=si
cmp	di,*0
je	L3
mov	si,#_proc
j	L4
L20001:cmp	*10.(si),di
jne	L6
push	*6.(bp)
push	si
call	_psignal
add	sp,*4.
L6:add	si,*32.
L4:cmp	si,_Nproca
jb	L20001
|NREG	2
L3:|RTYP	0
br	cret
.globl	_psignal
.even
_psignal:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_p=di
|NREG	1
mov	si,*6.(bp)
|	_sig=si
cmp	si,*17.
jae	L10
cmp	si,*0
je	L11
mov	cx,si
dec	cx
mov	dx,*1.
sal	dx,cl
or	*6.(di),dx
L11:cmpb	*2.(di),*50.
jle	L12
movb	*2.(di),*50.
L12:cmpb	(di),*1.
jne	L10
cmpb	*2.(di),*25.
jle	L10
push	di
call	_setrun
add	sp,*2.
L10:|RTYP	0
br	cret
.globl	_issig
.even
_issig:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_n=di
|NREG	1
|	_p=si
mov	si,120.+_u
j	L17
L20003:push	si
call	_fsig
add	sp,*2.
mov	di,ax
mov	bx,di
sal	bx,*1
cmp	272.+_u(bx),*1.
jne	L10000
movb	dx,*1.(si)
and	dx,*16.
je	L19
L10000:mov	dx,di
j	L20004
L19:mov	cx,di
dec	cx
mov	dx,*1.
sal	dx,cl
not	dx
and	*6.(si),dx
L17:cmp	*6.(si),*0
jne	L20003
sub	dx,dx
L20004:mov	ax,dx
|RTYP	0
br	cret
.globl	_stop
.even
_stop:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_pp=di
|NREG	1
|	_cp=si
j	L23
L20006:push	di
call	_wakeup
add	sp,*2.
movb	(si),*6.
call	_swtch
movb	dx,*1.(si)
and	dx,*16.
je	L22
call	_procxmt
test	ax,ax
jne	L22
L23:mov	si,120.+_u
cmp	*14.(si),*1.
je	L24
mov	di,#_proc
j	L25
L20008:mov	dx,*14.(si)
cmp	*12.(di),dx
je	L20006
add	di,*32.
L25:cmp	di,_Nproca
jb	L20008
L24:push	120.+_u
call	_fsig
add	sp,*2.
push	ax
call	_exit
add	sp,*2.
|NREG	3
L22:|RTYP	0
br	cret
.globl	_psig
.even
_psig:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_n=di
|NREG	1
|	_p=si
|	_rp=-6.
mov	dx,120.+_u
mov	*-6.(bp),dx
cmp	12.+_u,*0
jne	L32
mov	dx,#14.+_u
push	dx
call	_savfp
add	sp,*2.
mov	12.+_u,*1.
L32:mov	bx,*-6.(bp)
movb	dx,*1.(bx)
and	dx,*16.
je	L33
call	_stop
L33:push	*-6.(bp)
call	_fsig
add	sp,*2.
mov	di,ax
cmp	di,*0
beq	L31
mov	bx,*-6.(bp)
mov	cx,di
dec	cx
mov	dx,*1.
sal	dx,cl
not	dx
and	*6.(bx),dx
mov	bx,di
sal	bx,*1
mov	dx,#272.+_u(bx)
mov	si,dx
test	dx,dx
je	L34
movb	111.+_u,*0.
cmp	di,*4.
je	L35
cmp	di,*5.
je	L35
mov	bx,di
sal	bx,*1
mov	272.+_u(bx),*0.
L35:push	di
push	si
call	_sendsig
add	sp,*4.
j	L31
L34:mov	bx,di
sub	bx,#/3
cmp	bx,*9.
ja	L36
sal	bx,#1
seg	cs
br	@L10002(bx)
L46:call	_core
test	ax,ax
je	L36
add	di,#128.
j	L36
L10002:L46
L46
L46
L46
L46
L46
L36
L46
L46
L46
L36:push	di
call	_exit
add	sp,*2.
|NREG	3
L31:|RTYP	0
br	cret
.globl	_fsig
.even
_fsig:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_p=4.
|NREG	2
|	_n=di
|NREG	1
|	_i=si
mov	bx,*4.(bp)
mov	di,*6.(bx)
mov	si,*1.
L20010:test	di,*1.
je	L54
mov	dx,si
j	L20011
L54:sar	di,*1
inc	si
cmp	si,*17.
jl	L20010
sub	dx,dx
L20011:mov	ax,dx
|RTYP	0
br	cret
.globl	_core
.even
_core:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_ip=di
|NREG	1
|	_s=si
|	_t=-6.
movb	111.+_u,*0.
mov	154.+_u,#L58
mov	dx,*1.
push	dx
mov	dx,#_schar
push	dx
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
jne	L59
cmpb	111.+_u,*0
je	L60
L20012:sub	dx,dx
br	L10004
L60:mov	dx,#438.
push	dx
call	_maknode
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L20012
L59:mov	dx,#128.
push	dx
push	di
call	_access
add	sp,*4.
test	ax,ax
bne	L62
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#-32768.
bne	L62
mov	dx,116.+_u
cmp	112.+_u,dx
bne	L62
push	di
call	_itrunc
add	sp,*2.
sub	ax,ax
cwd
mov	134.+_u,ax
mov	132.+_u,dx
mov	128.+_u,#_u
mov	130.+_u,#2048.
movb	110.+_u,*1.
push	di
call	_writei
add	sp,*2.
mov	bx,120.+_u
mov	si,*22.(bx)
dec	si
mov	128.+_u,*0.
mov	dx,si
mov	cx,*11.
sal	dx,cl
mov	130.+_u,dx
movb	110.+_u,*0.
cmp	130.+_u,*0
jne	L63
cmp	si,*0
je	L63
mov	dx,si
shr	dx,*1
mov	*-6.(bp),dx
mov	cx,*11.
sal	dx,cl
mov	130.+_u,dx
push	di
call	_writei
add	sp,*2.
mov	dx,*-6.(bp)
mov	cx,*11.
sal	dx,cl
mov	128.+_u,dx
mov	dx,si
sub	dx,*-6.(bp)
mov	cx,*11.
sal	dx,cl
mov	130.+_u,dx
L63:push	di
call	_writei
add	sp,*2.
push	120.+_u
mov	bx,120.+_u
mov	si,*18.(bx)
pop	bx
sub	si,*22.(bx)
mov	dx,*32.
sub	dx,si
mov	cx,*11.
sal	dx,cl
mov	128.+_u,dx
mov	dx,si
mov	cx,*11.
sal	dx,cl
mov	130.+_u,dx
push	di
call	_writei
add	sp,*2.
L62:push	di
call	_iput
add	sp,*2.
cmpb	111.+_u,*0
bne	L20012
mov	dx,*1.
L10004:mov	ax,dx
|RTYP	0
br	cret
.globl	_ptrace
.even
_ptrace:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_p=di
|NREG	1
|	_uap=si
mov	si,122.+_u
cmp	*6.(si),*0
jg	L67
mov	bx,120.+_u
orb	*1.(bx),*16.
br	L66
L67:mov	di,#_proc
j	L68
L20014:cmpb	(di),*6.
jne	L70
mov	dx,*2.(si)
cmp	*12.(di),dx
jne	L70
mov	bx,120.+_u
mov	dx,*12.(bx)
cmp	dx,*14.(di)
je	L72
L70:add	di,*32.
L68:cmp	di,_Nproca
jb	L20014
movb	111.+_u,*3.
br	L66
L72:cmp	_ipc,*0
je	L73
mov	dx,*25.
push	dx
mov	dx,#_ipc
push	dx
call	_sleep
add	sp,*4.
j	L72
L73:mov	dx,*12.(di)
mov	_ipc,dx
mov	dx,(si)
mov	6.+_ipc,dx
mov	dx,*4.(si)
mov	4.+_ipc,dx
mov	dx,*6.(si)
mov	2.+_ipc,dx
andb	*1.(di),*-33.
push	di
call	_setrun
add	sp,*2.
j	L74
L20016:mov	dx,*25.
push	dx
mov	dx,#_ipc
push	dx
call	_sleep
add	sp,*4.
L74:cmp	2.+_ipc,*0
jg	L20016
mov	dx,6.+_ipc
mov	124.+_u,dx
cmp	2.+_ipc,*0
jge	L76
movb	111.+_u,*5.
L76:mov	_ipc,*0.
mov	dx,#_ipc
push	dx
call	_wakeup
add	sp,*2.
|NREG	3
L66:|RTYP	0
br	cret
.globl	_procxmt
.even
_procxmt:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_i=di
|NREG	1
|	_p=si
|	_xp=-6.
mov	bx,120.+_u
mov	dx,*12.(bx)
cmp	dx,_ipc
bne	L81
mov	di,2.+_ipc
mov	2.+_ipc,*0.
mov	dx,#_ipc
push	dx
call	_wakeup
add	sp,*2.
mov	bx,di
sub	bx,#/1
cmp	bx,*8.
bhi	L85
sal	bx,#1
seg	cs
br	@L10008(bx)
L83:cmpb	333.+_u,*0
je	L86
push	4.+_ipc
call	_fuibyte
add	sp,*2.
cmp	ax,*-1.
beq	L85
push	4.+_ipc
call	_fuiword
L20021:add	sp,*2.
mov	6.+_ipc,ax
br	L81
L86:push	4.+_ipc
call	_fubyte
add	sp,*2.
cmp	ax,*-1.
beq	L85
push	4.+_ipc
call	_fuword
j	L20021
L87:mov	di,4.+_ipc
cmp	di,*0
blt	L85
cmp	di,#2048.
bge	L85
mov	bx,di
sar	bx,*1
sal	bx,*1
mov	dx,#_u(bx)
mov	6.+_ipc,dx
br	L81
L88:cmpb	333.+_u,*0
je	L92
mov	bx,120.+_u
mov	dx,*26.(bx)
mov	*-6.(bp),dx
test	dx,dx
je	L90
mov	bx,dx
cmpb	*8.(bx),*1.
bne	L85
mov	bx,dx
mov	bx,*6.(bx)
test	*6.(bx),#512.
bne	L85
mov	bx,dx
mov	bx,*6.(bx)
andb	(bx),*-33.
L90:sub	dx,dx
push	dx
push	4.+_ipc
call	_suiword
add	sp,*4.
mov	di,ax
push	6.+_ipc
push	4.+_ipc
call	_suiword
add	sp,*4.
cmp	di,*0
blt	L85
cmp	*-6.(bp),*0
beq	L81
mov	bx,*-6.(bp)
orb	*10.(bx),*2.
br	L81
L92:sub	dx,dx
push	dx
push	4.+_ipc
call	_suword
add	sp,*4.
test	ax,ax
blt	L85
push	6.+_ipc
push	4.+_ipc
call	_suword
add	sp,*4.
br	L81
L93:mov	di,4.+_ipc
mov	si,di
sar	si,*1
sal	si,*1
add	si,#_u
cmp	si,#14.+_u
jb	L10006
cmp	si,#206.+_u
jb	L94
L10006:sub	di,di
j	L95
L20018:inc	di
L95:cmp	di,_reglocc
bge	L85
movb	ax,_regloc(di)
cbw
sal	ax,*1
add	ax,322.+_u
cmp	ax,si
jne	L20018
L94:mov	dx,6.+_ipc
mov	(si),dx
br	L81
L98:mov	bx,322.+_u
mov	dx,*16.(bx)
add	dx,*12.
push	dx
call	_fuword
add	sp,*2.
or	ax,#256.
push	ax
mov	bx,322.+_u
mov	dx,*16.(bx)
add	dx,*12.
push	dx
call	_suword
add	sp,*4.
L99:cmp	4.+_ipc,*1.
je	L100
push	4.+_ipc
mov	bx,322.+_u
mov	dx,*16.(bx)
add	dx,*8.
push	dx
call	_suword
add	sp,*4.
L100:mov	bx,120.+_u
mov	*6.(bx),*0.
cmp	6.+_ipc,*0
je	L101
push	6.+_ipc
push	120.+_u
call	_psignal
add	sp,*4.
L101:mov	dx,*1.
j	L20019
L102:push	120.+_u
call	_fsig
add	sp,*2.
push	ax
call	_exit
add	sp,*2.
L85:mov	2.+_ipc,*-1.
j	L81
L10008:L83
L86
L87
L88
L92
L93
L99
L102
L98
L81:sub	dx,dx
L20019:mov	ax,dx
|RTYP	0
br	cret
.data
L58:.byte	/63,/6f,/72,/65,/0
