.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.bss
_identDe:.blkb	512.
_devinfo:.blkb	12.
_hd:.blkb	14.
_part:.blkb	80.
.comm	_rhdbuf,26
.comm	_hdtab,26
.data
_currUni:/ffff
.data
_haveV30:/0
.globl	_hdinit
.text
.even
_hdinit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|	_valPA=-6.
mov	di,*4.
push	di
mov	di,#-8148.
push	di
call	_outb
add	sp,*4.
call	_delay
sub	di,di
push	di
mov	di,#-8148.
push	di
call	_outb
add	sp,*4.
call	_delay
mov	_hd,*0.
call	_isNEC
test	ax,ax
je	L4
sub	di,di
push	di
call	_ident
add	sp,*2.
mov	ax,_hd
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#_devinfo(bx)
mov	_haveV30,ax
L4:cmp	_Stand,*0
jne	L5
cmp	_haveV30,*0
je	L10000
mov	di,#L7
push	di
j	L10001
L10000:mov	di,#L8
push	di
L10001:mov	di,#-8192.
push	di
mov	di,#L6
push	di
call	_printf
add	sp,*6.
cmp	_haveV30,*0
je	L5
mov	_hd,*0.
j	L10
L20001:cmp	_hd,*0
je	L10002
mov	di,#L14
push	di
j	L10003
L10002:mov	di,#L15
push	di
L10003:mov	di,#L13
push	di
call	_printf
add	sp,*4.
mov	di,*1.
push	di
call	_ident
add	sp,*2.
inc	_hd
L10:cmp	_hd,*2.
jl	L20001
mov	_hd,*0.
L5:cmp	_haveV30,*0
je	L3
mov	di,#200.
push	di
call	_inb
add	sp,*2.
mov	*-6.(bp),ax
and	ax,#239.
mov	*-6.(bp),ax
mov	di,#132.
push	di
mov	di,#206.
push	di
call	_outb
add	sp,*4.
push	*-6.(bp)
mov	di,#200.
push	di
call	_outb
add	sp,*4.
mov	di,#148.
push	di
mov	di,#206.
push	di
call	_outb
add	sp,*4.
L3:|RTYP	0
br	cret
.globl	_hdopen
.even
_hdopen:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*6.
|NREG	2
|	_unit=di
|	_bp=-6.
|	_peptr=-8.
|	_i=-10.
cmp	_haveV30,*0
jne	L20
L20006:movb	111.+_u,*6.
br	L19
L20:mov	di,*4.(bp)
and	di,*8.
mov	cx,*3.
sar	di,cl
cmp	di,*1.
jne	L21
mov	_hd,di
mov	_currUni,*-1.
sub	si,si
push	si
call	_ident
add	sp,*2.
L21:mov	ax,di
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#_devinfo(bx)
test	ax,ax
je	L20006
cmp	di,*0
jne	L10004
mov	ax,di
mov	cx,*40.
imul	cx
mov	si,ax
mov	dx,#4.+_part +2(si)
mov	si,#4.+_part(si)
test	si,si
bne	L19
test	dx,dx
bne	L19
L10004:mov	*-10.(bp),*0.
L20003:sub	ax,ax
cwd
push	ax
mov	si,dx
mov	ax,di
mov	cx,*5.
imul	cx
add	ax,*-10.(bp)
mov	cx,*3.
sal	ax,cl
mov	bx,ax
pop	dx
mov	_part+2(bx),dx
mov	_part(bx),si
sub	ax,ax
cwd
push	ax
mov	si,dx
mov	ax,di
mov	cx,*5.
imul	cx
add	ax,*-10.(bp)
mov	cx,*3.
sal	ax,cl
mov	bx,ax
pop	dx
mov	4.+_part+2(bx),dx
mov	4.+_part(bx),si
inc	*-10.(bp)
cmp	*-10.(bp),*4.
jle	L20003
mov	ax,di
mov	cx,*6.
imul	cx
mov	si,ax
mov	dx,#2.+_devinfo +2(si)
mov	si,#2.+_devinfo(si)
push	dx
mov	ax,di
mov	cx,*40.
imul	cx
mov	bx,ax
pop	dx
mov	4.+_part+2(bx),dx
mov	4.+_part(bx),si
sub	ax,ax
cwd
push	ax
push	dx
mov	si,*4.(bp)
and	si,*8.
mov	cx,*3.
sar	si,cl
mov	cx,*3.
sal	si,cl
or	si,#768.
push	si
call	_bread
add	sp,*6.
mov	*-6.(bp),ax
mov	si,ax
test	(si),*4.
bne	L26
mov	si,*14.(si)
cmp	*510.(si),#-21931.
bne	L26
mov	si,ax
mov	si,*14.(si)
add	si,#446.
mov	*-8.(bp),si
mov	*-10.(bp),*1.
L20005:mov	ax,di
mov	cx,*5.
imul	cx
add	ax,*-10.(bp)
mov	cx,*3.
sal	ax,cl
mov	bx,ax
mov	si,*-8.(bp)
mov	si,*10.(si)
mov	_part(bx),si
mov	ax,di
mov	cx,*5.
imul	cx
add	ax,*-10.(bp)
mov	cx,*3.
sal	ax,cl
mov	bx,ax
mov	si,*-8.(bp)
mov	si,*8.(si)
mov	2.+_part(bx),si
mov	ax,di
mov	cx,*5.
imul	cx
add	ax,*-10.(bp)
mov	cx,*3.
sal	ax,cl
mov	bx,ax
mov	si,*-8.(bp)
mov	si,*14.(si)
mov	4.+_part(bx),si
mov	ax,di
mov	cx,*5.
imul	cx
add	ax,*-10.(bp)
mov	cx,*3.
sal	ax,cl
mov	bx,ax
mov	si,*-8.(bp)
mov	si,*12.(si)
mov	6.+_part(bx),si
add	*-8.(bp),*16.
inc	*-10.(bp)
cmp	*-10.(bp),*4.
ble	L20005
L26:push	*-6.(bp)
call	_brelse
add	sp,*2.
|NREG	3
L19:|RTYP	0
br	cret
.globl	_hdclose
.even
_hdclose:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_flag=6.
mov	di,_rootdev
cmp	*4.(bp),di
je	L32
mov	di,_swapdev
cmp	*4.(bp),di
je	L32
mov	di,*4.(bp)
and	di,*8.
mov	cx,*3.
sar	di,cl
mov	_hd,di
mov	di,#231.
push	di
call	_smplCmd
add	sp,*2.
test	ax,ax
je	L32
mov	di,#-8174.
push	di
call	_inb
add	sp,*2.
and	ax,*4.
jne	L32
movb	111.+_u,*5.
L32:|RTYP	0
br	cret
.globl	_hdstrat
.even
_hdstrat:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
sub	sp,*16.
|	_unit=-6.
|	_partnum=-8.
|	_bcnt=-10.
|	_scnt=-12.
|	_snum=-16.
|	_err=-18.
|	_addr=-20.
test	(di),*16.
je	L38
mov	ax,#512.
cwd
push	ax
push	dx
push	*18.(di)
push	*16.(di)
call	lrem
add	sp,*8.
test	dx,dx
jne	L10006
test	ax,ax
jne	L10006
test	*12.(di),#511.
je	L39
L10006:or	(di),*4.
movb	*21.(di),*22.
push	di
br	L20008
L39:mov	dx,*18.(di)
mov	si,*16.(di)
mov	cx,*9.
sar	si,*1
rcr	dx,*1
loop	.-4
mov	*-14.(bp),dx
mov	*-16.(bp),si
mov	si,*12.(di)
mov	cx,*9.
shr	si,cl
j	L20009
L38:mov	dx,*18.(di)
mov	si,*16.(di)
sal	dx,*1
rcl	si,*1
mov	*-14.(bp),dx
mov	*-16.(bp),si
mov	si,*12.(di)
add	si,#1023.
mov	cx,*10.
shr	si,cl
mov	*-10.(bp),si
sal	si,*1
L20009:mov	*-12.(bp),si
mov	si,*10.(di)
and	si,*8.
mov	cx,*3.
sar	si,cl
mov	*-6.(bp),si
mov	si,*10.(di)
and	si,*7.
mov	*-8.(bp),si
mov	ax,*-6.(bp)
mov	cx,*5.
imul	cx
add	ax,si
mov	cx,*3.
sal	ax,cl
mov	si,ax
mov	bx,#4.+_part +2(si)
mov	si,#4.+_part(si)
mov	cx,si
mov	ax,*-12.(bp)
cwd
add	ax,*-14.(bp)
adc	dx,*-16.(bp)
cmp	dx,cx
jl	L41
jg	L10008
cmp	ax,bx
jbe	L41
L10008:push	di
call	_bpEnxio
br	L20007
L41:cmp	*-12.(bp),*0
jne	L42
mov	*22.(di),*0.
push	di
br	L20008
L42:mov	si,*-6.(bp)
mov	_hd,si
mov	si,*-12.(bp)
mov	2.+_hd,si
mov	si,(di)
and	si,*1.
mov	4.+_hd,si
mov	ax,*-6.(bp)
mov	cx,*5.
imul	cx
add	ax,*-8.(bp)
mov	cx,*3.
sal	ax,cl
mov	si,ax
mov	dx,#_part +2(si)
mov	si,#_part(si)
add	dx,*-14.(bp)
adc	si,*-16.(bp)
mov	8.+_hd,dx
mov	6.+_hd,si
movb	ax,*20.(di)
cbw
mov	cx,*12.
sal	ax,cl
mov	12.+_hd,ax
mov	si,*14.(di)
mov	cx,*4.
shr	si,cl
mov	*-20.(bp),si
add	12.+_hd,si
mov	si,*14.(di)
and	si,*15.
mov	10.+_hd,si
call	_hdrw
mov	*-18.(bp),ax
cmp	ax,*0
je	L43
push	ax
cmp	4.+_hd,*0
je	L10009
mov	si,*32.
push	si
j	L10010
L10009:mov	si,*48.
push	si
L10010:push	di
call	_deverro
add	sp,*6.
or	(di),*4.
movb	*21.(di),*5.
L43:push	di
L20008:call	_iodone
L20007:add	sp,*2.
|RTYP	0
br	cret
.globl	_hdread
.even
_hdread:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
mov	di,*1.
push	di
push	*4.(bp)
mov	di,#_rhdbuf
push	di
mov	di,#_hdstrat
push	di
call	_physio
add	sp,*8.
|RTYP	0
br	cret
.globl	_hdwrite
.even
_hdwrite:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	di,di
push	di
push	*4.(bp)
mov	di,#_rhdbuf
push	di
mov	di,#_hdstrat
push	di
call	_physio
add	sp,*8.
|RTYP	0
br	cret
.globl
.even
_ident:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_verbose=4.
sub	sp,*58.
|	_i=-6.
|	_model=-48.
|	_firmware=-58.
|	_sectors=-62.
mov	ax,_hd
mov	cx,*6.
imul	cx
mov	di,ax
mov	_devinfo(di),*0.
sub	ax,ax
cwd
mov	si,ax
mov	di,dx
mov	ax,_hd
imul	cx
mov	bx,ax
mov	2.+_devinfo+2(bx),si
mov	2.+_devinfo(bx),di
mov	di,#236.
push	di
call	_smplCmd
add	sp,*2.
test	ax,ax
bne	L53
mov	di,*1.
push	di
push	di
mov	di,#_identDe
push	di
sub	di,di
push	di
call	_hdio
add	sp,*8.
test	ax,ax
bne	L53
mov	*-6.(bp),*0.
L20011:lea	dx,*-48.(bp)
add	dx,*-6.(bp)
push	dx
mov	bx,*-6.(bp)
movb	dx,#55.+_identDe(bx)
pop	bx
movb	(bx),dx
lea	dx,*-48.(bp)
add	dx,*-6.(bp)
push	dx
mov	bx,*-6.(bp)
movb	dx,#54.+_identDe(bx)
pop	bx
movb	*1.(bx),dx
add	*-6.(bp),*2.
cmp	*-6.(bp),*40.
jb	L20011
movb	*-8.(bp),*0.
mov	*-6.(bp),*0.
L20013:lea	dx,*-58.(bp)
add	dx,*-6.(bp)
push	dx
mov	bx,*-6.(bp)
movb	dx,#47.+_identDe(bx)
pop	bx
movb	(bx),dx
lea	dx,*-58.(bp)
add	dx,*-6.(bp)
push	dx
mov	bx,*-6.(bp)
movb	dx,#46.+_identDe(bx)
pop	bx
movb	*1.(bx),dx
add	*-6.(bp),*2.
cmp	*-6.(bp),*8.
jb	L20013
movb	*-50.(bp),*0.
mov	di,122.+_identDe
mov	*-62.(bp),di
mov	di,120.+_identDe
mov	*-60.(bp),di
test	98.+_identDe,#512.
je	L53
mov	ax,_hd
mov	cx,*6.
imul	cx
mov	di,ax
mov	_devinfo(di),*1.
mov	si,*-60.(bp)
mov	di,*-62.(bp)
mov	ax,_hd
imul	cx
mov	bx,ax
mov	2.+_devinfo+2(bx),si
mov	2.+_devinfo(bx),di
L53:cmp	*4.(bp),*0
beq	L52
mov	ax,_hd
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#_devinfo(bx)
test	ax,ax
je	L62
lea	di,*-58.(bp)
push	di
lea	di,*-48.(bp)
push	di
mov	di,#L63
push	di
call	_printf
add	sp,*6.
mov	ax,#1024.
cwd
push	ax
push	dx
mov	ax,*2.
cwd
push	ax
push	dx
push	*-60.(bp)
push	*-62.(bp)
call	ldiv
add	sp,*8.
push	ax
push	dx
call	ldiv
add	sp,*8.
push	ax
push	dx
mov	di,#1024.
push	di
mov	ax,*2.
cwd
push	ax
push	dx
push	*-60.(bp)
push	*-62.(bp)
call	ldiv
add	sp,*8.
push	ax
push	dx
push	*-60.(bp)
push	*-62.(bp)
mov	di,#L64
push	di
call	_printf
add	sp,*16.
j	L52
L62:mov	di,#L66
push	di
call	_printf
add	sp,*2.
L52:|RTYP	0
br	cret
.globl
.even
_hdrw:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_status=di
mov	si,#136.
push	si
mov	si,#-8162.
push	si
call	_await
add	sp,*4.
test	ax,ax
je	L70
L20015:mov	ax,*1.
br	L69
L70:mov	si,_hd
cmp	_currUni,si
je	L71
call	_swtchDe
mov	si,#136.
push	si
mov	si,#-8162.
push	si
call	_await
add	sp,*4.
test	ax,ax
jne	L20015
L71:mov	si,*2.
push	si
mov	si,#-8148.
push	si
call	_outb
add	sp,*4.
sub	si,si
push	si
mov	si,#-8174.
push	si
call	_outb
add	sp,*4.
push	2.+_hd
mov	si,#-8172.
push	si
call	_outb
add	sp,*4.
push	8.+_hd
mov	si,#-8170.
push	si
call	_outb
add	sp,*4.
mov	dx,8.+_hd
mov	si,6.+_hd
mov	cx,*8.
sar	si,*1
rcr	dx,*1
loop	.-4
push	dx
mov	si,#-8168.
push	si
call	_outb
add	sp,*4.
mov	dx,8.+_hd
mov	si,6.+_hd
mov	cx,*16.
sar	si,*1
rcr	dx,*1
loop	.-4
push	dx
mov	si,#-8166.
push	si
call	_outb
add	sp,*4.
mov	dx,8.+_hd
mov	si,6.+_hd
mov	cx,*24.
sar	si,*1
rcr	dx,*1
loop	.-4
and	dx,*15.
mov	si,dx
cmp	_hd,*0
je	L10011
mov	dx,#176.
j	L10012
L10011:mov	dx,#160.
L10012:or	si,dx
or	si,*64.
push	si
mov	si,#-8164.
push	si
call	_outb
add	sp,*4.
cmp	4.+_hd,*0
je	L10013
mov	si,*32.
push	si
j	L10014
L10013:mov	si,*48.
push	si
L10014:mov	si,#-8162.
push	si
call	_outb
add	sp,*4.
push	4.+_hd
push	2.+_hd
push	10.+_hd
push	12.+_hd
call	_hdio
add	sp,*8.
L69:|RTYP	0
br	cret
.globl
.even
_smplCmd:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_cmd=4.
|NREG	2
|	_status=di
mov	si,#136.
push	si
mov	si,#-8162.
push	si
call	_await
add	sp,*4.
test	ax,ax
je	L76
L20017:mov	si,*1.
j	L20016
L76:mov	si,_hd
cmp	_currUni,si
je	L77
call	_swtchDe
mov	si,#136.
push	si
mov	si,#-8162.
push	si
call	_await
add	sp,*4.
test	ax,ax
jne	L20017
L77:push	*4.(bp)
mov	si,#-8162.
push	si
call	_outb
add	sp,*4.
mov	si,#128.
push	si
mov	si,#-8162.
push	si
call	_await
add	sp,*4.
test	ax,ax
jne	L20017
mov	si,#-8162.
push	si
call	_inb
add	sp,*2.
mov	di,ax
test	di,#161.
je	L80
mov	si,di
mov	cx,*8.
sal	si,cl
or	si,*2.
j	L20016
L80:sub	si,si
L20016:mov	ax,si
|RTYP	0
br	cret
.globl
.even
_swtchDe:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
cmp	_hd,*0
je	L10015
mov	di,#176.
push	di
j	L10016
L10015:mov	di,#160.
push	di
L10016:mov	di,#-8164.
push	di
call	_outb
add	sp,*4.
mov	di,_hd
mov	_currUni,di
call	_delay
|RTYP	0
br	cret
.globl
.even
_delay:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,#-8162.
push	di
call	_inb
add	sp,*2.
mov	di,#-8162.
push	di
call	_inb
add	sp,*2.
mov	di,#-8162.
push	di
call	_inb
add	sp,*2.
mov	di,#-8162.
push	di
call	_inb
add	sp,*2.
|RTYP	0
br	cret
.globl
.even
_await:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_port=4.
|	_mask=6.
sub	sp,*2.
|	_i=-6.
mov	*-6.(bp),*0.
j	L90
L92:inc	*-6.(bp)
L90:mov	bx,*-6.(bp)
sub	cx,cx
mov	si,*-1.
jg	L10017
cmp	si,bx
jbe	L91
L10017:push	*4.(bp)
call	_inb
add	sp,*2.
and	ax,*6.(bp)
jne	L92
sub	di,di
L20020:mov	ax,di
|RTYP	0
br	cret
L91:mov	di,*-1.
j	L20020
.globl
.even
_bpEnxio:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_bp=4.
mov	di,*4.(bp)
or	(di),*4.
mov	bx,*4.(bp)
movb	*21.(bx),*6.
push	*4.(bp)
call	_iodone
add	sp,*2.
|RTYP	0
br	cret
.data
L6:.byte	/56,/33,/30,/20,/49,/44,/45,/20,/20
.byte	/42,/61,/73,/65,/64
.byte	/20,/25,/78,/20,/25,/73,/2e,/a,/0
L7:.byte	/66,/6f,/75,/6e,/64,/0
L8:.byte	/4e,/4f,/54,/20,/66,/6f,/75,/6e,/64
.byte	/0
L13:.byte	/20,/25,/73,/20,/0
L14:.byte	/53,/6c,/61,/76,/65,/3a,/20,/0
L15:.byte	/4d,/61,/73,/74,/65,/72,/3a,/0
L63:.byte	/25,/73,/20,/46,/57,/3a,/20,/25,/73
.byte	/a,/0
L64:.byte	/20,/20,/25,/44,/20,/35,/31,/32,/2d
.byte	/62,/79,/74,/65,/20
.byte	/73,/65,/63,/74,/6f,/72,/73,/2c,/20
.byte	/25,/44,/20,/25,/64,/2d
.byte	/62,/79,/74,/65,/20,/62,/6c,/6f,/63
.byte	/6b,/73,/20,/28,/25,/44
.byte	/20,/4d,/69,/42,/29,/a,/0
L66:.byte	/6e,/6f,/74,/20,/66,/6f,/75,/6e,/64
.byte	/a,/0
