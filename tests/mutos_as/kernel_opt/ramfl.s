.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_ramflbl,2
.comm	_ramflpa,2
.comm	_ramflta,26
.comm	_ramflrb,26
.globl	_ramstra
.text
.even
_ramstra:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
sub	sp,*14.
|	_a1=-6.
|	_a2=-8.
|	_unit=-10.
|	_rambno=-14.
|	_ramseg=-16.
|	_ramaddr=-18.
mov	si,*10.(di)
and	si,#255.
mov	*-10.(bp),si
cmp	si,*0
jl	L10000
cmp	si,*4.
jl	L4
L10000:or	(di),*4.
push	di
br	L20001
L4:mov	si,*-10.(bp)
sal	si,*1
sal	si,*1
mov	ax,2.+_ramcfg(si)
cwd
mov	cx,*2.
idiv	cx
mov	_ramflpa,ax
mov	si,*-10.(bp)
sal	si,*1
sal	si,*1
mov	ax,2.+_ramcfg(si)
cwd
mov	cx,*2.
idiv	cx
test	dx,dx
je	L5
inc	_ramflpa
L5:mov	si,_ramflpa
sal	si,*1
mov	_ramflbl,si
test	(di),*16.
je	L6
mov	ax,#1024.
cwd
push	ax
push	dx
push	*18.(di)
push	*16.(di)
call	ldiv
add	sp,*8.
mov	*-12.(bp),ax
mov	*-14.(bp),dx
mov	ax,#1024.
cwd
push	ax
push	dx
push	*18.(di)
push	*16.(di)
call	lrem
add	sp,*8.
mov	*-18.(bp),ax
j	L7
L6:mov	dx,*18.(di)
mov	si,*16.(di)
mov	*-12.(bp),dx
mov	*-14.(bp),si
mov	*-18.(bp),*0.
L7:mov	ax,_ramflbl
cwd
cmp	dx,*-14.(bp)
jg	L8
jl	L10001
cmp	ax,*-12.(bp)
ja	L8
L10001:or	(di),*4.
movb	*21.(di),*6.
push	di
j	L20001
L8:movb	ax,*20.(di)
cbw
mov	cx,*12.
sal	ax,cl
mov	*-6.(bp),ax
mov	ax,*14.(di)
sub	dx,dx
mov	cx,*16.
idiv	cx
mov	*-8.(bp),ax
add	*-6.(bp),ax
mov	ax,*14.(di)
sub	dx,dx
idiv	cx
mov	*-8.(bp),dx
mov	si,*-10.(bp)
sal	si,*1
sal	si,*1
mov	si,#_ramcfg(si)
add	si,*-12.(bp)
mov	cx,*6.
sal	si,cl
mov	*-16.(bp),si
test	(di),*1.
je	L9
push	*12.(di)
push	*-8.(bp)
push	*-6.(bp)
push	*-18.(bp)
push	si
j	L20003
L9:push	*12.(di)
push	*-18.(bp)
push	*-16.(bp)
push	*-8.(bp)
push	*-6.(bp)
L20003:call	_copymem
add	sp,*10.
push	di
L20001:call	_iodone
add	sp,*2.
|RTYP	0
br	cret
.globl	_ramread
.even
_ramread:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
mov	di,*1.
push	di
push	*4.(bp)
mov	di,#_ramflrb
push	di
mov	di,#_ramstra
push	di
call	_physio
add	sp,*8.
|RTYP	0
br	cret
.globl	_ramwrit
.even
_ramwrit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	di,di
push	di
push	*4.(bp)
mov	di,#_ramflrb
push	di
mov	di,#_ramstra
push	di
call	_physio
add	sp,*8.
|RTYP	0
br	cret
.data
