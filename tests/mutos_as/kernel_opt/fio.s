.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.globl	_getf
.text
.even
_getf:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_f=di
|NREG	1
|	_fp=si
cmp	di,*0
jl	L4
cmp	di,*20.
jge	L4
mov	bx,di
sal	bx,*1
mov	si,174.+_u(bx)
cmp	si,*0
je	L4
mov	dx,si
j	L20000
L4:movb	111.+_u,*9.
sub	dx,dx
L20000:mov	ax,dx
|RTYP	12
br	cret
.globl	_closef
.even
_closef:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_fp=di
sub	sp,*10.
|NREG	1
|	_ip=si
|	_flag=-6.
|	_mode=-8.
|	_dev=-10.
|	_cfunc=-12.
|	_c=-14.
cmp	di,*0
beq	L8
push	*4.(di)
call	_unlckf
add	sp,*2.
cmp	*2.(di),*1.
jbe	L9
dec	*2.(di)
br	L8
L9:mov	si,*4.(di)
push	si
call	_plock
add	sp,*2.
movb	ax,(di)
cbw
mov	*-6.(bp),ax
mov	dx,*22.(si)
mov	*-10.(bp),dx
mov	dx,*6.(si)
and	dx,#-4096.
mov	*-8.(bp),dx
mov	*2.(di),*0.
test	*-6.(bp),*4.
je	L10
and	*6.(si),#-385.
mov	dx,si
inc	dx
push	dx
call	_wakeup
add	sp,*2.
mov	dx,si
add	dx,*2.
push	dx
call	_wakeup
add	sp,*2.
L10:mov	ax,*-8.(bp)
cmp	ax,#8192.
je	L13
cmp	ax,#24576.
je	L14
push	si
br	L20008
L13:mov	ax,*-10.(bp)
mov	cx,*8.
sar	ax,cl
mov	cx,*14.
imul	cx
mov	bx,ax
mov	ax,#2.+_cdevsw(bx)
mov	*-12.(bp),ax
j	L11
L20002:mov	bx,*4.(di)
mov	*-14.(bp),bx
mov	dx,*20.+2(bx)
cmp	dx,*-10.(bp)
jne	L18
mov	dx,*6.(bx)
and	dx,#-4096.
cmp	dx,*-8.(bp)
jne	L18
push	si
j	L20008
L14:mov	bx,*-10.(bp)
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
mov	dx,#2.+_bdevsw(bx)
mov	*-12.(bp),dx
L11:mov	di,#_file
j	L16
L20004:cmp	*2.(di),*0
jne	L20002
L18:add	di,*10.
L16:cmp	di,10.+_v
jb	L20004
cmp	*-8.(bp),#24576.
jne	L20
mov	*-14.(bp),#_mount
j	L21
L20006:mov	bx,*-14.(bp)
mov	dx,*2.(bx)
test	dx,dx
je	L23
mov	dx,(bx)
cmp	dx,*-10.(bp)
jne	L23
push	si
j	L20008
L23:add	*-14.(bp),*6.
L21:mov	dx,14.+_v
cmp	*-14.(bp),dx
jb	L20006
push	*-10.(bp)
call	_bpurge
add	sp,*2.
L20:push	*-6.(bp)
push	*-10.(bp)
call	@*-12.(bp)
add	sp,*4.
push	si
L20008:call	_iput
add	sp,*2.
L8:|RTYP	0
br	cret
.globl	_openi
.even
_openi:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|	_rw=6.
sub	sp,*2.
|	_dev=-6.
|NREG	1
|	_maj=si
mov	si,*22.(di)
mov	*-6.(bp),si
mov	cx,*8.
shr	si,cl
mov	ax,*6.(di)
and	ax,#-4096.
cmp	ax,#8192.
je	L31
cmp	ax,#12288.
je	L31
cmp	ax,#24576.
je	L34
cmp	ax,#28672.
je	L34
j	L27
L31:cmp	si,_nchrdev
jae	L32
push	*6.(bp)
push	*-6.(bp)
mov	ax,si
mov	cx,*14.
imul	cx
mov	bx,ax
call	@_cdevsw(bx)
j	L20009
L20011:push	*6.(bp)
push	*-6.(bp)
mov	bx,si
mov	cx,*3.
sal	bx,cl
call	@_bdevsw(bx)
L20009:add	sp,*4.
j	L27
L34:cmp	si,_nblkdev
jb	L20011
L32:movb	111.+_u,*6.
|NREG	2
L27:|RTYP	0
br	cret
.globl	_access
.even
_access:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|	_mode=6.
|NREG	1
|	_m=si
mov	si,*6.(bp)
cmp	si,#128.
jne	L38
push	*2.(di)
call	_getfs
add	sp,*2.
mov	bx,ax
movb	ax,#613.(bx)
testb	ax,ax
je	L39
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#16384.
je	L10002
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#-32768.
je	L10002
cmp	_ROroot,*0
jne	L39
L10002:movb	111.+_u,*30.
j	L20013
L39:movb	dx,(di)
and	dx,*32.
je	L41
push	di
call	_xrele
add	sp,*2.
L41:movb	dx,(di)
and	dx,*32.
je	L38
movb	111.+_u,*26.
j	L20013
L38:cmp	112.+_u,*0
jne	L43
L20014:sub	dx,dx
j	L20012
L43:mov	dx,*10.(di)
cmp	112.+_u,dx
je	L44
mov	cx,*3.
sar	si,cl
mov	dx,*12.(di)
cmp	114.+_u,dx
je	L44
mov	cx,*3.
sar	si,cl
L44:mov	dx,*6.(di)
and	dx,si
jne	L20014
movb	111.+_u,*13.
L20013:mov	dx,*1.
L20012:mov	ax,dx
|RTYP	0
br	cret
.globl	_owner
.even
_owner:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
sub	si,si
push	si
mov	si,#_uchar
push	si
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
je	L20016
mov	si,*10.(di)
cmp	112.+_u,si
jne	L51
L20017:mov	si,di
j	L20015
L51:call	_suser
test	ax,ax
jne	L20017
push	di
call	_iput
add	sp,*2.
L20016:sub	si,si
L20015:mov	ax,si
|RTYP	12
br	cret
.globl	_suser
.even
_suser:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
cmp	112.+_u,*0
jne	L56
mov	di,*1.
L20018:mov	ax,di
|RTYP	0
br	cret
L56:movb	111.+_u,*1.
sub	di,di
j	L20018
.globl	_ufalloc
.even
_ufalloc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_i=di
sub	di,di
L20020:mov	si,di
sal	si,*1
mov	si,#174.+_u(si)
test	si,si
jne	L62
mov	124.+_u,di
movb	214.+_u(di),*0.
mov	si,di
j	L20021
L62:inc	di
cmp	di,*20.
jl	L20020
movb	111.+_u,*24.
mov	si,*-1.
L20021:mov	ax,si
|RTYP	0
br	cret
.globl	_falloc
.even
_falloc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_fp=di
|NREG	1
|	_i=si
call	_ufalloc
mov	si,ax
cmp	si,*0
jl	L20025
mov	di,#_file
j	L68
L20023:cmp	*2.(di),*0
jne	L70
mov	bx,si
sal	bx,*1
mov	174.+_u(bx),di
inc	*2.(di)
sub	ax,ax
cwd
mov	*8.(di),ax
mov	*6.(di),dx
mov	dx,di
j	L20024
L70:add	di,*10.
L68:cmp	di,10.+_v
jb	L20023
mov	dx,#L72
push	dx
call	_printf
add	sp,*2.
movb	111.+_u,*23.
L20025:sub	dx,dx
L20024:mov	ax,dx
|RTYP	12
br	cret
.data
L72:.byte	/6e,/6f,/20,/66,/69,/6c,/65,/a,/0
