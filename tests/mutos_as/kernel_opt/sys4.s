.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.globl	_gtime
.text
.even
_gtime:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	si,2.+_time
mov	di,_time
mov	126.+_u,si
mov	124.+_u,di
|RTYP	0
br	cret
.globl	_ftime
.even
_ftime:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*10.
|NREG	2
|	_uap=di
|	_t=-14.
|NREG	1
|	_ms=si
mov	di,122.+_u
call	_spl7
mov	ax,2.+_time
mov	dx,_time
mov	*-12.(bp),ax
mov	*-14.(bp),dx
mov	si,_lbolt
call	_spl0
cmp	si,*20.
jbe	L7
sub	si,*20.
add	*-12.(bp),*1.
adc	*-14.(bp),*0
L7:mov	ax,si
mov	cx,#1000.
imul	cx
sub	dx,dx
mov	cx,*20.
div	cx
mov	*-10.(bp),ax
mov	dx,_Timezon
mov	*-8.(bp),dx
mov	dx,_Dstflag
mov	*-6.(bp),dx
mov	dx,*10.
push	dx
push	(di)
lea	dx,*-14.(bp)
push	dx
call	_copyout
add	sp,*6.
test	ax,ax
jge	L8
movb	111.+_u,*14.
L8:|NREG	3
|RTYP	0
br	cret
.globl	_stime
.even
_stime:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_uap=di
mov	di,122.+_u
call	_suser
test	ax,ax
je	L12
mov	dx,*2.(di)
mov	si,(di)
mov	2.+_time,dx
mov	_time,si
L12:|NREG	3
|RTYP	0
br	cret
.globl	_setuid
.even
_setuid:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_uid=di
|NREG	1
|	_uap=si
mov	si,122.+_u
mov	di,(si)
cmp	116.+_u,di
je	L10000
call	_suser
test	ax,ax
je	L16
L10000:mov	112.+_u,di
mov	bx,120.+_u
mov	*8.(bx),di
mov	116.+_u,di
L16:|NREG	3
|RTYP	0
br	cret
.globl	_getuid
.even
_getuid:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,116.+_u
mov	124.+_u,di
mov	di,112.+_u
mov	126.+_u,di
|RTYP	0
br	cret
.globl	_setgid
.even
_setgid:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_gid=di
|NREG	1
|	_uap=si
mov	si,122.+_u
mov	di,(si)
cmp	118.+_u,di
je	L10001
call	_suser
test	ax,ax
je	L23
L10001:mov	114.+_u,di
mov	118.+_u,di
L23:|NREG	3
|RTYP	0
br	cret
.globl	_getgid
.even
_getgid:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,118.+_u
mov	124.+_u,di
mov	di,114.+_u
mov	126.+_u,di
|RTYP	0
br	cret
.globl	_getpid
.even
_getpid:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,120.+_u
mov	di,*12.(di)
mov	124.+_u,di
mov	di,120.+_u
mov	di,*14.(di)
mov	126.+_u,di
|RTYP	0
br	cret
.globl	_sync
.even
_sync:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
call	_update
|RTYP	0
br	cret
.globl	_nice
.even
_nice:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_n=di
|NREG	1
|	_uap=si
mov	si,122.+_u
mov	di,(si)
cmp	di,*0
jge	L36
call	_suser
test	ax,ax
jne	L36
sub	di,di
L36:mov	bx,120.+_u
movb	ax,*5.(bx)
cbw
add	di,ax
cmp	di,*40.
jl	L37
mov	di,*39.
L37:cmp	di,*0
jge	L38
sub	di,di
L38:mov	bx,120.+_u
mov	ax,di
movb	*5.(bx),ax
|NREG	3
|RTYP	0
br	cret
.globl	_unlink
.even
_unlink:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
|NREG	1
|	_pp=si
mov	dx,*2.
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	si,ax
cmp	si,*0
beq	L41
mov	dx,156.+_u
cmp	*4.(si),dx
jne	L42
mov	di,si
incb	*1.(di)
j	L43
L42:push	156.+_u
push	*2.(si)
call	_iget
add	sp,*4.
mov	di,ax
L43:cmp	di,*0
je	L44
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#16384.
jne	L10002
call	_suser
test	ax,ax
je	L45
L10002:mov	dx,*2.(si)
cmp	*2.(di),dx
je	L46
movb	111.+_u,*16.
j	L45
L46:movb	dx,(di)
and	dx,*32.
je	L47
push	di
call	_xrele
add	sp,*2.
L47:movb	dx,(di)
and	dx,*32.
je	L48
cmp	*8.(di),*1.
jne	L48
movb	111.+_u,*26.
j	L45
L48:sub	134.+_u,*16.
sbb	132.+_u,*0
mov	128.+_u,#156.+_u
mov	130.+_u,*16.
mov	156.+_u,*0.
push	si
call	_writei
add	sp,*2.
dec	*8.(di)
orb	(di),*64.
L45:push	di
call	_iput
add	sp,*2.
L44:push	si
call	_iput
add	sp,*2.
|NREG	3
L41:|RTYP	0
br	cret
.globl	_chdir
.even
_chdir:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,#136.+_u
push	di
call	_chdirec
add	sp,*2.
|RTYP	0
br	cret
.globl	_chroot
.even
_chroot:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
call	_suser
test	ax,ax
je	L54
mov	di,#138.+_u
push	di
call	_chdirec
add	sp,*2.
L54:|RTYP	0
br	cret
.globl	_chdirec
.even
_chdirec:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ipp=di
|NREG	1
|	_ip=si
sub	dx,dx
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	si,ax
cmp	si,*0
je	L58
mov	dx,*6.(si)
and	dx,#-4096.
cmp	dx,#16384.
je	L59
movb	111.+_u,*20.
L60:push	si
call	_iput
add	sp,*2.
|NREG	2
L58:|RTYP	0
br	cret
L59:mov	dx,*64.
push	dx
push	si
call	_access
add	sp,*4.
test	ax,ax
jne	L60
push	si
call	_prele
add	sp,*2.
cmp	(di),*0
je	L61
push	(di)
call	_plock
add	sp,*2.
push	(di)
call	_iput
add	sp,*2.
L61:mov	(di),si
j	L58
.globl	_chmod
.even
_chmod:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
|NREG	1
|	_uap=si
mov	si,122.+_u
call	_owner
mov	di,ax
test	ax,ax
je	L64
and	*6.(di),#-4096.
cmp	112.+_u,*0
je	L65
and	*2.(si),#-513.
L65:mov	dx,*2.(si)
and	dx,#4095.
or	*6.(di),dx
orb	(di),*64.
movb	dx,(di)
and	dx,*32.
je	L66
test	*6.(di),#512.
jne	L66
push	di
call	_xrele
add	sp,*2.
L66:push	di
call	_iput
add	sp,*2.
|NREG	3
L64:|RTYP	0
br	cret
.globl	_chown
.even
_chown:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
|NREG	1
|	_uap=si
mov	si,122.+_u
call	_suser
test	ax,ax
je	L69
call	_owner
mov	di,ax
test	ax,ax
je	L69
mov	dx,*2.(si)
mov	*10.(di),dx
mov	dx,*4.(si)
mov	*12.(di),dx
orb	(di),*64.
push	di
call	_iput
add	sp,*2.
|NREG	3
L69:|RTYP	0
br	cret
.globl	_ssig
.even
_ssig:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_a=di
|	_uap=-6.
mov	si,122.+_u
mov	*-6.(bp),si
mov	di,(si)
cmp	di,*0
jle	L10003
cmp	di,*17.
jge	L10003
cmp	di,*9.
jne	L73
L10003:movb	111.+_u,*22.
j	L72
L73:mov	si,di
sal	si,*1
mov	si,#272.+_u(si)
mov	124.+_u,si
mov	si,*-6.(bp)
mov	si,*2.(si)
mov	bx,di
sal	bx,*1
mov	272.+_u(bx),si
mov	si,*1.
mov	cx,di
dec	cx
sal	si,cl
not	si
mov	bx,120.+_u
and	*6.(bx),si
|NREG	3
L72:|RTYP	0
br	cret
.globl	_kill
.even
_kill:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*8.
|NREG	2
|	_p=di
|NREG	1
|	_q=si
|	_a=-6.
|	_uap=-8.
|	_f=-10.
|	_priv=-12.
mov	bx,122.+_u
mov	*-8.(bp),bx
mov	*-10.(bp),*0.
mov	dx,(bx)
mov	*-6.(bp),dx
mov	*-12.(bp),*0.
cmp	dx,*-1.
jne	L77
cmp	112.+_u,*0
jne	L77
inc	*-12.(bp)
mov	*-6.(bp),*0.
L77:mov	si,120.+_u
mov	di,#_proc
j	L78
L20001:cmpb	(di),*0
je	L80
cmp	*-6.(bp),*0
je	L10004
mov	dx,*-6.(bp)
cmp	*12.(di),dx
jne	L80
L10004:cmp	*-6.(bp),*0
jne	L10005
mov	dx,*10.(si)
cmp	*10.(di),dx
je	L10006
cmp	*-12.(bp),*0
je	L80
L10006:cmp	di,#32.+_proc
jbe	L80
L10005:cmp	112.+_u,*0
je	L10007
mov	dx,*8.(di)
cmp	112.+_u,dx
jne	L80
L10007:inc	*-10.(bp)
mov	bx,*-8.(bp)
push	*2.(bx)
push	di
call	_psignal
add	sp,*4.
L80:add	di,*32.
L78:cmp	di,_Nproca
jb	L20001
cmp	*-10.(bp),*0
jne	L81
movb	111.+_u,*3.
L81:|NREG	3
|RTYP	0
br	cret
.globl	_times
.even
_times:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_uap=di
mov	di,122.+_u
mov	si,*16.
push	si
push	(di)
mov	si,#306.+_u
push	si
call	_copyout
add	sp,*6.
test	ax,ax
jge	L85
movb	111.+_u,*14.
L85:|NREG	3
|RTYP	0
br	cret
.globl	_profil
.even
_profil:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_uap=di
mov	di,122.+_u
mov	si,(di)
mov	324.+_u,si
mov	si,*2.(di)
mov	326.+_u,si
mov	si,*4.(di)
mov	328.+_u,si
mov	si,*6.(di)
mov	330.+_u,si
|NREG	3
|RTYP	0
br	cret
.globl	_alarm
.even
_alarm:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_p=di
|NREG	1
|	_c=si
|	_uap=-6.
mov	bx,122.+_u
mov	*-6.(bp),bx
mov	di,120.+_u
mov	si,*30.(di)
mov	dx,(bx)
mov	*30.(di),dx
mov	124.+_u,si
|NREG	3
|RTYP	0
br	cret
.globl	_pause
.even
_pause:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
L95:mov	di,*40.
push	di
mov	di,#_u
push	di
call	_sleep
add	sp,*4.
j	L95
.globl	_umask
.even
_umask:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_uap=di
|NREG	1
|	_t=si
mov	di,122.+_u
mov	si,396.+_u
mov	dx,(di)
and	dx,#511.
mov	396.+_u,dx
mov	124.+_u,si
|NREG	3
|RTYP	0
br	cret
.globl	_utime
.even
_utime:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*8.
|NREG	2
|	_uap=di
|NREG	1
|	_ip=si
|	_tv=-12.
mov	di,122.+_u
call	_owner
mov	si,ax
test	ax,ax
je	L102
mov	dx,*8.
push	dx
lea	dx,*-12.(bp)
push	dx
push	*2.(di)
call	_copyin
add	sp,*6.
test	ax,ax
je	L103
movb	111.+_u,*14.
j	L102
L103:orb	(si),*70.
lea	dx,*-8.(bp)
push	dx
lea	dx,*-12.(bp)
push	dx
push	si
call	_iupdat
add	sp,*6.
push	si
call	_iput
add	sp,*2.
|NREG	3
L102:|RTYP	0
br	cret
.data
