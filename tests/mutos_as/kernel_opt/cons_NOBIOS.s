.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.globl	_kennung
.data
_kennung:.byte	/1b,/5b,/3f,/32,/3b,/20,/63,/0
.comm	_constyp,2
.globl	_COUNT
.data
.data
_COUNT:/1e
.globl	_CCOUNT
.data
.data
_CCOUNT:/0
.globl	_ERRCNT
.data
.data
_ERRCNT:/0
.globl	_SCNTMA
.data
.data
_SCNTMA:/0
.globl	_SCNTMI
.data
.data
_SCNTMI:/1d
.comm	_CYCNT,60
.globl	_consbau
.data
.data
_consbau:/0
/600
/400
/2ba
/0
/200
/180
/100
/80
/40
/0
/20
/10
/8
/0
/0
.comm	_constty,272
.comm	_consali,2
.comm	_consspe,2
.globl	_consini
.text
.even
_consini:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*6.
|NREG	2
|	_x=di
|NREG	1
|	_cons=si
|	_a=-6.
|	_b=-8.
|	_i=-10.
mov	si,#_conscfg
cmp	*2.(si),*0
beq	L4
mov	*-6.(bp),*1.
mov	dx,*27.
push	dx
call	_co
add	sp,*2.
mov	dx,*91.
push	dx
call	_co
add	sp,*2.
mov	dx,*99.
push	dx
call	_co
add	sp,*2.
L7:sub	dx,dx
push	dx
push	*6.(si)
call	_outb
add	sp,*4.
push	*6.(si)
call	_inb
add	sp,*2.
and	ax,*1.
je	L7
mov	*-10.(bp),*0.
L11:push	*6.(si)
call	_inb
add	sp,*2.
and	ax,*1.
je	L11
push	*4.(si)
call	_inb
add	sp,*2.
and	ax,*127.
mov	*-8.(bp),ax
cmp	*-10.(bp),*5.
jne	L13
mov	dx,ax
add	dx,*-48.
mov	_constyp,dx
j	L10
L13:mov	bx,*-10.(bp)
movb	ax,_kennung(bx)
cbw
cmp	ax,*-8.(bp)
je	L10
push	*-8.(bp)
push	*-10.(bp)
mov	dx,#L16
push	dx
call	_printf
add	sp,*6.
L10:inc	*-10.(bp)
cmp	*-10.(bp),*7.
jl	L11
cmp	_constyp,*5.
je	L17
mov	dx,#128.
push	dx
push	*2.(si)
call	_outb
add	sp,*4.
mov	dx,*10.
push	dx
call	_delay
add	sp,*2.
sub	dx,dx
push	dx
push	*2.(si)
call	_outb
add	sp,*4.
mov	dx,*10.
push	dx
call	_delay
add	sp,*2.
mov	dx,*64.
push	dx
push	*2.(si)
call	_outb
add	sp,*4.
mov	dx,#206.
push	dx
push	*2.(si)
call	_outb
add	sp,*4.
mov	dx,*23.
push	dx
push	*2.(si)
call	_outb
add	sp,*4.
push	*2.(si)
call	_inb
add	sp,*2.
and	ax,*15.
cmp	ax,*5.
je	L19
mov	*-6.(bp),*0.
j	L19
L17:mov	dx,*6.(si)
mov	*2.(si),dx
mov	dx,*4.(si)
mov	(si),dx
L19:cmp	_Stand,*0
jne	L20
cmp	*-6.(bp),*0
je	L10000
mov	dx,#L22
push	dx
j	L10001
L10000:mov	dx,#L23
push	dx
L10001:push	*12.(si)
push	(si)
mov	dx,#L21
push	dx
call	_printf
add	sp,*8.
L20:mov	dx,*-6.(bp)
mov	_consali,dx
|NREG	3
L4:|RTYP	0
br	cret
.globl	_conspar
.even
_conspar:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*6.
|NREG	2
|	_tp=di
|NREG	1
|	_cons=si
|	_speed=-6.
|	_baud=-8.
|	_unit=-10.
mov	di,#_constty
mov	si,*18.(di)
movb	ax,*23.(di)
cbw
mov	*-6.(bp),ax
mov	dx,_consspe
cmp	ax,dx
je	L26
mov	dx,ax
mov	_consspe,dx
mov	bx,ax
sal	bx,*1
mov	dx,#_consbau(bx)
mov	*-8.(bp),dx
cmp	*-6.(bp),*13.
je	L27
movb	111.+_u,*22.
j	L26
L27:mov	dx,*12.(si)
mov	cx,*6.
sal	dx,cl
or	dx,*54.
mov	*-10.(bp),dx
|NREG	3
L26:|RTYP	0
br	cret
.globl	_consope
.even
_consope:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
|	_tp=di
|NREG	1
|	_x=si
mov	dx,*4.(bp)
and	dx,*127.
cmp	dx,*1.
jge	L10002
cmp	_consali,*0
jne	L31
L10002:movb	111.+_u,*6.
br	L30
L31:mov	di,#_constty
test	*40.(di),#128.
je	L32
cmp	112.+_u,*0
je	L32
movb	111.+_u,*16.
j	L30
L32:mov	*18.(di),#_conscfg
mov	*12.(di),#_conssta
test	*40.(di),*4.
jne	L33
push	di
call	_ttychar
add	sp,*2.
mov	dx,*13.
movb	*23.(di),dx
movb	*22.(di),dx
mov	*26.(di),#8472.
cmp	_constyp,*5.
je	L34
mov	dx,*23.
push	dx
push	2.+_conscfg
call	_outb
add	sp,*4.
L34:mov	dx,*2.
push	dx
push	6.+_conscfg
call	_outb
add	sp,*4.
mov	dx,#L35
push	dx
call	_printf
add	sp,*2.
L33:or	*40.(di),*16.
push	di
push	*4.(bp)
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@_linesw(bx)
add	sp,*4.
|NREG	3
L30:|RTYP	0
br	cret
.globl	_consclo
.even
_consclo:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_tp=di
mov	di,#_constty
push	di
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	si,ax
call	@2.+_linesw(si)
add	sp,*2.
push	di
call	_ttyclos
add	sp,*2.
mov	*18.(di),*0.
|NREG	3
|RTYP	0
br	cret
.globl	_consrea
.even
_consrea:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_tp=di
mov	di,#_constty
push	di
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	si,ax
call	@4.+_linesw(si)
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_conswri
.even
_conswri:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_tp=di
mov	di,#_constty
push	di
movb	ax,*52.(di)
cbw
mov	cx,*20.
imul	cx
mov	si,ax
call	@6.+_linesw(si)
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_consiin
.even
_consiin:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
sub	sp,*14.
|NREG	1
|	_c=si
|	_a=-6.
|	_b=-8.
|	_cons=-10.
|	_tp=-12.
|	_timer=-14.
|	_error=-16.
|	_s=-18.
mov	*-10.(bp),#_conscfg
mov	*-12.(bp),#_constty
mov	*-18.(bp),*0.
mov	*-14.(bp),*1.
mov	*-16.(bp),*0.
br	L48
L20002:mov	bx,*-10.(bp)
push	*2.(bx)
call	_inb
add	sp,*2.
mov	*-6.(bp),ax
cmp	_constyp,*5.
je	L50
mov	dx,ax
and	dx,*2.
cmp	dx,*2.
je	L10005
L20006:sub	dx,dx
j	L10006
L50:mov	dx,*-6.(bp)
and	dx,*1.
cmp	dx,*1.
jne	L20006
L10005:mov	dx,*1.
L10006:mov	*-8.(bp),dx
cmp	dx,*0
beq	L48
mov	bx,*-10.(bp)
push	(bx)
call	_inb
add	sp,*2.
mov	si,ax
cmp	_constyp,*5.
je	L53
test	*-6.(bp),*56.
je	L53
inc	*-16.(bp)
inc	_ERRCNT
mov	dx,*23.
push	dx
mov	bx,*-10.(bp)
push	*2.(bx)
call	_outb
add	sp,*4.
L53:mov	dx,si
and	dx,*127.
cmp	dx,*27.
je	L10007
mov	bx,*-12.(bp)
test	*42.(bx),*64.
jne	L10007
cmp	*-16.(bp),*0
je	L54
L10007:mov	dx,_SCNTMA
cmp	*-14.(bp),dx
jle	L55
mov	dx,*-14.(bp)
mov	_SCNTMA,dx
L55:cmp	*-14.(bp),*0
je	L56
mov	dx,_SCNTMI
cmp	*-14.(bp),dx
jge	L56
mov	dx,*-14.(bp)
mov	_SCNTMI,dx
L56:mov	bx,*-14.(bp)
sal	bx,*1
inc	_CYCNT(bx)
inc	_CCOUNT
mov	dx,_COUNT
mov	*-14.(bp),dx
mov	bx,*-12.(bp)
or	*42.(bx),*64.
mov	dx,*-12.(bp)
add	dx,*56.
push	dx
push	si
call	_putc
j	L20005
L54:push	*-12.(bp)
push	si
mov	bx,*-12.(bp)
movb	ax,*52.(bx)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@10.+_linesw(bx)
L20005:add	sp,*4.
L48:mov	dx,*-14.(bp)
dec	*-14.(bp)
test	dx,dx
bne	L20002
mov	dx,*-12.(bp)
mov	bx,dx
test	*42.(bx),*64.
je	L58
mov	bx,dx
and	*42.(bx),*-65.
j	L59
L20004:cmp	*-16.(bp),*0
je	L10008
mov	bx,*-12.(bp)
test	*26.(bx),*34.
jne	L59
L10008:push	*-12.(bp)
push	si
mov	bx,*-12.(bp)
movb	ax,*52.(bx)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@10.+_linesw(bx)
add	sp,*4.
L59:mov	dx,*-12.(bp)
add	dx,*56.
push	dx
call	_getc
add	sp,*2.
mov	si,ax
test	ax,ax
jge	L20004
L58:|NREG	2
|RTYP	0
br	cret
.globl	_consoin
.even
_consoin:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
sub	sp,*6.
|NREG	1
|	_tp=si
|	_cons=-6.
|	_timer=-8.
|	_c=-10.
mov	si,#_constty
mov	*-6.(bp),#_conscfg
mov	bx,*-6.(bp)
push	*6.(bx)
call	_inb
add	sp,*2.
and	ax,*1.
cmp	ax,*1.
jne	L65
mov	bx,*-6.(bp)
push	*4.(bx)
call	_inb
add	sp,*2.
movb	*-10.(bp),ax
push	si
movb	ax,*-10.(bp)
cbw
push	ax
movb	ax,*52.(si)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@10.+_linesw(bx)
add	sp,*4.
L65:mov	dx,*2.
push	dx
mov	bx,*-6.(bp)
push	*6.(bx)
call	_outb
add	sp,*4.
test	*40.(si),*32.
je	L64
and	*40.(si),*-33.
push	si
movb	ax,*52.(si)
cbw
mov	cx,*20.
imul	cx
mov	bx,ax
call	@16.+_linesw(bx)
add	sp,*2.
test	*40.(si),*64.
je	L64
cmp	*6.(si),*30.
jg	L64
mov	dx,si
add	dx,*6.
push	dx
call	_wakeup
add	sp,*2.
|NREG	2
L64:|RTYP	0
br	cret
.globl	_conssta
.even
_conssta:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_tp=di
sub	sp,*4.
|NREG	1
|	_cons=si
|	_b=-6.
|	_a=-8.
call	_spl3
mov	*-8.(bp),ax
test	*40.(di),*33.
je	L69
push	ax
call	_splx
br	L20007
L69:push	*-8.(bp)
call	_splx
add	sp,*2.
mov	si,*18.(di)
mov	dx,di
add	dx,*6.
push	dx
call	_getc
add	sp,*2.
mov	*-6.(bp),ax
test	ax,ax
jl	L71
test	*26.(di),*32.
jne	L10009
test	*26.(di),#8192.
je	L10010
cmp	ax,#128.
jne	L10009
L10010:test	*26.(di),#16384.
je	L72
L10009:push	*-6.(bp)
push	*4.(si)
j	L20010
L20012:push	*-6.(bp)
push	*4.(si)
L20010:call	_outb
add	sp,*4.
or	*40.(di),*32.
j	L70
L72:cmp	*-6.(bp),*127.
jle	L20012
cmp	*-6.(bp),#128.
jne	L76
or	*42.(di),*2.
j	L70
L76:or	*40.(di),*1.
mov	dx,*-6.(bp)
and	dx,*127.
push	dx
push	di
mov	dx,#_ttrstrt
push	dx
call	_timeout
add	sp,*6.
j	L70
L71:test	*40.(di),*64.
je	L70
and	*40.(di),*-65.
mov	dx,di
add	dx,*6.
push	dx
call	_wakeup
L20007:add	sp,*2.
L70:|NREG	2
|RTYP	0
br	cret
.globl	_consioc
.even
_consioc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_com=6.
|	_addr=8.
sub	sp,*6.
|NREG	2
|	_tp=di
|	_zs=-6.
|	_i=-8.
|	_s=-10.
mov	di,#_constty
cmp	*6.(bp),#27405.
bne	L83
push	di
call	_wflusht
add	sp,*2.
call	_spl3
mov	*-10.(bp),ax
mov	*-8.(bp),*0.
L84:cmp	*-8.(bp),#4608.
jge	L93
push	*8.(bp)
inc	*8.(bp)
call	_fubyte
add	sp,*2.
movb	*-6.(bp),ax
mov	ax,*-8.(bp)
cwd
mov	cx,*18.
idiv	cx
test	dx,dx
jne	L90
cmpb	*-6.(bp),*16.
je	L90
cmpb	*-6.(bp),*0
je	L93
movb	111.+_u,*14.
L93:sub	si,si
push	si
mov	si,#512.
push	si
call	_outb
add	sp,*4.
mov	si,#512.
push	si
call	_inb
add	sp,*2.
and	ax,*2.
jne	L93
push	*-10.(bp)
call	_splx
add	sp,*2.
j	L82
L90:sub	si,si
push	si
mov	si,#512.
push	si
call	_outb
add	sp,*4.
mov	si,#512.
push	si
call	_inb
add	sp,*2.
and	ax,*2.
jne	L90
movb	ax,*-6.(bp)
cbw
push	ax
mov	si,#514.
push	si
call	_outb
add	sp,*4.
inc	*-8.(bp)
br	L84
L83:push	*4.(bp)
push	*8.(bp)
push	di
push	*6.(bp)
call	_ttiocco
add	sp,*8.
test	ax,ax
je	L94
cmp	*6.(bp),#29705.
je	L10011
cmp	*6.(bp),#29706.
jne	L96
L10011:push	*4.(bp)
call	_conspar
add	sp,*2.
j	L96
L94:movb	111.+_u,*25.
L96:|NREG	3
L82:|RTYP	0
br	cret
.data
L16:.byte	/46,/65,/68,/6c,/65,/72,/20,/4b,/65
.byte	/6e,/6e,/75,/6e,/67
.byte	/73,/61,/62,/66,/72,/61,/67,/65,/20
.byte	/41,/42,/53,/2f,/4b,/47
.byte	/53,/20,/25,/6f,/20,/25,/6f,/a,/0
L21:.byte	/43,/6f,/6e,/73,/6f,/6c,/65,/20,/20
.byte	/44,/61,/74,/61,/20
.byte	/20,/25,/78,/20,/54,/69,/6d,/65,/72
.byte	/20,/25,/64,/20,/25,/73
.byte	/2e,/a,/0
L22:.byte	/66,/6f,/75,/6e,/64,/0
L23:.byte	/4e,/4f,/54,/20,/66,/6f,/75,/6e,/64
.byte	/0
L35:.byte	/1b,/5b,/3f,/37,/68,/0
