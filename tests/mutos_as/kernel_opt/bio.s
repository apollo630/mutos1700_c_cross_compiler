.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_bfreeli,26
.globl	_bread
.text
.even
_bread:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
|	_blkno=6.
|NREG	1
|	_bp=si
push	*8.(bp)
push	*6.(bp)
push	di
call	_getblk
add	sp,*6.
mov	si,ax
test	(si),*2.
je	L4
add	12.+_io_info,*1.
adc	10.+_io_info,*0
j	L20001
L4:or	(si),*1.
mov	*12.(si),#1024.
push	si
mov	bx,di
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@4.+_bdevsw(bx)
add	sp,*2.
add	4.+_io_info,*1.
adc	2.+_io_info,*0
push	si
call	_iowait
add	sp,*2.
L20001:mov	ax,si
|RTYP	12
br	cret
.globl	_breada
.even
_breada:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
|	_blkno=6.
|	_rablkno=10.
sub	sp,*2.
|NREG	1
|	_bp=si
|	_rabp=-6.
sub	si,si
push	*8.(bp)
push	*6.(bp)
push	di
call	_incore
add	sp,*6.
test	ax,ax
jne	L8
push	*8.(bp)
push	*6.(bp)
push	di
call	_getblk
add	sp,*6.
mov	si,ax
test	(si),*2.
jne	L8
or	(si),*1.
mov	*12.(si),#1024.
push	si
mov	bx,di
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@4.+_bdevsw(bx)
add	sp,*2.
add	4.+_io_info,*1.
adc	2.+_io_info,*0
L8:mov	ax,*12.(bp)
mov	dx,*10.(bp)
cmp	dx,*0
jne	L10000
cmp	ax,*0
je	L10
L10000:push	*12.(bp)
push	*10.(bp)
push	di
call	_incore
add	sp,*6.
test	ax,ax
jne	L10
push	*12.(bp)
push	*10.(bp)
push	di
call	_getblk
add	sp,*6.
mov	*-6.(bp),ax
mov	bx,ax
test	(bx),*2.
je	L11
push	ax
call	_brelse
add	sp,*2.
j	L10
L11:mov	bx,*-6.(bp)
or	(bx),#257.
mov	bx,*-6.(bp)
mov	*12.(bx),#1024.
push	*-6.(bp)
mov	bx,di
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@4.+_bdevsw(bx)
add	sp,*2.
add	8.+_io_info,*1.
adc	6.+_io_info,*0
L10:cmp	si,*0
jne	L13
push	*8.(bp)
push	*6.(bp)
push	di
call	_bread
add	sp,*6.
j	L7
L13:push	si
call	_iowait
add	sp,*2.
mov	ax,si
L7:|RTYP	12
br	cret
.globl	_bwrite
.even
_bwrite:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
|NREG	1
|	_flag=si
mov	si,(di)
and	(di),#-648.
mov	*12.(di),#1024.
add	16.+_io_info,*1.
adc	14.+_io_info,*0
push	di
mov	bx,*10.(di)
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@4.+_bdevsw(bx)
add	sp,*2.
test	si,#256.
jne	L17
push	di
call	_iowait
add	sp,*2.
push	di
call	_brelse
j	L20002
L17:test	si,#512.
je	L19
or	(di),#128.
j	L18
L19:push	di
call	_geterro
L20002:add	sp,*2.
L18:|NREG	2
|RTYP	0
br	cret
.globl	_bdwrite
.even
_bdwrite:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
|NREG	1
|	_dp=si
mov	bx,*10.(di)
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
mov	si,6.+_bdevsw(bx)
test	(si),#1024.
je	L24
push	di
call	_bawrite
j	L20003
L24:or	(di),#514.
push	di
call	_brelse
L20003:add	sp,*2.
|NREG	2
|RTYP	0
br	cret
.globl	_bawrite
.even
_bawrite:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
or	(di),#256.
push	di
call	_bwrite
add	sp,*2.
|RTYP	0
br	cret
.globl	_brelse
.even
_brelse:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
sub	sp,*2.
|NREG	1
|	_backp=si
|	_s=-6.
test	(di),*64.
je	L32
push	di
call	_wakeup
add	sp,*2.
L32:test	_bfreeli,*64.
je	L33
and	_bfreeli,*-65.
mov	dx,#_bfreeli
push	dx
call	_wakeup
add	sp,*2.
L33:call	_spl6
mov	*-6.(bp),ax
test	(di),#8196.
je	L34
mov	*10.(di),*-1.
L34:test	(di),#128.
jne	L10001
cmp	*10.(di),*-1.
jne	L35
L10001:mov	si,#6.+_bfreeli
mov	bx,(si)
mov	*8.(bx),di
mov	dx,(si)
mov	*6.(di),dx
mov	(si),di
mov	*8.(di),#_bfreeli
j	L36
L35:mov	si,#8.+_bfreeli
mov	bx,(si)
mov	*6.(bx),di
mov	dx,(si)
mov	*8.(di),dx
mov	(si),di
mov	*6.(di),#_bfreeli
L36:and	(di),#-8649.
push	*-6.(bp)
call	_splx
add	sp,*2.
|NREG	2
|RTYP	0
br	cret
.globl	_incore
.even
_incore:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
|	_blkno=6.
sub	sp,*2.
|NREG	1
|	_bp=si
|	_dp=-6.
mov	bx,di
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
mov	bx,#6.+_bdevsw(bx)
mov	*-6.(bp),bx
mov	si,*2.(bx)
j	L40
L20005:mov	ax,*18.(si)
mov	dx,*16.(si)
cmp	dx,*6.(bp)
jne	L42
cmp	ax,*8.(bp)
jne	L42
cmp	*10.(si),di
jne	L42
mov	dx,*1.
j	L20006
L42:mov	si,*2.(si)
L40:cmp	si,*-6.(bp)
jne	L20005
sub	dx,dx
L20006:mov	ax,dx
|RTYP	0
br	cret
.globl	_getblk
.even
_getblk:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
|	_blkno=6.
sub	sp,*4.
|NREG	1
|	_bp=si
|	_dp=-6.
|	_i=-8.
mov	dx,di
mov	cx,*8.
sar	dx,cl
cmp	dx,_nblkdev
jl	L49
mov	dx,#L48
push	dx
call	_panic
L20012:add	sp,*2.
L49:call	_spl0
mov	bx,di
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
mov	dx,#6.+_bdevsw(bx)
mov	*-6.(bp),dx
cmp	dx,*0
jne	L50
mov	dx,#L51
push	dx
call	_panic
add	sp,*2.
L50:mov	bx,*-6.(bp)
mov	si,*2.(bx)
br	L52
L20011:mov	ax,*18.(si)
mov	dx,*16.(si)
cmp	dx,*6.(bp)
jne	L54
cmp	ax,*8.(bp)
jne	L54
cmp	*10.(si),di
jne	L54
call	_spl6
test	(si),*8.
je	L55
or	(si),*64.
mov	dx,*21.
push	dx
push	si
L20016:call	_sleep
add	sp,*4.
j	L49
L55:call	_spl0
mov	*-8.(bp),*0.
mov	dx,*6.(si)
L20007:mov	*-6.(bp),dx
cmp	dx,#_bfreeli
jne	L20009
cmp	*-8.(bp),*50.
jge	L58
mov	bx,*-8.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,18.+_io_info+2(bx)
mov	dx,18.+_io_info(bx)
add	18.+_io_info+2(bx),*1
adc	18.+_io_info(bx),*0
L58:push	si
call	_notavai
add	sp,*2.
br	L20014
L20009:inc	*-8.(bp)
mov	bx,*-6.(bp)
mov	dx,*6.(bx)
j	L20007
L54:mov	si,*2.(si)
L52:cmp	si,*-6.(bp)
bne	L20011
call	_spl6
cmp	6.+_bfreeli,#_bfreeli
jne	L59
or	_bfreeli,*64.
mov	dx,*21.
push	dx
mov	dx,#_bfreeli
push	dx
br	L20016
L59:call	_spl0
mov	si,6.+_bfreeli
push	si
call	_notavai
add	sp,*2.
test	(si),#512.
je	L60
or	(si),#256.
push	si
call	_bwrite
br	L20012
L60:mov	(si),*8.
mov	bx,*4.(si)
mov	dx,*2.(si)
mov	*2.(bx),dx
mov	bx,*2.(si)
mov	dx,*4.(si)
mov	*4.(bx),dx
mov	bx,*-6.(bp)
mov	dx,*2.(bx)
mov	*2.(si),dx
mov	dx,*-6.(bp)
mov	*4.(si),dx
mov	bx,*-6.(bp)
mov	bx,*2.(bx)
mov	*4.(bx),si
mov	bx,*-6.(bp)
mov	*2.(bx),si
mov	*10.(si),di
mov	ax,*8.(bp)
mov	dx,*6.(bp)
mov	*18.(si),ax
mov	*16.(si),dx
L20014:mov	ax,si
|RTYP	12
br	cret
.globl	_geteblk
.even
_geteblk:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_bp=di
|NREG	1
|	_dp=si
L64:call	_spl6
j	L65
L20018:or	_bfreeli,*64.
mov	dx,*21.
push	dx
mov	dx,#_bfreeli
push	dx
call	_sleep
add	sp,*4.
L65:cmp	6.+_bfreeli,#_bfreeli
je	L20018
call	_spl0
mov	si,#_bfreeli
mov	di,6.+_bfreeli
push	di
call	_notavai
add	sp,*2.
test	(di),#512.
je	L67
or	(di),#256.
push	di
call	_bwrite
add	sp,*2.
j	L64
L67:mov	(di),*8.
mov	bx,*4.(di)
mov	dx,*2.(di)
mov	*2.(bx),dx
mov	bx,*2.(di)
mov	dx,*4.(di)
mov	*4.(bx),dx
mov	dx,*2.(si)
mov	*2.(di),dx
mov	*4.(di),si
mov	bx,*2.(si)
mov	*4.(bx),di
mov	*2.(si),di
mov	*10.(di),*-1.
mov	ax,di
|RTYP	12
br	cret
.globl	_iowait
.even
_iowait:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
call	_spl6
j	L71
L20020:mov	si,*20.
push	si
push	di
call	_sleep
add	sp,*4.
L71:test	(di),*2.
je	L20020
call	_spl0
push	di
call	_geterro
add	sp,*2.
|RTYP	0
br	cret
.globl	_notavai
.even
_notavai:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
|NREG	1
|	_s=si
call	_spl6
mov	si,ax
mov	bx,*8.(di)
mov	dx,*6.(di)
mov	*6.(bx),dx
mov	bx,*6.(di)
mov	dx,*8.(di)
mov	*8.(bx),dx
or	(di),*8.
push	si
call	_splx
add	sp,*2.
|NREG	2
|RTYP	0
br	cret
.globl	_iodone
.even
_iodone:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
or	(di),*2.
test	(di),#256.
je	L79
push	di
call	_brelse
j	L20021
L79:and	(di),*-65.
push	di
call	_wakeup
L20021:add	sp,*2.
|RTYP	0
br	cret
.globl	_clrbuf
.even
_clrbuf:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
sub	sp,*2.
|NREG	1
|	_p=si
|	_c=-6.
mov	si,*14.(di)
mov	*-6.(bp),#512.
L86:mov	(si),*0.
add	si,*2.
dec	*-6.(bp)
jne	L86
mov	*22.(di),*0.
|NREG	2
|RTYP	0
br	cret
.globl	_bpurge
.even
_bpurge:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
sub	sp,*6.
|NREG	1
|	_bp=si
|	_dp=-6.
|	_s=-8.
|	_d=-10.
mov	*-10.(bp),*0.
mov	dx,di
mov	cx,*8.
sar	dx,cl
cmp	dx,_nblkdev
jl	L90
mov	dx,#L91
push	dx
call	_panic
add	sp,*2.
L90:push	di
call	_bflush
add	sp,*2.
mov	bx,di
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
mov	bx,#6.+_bdevsw(bx)
mov	*-6.(bp),bx
mov	si,*2.(bx)
j	L92
L20023:cmp	di,*10.(si)
jne	L94
call	_spl6
mov	*-8.(bp),ax
or	(si),#8192.
test	(si),#512.
je	L96
mov	*-10.(bp),*-1.
j	L97
L96:test	(si),*8.
jne	L97
push	si
call	_notavai
add	sp,*2.
push	si
call	_brelse
add	sp,*2.
L97:push	*-8.(bp)
call	_splx
add	sp,*2.
L94:mov	si,*2.(si)
L92:cmp	si,*-6.(bp)
jne	L20023
L99:mov	bx,*-6.(bp)
mov	si,*2.(bx)
j	L100
L20025:cmp	ax,#8192.
je	L108
cmp	ax,#8200.
je	L109
L104:push	*-8.(bp)
call	_splx
add	sp,*2.
L102:mov	si,*2.(si)
L100:cmp	si,*-6.(bp)
je	L101
cmp	di,*10.(si)
jne	L102
call	_spl6
mov	*-8.(bp),ax
mov	ax,(si)
and	ax,#8200.
test	ax,ax
je	L104
cmp	ax,*8.
jne	L20025
L108:mov	*-10.(bp),*-1.
j	L104
L109:or	(si),*64.
mov	dx,*21.
push	dx
push	si
call	_sleep
add	sp,*4.
push	*-8.(bp)
call	_splx
add	sp,*2.
j	L99
L101:mov	ax,*-10.(bp)
|RTYP	0
br	cret
.globl	_bflush
.even
_bflush:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
|NREG	1
|	_bp=si
j	L113
L20027:cmp	di,*-1.
je	L10003
cmp	di,*10.(si)
jne	L116
L10003:push	si
call	_notavai
add	sp,*2.
push	si
call	_bwrite
add	sp,*2.
L113:call	_spl6
mov	si,6.+_bfreeli
j	L114
L20029:test	(si),#512.
jne	L20027
L116:mov	si,*6.(si)
L114:cmp	si,#_bfreeli
jne	L20029
call	_spl0
|NREG	2
|RTYP	0
br	cret
.globl	_geterro
.even
_geterro:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
test	(di),*4.
je	L120
movb	dx,*21.(di)
movb	111.+_u,dx
testb	dx,dx
jne	L120
movb	111.+_u,*5.
L120:|RTYP	0
br	cret
.data
L48:.byte	/62,/6c,/6b,/64,/65,/76,/0
L51:.byte	/64,/65,/76,/74,/61,/62,/0
L91:.byte	/62,/6c,/6b,/64,/65,/76,/0
