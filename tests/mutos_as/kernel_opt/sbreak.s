.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.globl	_expand
.text
.even
_expand:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_newsize=4.
sub	sp,*10.
|NREG	2
|	_n=di
|NREG	1
|	_p=si
|	_a1=-6.
|	_a2=-8.
|	_i=-10.
|	_nsp=-12.
|	_a3=-14.
mov	si,120.+_u
mov	di,*18.(si)
mov	dx,*4.(bp)
mov	*18.(si),dx
mov	dx,*16.(si)
mov	*-6.(bp),dx
cmp	di,*4.(bp)
beq	L3
cmp	di,*0
je	L10000
push	*4.(bp)
push	di
call	_max
add	sp,*4.
cmp	ax,*33.
ble	L3
L10000:cmp	di,*4.(bp)
jl	L4
mov	dx,*-6.(bp)
add	dx,*4.(bp)
push	dx
mov	dx,di
sub	dx,*4.(bp)
push	dx
mov	dx,#_coremap
push	dx
call	_mfree
add	sp,*6.
br	L3
L4:mov	dx,#262.+_u
push	dx
call	_save
add	sp,*2.
test	ax,ax
bne	L3
push	*16.(si)
mov	dx,*31.
push	dx
call	_copyseg
add	sp,*4.
push	*4.(bp)
mov	dx,#_coremap
push	dx
call	_malloc
add	sp,*4.
mov	*-8.(bp),ax
cmp	ax,*0
jne	L6
push	di
mov	dx,*1.
push	dx
push	si
call	_xswap
add	sp,*6.
orb	*1.(si),*8.
call	_qswtch
L6:mov	dx,*-8.(bp)
mov	*16.(si),dx
mov	*-10.(bp),*0.
j	L7
L20001:mov	dx,*-8.(bp)
add	dx,*-10.(bp)
push	dx
mov	dx,*-6.(bp)
add	dx,*-10.(bp)
push	dx
call	_copyseg
add	sp,*4.
inc	*-10.(bp)
L7:cmp	*-10.(bp),di
jl	L20001
push	*-6.(bp)
push	di
mov	dx,#_coremap
push	dx
call	_mfree
add	sp,*6.
mov	dx,#262.+_u
push	dx
push	*-8.(bp)
call	_resume
add	sp,*4.
|NREG	3
L3:|RTYP	0
br	cret
.globl	_sureg
.even
_sureg:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*8.
|	_p=-6.
|	_psp=-8.
|	_pn=-10.
|	_pt=-12.
mov	di,120.+_u
mov	*-6.(bp),di
mov	di,322.+_u
mov	di,*16.(di)
mov	*-8.(bp),di
mov	di,*-6.(bp)
mov	di,*16.(di)
inc	di
mov	cx,*7.
sal	di,cl
mov	*-10.(bp),di
mov	si,322.+_u
mov	*10.(si),di
mov	di,*-10.(bp)
mov	si,322.+_u
mov	*14.(si),di
push	*-10.(bp)
mov	di,*-8.(bp)
add	di,*2.
push	di
call	_suword
add	sp,*4.
mov	di,*-6.(bp)
mov	di,*26.(di)
mov	di,*2.(di)
mov	cx,*7.
sal	di,cl
mov	*-12.(bp),di
cmpb	333.+_u,*0
je	L10001
mov	di,*-6.(bp)
mov	di,*26.(di)
test	di,di
jne	L13
L10001:push	*-10.(bp)
mov	di,*-8.(bp)
add	di,*10.
push	di
call	_suword
add	sp,*4.
push	*-10.(bp)
mov	di,400.+_u
add	di,*4.
push	di
call	_suword
add	sp,*4.
push	*-10.(bp)
mov	di,400.+_u
add	di,*8.
push	di
j	L20003
L13:mov	di,*-6.(bp)
mov	di,*26.(di)
test	di,di
je	L12
push	*-12.(bp)
mov	di,*-8.(bp)
add	di,*10.
push	di
call	_suword
add	sp,*4.
push	*-12.(bp)
mov	di,*4.
push	di
call	_suword
add	sp,*4.
push	*-12.(bp)
mov	di,*8.
push	di
L20003:call	_suword
add	sp,*4.
L12:|RTYP	0
br	cret
.globl	_grow
.even
_grow:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_sp=4.
sub	sp,*4.
|NREG	2
|	_si=di
|NREG	1
|	_i=si
|	_p=-6.
|	_a=-8.
mov	dx,248.+_u
mov	cx,*11.
sal	dx,cl
neg	dx
cmp	dx,*4.(bp)
ja	L19
L20007:sub	dx,dx
j	L20006
L19:mov	di,248.+_u
mov	cx,*11.
sal	di,cl
neg	di
sub	di,*4.(bp)
add	di,#3071.
mov	cx,*11.
shr	di,cl
cmp	di,*0
jle	L20007
mov	dx,*32.
sub	dx,248.+_u
sub	dx,di
cmp	dx,246.+_u
jge	L21
movb	111.+_u,*12.
j	L20007
L21:add	248.+_u,di
mov	bx,120.+_u
mov	*-6.(bp),bx
mov	dx,*16.(bx)
add	dx,*33.
sub	dx,248.+_u
mov	*-8.(bp),dx
add	*18.(bx),di
j	L23
L20005:push	*-8.(bp)
inc	*-8.(bp)
call	_clearse
add	sp,*2.
L23:mov	dx,di
dec	di
test	dx,dx
jne	L20005
mov	dx,*1.
L20006:mov	ax,dx
|RTYP	0
br	cret
.globl	_sbreak
.even
_sbreak:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_debug=4.
sub	sp,*12.
|NREG	2
|	_n=di
|NREG	1
|	_d=si
|	_a=-6.
|	_b=-8.
|	_nsp=-10.
|	_p=-12.
|	_e=-14.
|	_t=-16.
mov	bx,122.+_u
mov	di,(bx)
add	di,#2047.
mov	cx,*11.
shr	di,cl
cmp	di,*0
jne	L28
mov	di,*32.
L28:cmpb	333.+_u,*0
jne	L29
cmp	244.+_u,*0
je	L29
mov	dx,#L31
push	dx
call	_panic
add	sp,*2.
L29:mov	dx,di
sub	dx,246.+_u
mov	si,dx
test	dx,dx
beq	L27
cmp	si,*0
jle	L32
mov	bx,322.+_u
mov	dx,*16.(bx)
mov	cx,*11.
shr	dx,cl
mov	*-10.(bp),dx
mov	bx,*32.
sub	bx,248.+_u
mov	dx,246.+_u
add	dx,si
cmp	dx,bx
je	L10002
mov	dx,246.+_u
add	dx,si
cmp	dx,*-10.(bp)
jl	L32
L10002:movb	111.+_u,*12.
j	L27
L32:add	246.+_u,si
mov	bx,120.+_u
mov	*-12.(bp),bx
push	bx
mov	dx,*16.(bx)
pop	bx
add	dx,*22.(bx)
mov	*-8.(bp),dx
mov	bx,*-12.(bp)
add	*22.(bx),si
mov	bx,*-12.(bp)
mov	dx,*22.(bx)
add	dx,248.+_u
push	dx
call	_expand
add	sp,*2.
cmp	si,*0
jle	L38
L35:mov	dx,si
dec	si
test	dx,dx
je	L37
push	*-8.(bp)
inc	*-8.(bp)
call	_clearse
add	sp,*2.
j	L35
L38:mov	dx,si
inc	si
test	dx,dx
je	L37
dec	*-8.(bp)
push	*-8.(bp)
call	_clearse
add	sp,*2.
j	L38
L37:|NREG	3
L27:|RTYP	0
br	cret
.data
L31:.byte	/20,/54,/65,/78,/74,/20,/6f,/6e,/20
.byte	/4e,/6f,/6e,/2d,/53
.byte	/45,/50,/49,/44,/0
