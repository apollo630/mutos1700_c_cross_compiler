.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.globl	_disksor
.text
.even
_disksor:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_p=di
|NREG	1
mov	si,*6.(bp)
|	_q=si
sub	sp,*4.
|	_a=-6.
|	_b=-8.
mov	dx,*6.(di)
mov	*-6.(bp),dx
cmp	dx,*0
jne	L4
mov	*6.(di),si
mov	*8.(di),si
mov	*6.(si),*0.
j	L3
L4:mov	*-8.(bp),*0.
L5:cmp	*-6.(bp),*0
je	L6
test	(si),*1.
je	L7
mov	dx,*-6.(bp)
mov	bx,dx
test	(bx),*1.
jne	L7
cmp	*-8.(bp),*0
jne	L6
mov	*-8.(bp),dx
L6:cmp	*-8.(bp),*0
jne	L13
mov	dx,*8.(di)
mov	*-8.(bp),dx
L13:mov	bx,*-8.(bp)
mov	dx,*6.(bx)
mov	*6.(si),dx
mov	bx,*-8.(bp)
mov	*6.(bx),si
mov	dx,*8.(di)
cmp	*-8.(bp),dx
jne	L3
mov	*8.(di),si
L3:|RTYP	0
br	cret
L7:test	(si),*1.
jne	L10000
mov	bx,*-6.(bp)
test	(bx),*1.
jne	L10
L10000:mov	bx,*-6.(bp)
mov	dx,*24.(bx)
cmp	dx,*24.(si)
ja	L10
cmp	*-8.(bp),*0
je	L10001
push	bx
mov	bx,*-8.(bp)
mov	dx,*24.(bx)
pop	bx
cmp	*24.(bx),dx
jb	L10
L10001:mov	dx,*-6.(bp)
mov	*-8.(bp),dx
L10:mov	bx,*-6.(bp)
mov	dx,*6.(bx)
mov	*-6.(bp),dx
br	L5
.data
