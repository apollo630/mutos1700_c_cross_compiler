.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_lpcfg,10
.comm	_lps,24
.comm	_lplbs,16
.comm	_lplbd,16
.globl	_lpinit
.text
.even
_lpinit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*4.
|NREG	2
|	_cfg=di
|NREG	1
|	_lp=si
|	_i=-6.
|	_alive=-8.
mov	*-6.(bp),*0.
L20001:mov	ax,*-6.(bp)
mov	cx,*24.
imul	cx
mov	si,ax
add	si,#_lps
mov	ax,*-6.(bp)
mov	cx,*10.
imul	cx
mov	di,ax
add	di,#_lpcfg
mov	dx,#148.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*9.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*15.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*5.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,#170.
push	dx
push	*4.(di)
call	_outb
add	sp,*4.
push	*4.(di)
call	_inb
add	sp,*2.
cmp	ax,#170.
je	L7
mov	*-8.(bp),*0.
j	L8
L7:mov	*-8.(bp),*8.
L8:cmp	_Stand,*0
jne	L9
cmp	*-8.(bp),*0
je	L10000
mov	dx,#L11
push	dx
j	L10001
L10000:mov	dx,#L12
push	dx
L10001:push	(di)
push	*2.(di)
mov	dx,#L10
push	dx
call	_printf
add	sp,*8.
L9:mov	dx,*-8.(bp)
or	*14.(si),dx
mov	bx,(di)
sal	bx,*1
mov	_lplbs(bx),si
mov	bx,(di)
sal	bx,*1
mov	_lplbd(bx),di
inc	*-6.(bp)
cmp	*-6.(bp),*1.
blt	L20001
|NREG	3
|RTYP	0
br	cret
.globl	_lpopen
.even
_lpopen:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
|	_unit=di
|NREG	1
|	_lp=si
mov	di,*4.(bp)
and	di,#255.
mov	cx,*3.
sar	di,cl
mov	ax,di
mov	cx,*24.
imul	cx
mov	si,ax
add	si,#_lps
cmp	di,*1.
jge	L10002
test	*14.(si),*8.
jne	L16
L10002:movb	111.+_u,*6.
j	L15
L16:mov	ax,di
mov	cx,*10.
imul	cx
add	ax,#_lpcfg
mov	*18.(si),ax
test	*14.(si),*1.
je	L17
movb	111.+_u,*16.
j	L15
L17:or	*14.(si),*1.
call	_geteblk
mov	*22.(si),ax
mov	dx,*4.(bp)
subb	dh,dh
mov	*16.(si),dx
mov	ax,di
mov	cx,*10.
imul	cx
mov	bx,ax
push	#_lpcfg(bx)
call	_lpintr
add	sp,*2.
cmp	*16.(si),*0
jne	L18
mov	dx,*12.
push	dx
push	*4.(bp)
call	_lp_cano
add	sp,*4.
L18:|NREG	3
L15:|RTYP	0
br	cret
.globl	_lpwrite
.even
_lpwrite:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*6.
|NREG	2
|	_lp=di
|NREG	1
|	_unit=si
|	_a=-6.
|	_c=-8.
|	_i=-10.
mov	si,*4.(bp)
and	si,#255.
mov	cx,*3.
sar	si,cl
mov	ax,si
mov	cx,*24.
imul	cx
mov	di,ax
add	di,#_lps
L22:push	130.+_u
mov	dx,#1024.
push	dx
call	_min
add	sp,*4.
mov	si,ax
test	ax,ax
beq	L21
mov	bx,*22.(di)
mov	dx,*14.(bx)
mov	*-6.(bp),dx
sub	dx,dx
push	dx
push	si
push	*-6.(bp)
call	_iomove
add	sp,*6.
j	L23
L20003:mov	bx,*-6.(bp)
movb	ax,(bx)
cbw
push	ax
inc	*-6.(bp)
push	di
call	_lp_outc
L20004:add	sp,*4.
j	L23
L10003:mov	bx,*-6.(bp)
movb	ax,(bx)
cbw
push	ax
inc	*-6.(bp)
push	*4.(bp)
call	_lp_cano
j	L20004
L29:mov	dx,*13.
push	dx
push	di
call	_lp_outc
add	sp,*4.
mov	dx,*10.
push	dx
push	di
call	_lp_outc
add	sp,*4.
mov	*8.(di),*0.
L23:mov	dx,si
dec	si
test	dx,dx
je	L22
cmp	*16.(di),*4.
je	L20003
cmp	*16.(di),*2.
je	L10003
cmp	*16.(di),*3.
je	L10003
mov	bx,*-6.(bp)
movb	ax,(bx)
movb	*-8.(bp),ax
inc	*-6.(bp)
cbw
sub	ax,#/8
cmp	ax,*2.
ja	L34
sal	ax,#1
xchg	bx,ax
seg	cs
br	@L10006(bx)
L30:mov	dx,*8.
push	dx
push	di
call	_lp_outc
add	sp,*4.
dec	*8.(di)
j	L23
L31:mov	ax,*8.(di)
cwd
mov	cx,*8.
idiv	cx
sub	cx,dx
mov	*-10.(bp),cx
L32:mov	dx,*-10.(bp)
dec	*-10.(bp)
test	dx,dx
je	L23
mov	dx,*32.
push	dx
push	di
call	_lp_outc
add	sp,*4.
inc	*8.(di)
j	L32
L34:cmp	*16.(di),*0
jne	L35
movb	ax,*-8.(bp)
cbw
push	ax
push	di
j	L20010
L20016:cmpb	*-8.(bp),*122.
jg	L37
addb	*-8.(bp),*-32.
movb	ax,*-8.(bp)
cbw
push	ax
push	di
j	L20010
L20018:cmp	ax,*124.
je	L45
cmp	ax,*125.
je	L43
cmp	ax,*126.
je	L46
movb	ax,*-8.(bp)
cbw
push	ax
push	di
L20010:call	_lp_outc
add	sp,*4.
inc	*8.(di)
br	L23
L35:cmpb	*-8.(bp),*97.
jge	L20016
L37:movb	ax,*-8.(bp)
cbw
cmp	ax,*96.
je	L44
cmp	ax,*123.
jne	L20018
mov	dx,*40.
push	dx
push	di
j	L20007
L43:mov	dx,*41.
push	dx
push	di
j	L20007
L44:mov	dx,*39.
push	dx
push	di
j	L20007
L45:mov	dx,*33.
push	dx
push	di
j	L20007
L46:mov	dx,*94.
push	dx
push	di
L20007:call	_lp_outc
add	sp,*4.
mov	dx,*8.
push	dx
push	di
call	_lp_outc
add	sp,*4.
mov	dx,*45.
push	dx
push	di
j	L20010
L10006:L30
L31
L29
br	L23
L21:|RTYP	0
br	cret
.globl	_lpclose
.even
_lpclose:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
|	_lp=di
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*3.
sar	ax,cl
mov	cx,*24.
imul	cx
mov	di,ax
add	di,#_lps
cmp	*16.(di),*2.
je	L10007
cmp	*16.(di),*3.
jne	L51
L10007:mov	si,*12.
push	si
push	*4.(bp)
call	_lp_cano
add	sp,*4.
L51:push	*22.(di)
call	_brelse
add	sp,*2.
and	*14.(di),*-2.
|NREG	3
|RTYP	0
br	cret
.globl	_lp_cano
.even
_lp_cano:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
mov	di,*6.(bp)
|	_c=di
sub	sp,*8.
|NREG	1
|	_a=si
|	_lp=-6.
|	_b=-8.
|	_x=-10.
|	_ch=-12.
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*3.
sar	ax,cl
mov	cx,*24.
imul	cx
add	ax,#_lps
mov	*-6.(bp),ax
mov	bx,ax
cmp	*16.(bx),*3.
jne	L55
cmp	di,*97.
jl	L56
cmp	di,*122.
jg	L56
add	di,*-32.
L55:mov	dx,*-6.(bp)
mov	bx,dx
mov	si,*8.(bx)
mov	bx,dx
mov	dx,*6.(bx)
mov	*-8.(bp),dx
mov	dx,*10.(bx)
mov	*-10.(bp),dx
cmp	di,*32.
beq	L66
mov	ax,di
sub	ax,#/8
cmp	ax,*5.
bhi	L81
sal	ax,#1
xchg	bx,ax
seg	cs
br	@L10011(bx)
L56:mov	ax,di
cmp	ax,*96.
je	L63
cmp	ax,*123.
je	L60
cmp	ax,*124.
je	L64
cmp	ax,*125.
je	L62
cmp	ax,*126.
je	L65
j	L55
L60:mov	*-12.(bp),*40.
j	L61
L62:mov	*-12.(bp),*41.
j	L61
L63:mov	*-12.(bp),*39.
j	L61
L64:mov	*-12.(bp),*33.
j	L61
L65:mov	*-12.(bp),*94.
L61:push	*-12.(bp)
push	*4.(bp)
call	_lp_cano
add	sp,*4.
mov	bx,*-6.(bp)
dec	*8.(bx)
mov	di,*45.
br	L55
L69:add	si,*8.
and	si,*-8.
br	L86
L70:mov	bx,*-6.(bp)
mov	dx,*10.(bx)
test	dx,dx
jne	L71
cmp	*-8.(bp),*0
beq	L86
L71:cmp	di,*12.
jne	L72
mov	dx,*13.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
mov	bx,*-6.(bp)
test	*16.(bx),*1.
jne	L73
mov	dx,*72.
sub	dx,*-10.(bp)
dec	dx
mov	*-10.(bp),dx
mov	di,*10.
L74:mov	dx,*-10.(bp)
dec	*-10.(bp)
test	dx,dx
jle	L76
push	di
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
j	L74
L73:mov	dx,*10.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
L76:mov	bx,*-6.(bp)
mov	*10.(bx),*0.
j	L77
L72:cmp	si,*0
jle	L78
mov	dx,*13.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
L78:mov	bx,*-6.(bp)
inc	*10.(bx)
L77:mov	*-8.(bp),*0.
push	di
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
L79:sub	si,si
j	L86
L80:cmp	si,*0
jle	L86
dec	si
j	L86
L81:cmp	si,*-8.(bp)
jge	L82
mov	dx,*13.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
mov	*-8.(bp),*0.
L82:cmp	si,#132.
jl	L84
j	L66
L20020:mov	dx,*32.
push	dx
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
inc	*-8.(bp)
L84:cmp	si,*-8.(bp)
jg	L20020
push	di
push	*-6.(bp)
call	_lp_outc
add	sp,*4.
inc	*-8.(bp)
L66:inc	si
L86:cmp	si,#132.
jle	L87
mov	si,#132.
L87:mov	dx,*-6.(bp)
mov	bx,dx
cmp	*10.(bx),*71.
jle	L88
mov	bx,dx
mov	*10.(bx),*0.
L88:mov	bx,*-6.(bp)
mov	*8.(bx),si
mov	bx,*-6.(bp)
mov	dx,*-8.(bp)
mov	*6.(bx),dx
|NREG	2
|RTYP	0
br	cret
L10011:L80
L69
L71
L81
L70
L79
j	L86
.globl	_lp_outc
.even
_lp_outc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_lp=di
|	_c=6.
sub	sp,*2.
|	_s=-6.
call	_spl5
mov	*-6.(bp),ax
j	L92
L20022:or	*14.(di),*2.
mov	si,*26.
push	si
push	di
call	_sleep
add	sp,*4.
L92:cmp	(di),*75.
jge	L20022
cmp	(di),*0
jg	L96
test	*14.(di),*4.
jne	L96
push	*6.(bp)
push	di
push	*18.(di)
call	_lpoutpu
add	sp,*6.
L95:push	*-6.(bp)
call	_splx
add	sp,*2.
|RTYP	0
br	cret
L96:push	di
push	*6.(bp)
call	_putc
add	sp,*4.
test	ax,ax
je	L95
mov	si,*26.
push	si
mov	si,#_lbolt
push	si
call	_sleep
add	sp,*4.
j	L96
.globl	_lpintr
.even
_lpintr:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_level=4.
sub	sp,*2.
|NREG	2
|	_lp=di
|NREG	1
|	_cfg=si
|	_a=-6.
mov	bx,*4.(bp)
sal	bx,*1
mov	di,_lplbs(bx)
mov	bx,*4.(bp)
sal	bx,*1
mov	si,_lplbd(bx)
push	*2.(si)
call	_inb
add	sp,*2.
mov	*-6.(bp),ax
test	ax,*1.
jne	L101
and	*14.(di),*-5.
j	L103
L101:or	*14.(di),*4.
j	L103
L20024:cmp	(di),*0
jle	L104
push	di
call	_getc
add	sp,*2.
push	ax
push	di
push	si
call	_lpoutpu
add	sp,*6.
L103:test	*14.(di),*4.
je	L20024
L104:test	*14.(di),*2.
je	L100
cmp	(di),*50.
jge	L100
and	*14.(di),*-3.
push	di
call	_wakeup
add	sp,*2.
|NREG	3
L100:|RTYP	0
br	cret
.globl	_lpoutpu
.even
_lpoutpu:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_cfg=di
|NREG	1
mov	si,*6.(bp)
|	_lp=si
|	_c=8.
or	*14.(si),*4.
push	*8.(bp)
push	*4.(di)
call	_outb
add	sp,*4.
mov	dx,*7.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
mov	dx,*6.
push	dx
push	*8.(di)
call	_outb
add	sp,*4.
push	*2.(di)
call	_inb
add	sp,*2.
and	ax,*9.
jne	L109
and	*14.(si),*-5.
L109:|RTYP	0
br	cret
.data
L10:.byte	/4c,/70,/20,/20,/20,/20,/20,/20,/20
.byte	/42,/61,/73,/65,/64
.byte	/20,/25,/78,/20,/6c,/65,/76,/65,/6c
.byte	/20,/25,/64,/20,/25,/73
.byte	/2e,/a,/0
L11:.byte	/66,/6f,/75,/6e,/64,/0
L12:.byte	/4e,/4f,/54,/20,/66,/6f,/75,/6e,/64
.byte	/0
