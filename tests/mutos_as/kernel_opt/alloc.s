.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_mpxip,2
.globl	_alloc
.text
.even
_alloc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*4.
|	_bno=-8.
|NREG	2
|	_fp=di
|NREG	1
|	_bp=si
push	*4.(bp)
call	_getfs
add	sp,*2.
mov	di,ax
j	L4
L20001:mov	dx,*10.
push	dx
mov	dx,di
add	dx,#610.
push	dx
call	_sleep
add	sp,*4.
L4:cmpb	#610.(di),*0
jne	L20001
L8:cmp	*6.(di),*0
jle	L9
cmp	*6.(di),*100.
jle	L10
push	*4.(bp)
mov	dx,#L11
push	dx
call	_prdev
add	sp,*4.
L9:mov	*6.(di),*0.
sub	ax,ax
cwd
mov	#620.(di),ax
mov	#618.(di),dx
push	*4.(bp)
mov	dx,#L15
push	dx
call	_prdev
add	sp,*4.
movb	111.+_u,*28.
sub	dx,dx
br	L20002
L10:dec	*6.(di)
mov	bx,*6.(di)
sal	bx,*1
sal	bx,*1
add	bx,di
mov	ax,*8. +2(bx)
mov	dx,*8.(bx)
mov	*-6.(bp),ax
mov	*-8.(bp),dx
cmp	dx,*0
jne	L6
test	ax,ax
je	L9
L6:push	*4.(bp)
push	*-6.(bp)
push	*-8.(bp)
push	di
call	_badbloc
add	sp,*8.
test	ax,ax
bne	L8
cmp	*6.(di),*0
jg	L12
incb	#610.(di)
push	*-6.(bp)
push	*-8.(bp)
push	*4.(bp)
call	_bread
add	sp,*6.
mov	si,ax
test	(si),*4.
jne	L13
mov	bx,*14.(si)
mov	dx,(bx)
mov	*6.(di),dx
mov	dx,#400.
push	dx
mov	dx,di
add	dx,*8.
push	dx
mov	dx,*14.(si)
add	dx,*2.
push	dx
call	_bcopy
add	sp,*6.
L13:push	si
call	_brelse
add	sp,*2.
movb	#610.(di),*0.
mov	dx,di
add	dx,#610.
push	dx
call	_wakeup
add	sp,*2.
cmp	*6.(di),*0
ble	L9
L12:push	*-6.(bp)
push	*-8.(bp)
push	*4.(bp)
call	_getblk
add	sp,*6.
mov	si,ax
push	si
call	_clrbuf
add	sp,*2.
mov	ax,#620.(di)
mov	dx,#618.(di)
cmp	dx,*0
jne	L10001
cmp	ax,*0
je	L14
L10001:sub	#620.(di),*1.
sbb	#618.(di),*0
L14:movb	#612.(di),*1.
mov	dx,si
L20002:mov	ax,dx
|RTYP	12
br	cret
.globl	_free
.even
_free:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_bno=6.
|NREG	2
|	_fp=di
|NREG	1
|	_bp=si
push	*4.(bp)
call	_getfs
add	sp,*2.
mov	di,ax
movb	#612.(di),*1.
j	L19
L20004:mov	dx,*10.
push	dx
mov	dx,di
add	dx,#610.
push	dx
call	_sleep
add	sp,*4.
L19:cmpb	#610.(di),*0
jne	L20004
push	*4.(bp)
push	*8.(bp)
push	*6.(bp)
push	di
call	_badbloc
add	sp,*8.
test	ax,ax
bne	L18
cmp	*6.(di),*0
jg	L21
mov	*6.(di),*1.
sub	ax,ax
cwd
mov	*10.(di),ax
mov	*8.(di),dx
L21:cmp	*6.(di),*100.
jl	L22
incb	#610.(di)
push	*8.(bp)
push	*6.(bp)
push	*4.(bp)
call	_getblk
add	sp,*6.
mov	si,ax
mov	bx,*14.(si)
mov	dx,*6.(di)
mov	(bx),dx
mov	dx,#400.
push	dx
mov	dx,*14.(si)
add	dx,*2.
push	dx
mov	dx,di
add	dx,*8.
push	dx
call	_bcopy
add	sp,*6.
mov	*6.(di),*0.
push	si
call	_bwrite
add	sp,*2.
movb	#610.(di),*0.
mov	dx,di
add	dx,#610.
push	dx
call	_wakeup
add	sp,*2.
L22:mov	ax,*8.(bp)
mov	dx,*6.(bp)
mov	bx,*6.(di)
sal	bx,*1
inc	*6.(di)
sal	bx,*1
add	bx,di
mov	*8.+2(bx),ax
mov	*8.(bx),dx
add	#620.(di),*1.
adc	#618.(di),*0
movb	#612.(di),*1.
|NREG	3
L18:|RTYP	0
br	cret
.globl	_badbloc
.even
_badbloc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_fp=di
|	_bn=6.
|	_dev=10.
mov	bx,(di)
sub	cx,cx
mov	dx,*8.(bp)
mov	si,*6.(bp)
cmp	si,cx
jl	L10002
jg	L10003
cmp	dx,bx
jb	L10002
L10003:mov	dx,*8.(bp)
mov	si,*6.(bp)
cmp	si,*2.(di)
jl	L26
jg	L10002
cmp	dx,*4.(di)
jb	L26
L10002:push	*10.(bp)
mov	si,#L27
push	si
call	_prdev
add	sp,*4.
mov	si,*1.
L20005:mov	ax,si
|RTYP	0
br	cret
L26:sub	si,si
j	L20005
.globl	_ialloc
.even
_ialloc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*12.
|NREG	2
|	_fp=di
|NREG	1
|	_bp=si
|	_ip=-6.
|	_i=-8.
|	_dp=-10.
|	_ino=-12.
|	_adr=-16.
push	*4.(bp)
call	_getfs
add	sp,*2.
mov	di,ax
j	L31
L20007:mov	dx,*10.
push	dx
mov	dx,di
add	dx,#611.
push	dx
call	_sleep
add	sp,*4.
L31:cmpb	#611.(di),*0
jne	L20007
br	L33
L20011:dec	#408.(di)
mov	bx,#408.(di)
sal	bx,*1
mov	dx,#410.(bx)(di)
mov	*-12.(bp),dx
cmp	dx,*2.
jb	L33
push	dx
push	*4.(bp)
call	_iget
add	sp,*4.
mov	*-6.(bp),ax
cmp	ax,*0
beq	L20019
mov	bx,ax
mov	dx,*6.(bx)
test	dx,dx
jne	L36
mov	*-8.(bp),*0.
L20009:sub	ax,ax
cwd
mov	bx,*-8.(bp)
sal	bx,*1
sal	bx,*1
add	bx,*-6.(bp)
mov	*20.+2(bx),ax
mov	*20.(bx),dx
inc	*-8.(bp)
cmp	*-8.(bp),*13.
jl	L20009
cmp	#622.(di),*0
je	L40
dec	#622.(di)
L40:movb	#612.(di),*1.
mov	dx,*-6.(bp)
br	L20018
L36:push	*-6.(bp)
call	_iput
add	sp,*2.
L33:cmp	#408.(di),*0
bgt	L20011
incb	#611.(di)
mov	*-12.(bp),*1.
mov	ax,*2.
sub	dx,dx
mov	*-14.(bp),ax
mov	*-16.(bp),dx
br	L41
L20017:jl	L10005
cmp	ax,bx
bhis	L42
L10005:push	*-14.(bp)
push	*-16.(bp)
push	*4.(bp)
call	_bread
add	sp,*6.
mov	si,ax
test	(si),*4.
je	L44
push	si
call	_brelse
add	sp,*2.
add	*-12.(bp),*16.
j	L43
L44:mov	dx,*14.(si)
mov	*-10.(bp),dx
mov	*-8.(bp),*0.
L20015:mov	bx,*-10.(bp)
mov	dx,(bx)
test	dx,dx
jne	L48
mov	*-6.(bp),#_inode
j	L49
L20013:mov	bx,*-6.(bp)
mov	dx,*2.(bx)
cmp	dx,*4.(bp)
jne	L51
mov	dx,*4.(bx)
cmp	dx,*-12.(bp)
je	L48
L51:add	*-6.(bp),*76.
L49:mov	dx,6.+_v
cmp	*-6.(bp),dx
jb	L20013
mov	bx,#408.(di)
sal	bx,*1
inc	#408.(di)
add	bx,di
mov	dx,*-12.(bp)
mov	*410.(bx),dx
cmp	#408.(di),*100.
jge	L46
L48:inc	*-12.(bp)
add	*-10.(bp),*64.
inc	*-8.(bp)
cmp	*-8.(bp),*16.
jl	L20015
L46:push	si
call	_brelse
add	sp,*2.
cmp	#408.(di),*100.
jge	L42
L43:add	*-14.(bp),*1.
adc	*-16.(bp),*0
L41:mov	bx,(di)
sub	cx,cx
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
cmp	dx,cx
ble	L20017
L42:movb	#611.(di),*0.
mov	dx,di
add	dx,#611.
push	dx
call	_wakeup
add	sp,*2.
cmp	#408.(di),*0
bgt	L33
push	*4.(bp)
mov	dx,#L52
push	dx
call	_prdev
add	sp,*4.
movb	111.+_u,*28.
mov	#622.(di),*0.
L20019:sub	dx,dx
L20018:mov	ax,dx
|RTYP	12
br	cret
.globl	_ifree
.even
_ifree:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_ino=6.
|NREG	2
|	_fp=di
push	*4.(bp)
call	_getfs
add	sp,*2.
mov	di,ax
inc	#622.(di)
cmpb	#611.(di),*0
jne	L55
cmp	#408.(di),*100.
jge	L55
mov	si,*6.(bp)
mov	bx,#408.(di)
sal	bx,*1
inc	#408.(di)
add	bx,di
mov	*410.(bx),si
movb	#612.(di),*1.
|NREG	3
L55:|RTYP	0
br	cret
.globl	_getfs
.even
_getfs:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|NREG	2
|	_mp=di
|NREG	1
|	_fp=si
mov	di,#_mount
j	L59
L20021:cmp	*2.(di),*0
je	L61
mov	dx,*4.(bp)
cmp	(di),dx
jne	L61
mov	bx,*2.(di)
mov	si,*14.(bx)
cmp	*6.(si),*100.
jg	L10007
cmp	#408.(si),*100.
jle	L63
L10007:push	*4.(bp)
mov	dx,#L64
push	dx
call	_prdev
add	sp,*4.
mov	*6.(si),*0.
mov	#408.(si),*0.
L63:mov	dx,si
j	L20022
L61:add	di,*6.
L59:cmp	di,14.+_v
jb	L20021
mov	dx,#L65
push	dx
call	_panic
add	sp,*2.
sub	dx,dx
L20022:mov	ax,dx
|RTYP	12
br	cret
.globl	_update
.even
_update:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*4.
|NREG	2
|	_ip=di
|NREG	1
|	_mp=si
|	_bp=-6.
|	_fp=-8.
cmp	_updlock,*0
bne	L68
inc	_updlock
mov	si,#_mount
br	L69
L20024:cmp	*2.(si),*0
beq	L71
mov	bx,*2.(si)
mov	bx,*14.(bx)
mov	*-8.(bp),bx
movb	dx,#612.(bx)
testb	dx,dx
je	L71
mov	bx,*-8.(bp)
movb	dx,#611.(bx)
testb	dx,dx
jne	L71
mov	bx,*-8.(bp)
movb	dx,#610.(bx)
testb	dx,dx
jne	L71
mov	bx,*-8.(bp)
movb	dx,#613.(bx)
testb	dx,dx
jne	L71
mov	ax,*1.
cwd
push	ax
push	dx
push	(si)
call	_getblk
add	sp,*6.
mov	*-6.(bp),ax
mov	bx,ax
test	(bx),*4.
jne	L71
mov	bx,*-8.(bp)
movb	*612.(bx),*0.
mov	ax,2.+_time
mov	dx,_time
mov	bx,*-8.(bp)
mov	*614.+2(bx),ax
mov	*614.(bx),dx
mov	dx,#1024.
push	dx
mov	bx,*-6.(bp)
push	*14.(bx)
push	*-8.(bp)
call	_bcopy
add	sp,*6.
push	*-6.(bp)
call	_bwrite
add	sp,*2.
L71:add	si,*6.
L69:cmp	si,14.+_v
blo	L20024
mov	di,#_inode
j	L73
L20026:movb	dx,(di)
and	dx,*1.
jne	L75
cmpb	*1.(di),*0
je	L75
orb	(di),*1.
incb	*1.(di)
mov	dx,#_time
push	dx
push	dx
push	di
call	_iupdat
add	sp,*6.
push	di
call	_iput
add	sp,*2.
L75:add	di,*76.
L73:cmp	di,6.+_v
jb	L20026
mov	_updlock,*0.
mov	dx,*-1.
push	dx
call	_bflush
add	sp,*2.
|NREG	3
L68:|RTYP	0
br	cret
.data
L11:.byte	/42,/61,/64,/20,/66,/72,/65,/65,/20
.byte	/63,/6f,/75,/6e,/74,/0
L15:.byte	/6e,/6f,/20,/73,/70,/61,/63,/65,/0
L27:.byte	/62,/61,/64,/20,/62,/6c,/6f,/63,/6b
.byte	/0
L52:.byte	/4f,/75,/74,/20,/6f,/66,/20,/69,/6e
.byte	/6f,/64,/65,/73,/0
L64:.byte	/62,/61,/64,/20,/63,/6f,/75,/6e,/74
.byte	/0
L65:.byte	/6e,/6f,/20,/66,/73,/0
