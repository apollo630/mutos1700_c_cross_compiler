.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_io_info,222
.globl	_bmap
.text
.even
_bmap:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|	_bn=6.
|	_rwflg=10.
sub	sp,*16.
|NREG	1
|	_i=si
|	_bp=-6.
|	_nbp=-8.
|	_j=-10.
|	_sh=-12.
|	_nb=-16.
|	_bap=-18.
|	_dev=-20.
cmp	*6.(bp),*0
jg	L4
jl	L10000
cmp	*8.(bp),*0.
jae	L4
L10000:movb	111.+_u,*27.
L20011:sub	ax,ax
cwd
br	L3
L4:mov	dx,*2.(di)
mov	*-20.(bp),dx
sub	ax,ax
cwd
mov	2.+_rablock,ax
mov	_rablock,dx
cmp	*6.(bp),*0
bgt	L5
jl	L10001
cmp	*8.(bp),*10.
bhis	L5
L10001:mov	si,*8.(bp)
mov	bx,si
sal	bx,*1
sal	bx,*1
mov	ax,*20. +2(bx)(di)
mov	dx,*20.(bx)(di)
mov	*-14.(bp),ax
mov	*-16.(bp),dx
cmp	dx,*0
jne	L6
test	ax,ax
jne	L6
cmp	*10.(bp),*1.
je	L10002
push	*-20.(bp)
call	_alloc
add	sp,*2.
mov	*-6.(bp),ax
test	ax,ax
jne	L7
L10002:mov	ax,*-1.
mov	dx,ax
br	L3
L7:mov	bx,*-6.(bp)
mov	ax,*16. +2(bx)
mov	dx,*16.(bx)
mov	*-14.(bp),ax
mov	*-16.(bp),dx
push	bx
call	_bdwrite
add	sp,*2.
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
mov	bx,si
sal	bx,*1
sal	bx,*1
add	bx,di
mov	*20.+2(bx),ax
mov	*20.(bx),dx
orb	(di),*66.
L6:cmp	si,*9.
bge	L22
mov	bx,si
sal	bx,*1
sal	bx,*1
mov	ax,*24. +2(bx)(di)
mov	dx,*24.(bx)(di)
br	L20007
L5:mov	*-12.(bp),*0.
mov	ax,*1.
cwd
mov	*-14.(bp),ax
mov	*-16.(bp),dx
sub	*8.(bp),*10.
sbb	*6.(bp),*0
mov	*-10.(bp),*3.
L20001:add	*-12.(bp),*8.
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
mov	cx,*8.
sal	ax,*1
rcl	dx,*1
loop	.-4
mov	*-16.(bp),dx
mov	*-14.(bp),ax
mov	ax,*8.(bp)
mov	dx,*6.(bp)
cmp	dx,*-16.(bp)
jl	L10
jg	L10003
cmp	ax,*-14.(bp)
jb	L10
L10003:mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
sub	*8.(bp),ax
sbb	*6.(bp),dx
dec	*-10.(bp)
cmp	*-10.(bp),*0
jg	L20001
L10:cmp	*-10.(bp),*0
beq	L10000
mov	bx,*13.
sub	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	ax,*20. +2(bx)(di)
mov	dx,*20.(bx)(di)
mov	*-14.(bp),ax
mov	*-16.(bp),dx
cmp	dx,*0
bne	L15
test	ax,ax
bne	L15
cmp	*10.(bp),*1.
beq	L10002
push	*-20.(bp)
call	_alloc
add	sp,*2.
mov	*-6.(bp),ax
test	ax,ax
beq	L10002
mov	bx,ax
mov	ax,*16. +2(bx)
mov	dx,*16.(bx)
mov	*-14.(bp),ax
mov	*-16.(bp),dx
push	bx
call	_bdwrite
add	sp,*2.
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
mov	bx,*13.
sub	bx,*-10.(bp)
sal	bx,*1
sal	bx,*1
mov	*20.+2(bx)(di),ax
mov	*20.(bx)(di),dx
orb	(di),*66.
br	L15
L20004:push	*-14.(bp)
push	*-16.(bp)
push	*-20.(bp)
call	_bread
add	sp,*6.
mov	*-6.(bp),ax
mov	bx,ax
test	(bx),*4.
je	L18
push	ax
call	_brelse
add	sp,*2.
br	L20011
L18:mov	bx,*-6.(bp)
mov	dx,*14.(bx)
mov	*-18.(bp),dx
sub	*-12.(bp),*8.
mov	ax,*8.(bp)
mov	dx,*6.(bp)
mov	cx,*-12.(bp)
test	cx,cx
jz	.+8
sar	dx,*1
rcr	ax,*1
loop	.-4
mov	si,ax
and	si,#255.
mov	bx,si
sal	bx,*1
sal	bx,*1
add	bx,*-18.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
mov	*-14.(bp),ax
mov	*-16.(bp),dx
cmp	dx,*0
jne	L19
test	ax,ax
jne	L19
cmp	*10.(bp),*1.
je	L10005
push	*-20.(bp)
call	_alloc
add	sp,*2.
mov	*-8.(bp),ax
test	ax,ax
jne	L20
L10005:push	*-6.(bp)
call	_brelse
add	sp,*2.
br	L10002
L20:mov	bx,*-8.(bp)
mov	ax,*16. +2(bx)
mov	dx,*16.(bx)
mov	*-14.(bp),ax
mov	*-16.(bp),dx
push	bx
call	_bdwrite
add	sp,*2.
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
mov	bx,si
sal	bx,*1
sal	bx,*1
add	bx,*-18.(bp)
mov	+2(bx),ax
mov	(bx),dx
push	*-6.(bp)
call	_bdwrite
j	L20002
L19:push	*-6.(bp)
call	_brelse
L20002:add	sp,*2.
inc	*-10.(bp)
L15:cmp	*-10.(bp),*3.
ble	L20004
cmp	si,#255.
jge	L22
mov	bx,si
sal	bx,*1
sal	bx,*1
add	bx,*-18.(bp)
mov	ax,*4. +2(bx)
mov	dx,*4.(bx)
L20007:mov	2.+_rablock,ax
mov	_rablock,dx
L22:mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
L3:|RTYP	6
br	cret
.globl	_passc
.even
_passc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_c=di
|NREG	1
|	_id=si
movb	ax,110.+_u
cbw
mov	si,ax
cmp	si,*1.
jne	L26
mov	bx,128.+_u
mov	ax,di
movb	(bx),ax
L27:dec	130.+_u
add	134.+_u,*1.
adc	132.+_u,*0
inc	128.+_u
cmp	130.+_u,*0
jne	L10010
L20014:mov	dx,*-1.
j	L10011
L26:cmp	si,*0
je	L10006
push	di
push	128.+_u
call	_suibyte
add	sp,*4.
j	L10007
L10006:push	di
push	128.+_u
call	_subyte
add	sp,*4.
test	ax,ax
jl	L10008
sub	ax,ax
j	L27
L10008:mov	ax,*1.
L10007:test	ax,ax
je	L27
movb	111.+_u,*14.
j	L20014
L10010:sub	dx,dx
L10011:mov	ax,dx
|RTYP	0
br	cret
.globl	_cpass
.even
_cpass:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_c=di
|NREG	1
|	_id=si
cmp	130.+_u,*0
je	L20017
movb	ax,110.+_u
cbw
mov	si,ax
cmp	si,*1.
jne	L33
mov	bx,128.+_u
movb	ax,(bx)
cbw
mov	di,ax
L34:dec	130.+_u
add	134.+_u,*1.
adc	132.+_u,*0
inc	128.+_u
mov	dx,di
subb	dh,dh
j	L20016
L33:cmp	si,*0
jne	L10012
push	128.+_u
call	_fubyte
j	L20015
L10012:push	128.+_u
call	_fuibyte
L20015:add	sp,*2.
mov	di,ax
test	ax,ax
jge	L34
movb	111.+_u,*14.
L20017:mov	dx,*-1.
L20016:mov	ax,dx
|RTYP	0
br	cret
.globl	_nodev
.even
_nodev:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
movb	111.+_u,*19.
|RTYP	0
br	cret
.globl	_novec
.even
_novec:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
push	*4.(bp)
mov	di,#L42
push	di
call	_printf
add	sp,*4.
|RTYP	0
br	cret
.globl	_nulldev
.even
_nulldev:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|RTYP	0
br	cret
.globl	_bcopy
.even
_bcopy:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_from=4.
|	_to=6.
|NREG	2
mov	di,*8.(bp)
|	_count=di
sub	sp,*2.
|NREG	1
|	_f=si
|	_t=-6.
mov	si,*4.(bp)
mov	dx,*6.(bp)
mov	*-6.(bp),dx
L51:mov	bx,*-6.(bp)
movb	dx,(si)
movb	(bx),dx
inc	*-6.(bp)
inc	si
dec	di
jne	L51
|NREG	2
|RTYP	0
br	cret
.data
L42:.byte	/69,/6e,/74,/65,/72,/72,/75,/70,/74
.byte	/20,/66,/72,/6f,/6d
.byte	/20,/75,/6e,/6b,/6e,/6f,/77,/6e,/20
.byte	/64,/65,/76,/69,/63,/65
.byte	/20,/28,/69,/67,/6e,/6f,/72,/65,/64
.byte	/29,/2c,/20,/76,/65,/63
.byte	/3d,/25,/64,/a,/0
