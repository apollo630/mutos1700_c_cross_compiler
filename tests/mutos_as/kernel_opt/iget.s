.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_io_info,222
.globl	_iget
.text
.even
_iget:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_ino=6.
sub	sp,*6.
|NREG	2
|	_ip=di
|NREG	1
|	_mp=si
|	_oip=-6.
|	_bp=-8.
|	_dp=-10.
j	L4
L20001:orb	(di),*16.
mov	dx,*10.
push	dx
push	di
call	_sleep
add	sp,*4.
L4:mov	*-6.(bp),*0.
mov	di,#_inode
j	L5
L20005:mov	dx,*4.(di)
cmp	*6.(bp),dx
jne	L8
mov	dx,*2.(di)
cmp	*4.(bp),dx
jne	L8
movb	dx,(di)
and	dx,*1.
jne	L20001
movb	dx,(di)
and	dx,*8.
je	L10
mov	si,#_mount
j	L11
L20003:cmp	*4.(si),di
jne	L13
mov	dx,(si)
mov	*4.(bp),dx
mov	*6.(bp),*2.
j	L4
L13:add	si,*6.
L11:cmp	si,14.+_v
jb	L20003
mov	dx,#L15
push	dx
call	_panic
add	sp,*2.
L10:incb	*1.(di)
orb	(di),*1.
br	L20007
L8:cmp	*-6.(bp),*0
jne	L7
cmpb	*1.(di),*0
jne	L7
mov	*-6.(bp),di
L7:add	di,*76.
L5:cmp	di,6.+_v
jb	L20005
mov	di,*-6.(bp)
cmp	di,*0
jne	L17
mov	dx,#L18
push	dx
call	_printf
add	sp,*2.
movb	111.+_u,*23.
L20008:sub	dx,dx
br	L20006
L17:mov	dx,*4.(bp)
mov	*2.(di),dx
mov	dx,*6.(bp)
mov	*4.(di),dx
movb	(di),*1.
incb	*1.(di)
sub	ax,ax
cwd
mov	*74.(di),ax
mov	*72.(di),dx
mov	ax,*6.(bp)
add	ax,*31.
mov	cx,*4.
shr	ax,cl
sub	dx,dx
push	ax
push	dx
push	*4.(bp)
call	_bread
add	sp,*6.
mov	*-8.(bp),ax
mov	bx,ax
test	(bx),*4.
je	L19
push	ax
call	_brelse
add	sp,*2.
push	di
call	_iput
add	sp,*2.
j	L20008
L19:mov	bx,*-8.(bp)
mov	dx,*14.(bx)
mov	*-10.(bp),dx
mov	dx,*6.(bp)
add	dx,*31.
and	dx,*15.
mov	cx,*6.
sal	dx,cl
add	*-10.(bp),dx
push	*-10.(bp)
push	di
call	_iexpand
add	sp,*4.
push	*-8.(bp)
call	_brelse
add	sp,*2.
L20007:mov	dx,di
L20006:mov	ax,dx
|RTYP	12
br	cret
.globl	_iexpand
.even
_iexpand:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|NREG	1
mov	si,*6.(bp)
|	_dp=si
sub	sp,*6.
|	_p1=-6.
|	_p2=-8.
|	_i=-10.
mov	dx,(si)
mov	*6.(di),dx
mov	dx,*2.(si)
mov	*8.(di),dx
mov	dx,*4.(si)
mov	*10.(di),dx
mov	dx,*6.(si)
mov	*12.(di),dx
mov	ax,*10.(si)
mov	dx,*8.(si)
mov	*16.(di),ax
mov	*14.(di),dx
mov	dx,di
add	dx,*20.
mov	*-6.(bp),dx
mov	dx,si
add	dx,*12.
mov	*-8.(bp),dx
mov	*-10.(bp),*0.
L20010:push	*-6.(bp)
mov	bx,*-8.(bp)
movb	dx,(bx)
pop	bx
movb	(bx),dx
inc	*-6.(bp)
inc	*-8.(bp)
mov	bx,*-6.(bp)
movb	(bx),*0.
inc	*-6.(bp)
push	*-6.(bp)
mov	bx,*-8.(bp)
movb	dx,(bx)
pop	bx
movb	(bx),dx
inc	*-6.(bp)
inc	*-8.(bp)
push	*-6.(bp)
mov	bx,*-8.(bp)
movb	dx,(bx)
pop	bx
movb	(bx),dx
inc	*-6.(bp)
inc	*-8.(bp)
inc	*-10.(bp)
cmp	*-10.(bp),*13.
jl	L20010
|RTYP	0
br	cret
.globl	_iput
.even
_iput:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
cmpb	*1.(di),*1.
jne	L29
orb	(di),*1.
cmp	*8.(di),*0
jg	L30
push	di
call	_itrunc
add	sp,*2.
mov	*6.(di),*0.
orb	(di),*66.
push	*4.(di)
push	*2.(di)
call	_ifree
add	sp,*4.
L30:mov	si,#_time
push	si
push	si
push	di
call	_iupdat
add	sp,*6.
push	di
call	_prele
add	sp,*2.
movb	(di),*0.
mov	*4.(di),*0.
L29:decb	*1.(di)
push	di
call	_prele
add	sp,*2.
|RTYP	0
br	cret
.globl	_iupdat
.even
_iupdat:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|	_ta=6.
|	_tm=8.
sub	sp,*8.
|NREG	1
|	_bp=si
|	_dp=-6.
|	_p1=-8.
|	_p2=-10.
|	_i=-12.
movb	dx,(di)
and	dx,*70.
beq	L34
push	*2.(di)
call	_getfs
add	sp,*2.
mov	bx,ax
movb	ax,#613.(bx)
testb	ax,ax
bne	L33
mov	ax,*4.(di)
add	ax,*31.
mov	cx,*4.
shr	ax,cl
sub	dx,dx
push	ax
push	dx
push	*2.(di)
call	_bread
add	sp,*6.
mov	si,ax
test	(si),*4.
je	L35
push	si
call	_brelse
add	sp,*2.
br	L33
L35:mov	dx,*14.(si)
mov	*-6.(bp),dx
mov	dx,*4.(di)
add	dx,*31.
and	dx,*15.
mov	cx,*6.
sal	dx,cl
add	*-6.(bp),dx
mov	bx,*-6.(bp)
mov	dx,*6.(di)
mov	(bx),dx
mov	bx,*-6.(bp)
mov	dx,*8.(di)
mov	*2.(bx),dx
mov	bx,*-6.(bp)
mov	dx,*10.(di)
mov	*4.(bx),dx
mov	bx,*-6.(bp)
mov	dx,*12.(di)
mov	*6.(bx),dx
mov	ax,*16.(di)
mov	dx,*14.(di)
mov	bx,*-6.(bp)
mov	*8.+2(bx),ax
mov	*8.(bx),dx
mov	dx,*-6.(bp)
add	dx,*12.
mov	*-8.(bp),dx
mov	dx,di
add	dx,*20.
mov	*-10.(bp),dx
mov	*-12.(bp),*0.
L20012:push	*-8.(bp)
mov	bx,*-10.(bp)
movb	dx,(bx)
pop	bx
movb	(bx),dx
inc	*-8.(bp)
inc	*-10.(bp)
mov	bx,*-10.(bp)
inc	*-10.(bp)
movb	dx,(bx)
testb	dx,dx
je	L39
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#12288.
je	L39
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#28672.
je	L39
mov	dx,#L40
push	dx
call	_printf
add	sp,*2.
L39:push	*-8.(bp)
mov	bx,*-10.(bp)
movb	dx,(bx)
pop	bx
movb	(bx),dx
inc	*-8.(bp)
inc	*-10.(bp)
push	*-8.(bp)
mov	bx,*-10.(bp)
movb	dx,(bx)
pop	bx
movb	(bx),dx
inc	*-8.(bp)
inc	*-10.(bp)
inc	*-12.(bp)
cmp	*-12.(bp),*13.
jl	L20012
movb	dx,(di)
and	dx,*4.
je	L41
mov	bx,*6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
mov	bx,*-6.(bp)
mov	*52.+2(bx),ax
mov	*52.(bx),dx
L41:movb	dx,(di)
and	dx,*2.
je	L42
mov	bx,*8.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
mov	bx,*-6.(bp)
mov	*56.+2(bx),ax
mov	*56.(bx),dx
L42:movb	dx,(di)
and	dx,*64.
je	L43
mov	ax,2.+_time
mov	dx,_time
mov	bx,*-6.(bp)
mov	*60.+2(bx),ax
mov	*60.(bx),dx
L43:andb	(di),*-71.
push	si
call	_bdwrite
add	sp,*2.
L34:|NREG	2
L33:|RTYP	0
br	cret
.globl	_itrunc
.even
_itrunc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
sub	sp,*6.
|NREG	1
|	_i=si
|	_dev=-6.
|	_bn=-10.
mov	si,*6.(di)
and	si,#-4096.
cmp	si,#-32768.
je	L10000
cmp	si,#16384.
bne	L46
L10000:mov	dx,*2.(di)
mov	*-6.(bp),dx
mov	si,*12.
L20014:mov	bx,si
sal	bx,*1
sal	bx,*1
add	bx,di
mov	ax,*20. +2(bx)
mov	dx,*20.(bx)
mov	*-8.(bp),ax
mov	*-10.(bp),dx
cmp	dx,*0
jne	L10001
test	ax,ax
beq	L49
L10001:sub	ax,ax
cwd
mov	bx,si
sal	bx,*1
sal	bx,*1
mov	*20.+2(bx)(di),ax
mov	*20.(bx)(di),dx
mov	bx,si
sub	bx,#/a
cmp	bx,*2.
ja	L52
sal	bx,#1
seg	cs
br	@L10003(bx)
L52:push	*-8.(bp)
push	*-10.(bp)
push	*-6.(bp)
call	_free
add	sp,*6.
j	L49
L53:sub	dx,dx
push	dx
push	dx
push	*-8.(bp)
push	*-10.(bp)
push	*-6.(bp)
L20016:call	_tloop
add	sp,*10.
j	L49
L54:sub	dx,dx
push	dx
mov	dx,*1.
push	dx
push	*-8.(bp)
push	*-10.(bp)
push	*-6.(bp)
j	L20016
L55:mov	dx,*1.
push	dx
push	dx
push	*-8.(bp)
push	*-10.(bp)
push	*-6.(bp)
j	L20016
L10003:L53
L54
L55
L49:dec	si
cmp	si,*0
bge	L20014
sub	ax,ax
cwd
mov	*16.(di),ax
mov	*14.(di),dx
orb	(di),*66.
|NREG	2
L46:|RTYP	0
br	cret
.globl	_tloop
.even
_tloop:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_bn=6.
|	_f1=10.
|	_f2=12.
sub	sp,*6.
|NREG	2
|	_i=di
|NREG	1
|	_bp=si
|	_bap=-6.
|	_nb=-10.
sub	si,si
mov	di,#255.
br	L59
L20019:cmp	si,*0
jne	L62
push	*8.(bp)
push	*6.(bp)
push	*4.(bp)
call	_bread
add	sp,*6.
mov	si,ax
test	(si),*4.
je	L63
push	si
call	_brelse
add	sp,*2.
br	L58
L63:mov	dx,*14.(si)
mov	*-6.(bp),dx
L62:mov	bx,di
sal	bx,*1
sal	bx,*1
add	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
mov	*-8.(bp),ax
mov	*-10.(bp),dx
cmp	dx,*0
jne	L10004
test	ax,ax
je	L61
L10004:cmp	*10.(bp),*0
je	L64
push	si
call	_brelse
add	sp,*2.
sub	si,si
push	si
push	*12.(bp)
push	*-8.(bp)
push	*-10.(bp)
push	*4.(bp)
call	_tloop
add	sp,*10.
j	L61
L64:push	*-8.(bp)
push	*-10.(bp)
push	*4.(bp)
call	_free
add	sp,*6.
L61:dec	di
L59:cmp	di,*0
bge	L20019
cmp	si,*0
je	L66
push	si
call	_brelse
add	sp,*2.
L66:push	*8.(bp)
push	*6.(bp)
push	*4.(bp)
call	_free
add	sp,*6.
|NREG	3
L58:|RTYP	0
br	cret
.globl	_maknode
.even
_maknode:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_mode=4.
|NREG	2
|	_ip=di
mov	si,172.+_u
push	*2.(si)
call	_ialloc
add	sp,*2.
mov	di,ax
cmp	di,*0
jne	L70
push	172.+_u
call	_iput
add	sp,*2.
sub	si,si
j	L20020
L70:orb	(di),*70.
test	*4.(bp),#-4096.
jne	L71
or	*4.(bp),#-32768.
L71:mov	si,396.+_u
not	si
and	si,*4.(bp)
mov	*6.(di),si
mov	*8.(di),*1.
mov	si,112.+_u
mov	*10.(di),si
mov	si,114.+_u
mov	*12.(di),si
push	di
call	_wdir
add	sp,*2.
mov	si,di
L20020:mov	ax,si
|RTYP	12
br	cret
.globl	_wdir
.even
_wdir:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_ip=4.
mov	di,172.+_u
mov	di,*8.(di)
test	di,di
jg	L75
movb	111.+_u,*20.
j	L76
L75:mov	di,*4.(bp)
mov	di,*4.(di)
mov	156.+_u,di
mov	di,*14.
push	di
mov	di,#158.+_u
push	di
mov	di,#140.+_u
push	di
call	_bcopy
add	sp,*6.
mov	130.+_u,*16.
movb	110.+_u,*1.
mov	128.+_u,#156.+_u
push	172.+_u
call	_writei
add	sp,*2.
L76:push	172.+_u
call	_iput
add	sp,*2.
|RTYP	0
br	cret
.data
L15:.byte	/6e,/6f,/20,/69,/6d,/74,/0
L18:.byte	/49,/6e,/6f,/64,/65,/20,/74,/61,/62
.byte	/6c,/65,/20,/6f,/76
.byte	/65,/72,/66,/6c,/6f,/77,/a,/0
L40:.byte	/69,/61,/64,/64,/72,/65,/73,/73,/20
.byte	/3e,/20,/32,/5e,/32
.byte	/34,/a,/0
