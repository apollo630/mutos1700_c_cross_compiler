.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_k170cfg,20
.comm	_k170dev,258
.comm	_k170tab,26
.comm	_k170rbu,26
.comm	_k170vol,20
.comm	_k170ldd,16
.comm	_k170bdd,2
.globl	_k170ini
.text
.even
_k170ini:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_board=di
sub	di,di
L20001:push	di
call	_k170bin
add	sp,*2.
inc	di
cmp	di,*1.
jb	L20001
|NREG	3
|RTYP	0
br	cret
.globl	_k170bin
.even
_k170bin:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_board=4.
sub	sp,*6.
|NREG	2
|	_cf=di
|NREG	1
|	_dd=si
|	_wub=-6.
|	_i=-8.
|	_nalt=-10.
mov	ax,*4.(bp)
mov	cx,*20.
imul	cx
mov	di,ax
add	di,#_k170cfg
mov	ax,*4.(bp)
mov	cx,#258.
imul	cx
mov	si,ax
add	si,#_k170dev
mov	*-8.(bp),*0.
L10:cmp	*-8.(bp),*0
bne	L11
mov	bx,(di)
mov	cx,*4.
sal	bx,cl
mov	*-6.(bp),bx
movb	(bx),*1.
mov	bx,*-6.(bp)
mov	dx,si
add	dx,*52.
mov	*2.(bx),dx
mov	bx,*-6.(bp)
mov	*4.(bx),*0.
mov	dx,*2.
push	dx
push	(di)
call	_out
add	sp,*4.
sub	dx,dx
push	dx
push	(di)
call	_out
add	sp,*4.
mov	dx,(di)
mov	*4.(si),dx
movb	dx,*3.(di)
movb	*3.(si),dx
mov	dx,di
add	dx,*4.
mov	*46.(si),dx
mov	ax,*4.(bp)
mov	cx,*26.
imul	cx
add	ax,#_k170tab
mov	*48.(si),ax
movb	ax,*3.(di)
cbw
sal	ax,*1
mov	bx,ax
mov	_k170ldd(bx),si
mov	bx,*4.(bp)
sal	bx,*1
mov	_k170bdd(bx),si
movb	*52.(si),*1.
movb	*53.(si),#255.
mov	dx,si
add	dx,*72.
mov	*54.(si),dx
movb	*60.(si),*1.
mov	dx,si
add	dx,*66.
mov	*62.(si),dx
mov	*66.(si),*4.
mov	dx,si
add	dx,*84.
mov	*76.(si),dx
mov	dx,*1.
push	dx
push	*4.(si)
call	_out
add	sp,*4.
mov	dx,*100.
push	dx
call	_delay
add	sp,*2.
cmpb	*53.(si),*0
jne	L12
incb	(si)
cmp	_Stand,*0
jne	L14
cmpb	(si),*0
je	L10002
mov	dx,#L16
push	dx
j	L10003
L12:cmp	(di),*74.
jne	L10000
mov	dx,#256.
j	L10001
L10000:mov	dx,*74.
L10001:mov	(di),dx
br	L10
L10002:mov	dx,#L17
push	dx
L10003:movb	ax,*3.(di)
cbw
push	ax
push	*4.(si)
mov	dx,#L15
push	dx
call	_printf
add	sp,*8.
L14:mov	*-8.(bp),*0.
L20:mov	bx,si
add	bx,*-8.(bp)
movb	*22.(bx),*0.
mov	bx,si
add	bx,*-8.(bp)
mov	dx,*-8.(bp)
movb	*30.(bx),dx
inc	*-8.(bp)
cmp	*-8.(bp),*4.
jl	L20
L21:cmp	*-8.(bp),*8.
bge	L10
mov	bx,si
add	bx,*-8.(bp)
movb	dx,*2.(di)
movb	*22.(bx),dx
mov	bx,si
add	bx,*-8.(bp)
mov	dx,*-8.(bp)
and	dx,*3.
or	dx,*16.
movb	*30.(bx),dx
inc	*-8.(bp)
sub	dx,dx
push	dx
push	*4.(si)
call	_out
add	sp,*4.
j	L21
L11:|NREG	3
|RTYP	0
br	cret
.globl	_k170ope
.even
_k170ope:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*6.
|NREG	2
|	_dd=di
|NREG	1
|	_bp=si
|	_board=-6.
|	_unit=-8.
|	_s=-10.
mov	bx,*4.(bp)
mov	cx,*8.
sar	bx,cl
sub	bx,_k170fma
mov	*-6.(bp),bx
sal	bx,*1
mov	di,_k170bdd(bx)
mov	si,*48.(di)
mov	dx,*4.(bp)
and	dx,*7.
mov	*-8.(bp),dx
cmp	*-6.(bp),*1.
jae	L10004
cmpb	(di),*0
jne	L26
L10004:movb	111.+_u,*6.
br	L25
L26:call	_spl5
mov	*-10.(bp),ax
mov	bx,di
add	bx,*-8.(bp)
movb	dx,*6.(bx)
and	dx,*1.
je	L28
j	L27
L20003:mov	*12.(si),*1.
mov	dx,*20.
push	dx
push	di
call	_sleep
add	sp,*4.
L28:cmp	*12.(si),*0
jne	L20003
mov	*12.(si),*2.
push	*4.(bp)
push	di
call	_k170swe
add	sp,*4.
cmpb	111.+_u,*0
jne	L30
mov	bx,di
add	bx,*-8.(bp)
movb	*6.(bx),*3.
L30:mov	bx,di
add	bx,*-8.(bp)
movb	*14.(bx),*0.
push	di
call	_k170sta
add	sp,*2.
L27:push	*-10.(bp)
call	_splx
add	sp,*2.
cmp	*-8.(bp),*4.
jge	L31
mov	bx,di
add	bx,*-8.(bp)
mov	dx,*4.(bp)
subb	dh,dh
mov	cx,*5.
sar	dx,cl
and	dx,*7.
j	L20007
L31:mov	bx,di
add	bx,*-8.(bp)
mov	dx,*4.(bp)
subb	dh,dh
mov	cx,*6.
sar	dx,cl
and	dx,*3.
L20007:mov	cx,dx
mov	dx,*1.
sal	dx,cl
orb	*14.(bx),dx
|NREG	3
L25:|RTYP	0
br	cret
.globl	_k170clo
.even
_k170clo:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
|	_flag=6.
sub	sp,*2.
|NREG	1
|	_dd=si
|	_unit=-6.
cmp	di,_rootdev
beq	L35
cmp	di,_swapdev
beq	L35
mov	bx,di
mov	cx,*8.
sar	bx,cl
sub	bx,_k170fma
sal	bx,*1
mov	si,_k170bdd(bx)
mov	dx,di
and	dx,*7.
mov	*-6.(bp),dx
cmp	dx,*4.
jge	L36
mov	bx,si
add	bx,dx
mov	dx,di
subb	dh,dh
mov	cx,*5.
sar	dx,cl
and	dx,*7.
j	L20012
L36:mov	bx,si
add	bx,*-6.(bp)
mov	dx,di
subb	dh,dh
mov	cx,*6.
sar	dx,cl
and	dx,*3.
L20012:mov	cx,dx
mov	dx,*1.
sal	dx,cl
not	dx
andb	*14.(bx),dx
mov	bx,si
add	bx,*-6.(bp)
movb	dx,*14.(bx)
testb	dx,dx
jne	L38
mov	bx,si
add	bx,*-6.(bp)
movb	*6.(bx),*0.
L38:|NREG	2
L35:|RTYP	0
br	cret
.globl	_k170str
.even
_k170str:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_bp=di
sub	sp,*12.
|NREG	1
|	_dd=si
|	_ptab=-6.
|	_par=-8.
|	_bno=-12.
|	_s=-14.
|	_unit=-16.
mov	bx,*10.(di)
mov	cx,*8.
sar	bx,cl
sub	bx,_k170fma
sal	bx,*1
mov	si,_k170bdd(bx)
mov	dx,*10.(di)
and	dx,*7.
mov	*-16.(bp),dx
mov	cx,*4.
sal	dx,cl
add	dx,si
add	dx,*110.
mov	*-6.(bp),dx
cmp	*-16.(bp),*4.
jge	L42
mov	dx,*10.(di)
subb	dh,dh
mov	cx,*5.
sar	dx,cl
and	dx,*7.
mov	cx,*3.
sal	dx,cl
push	dx
mov	bx,*-6.(bp)
mov	dx,*14.(bx)
pop	bx
j	L20014
L42:mov	dx,*10.(di)
subb	dh,dh
mov	cx,*6.
sar	dx,cl
and	dx,*3.
mov	cx,*3.
sal	dx,cl
push	dx
mov	bx,*-6.(bp)
mov	dx,*14.(bx)
pop	bx
L20014:add	dx,bx
mov	*-8.(bp),dx
mov	ax,*18.(di)
mov	dx,*16.(di)
mov	*-10.(bp),ax
mov	*-12.(bp),dx
test	(di),#16384.
jne	L44
test	(di),*16.
je	L10005
mov	bx,*-6.(bp)
mov	ax,*12.(bx)
sub	dx,dx
push	ax
push	dx
push	*-10.(bp)
push	*-12.(bp)
call	lrem
add	sp,*8.
test	dx,dx
jne	L45
test	ax,ax
jne	L45
mov	bx,*-6.(bp)
mov	cx,*12.(bx)
mov	ax,*12.(di)
sub	dx,dx
div	cx
test	dx,dx
jne	L45
L10005:test	(di),*16.
je	L46
mov	bx,*-6.(bp)
mov	ax,*12.(bx)
sub	dx,dx
push	ax
push	dx
lea	dx,*-12.(bp)
push	dx
call	aldiv
j	L20015
L46:mov	bx,*-6.(bp)
mov	ax,*10.(bx)
sub	dx,dx
push	ax
push	dx
lea	dx,*-12.(bp)
push	dx
call	almul
L20015:add	sp,*6.
L44:mov	bx,si
add	bx,*-16.(bp)
movb	dx,*6.(bx)
and	dx,*2.
je	L45
mov	bx,*-8.(bp)
mov	ax,*4. +2(bx)
mov	dx,*4.(bx)
cmp	dx,*-12.(bp)
jg	L48
jl	L45
cmp	ax,*-10.(bp)
jae	L48
L45:or	(di),*4.
movb	*21.(di),*6.
push	di
j	L20017
L20019:cmp	ax,*-10.(bp)
jne	L50
test	(di),*1.
je	L51
mov	dx,*12.(di)
mov	*22.(di),dx
j	L52
L51:movb	*21.(di),*6.
or	(di),*4.
L52:push	di
L20017:call	_iodone
br	L20016
L48:mov	bx,*-8.(bp)
mov	ax,*4. +2(bx)
mov	dx,*4.(bx)
cmp	dx,*-12.(bp)
je	L20019
L50:mov	bx,*-8.(bp)
mov	ax,*4. +2(bx)
mov	dx,*4.(bx)
push	ax
push	dx
mov	bx,*-6.(bp)
mov	cx,*12.(bx)
mov	ax,*12.(bx)
add	ax,*12.(di)
dec	ax
sub	dx,dx
div	cx
sub	dx,dx
add	ax,*-10.(bp)
adc	dx,*-12.(bp)
pop	cx
pop	bx
cmp	dx,cx
jl	L54
jg	L10009
cmp	ax,bx
jbe	L54
L10009:mov	dx,*12.(di)
mov	*22.(di),dx
push	di
j	L20017
L54:mov	bx,*-8.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
add	*-10.(bp),ax
adc	*-12.(bp),dx
mov	bx,*-6.(bp)
mov	ax,*8.(bx)
sub	dx,dx
push	ax
push	dx
push	*-10.(bp)
push	*-12.(bp)
call	ldiv
add	sp,*8.
mov	*24.(di),ax
mov	bx,*-6.(bp)
mov	ax,*8.(bx)
sub	dx,dx
push	ax
push	dx
push	*-10.(bp)
push	*-12.(bp)
call	lrem
add	sp,*8.
mov	*22.(di),ax
call	_spl5
mov	*-14.(bp),ax
push	di
push	*48.(si)
call	_disksor
add	sp,*4.
mov	bx,*48.(si)
mov	dx,*12.(bx)
test	dx,dx
jne	L56
push	si
call	_k170sta
add	sp,*2.
L56:push	*-14.(bp)
call	_splx
L20016:add	sp,*2.
|NREG	2
|RTYP	0
br	cret
.globl	_k170sta
.even
_k170sta:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dd=di
sub	sp,*4.
|NREG	1
|	_bp=si
|	_bpp=-6.
|	_board=-8.
mov	si,*48.(di)
cmp	*12.(si),*1.
jne	L60
mov	*12.(si),*0.
push	di
call	_wakeup
add	sp,*2.
br	L59
L60:mov	dx,*6.(si)
mov	*-6.(bp),dx
test	dx,dx
jne	L61
mov	*12.(si),*0.
br	L59
L61:mov	*12.(si),*2.
mov	*22.(si),*0.
mov	bx,*-6.(bp)
mov	dx,*10.(bx)
and	dx,*7.
movb	*2.(di),dx
cmpb	*2.(di),*4.
jge	L10010
sub	dx,dx
j	L10011
L10010:mov	dx,*1.
L10011:mov	*-8.(bp),dx
inc	dx
or	_dk_busy,dx
mov	bx,*-6.(bp)
test	(bx),*1.
je	L10012
mov	dx,*4.
push	dx
j	L10013
L10012:mov	bx,*-6.(bp)
test	(bx),#16384.
je	L10014
mov	dx,*2.
push	dx
j	L10013
L10014:mov	dx,*6.
push	dx
L10013:push	*-6.(bp)
push	di
call	_k170io
add	sp,*6.
mov	bx,*-8.(bp)
sal	bx,*1
sal	bx,*1
add	_dk_numb+2(bx),*1.
adc	_dk_numb(bx),*0.
|NREG	2
L59:|RTYP	0
br	cret
.globl	_k170int
.even
_k170int:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_level=4.
sub	sp,*8.
|NREG	2
|	_dd=di
|NREG	1
|	_bpp=si
|	_bp=-6.
|	_unit=-8.
|	_status=-10.
|	_error=-12.
mov	bx,*4.(bp)
sal	bx,*1
mov	di,_k170ldd(bx)
mov	dx,*48.(di)
mov	*-6.(bp),dx
cmpb	*2.(di),*4.
jge	L10016
mov	dx,*-2.
j	L10017
L10016:mov	dx,*-3.
L10017:and	_dk_busy,dx
mov	bx,*-6.(bp)
mov	si,*6.(bx)
sub	dx,dx
push	dx
push	*4.(di)
call	_out
add	sp,*4.
cmpb	(di),*0
beq	L65
cmpb	*71.(di),*0
beq	L65
movb	ax,*69.(di)
cbw
mov	*-10.(bp),ax
movb	*71.(di),*0.
mov	dx,*-10.(bp)
and	dx,*48.
mov	cx,*4.
shr	dx,cl
mov	*-8.(bp),dx
test	*-10.(bp),*8.
je	L66
add	*-8.(bp),*4.
L66:test	*-10.(bp),*3.
beq	L67
movb	ax,*1.(di)
cbw
cmp	ax,*3.
ja	L80
sal	ax,#1
xchg	bx,ax
seg	cs
br	@L10031(bx)
L70:test	*-10.(bp),#128.
je	L71
movb	*1.(di),*1.
mov	dx,*1.
push	dx
push	si
push	di
L20021:call	_k170io
add	sp,*6.
j	L64
L71:mov	*22.(si),*0.
L80:mov	bx,*-6.(bp)
mov	dx,*6.(si)
mov	*6.(bx),dx
push	si
call	_iodone
add	sp,*2.
push	di
call	_k170sta
add	sp,*2.
L65:|NREG	3
L64:|RTYP	0
br	cret
L72:movb	*1.(di),*0.
mov	dx,#238.(di)
mov	*-12.(bp),dx
test	dx,#-4097.
je	L73
push	dx
test	(si),*1.
je	L10018
mov	dx,*4.
push	dx
j	L10019
L10018:test	(si),#16384.
je	L10020
mov	dx,*2.
push	dx
j	L10019
L10020:mov	dx,*6.
push	dx
L10019:push	si
call	_deverro
add	sp,*6.
or	(si),*4.
movb	*21.(si),*5.
test	*-12.(bp),#16384.
je	L80
push	*-8.(bp)
push	di
call	_k170not
add	sp,*4.
br	L80
L73:movb	*1.(di),*2.
mov	dx,*24.(si)
mov	*50.(di),dx
mov	*24.(si),*0.
mov	dx,*8.
push	dx
push	si
push	di
br	L20021
L76:test	*-10.(bp),*2.
beq	L64
movb	*1.(di),*0.
mov	bx,*-6.(bp)
inc	*22.(bx)
movb	*21.(si),*5.
or	(si),*4.
mov	dx,*12.(si)
mov	*22.(si),dx
test	#238.(di),#4096.
je	L78
orb	#240.(di),*1.
L78:movb	ax,#240.(di)
cbw
push	ax
test	(si),*1.
je	L10026
mov	dx,*4.
push	dx
j	L10027
L10026:test	(si),#16384.
je	L10028
mov	dx,*2.
push	dx
j	L10027
L10028:mov	dx,*6.
push	dx
L10027:push	si
call	_deverro
add	sp,*6.
br	L80
L79:movb	ax,*2.(di)
cbw
add	ax,di
mov	bx,ax
mov	dx,*-10.(bp)
movb	*38.(bx),dx
movb	*1.(di),*0.
push	di
call	_wakeup
add	sp,*2.
br	L64
L10031:L70
L72
L76
L79
br	L80
L67:test	*-10.(bp),*4.
beq	L80
push	*-8.(bp)
push	di
call	_k170not
add	sp,*4.
br	L64
.globl	_k170swe
.even
_k170swe:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dd=di
|	_dev=6.
sub	sp,*4.
|NREG	1
|	_dtab=si
|	_cd=-6.
|	_unit=-8.
mov	si,*6.(bp)
and	si,*7.
mov	*-8.(bp),si
mov	cx,*4.
sal	si,cl
add	si,di
add	si,*110.
cmp	*-8.(bp),*4.
jae	L85
mov	bx,*6.(bp)
and	bx,*7.
sal	bx,*1
add	bx,*46.(di)
mov	ax,*6.(bp)
subb	ah,ah
mov	cx,*3.
sar	ax,cl
and	ax,*2.
j	L20026
L85:mov	bx,*6.(bp)
and	bx,*7.
sal	bx,*1
add	bx,*46.(di)
mov	ax,*6.(bp)
subb	ah,ah
mov	cx,*3.
sar	ax,cl
and	ax,*7.
L20026:mov	cx,*12.
imul	cx
add	ax,(bx)
mov	*-6.(bp),ax
mov	dx,ax
mov	bx,dx
cmp	(bx),*40.
jne	L10032
mov	bx,dx
mov	dx,(bx)
or	dx,#-32768.
j	L10033
L10032:mov	bx,*-6.(bp)
mov	dx,(bx)
L10033:mov	(si),dx
mov	bx,*-6.(bp)
movb	dx,*2.(bx)
movb	*2.(si),dx
mov	bx,*-6.(bp)
movb	dx,*3.(bx)
movb	*3.(si),dx
mov	bx,*-6.(bp)
movb	dx,*4.(bx)
movb	*4.(si),dx
mov	bx,*-6.(bp)
mov	dx,*6.(bx)
subb	dh,dh
movb	*5.(si),dx
mov	bx,*-6.(bp)
mov	dx,*6.(bx)
mov	cx,*8.
shr	dx,cl
subb	dh,dh
movb	*6.(si),dx
mov	bx,*-6.(bp)
movb	dx,*8.(bx)
movb	*7.(si),dx
cmp	*-8.(bp),*4.
jae	L10034
movb	ax,*4.(si)
cbw
mov	cx,ax
movb	ax,*2.(si)
j	L20027
L10034:movb	ax,*4.(si)
cbw
mov	cx,ax
movb	ax,*3.(si)
L20027:cbw
imul	cx
mov	*8.(si),ax
mov	bx,*-6.(bp)
mov	dx,*6.(bx)
mov	*12.(si),dx
mov	ax,#1024.
sub	dx,dx
div	*12.(si)
mov	*10.(si),ax
mov	bx,*-6.(bp)
mov	dx,*10.(bx)
mov	*14.(si),dx
movb	*1.(di),*3.
mov	dx,*-8.(bp)
movb	*2.(di),dx
sub	dx,dx
push	dx
push	dx
push	di
call	_k170io
add	sp,*6.
mov	dx,*20.
push	dx
push	di
call	_sleep
add	sp,*4.
mov	bx,di
add	bx,*-8.(bp)
movb	dx,*38.(bx)
and	dx,#128.
je	L87
movb	111.+_u,*6.
L87:|NREG	2
|RTYP	0
br	cret
.globl	_k170io
.even
_k170io:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dd=di
|NREG	1
mov	si,*6.(bp)
|	_bp=si
|	_fct=8.
mov	dx,*8.(bp)
movb	*95.(di),dx
mov	*96.(di),*0.
mov	bx,*8.(bp)
cmp	bx,*2.
ja	L97
sal	bx,#1
seg	cs
br	@L10037(bx)
L93:mov	dx,di
add	dx,#238.
mov	*102.(di),dx
L20030:mov	*104.(di),*0.
j	L91
L94:movb	ax,*2.(di)
cbw
mov	cx,*4.
sal	ax,cl
add	ax,di
add	ax,*110.
mov	*102.(di),ax
j	L20030
L95:mov	dx,di
add	dx,#252.
mov	*102.(di),dx
mov	*104.(di),*0.
movb	ax,*2.(di)
cbw
add	ax,di
mov	bx,ax
movb	ax,*22.(bx)
testb	ax,ax
jne	L91
or	*96.(di),*2.
j	L91
L97:mov	dx,*14.(si)
mov	*102.(di),dx
movb	dx,*20.(si)
subb	dh,dh
mov	cx,*12.
sal	dx,cl
mov	*104.(di),dx
mov	dx,*12.(si)
mov	*106.(di),dx
j	L91
L10037:L94
L93
L95
L91:movb	ax,*2.(di)
cbw
add	ax,di
mov	bx,ax
movb	ax,*22.(bx)
cbw
mov	*92.(di),ax
movb	ax,*2.(di)
cbw
add	ax,di
mov	bx,ax
movb	ax,*30.(bx)
movb	*94.(di),ax
cmp	*8.(bp),*1.
jle	L100
mov	dx,*24.(si)
mov	*98.(di),dx
movb	ax,*2.(di)
cbw
mov	cx,*4.
sal	ax,cl
add	ax,di
mov	bx,ax
movb	ax,*114.(bx)
cbw
mov	cx,ax
mov	ax,*22.(si)
sub	dx,dx
div	cx
movb	*100.(di),ax
movb	ax,*2.(di)
cbw
mov	cx,*4.
sal	ax,cl
add	ax,di
mov	bx,ax
movb	ax,*114.(bx)
cbw
mov	cx,ax
mov	ax,*22.(si)
sub	dx,dx
div	cx
movb	*101.(di),dx
cmp	*92.(di),*1.
je	L10038
cmp	*92.(di),*3.
jne	L100
L10038:incb	*101.(di)
j	L100
L20029:movb	ax,*3.(di)
cbw
push	ax
call	_k170int
add	sp,*2.
L100:cmpb	*71.(di),*0
jne	L20029
mov	dx,*1.
push	dx
push	*4.(di)
call	_out
add	sp,*4.
|RTYP	0
br	cret
.globl	_k170rea
.even
_k170rea:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
mov	di,*1.
push	di
push	*4.(bp)
mov	ax,*4.(bp)
mov	cx,*8.
sar	ax,cl
sub	ax,_k170fma
mov	cx,*26.
imul	cx
add	ax,#_k170rbu
push	ax
mov	di,#_k170str
push	di
call	_physio
add	sp,*8.
|RTYP	0
br	cret
.globl	_k170wri
.even
_k170wri:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	di,di
push	di
push	*4.(bp)
mov	ax,*4.(bp)
mov	cx,*8.
sar	ax,cl
sub	ax,_k170fma
mov	cx,*26.
imul	cx
add	ax,#_k170rbu
push	ax
mov	di,#_k170str
push	di
call	_physio
add	sp,*8.
|RTYP	0
br	cret
.globl	_k170ioc
.even
_k170ioc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
|	_fmt=6.
|	_pftk=8.
sub	sp,*14.
|NREG	2
|	_bp=di
|NREG	1
|	_dd=si
|	_cd=-6.
|	_cf=-8.
|	_vl=-10.
|	_par=-12.
|	_i=-14.
|	_board=-16.
|	_s=-18.
mov	ax,*4.(bp)
mov	cx,*8.
sar	ax,cl
sub	ax,_k170fma
mov	*-16.(bp),ax
mov	cx,*26.
imul	cx
mov	di,ax
add	di,#_k170rbu
mov	bx,*-16.(bp)
sal	bx,*1
mov	si,_k170bdd(bx)
mov	ax,*-16.(bp)
mov	cx,*20.
imul	cx
add	ax,#_k170cfg
mov	*-8.(bp),ax
mov	*-10.(bp),#_k170vol
cmp	*6.(bp),#22016.
bne	L111
mov	dx,*4.(bp)
and	dx,*7.
cmp	dx,*4.
jge	L112
mov	bx,*4.(bp)
and	bx,*7.
sal	bx,*1
add	bx,*-8.(bp)
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*3.
sar	ax,cl
and	ax,*2.
mov	cx,*12.
imul	cx
add	ax,*4.(bx)
mov	*-6.(bp),ax
mov	dx,*4.(bp)
subb	dh,dh
mov	cx,*5.
sar	dx,cl
and	dx,*7.
mov	cx,*3.
sal	dx,cl
push	dx
mov	bx,*-6.(bp)
mov	dx,*10.(bx)
pop	bx
j	L20032
L112:mov	bx,*4.(bp)
and	bx,*7.
sal	bx,*1
add	bx,*-8.(bp)
mov	ax,*4.(bp)
subb	ah,ah
mov	cx,*3.
sar	ax,cl
and	ax,*7.
mov	cx,*12.
imul	cx
add	ax,*4.(bx)
mov	*-6.(bp),ax
mov	dx,*4.(bp)
subb	dh,dh
mov	cx,*6.
sar	dx,cl
and	dx,*3.
mov	cx,*3.
sal	dx,cl
push	dx
mov	bx,*-6.(bp)
mov	dx,*10.(bx)
pop	bx
L20032:add	dx,bx
mov	*-12.(bp),dx
push	*-10.(bp)
mov	bx,*-6.(bp)
mov	dx,(bx)
pop	bx
mov	(bx),dx
push	*-10.(bp)
mov	bx,*-6.(bp)
movb	dx,*2.(bx)
pop	bx
movb	*2.(bx),dx
push	*-10.(bp)
mov	bx,*-6.(bp)
movb	dx,*3.(bx)
pop	bx
movb	*3.(bx),dx
push	*-10.(bp)
mov	bx,*-6.(bp)
movb	dx,*4.(bx)
pop	bx
movb	*4.(bx),dx
push	*-10.(bp)
mov	bx,*-6.(bp)
mov	dx,*6.(bx)
pop	bx
mov	*6.(bx),dx
push	*-10.(bp)
mov	bx,*-6.(bp)
movb	dx,*8.(bx)
pop	bx
movb	*8.(bx),dx
push	*-10.(bp)
mov	bx,*-6.(bp)
mov	dx,*10.(bx)
pop	bx
mov	*10.(bx),dx
mov	bx,*-12.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
mov	bx,*-10.(bp)
mov	*12.+2(bx),ax
mov	*12.(bx),dx
mov	bx,*-12.(bp)
mov	ax,*4. +2(bx)
mov	dx,*4.(bx)
mov	bx,*-10.(bp)
mov	*16.+2(bx),ax
mov	*16.(bx),dx
mov	dx,*20.
push	dx
push	*8.(bp)
push	*-10.(bp)
call	_copyout
add	sp,*6.
br	L114
L111:cmp	*6.(bp),#22272.
je	L115
movb	111.+_u,*6.
br	L110
L115:call	_spl5
mov	*-18.(bp),ax
j	L116
L20034:or	(di),*64.
mov	dx,*21.
push	dx
push	di
call	_sleep
add	sp,*4.
L116:test	(di),*8.
jne	L20034
mov	(di),#16392.
mov	dx,*4.(bp)
mov	*10.(di),dx
push	*-18.(bp)
call	_splx
add	sp,*2.
mov	bx,*4.(bp)
and	bx,*7.
mov	cx,*4.
sal	bx,cl
movb	ax,*114.(bx)(si)
cbw
cwd
push	ax
push	dx
push	*8.(bp)
call	_fuword
add	sp,*2.
cwd
push	ax
push	dx
call	lmul
add	sp,*8.
mov	*18.(di),ax
mov	*16.(di),dx
mov	*12.(di),*0.
mov	dx,*8.(bp)
add	dx,*6.
push	dx
call	_fubyte
add	sp,*2.
movb	#252.(si),ax
mov	dx,*8.(bp)
add	dx,*2.
push	dx
call	_fuword
add	sp,*2.
movb	#257.(si),ax
mov	*-14.(bp),*0.
L20036:mov	dx,si
add	dx,*-14.(bp)
push	dx
mov	dx,*8.(bp)
add	dx,*-14.(bp)
add	dx,*7.
push	dx
call	_fubyte
add	sp,*2.
pop	bx
movb	*253.(bx),ax
inc	*-14.(bp)
cmp	*-14.(bp),*4.
jb	L20036
push	di
call	_k170str
add	sp,*2.
push	di
call	_iowait
add	sp,*2.
and	(di),#-16393.
L114:|NREG	3
L110:|RTYP	0
br	cret
.globl	_k170not
.even
_k170not:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dd=di
|NREG	1
mov	si,*6.(bp)
|	_unit=si
cmp	si,*4.
jb	L123
mov	bx,di
movb	dx,*6.(bx)(si)
and	dx,*3.
cmp	dx,*3.
jne	L123
push	si
mov	ax,di
sub	ax,#_k170dev
mov	dx,*0
sbb	dx,*0
mov	cx,#258.
idiv	cx
add	ax,_k170fma
push	ax
mov	dx,#L126
push	dx
call	_printf
add	sp,*6.
mov	bx,di
add	bx,si
andb	*6.(bx),*-3.
L123:|RTYP	0
br	cret
.data
L15:.byte	/4b,/45,/53,/20,/20,/20,/20,/20,/20
.byte	/42,/61,/73,/65,/64
.byte	/20,/25,/78,/20,/6c,/65,/76,/65,/6c
.byte	/20,/25,/64,/20,/25,/73
.byte	/2e,/a,/0
L16:.byte	/66,/6f,/75,/6e,/64,/0
L17:.byte	/4e,/4f,/54,/20,/66,/6f,/75,/6e,/64
.byte	/0
L126:.byte	/4b,/45,/53,/20,/20,/3c,/25,/64,/2c
.byte	/25,/64,/3e,/20,/77
.byte	/65,/6e,/74,/20,/6e,/6f,/74,/2d,/72
.byte	/65,/61,/64,/79,/2e,/a,/0
