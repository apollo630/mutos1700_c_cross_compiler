.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_io_info,222
.globl	_xfree
.text
.even
_xfree:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_xp=di
|NREG	1
|	_ip=si
mov	bx,120.+_u
mov	dx,*26.(bx)
mov	di,dx
test	dx,dx
beq	L3
push	di
call	_xlock
add	sp,*2.
andb	*10.(di),*-9.
mov	bx,120.+_u
mov	*26.(bx),*0.
mov	244.+_u,*0.
mov	si,*6.(di)
decb	*8.(di)
jne	L4
test	*6.(si),#512.
jne	L4
mov	*6.(di),*0.
cmp	_Bigswap,*0
je	L5
push	(di)
mov	dx,*4.(di)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
j	L6
L5:cmp	(di),*0
je	L6
push	(di)
mov	dx,*4.(di)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
mov	(di),*0.
L6:push	*2.(di)
push	*4.(di)
mov	dx,#_coremap
push	dx
call	_mfree
add	sp,*6.
andb	(si),*-33.
movb	dx,(si)
and	dx,*1.
je	L8
decb	*1.(si)
j	L10
L8:push	si
call	_iput
j	L20000
L4:push	di
call	_xccdec
L20000:add	sp,*2.
L10:|NREG	3
L3:|RTYP	0
br	cret
.globl	_xalloc
.even
_xalloc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
sub	sp,*14.
|NREG	1
|	_xp=si
|	_ts=-6.
|	_xp1=-8.
|	_ds=-10.
|	_ss=-12.
|	_sep=-14.
|	_aa=-16.
|	_bb=-18.
cmp	344.+_u,*0
jne	L10000
cmp	346.+_u,*0.
beq	L13
L10000:mov	*-8.(bp),*0.
mov	si,#_text
j	L14
L20002:cmp	*-8.(bp),*0
jne	L16
mov	*-8.(bp),si
L16:add	si,*12.
L14:cmp	si,22.+_v
jae	L15
cmp	*6.(si),*0
je	L20002
cmp	*6.(si),di
jne	L16
push	si
call	_xlock
add	sp,*2.
incb	*8.(si)
mov	bx,120.+_u
mov	*26.(bx),si
cmpb	*9.(si),*0
jne	L20
push	si
call	_xexpand
j	L20003
L20:incb	*9.(si)
push	si
call	_xunlock
L20003:add	sp,*2.
mov	dx,*4.(si)
mov	244.+_u,dx
br	L13
L15:mov	dx,*-8.(bp)
mov	si,dx
test	dx,dx
jne	L22
mov	dx,#L23
push	dx
call	_printf
add	sp,*2.
mov	dx,*9.
push	dx
push	120.+_u
call	_psignal
add	sp,*4.
br	L13
L22:movb	*10.(si),*12.
movb	*8.(si),*1.
movb	*9.(si),*0.
mov	*6.(si),di
orb	(di),*32.
incb	*1.(di)
mov	dx,346.+_u
add	dx,#2047.
mov	cx,*11.
shr	dx,cl
mov	*-6.(bp),dx
cmp	dx,*0
jne	L24
mov	*-6.(bp),*32.
L24:mov	dx,*-6.(bp)
mov	*4.(si),dx
cmp	_Bigswap,*0
je	L25
mov	dx,*-6.(bp)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_malloc
add	sp,*4.
mov	(si),ax
test	ax,ax
jne	L25
mov	dx,#L27
push	dx
call	_panic
add	sp,*2.
L25:mov	bx,120.+_u
mov	*26.(bx),si
push	si
call	_xexpand
add	sp,*2.
mov	dx,*-6.(bp)
mov	244.+_u,dx
mov	dx,246.+_u
mov	*-10.(bp),dx
mov	dx,248.+_u
mov	*-12.(bp),dx
movb	ax,333.+_u
cbw
mov	*-14.(bp),ax
mov	246.+_u,*0.
mov	248.+_u,*0.
movb	333.+_u,*1.
mov	dx,346.+_u
mov	130.+_u,dx
mov	ax,372.+_u
cwd
mov	134.+_u,ax
mov	132.+_u,dx
mov	128.+_u,*0.
movb	110.+_u,*2.
mov	bx,120.+_u
orb	*1.(bx),*4.
push	di
call	_readi
add	sp,*2.
mov	dx,*1.
push	dx
call	_ucodech
add	sp,*2.
mov	bx,120.+_u
andb	*1.(bx),*-5.
movb	110.+_u,*0.
movb	111.+_u,*0.
mov	dx,*-10.(bp)
mov	246.+_u,dx
mov	dx,*-12.(bp)
mov	248.+_u,dx
mov	dx,*-14.(bp)
movb	333.+_u,dx
movb	*10.(si),*2.
|NREG	2
L13:|RTYP	0
br	cret
.globl	_xexpand
.even
_xexpand:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_xp=di
sub	sp,*2.
|NREG	1
|	_mmp=si
|	_a=-6.
push	*4.(di)
mov	dx,#_coremap
push	dx
call	_malloc
add	sp,*4.
mov	*-6.(bp),ax
test	ax,ax
je	L31
mov	*2.(di),ax
movb	dx,*10.(di)
and	dx,*4.
jne	L32
mov	dx,*1.
push	dx
push	*4.(di)
push	*2.(di)
push	(di)
call	_swap
add	sp,*8.
cmp	_Bigswap,*0
jne	L32
push	(di)
mov	dx,*4.(di)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
mov	(di),*0.
L32:incb	*9.(di)
push	di
call	_xunlock
add	sp,*2.
j	L30
L31:mov	dx,#262.+_u
push	dx
call	_save
add	sp,*2.
test	ax,ax
je	L34
call	_sureg
j	L30
L34:mov	bx,120.+_u
push	*16.(bx)
mov	dx,*31.
push	dx
call	_copyseg
add	sp,*4.
sub	dx,dx
push	dx
mov	dx,*1.
push	dx
push	120.+_u
call	_xswap
add	sp,*6.
push	di
call	_xunlock
add	sp,*2.
mov	bx,120.+_u
orb	*1.(bx),*8.
call	_qswtch
|NREG	2
L30:|RTYP	0
br	cret
.globl	_xlock
.even
_xlock:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_xp=di
j	L38
L20005:orb	*10.(di),*16.
sub	si,si
push	si
push	di
call	_sleep
add	sp,*4.
L38:movb	dx,*10.(di)
and	dx,*8.
jne	L20005
orb	*10.(di),*8.
|RTYP	0
br	cret
.globl	_xunlock
.even
_xunlock:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_xp=di
movb	dx,*10.(di)
and	dx,*16.
je	L43
push	di
call	_wakeup
add	sp,*2.
L43:andb	*10.(di),*-25.
cmpb	_runout,*0
je	L42
cmpb	_runtxt,*0
je	L42
movb	_runout,*0.
mov	si,#_runout
push	si
call	_wakeup
add	sp,*2.
L42:|RTYP	0
br	cret
.globl	_xccdec
.even
_xccdec:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_xp=di
cmp	di,*0
je	L46
cmpb	*9.(di),*0
je	L46
push	di
call	_xlock
add	sp,*2.
decb	*9.(di)
jne	L47
movb	dx,*10.(di)
and	dx,*2.
je	L48
cmp	_Bigswap,*0
jne	L49
mov	si,*4.(di)
sal	si,*1
push	si
mov	si,#_swapmap
push	si
call	_malloc
add	sp,*4.
mov	(di),ax
test	ax,ax
jne	L52
mov	si,#L51
push	si
call	_panic
add	sp,*2.
j	L52
L49:andb	*10.(di),*-3.
L52:sub	si,si
push	si
push	*4.(di)
push	*2.(di)
push	(di)
call	_swap
add	sp,*8.
L48:push	*2.(di)
push	*4.(di)
mov	si,#_coremap
push	si
call	_mfree
add	sp,*6.
L47:push	di
call	_xunlock
add	sp,*2.
L46:|RTYP	0
br	cret
.globl	_xumount
.even
_xumount:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dev=di
|NREG	1
|	_xp=si
mov	si,#_text
j	L56
L20007:cmp	*6.(si),*0
je	L58
mov	bx,*6.(si)
cmp	*2.(bx),di
jne	L58
push	si
call	_xuntext
add	sp,*2.
L58:add	si,*12.
L56:cmp	si,22.+_v
jb	L20007
|NREG	2
|RTYP	0
br	cret
.globl	_xrele
.even
_xrele:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|NREG	1
|	_xp=si
movb	dx,(di)
and	dx,*32.
je	L62
mov	si,#_text
j	L63
L20009:cmp	di,*6.(si)
jne	L65
push	si
call	_xuntext
add	sp,*2.
L65:add	si,*12.
L63:cmp	si,22.+_v
jb	L20009
|NREG	2
L62:|RTYP	0
br	cret
.globl	_xuntext
.even
_xuntext:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_xp=di
|NREG	1
|	_ip=si
push	di
call	_xlock
add	sp,*2.
cmpb	*8.(di),*0
je	L70
push	di
call	_xunlock
add	sp,*2.
j	L69
L70:mov	si,*6.(di)
andb	*10.(di),*-9.
mov	*6.(di),*0.
cmp	_Bigswap,*0
je	L71
push	(di)
mov	dx,*4.(di)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
j	L72
L71:cmp	(di),*0
je	L72
push	(di)
mov	dx,*4.(di)
sal	dx,*1
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
mov	(di),*0.
L72:andb	(si),*-33.
movb	dx,(si)
and	dx,*1.
je	L74
decb	*1.(si)
j	L75
L74:push	si
call	_iput
add	sp,*2.
L75:|NREG	2
L69:|RTYP	0
br	cret
.data
L23:.byte	/6f,/75,/74,/20,/6f,/66,/20,/74,/65
.byte	/78,/74,/a,/0
L27:.byte	/6f,/75,/74,/20,/6f,/66,/20,/73,/77
.byte	/61,/70,/20,/73,/70
.byte	/61,/63,/65,/0
L51:.byte	/6f,/75,/74,/20,/6f,/66,/20,/73,/77
.byte	/61,/70,/20,/73,/70
.byte	/61,/63,/65,/0
