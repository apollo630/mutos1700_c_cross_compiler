.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_mpxip,2
.globl	_fstat
.text
.even
_fstat:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_fp=di
|NREG	1
|	_uap=si
mov	si,122.+_u
push	(si)
call	_getf
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L3
movb	dx,(di)
and	dx,*4.
je	L10000
push	*8.(di)
push	*6.(di)
j	L10001
L10000:sub	ax,ax
cwd
push	ax
push	dx
L10001:push	*2.(si)
push	*4.(di)
call	_stat1
add	sp,*8.
|NREG	3
L3:|RTYP	0
br	cret
.globl	_stat
.even
_stat:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_ip=di
|NREG	1
|	_uap=si
mov	si,122.+_u
sub	dx,dx
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
je	L6
sub	ax,ax
cwd
push	ax
push	dx
push	*2.(si)
push	di
call	_stat1
add	sp,*8.
push	di
call	_iput
add	sp,*2.
|NREG	3
L6:|RTYP	0
br	cret
.globl	_stat1
.even
_stat1:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|	_ub=6.
|	_pipeadj=8.
sub	sp,*32.
|NREG	1
|	_dp=si
|	_bp=-6.
|	_ds=-36.
mov	dx,#_time
push	dx
push	dx
push	di
call	_iupdat
add	sp,*6.
mov	dx,*2.(di)
mov	*-36.(bp),dx
mov	dx,*4.(di)
mov	*-34.(bp),dx
mov	dx,*6.(di)
mov	*-32.(bp),dx
mov	dx,*8.(di)
mov	*-30.(bp),dx
mov	dx,*10.(di)
mov	*-28.(bp),dx
mov	dx,*12.(di)
mov	*-26.(bp),dx
mov	dx,*22.(di)
mov	*-24.(bp),dx
mov	ax,*16.(di)
mov	dx,*14.(di)
sub	ax,*10.(bp)
sbb	dx,*8.(bp)
mov	*-20.(bp),ax
mov	*-22.(bp),dx
mov	ax,*4.(di)
add	ax,*31.
mov	cx,*4.
shr	ax,cl
sub	dx,dx
push	ax
push	dx
push	*2.(di)
call	_bread
add	sp,*6.
mov	*-6.(bp),ax
mov	bx,ax
mov	si,*14.(bx)
mov	dx,*4.(di)
add	dx,*31.
and	dx,*15.
mov	cx,*6.
sal	dx,cl
add	si,dx
mov	ax,*54.(si)
mov	dx,*52.(si)
mov	*-16.(bp),ax
mov	*-18.(bp),dx
mov	ax,*58.(si)
mov	dx,*56.(si)
mov	*-12.(bp),ax
mov	*-14.(bp),dx
mov	ax,*62.(si)
mov	dx,*60.(si)
mov	*-8.(bp),ax
mov	*-10.(bp),dx
push	*-6.(bp)
call	_brelse
add	sp,*2.
mov	dx,*30.
push	dx
push	*6.(bp)
lea	dx,*-36.(bp)
push	dx
call	_copyout
add	sp,*6.
test	ax,ax
jge	L10
movb	111.+_u,*14.
L10:|NREG	2
|RTYP	0
br	cret
.globl	_dup
.even
_dup:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*4.
|NREG	2
|	_fp=di
|NREG	1
|	_uap=si
|	_i=-6.
|	_m=-8.
mov	si,122.+_u
mov	dx,(si)
and	dx,*-64.
mov	*-8.(bp),dx
and	(si),*63.
push	(si)
call	_getf
add	sp,*2.
mov	di,ax
cmp	di,*0
je	L13
test	*-8.(bp),*64.
jne	L14
call	_ufalloc
mov	*-6.(bp),ax
test	ax,ax
jge	L15
j	L13
L14:mov	dx,*2.(si)
mov	*-6.(bp),dx
cmp	dx,*0
jl	L10002
cmp	dx,*20.
jl	L16
L10002:movb	111.+_u,*9.
j	L13
L16:mov	dx,*-6.(bp)
mov	124.+_u,dx
L15:mov	dx,(si)
cmp	*-6.(bp),dx
je	L17
mov	bx,*-6.(bp)
sal	bx,*1
mov	dx,#174.+_u(bx)
test	dx,dx
je	L18
mov	bx,*-6.(bp)
sal	bx,*1
push	#174.+_u(bx)
call	_closef
add	sp,*2.
L18:mov	bx,*-6.(bp)
sal	bx,*1
mov	174.+_u(bx),di
inc	*2.(di)
L17:|NREG	3
L13:|RTYP	0
br	cret
.globl	_smount
.even
_smount:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*10.
|	_dev=-6.
|NREG	2
|	_ip=di
|NREG	1
|	_mp=si
|	_smp=-8.
|	_fp=-10.
|	_bp=-12.
|	_uap=-14.
mov	dx,122.+_u
mov	*-14.(bp),dx
call	_getmdev
mov	*-6.(bp),ax
cmpb	111.+_u,*0
jne	L21
mov	bx,*-14.(bp)
mov	dx,*2.(bx)
mov	154.+_u,dx
sub	dx,dx
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
je	L21
mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#16384.
je	L22
movb	111.+_u,*20.
j	L23
L22:cmpb	*1.(di),*1.
jne	L24
cmp	*4.(di),*2.
je	L24
mov	*-8.(bp),*0.
mov	si,#_mount
j	L25
L20001:cmp	*2.(si),*0
je	L28
mov	dx,(si)
cmp	*-6.(bp),dx
jne	L27
L24:movb	111.+_u,*16.
L23:push	di
call	_iput
add	sp,*2.
|NREG	3
L21:|RTYP	0
br	cret
L28:cmp	*-8.(bp),*0
jne	L27
mov	*-8.(bp),si
L27:add	si,*6.
L25:cmp	si,14.+_v
jb	L20001
mov	si,*-8.(bp)
cmp	si,*0
je	L24
mov	bx,*-14.(bp)
mov	dx,*4.(bx)
test	dx,dx
je	L10003
sub	dx,dx
push	dx
j	L10004
L10003:mov	dx,*1.
push	dx
L10004:push	*-6.(bp)
mov	bx,*-6.(bp)
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@_bdevsw(bx)
add	sp,*4.
cmpb	111.+_u,*0
jne	L23
mov	ax,*1.
cwd
push	ax
push	dx
push	*-6.(bp)
call	_bread
add	sp,*6.
mov	*-12.(bp),ax
cmpb	111.+_u,*0
jne	L31
mov	bx,ax
mov	bx,*14.(bx)
cmpb	*640.(bx),*70.
je	L34
movb	111.+_u,*35.
L31:push	*-12.(bp)
call	_brelse
add	sp,*2.
mov	bx,*-14.(bp)
mov	dx,*4.(bx)
test	dx,dx
je	L10005
sub	dx,dx
push	dx
j	L10006
L10005:mov	dx,*1.
push	dx
L10006:push	*-6.(bp)
mov	bx,*-6.(bp)
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@2.+_bdevsw(bx)
add	sp,*4.
br	L23
L34:mov	*4.(si),di
mov	dx,*-6.(bp)
mov	(si),dx
call	_geteblk
mov	*2.(si),ax
mov	dx,#1024.
push	dx
mov	bx,*2.(si)
push	*14.(bx)
mov	bx,*-12.(bp)
push	*14.(bx)
call	_bcopy
add	sp,*6.
mov	bx,*2.(si)
mov	bx,*14.(bx)
mov	*-10.(bp),bx
movb	*611.(bx),*0.
mov	bx,*-10.(bp)
movb	*610.(bx),*0.
push	*-10.(bp)
mov	bx,*-14.(bp)
mov	dx,*4.(bx)
and	dx,*1.
pop	bx
movb	*613.(bx),dx
push	*-12.(bp)
call	_brelse
add	sp,*2.
orb	(di),*8.
push	di
call	_prele
add	sp,*2.
mov	bx,*-10.(bp)
movb	dx,#613.(bx)
testb	dx,dx
bne	L21
mov	bx,*-10.(bp)
movb	*640.(bx),*0.
mov	bx,*-10.(bp)
movb	*612.(bx),*1.
call	_update
br	L21
.globl	_sumount
.even
_sumount:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*6.
|	_dev=-6.
|NREG	2
|	_ip=di
|NREG	1
|	_mp=si
|	_fp=-8.
|	_bp=-10.
call	_getmdev
mov	*-6.(bp),ax
cmpb	111.+_u,*0
bne	L38
push	ax
call	_xumount
add	sp,*2.
call	_update
mov	si,#_mount
j	L39
L20003:cmp	*2.(si),*0
je	L41
mov	dx,(si)
cmp	*-6.(bp),dx
je	L42
L41:add	si,*6.
L39:cmp	si,14.+_v
jb	L20003
movb	111.+_u,*22.
br	L38
L42:mov	di,#_inode
j	L43
L20005:cmp	*4.(di),*0
je	L45
mov	dx,*2.(di)
cmp	*-6.(bp),dx
jne	L45
movb	111.+_u,*16.
j	L38
L45:add	di,*76.
L43:cmp	di,6.+_v
jb	L20005
mov	bx,*2.(si)
mov	bx,*14.(bx)
mov	*-8.(bp),bx
movb	*640.(bx),*70.
mov	bx,*-8.(bp)
movb	*612.(bx),*1.
call	_update
push	*-6.(bp)
call	_bpurge
add	sp,*2.
sub	dx,dx
push	dx
push	*-6.(bp)
mov	bx,*-6.(bp)
mov	cx,*8.
sar	bx,cl
mov	cx,*3.
sal	bx,cl
call	@2.+_bdevsw(bx)
add	sp,*4.
mov	di,*4.(si)
andb	(di),*-9.
push	di
call	_plock
add	sp,*2.
push	di
call	_iput
add	sp,*2.
mov	dx,*2.(si)
mov	*-10.(bp),dx
mov	*2.(si),*0.
push	*-10.(bp)
call	_brelse
add	sp,*2.
|NREG	3
L38:|RTYP	0
br	cret
.globl	_getmdev
.even
_getmdev:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|	_dev=-6.
|NREG	2
|	_ip=di
sub	si,si
push	si
mov	si,#_uchar
push	si
call	_namei
add	sp,*4.
mov	di,ax
cmp	di,*0
jne	L50
mov	si,*-1.
j	L20006
L50:mov	si,*6.(di)
and	si,#-4096.
cmp	si,#24576.
je	L51
movb	111.+_u,*15.
L51:mov	si,*22.(di)
mov	*-6.(bp),si
mov	cx,*8.
sar	si,cl
cmp	si,_nblkdev
jl	L52
movb	111.+_u,*6.
L52:push	di
call	_iput
add	sp,*2.
mov	si,*-6.(bp)
L20006:mov	ax,si
|RTYP	0
br	cret
.data
