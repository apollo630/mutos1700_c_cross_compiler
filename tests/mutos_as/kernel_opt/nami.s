.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_io_info,222
.globl	_namei
.text
.even
_namei:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_func=4.
|	_flag=6.
sub	sp,*12.
|NREG	2
|	_dp=di
|NREG	1
|	_c=si
|	_cp=-6.
|	_bp=-8.
|	_i=-10.
|	_d=-12.
|	_eo=-16.
mov	di,136.+_u
call	@*4.(bp)
mov	si,ax
cmp	si,*47.
jne	L4
mov	dx,138.+_u
mov	di,dx
test	dx,dx
jne	L4
mov	di,_rootdir
L4:push	*4.(di)
push	*2.(di)
call	_iget
add	sp,*4.
j	L6
L20001:call	@*4.(bp)
mov	si,ax
L6:cmp	si,*47.
je	L20001
cmp	si,*0
jne	L9
cmp	*6.(bp),*0
je	L9
movb	111.+_u,*2.
L9:cmpb	111.+_u,*0
bne	L10
cmp	si,*0
jne	L11
L20014:mov	dx,di
br	L20012
L11:mov	*-6.(bp),#140.+_u
j	L12
L20003:cmp	si,*0
je	L15
cmpb	111.+_u,*0
jne	L15
cmp	*-6.(bp),#154.+_u
jae	L14
mov	bx,*-6.(bp)
mov	ax,si
movb	(bx),ax
inc	*-6.(bp)
L14:call	@*4.(bp)
mov	si,ax
L12:cmp	si,*47.
je	L15
j	L20003
L20005:mov	bx,*-6.(bp)
movb	(bx),*0.
inc	*-6.(bp)
L15:cmp	*-6.(bp),#154.+_u
jae	L17
j	L20005
L20007:call	@*4.(bp)
mov	si,ax
L17:cmp	si,*47.
je	L20007
L19:mov	dx,*6.(di)
and	dx,#-4096.
cmp	dx,#16384.
je	L20
movb	111.+_u,*20.
L20:mov	dx,*64.
push	dx
push	di
call	_access
add	sp,*4.
test	ax,ax
bne	L10
sub	ax,ax
cwd
mov	134.+_u,ax
mov	132.+_u,dx
movb	110.+_u,*1.
sub	ax,ax
cwd
mov	*-14.(bp),ax
mov	*-16.(bp),dx
mov	*-8.(bp),ax
L21:mov	ax,134.+_u
mov	dx,132.+_u
cmp	dx,*14.(di)
blt	L22
jg	L10000
cmp	ax,*16.(di)
blo	L22
L10000:cmp	*-8.(bp),*0
je	L23
push	*-8.(bp)
call	_brelse
add	sp,*2.
L23:cmp	*6.(bp),*1.
jne	L24
cmp	si,*0
jne	L24
mov	dx,#128.
push	dx
push	di
call	_access
add	sp,*4.
test	ax,ax
bne	L10
mov	172.+_u,di
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
cmp	dx,*0
jne	L10001
cmp	ax,*0
je	L25
L10001:mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
sub	ax,*16.
sbb	dx,*0
mov	134.+_u,ax
mov	132.+_u,dx
br	L20013
L25:orb	(di),*66.
br	L20013
L24:movb	111.+_u,*2.
j	L10
L20016:cmp	ax,*0
jne	L27
cmp	*-8.(bp),*0
je	L28
push	*-8.(bp)
call	_brelse
add	sp,*2.
L28:mov	dx,*1.
push	dx
mov	ax,134.+_u
mov	dx,132.+_u
mov	cx,*10.
sar	dx,*1
rcr	ax,*1
loop	.-4
push	ax
push	dx
push	di
call	_bmap
add	sp,*8.
push	ax
push	dx
push	*2.(di)
call	_bread
add	sp,*6.
mov	*-8.(bp),ax
mov	bx,ax
test	(bx),*4.
je	L27
push	ax
call	_brelse
add	sp,*2.
L10:push	di
call	_iput
add	sp,*2.
br	L20013
L22:mov	ax,#1023.
cwd
mov	cx,ax
mov	bx,dx
mov	ax,134.+_u
mov	dx,132.+_u
and	ax,cx
and	dx,bx
cmp	dx,*0
beq	L20016
L27:mov	dx,*16.
push	dx
mov	dx,#156.+_u
push	dx
mov	dx,134.+_u
and	dx,#1023.
push	dx
mov	bx,*-8.(bp)
mov	dx,*14.(bx)
pop	bx
add	dx,bx
push	dx
call	_bcopy
add	sp,*6.
add	134.+_u,*16.
adc	132.+_u,*0
cmp	156.+_u,*0
jne	L30
cmp	*-16.(bp),*0
bne	L21
cmp	*-14.(bp),*0.
bne	L21
mov	ax,134.+_u
mov	dx,132.+_u
mov	*-14.(bp),ax
mov	*-16.(bp),dx
br	L21
L30:mov	*-10.(bp),*0.
L20009:push	*-10.(bp)
mov	bx,*-10.(bp)
movb	dx,#158.+_u(bx)
pop	bx
cmpb	140.+_u(bx),dx
bne	L21
inc	*-10.(bp)
cmp	*-10.(bp),*14.
jl	L20009
cmp	*-8.(bp),*0
je	L35
push	*-8.(bp)
call	_brelse
add	sp,*2.
L35:cmp	*6.(bp),*2.
jne	L36
cmp	si,*0
jne	L36
mov	dx,#128.
push	dx
push	di
call	_access
add	sp,*4.
test	ax,ax
bne	L10
br	L20014
L36:mov	dx,*2.(di)
mov	*-12.(bp),dx
cmp	156.+_u,*2.
jne	L37
cmp	*4.(di),*2.
jne	L37
cmpb	159.+_u,*46.
jne	L37
mov	*-10.(bp),*1.
j	L40
L20011:mov	ax,*-10.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	ax,#2.+_mount(bx)
test	ax,ax
je	L42
mov	ax,*-10.(bp)
imul	cx
mov	bx,ax
mov	ax,#_mount(bx)
cmp	ax,*-12.(bp)
jne	L42
push	di
call	_iput
add	sp,*2.
mov	ax,*-10.(bp)
mov	cx,*6.
imul	cx
mov	bx,ax
mov	di,4.+_mount(bx)
incb	*1.(di)
push	di
call	_plock
add	sp,*2.
br	L19
L42:inc	*-10.(bp)
L40:mov	dx,12.+_v
cmp	*-10.(bp),dx
jl	L20011
L37:push	di
call	_iput
add	sp,*2.
push	156.+_u
push	*-12.(bp)
call	_iget
add	sp,*4.
mov	di,ax
cmp	di,*0
bne	L9
L20013:sub	dx,dx
L20012:mov	ax,dx
|RTYP	12
br	cret
.globl	_schar
.even
_schar:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	bx,154.+_u
movb	ax,(bx)
inc	154.+_u
subb	ah,ah
|RTYP	0
br	cret
.globl	_uchar
.even
_uchar:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_c=di
push	154.+_u
inc	154.+_u
call	_fubyte
add	sp,*2.
mov	di,ax
cmp	di,*-1.
jne	L52
movb	111.+_u,*14.
L52:mov	ax,di
|RTYP	0
br	cret
.data
