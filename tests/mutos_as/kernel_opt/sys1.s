.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_mpxip,2
.globl	_execnt
.data
_execnt:/0
.globl	_exec
.text
.even
_exec:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
mov	di,122.+_u
mov	*4.(di),*0.
call	_exece
|RTYP	0
br	cret
.globl	_exece
.even
_exece:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*32.
|NREG	2
|	_nc=di
|NREG	1
|	_cp=si
|	_addr=-6.
|	_bp=-8.
|	_bp1=-10.
|	_uap=-12.
|	_na=-14.
|	_ne=-16.
|	_bno=-18.
|	_ucp=-20.
|	_ap=-22.
|	_c=-24.
|	_nec=-26.
|	_arg2=-28.
|	_arg2p=-30.
|	_getxret=-32.
|	_argseg=-34.
|	_ip=-36.
sub	dx,dx
push	dx
mov	dx,#_uchar
push	dx
call	_namei
add	sp,*4.
mov	*-36.(bp),ax
test	ax,ax
beq	L6
mov	*-18.(bp),*0.
mov	*-8.(bp),*0.
j	L7
L20001:mov	dx,*-1.
push	dx
mov	dx,#_execnt
push	dx
call	_sleep
add	sp,*4.
L7:cmp	_execnt,*2.
jge	L20001
inc	_execnt
mov	*-10.(bp),*0.
mov	dx,*64.
push	dx
push	*-36.(bp)
call	_access
add	sp,*4.
test	ax,ax
bne	L9
mov	bx,*-36.(bp)
mov	dx,*6.(bx)
and	dx,#-4096.
cmp	dx,#-32768.
jne	L10000
test	*6.(bx),*73.
jne	L10
L10000:movb	111.+_u,*13.
br	L9
L10:mov	*-14.(bp),*0.
mov	*-16.(bp),*0.
sub	di,di
mov	bx,122.+_u
mov	*-12.(bp),bx
mov	dx,*2.(bx)
cmp	dx,di
beq	L11
L12:mov	*-22.(bp),*0.
mov	bx,*-12.(bp)
mov	dx,*2.(bx)
test	dx,dx
je	L14
push	*2.(bx)
call	_fuword
add	sp,*2.
mov	*-22.(bp),ax
mov	bx,*-12.(bp)
add	*2.(bx),*2.
L14:cmp	*-22.(bp),*0
jne	L15
mov	bx,*-12.(bp)
mov	dx,*4.(bx)
test	dx,dx
je	L15
cmp	*-16.(bp),*0
jne	L16
mov	*-26.(bp),*0.
L16:mov	bx,*-12.(bp)
mov	*2.(bx),*0.
mov	bx,*-12.(bp)
push	*4.(bx)
call	_fuword
add	sp,*2.
mov	*-22.(bp),ax
test	ax,ax
beq	L11
mov	bx,*-12.(bp)
add	*4.(bx),*2.
inc	*-16.(bp)
L15:cmp	*-22.(bp),*0
beq	L11
inc	*-14.(bp)
cmp	*-22.(bp),*-1.
jne	L20
movb	111.+_u,*14.
L20:cmp	di,#5119.
jl	L21
movb	111.+_u,*7.
L21:push	*-22.(bp)
inc	*-22.(bp)
call	_fubyte
add	sp,*2.
mov	*-24.(bp),ax
test	ax,ax
jge	L22
movb	111.+_u,*14.
L22:cmpb	111.+_u,*0
bne	L9
test	di,#1023.
jne	L23
cmp	di,*0
jne	L24
call	_geteblk
mov	*-10.(bp),ax
j	L20002
L24:push	*-8.(bp)
call	_geteblk
pop	bx
mov	*6.(bx),ax
L20002:mov	*-8.(bp),ax
mov	bx,ax
mov	*6.(bx),*0.
mov	bx,*-8.(bp)
mov	si,*14.(bx)
L23:inc	*-26.(bp)
inc	di
mov	dx,*-24.(bp)
movb	(si),dx
inc	si
cmp	*-24.(bp),*0
jg	L20
br	L12
L11:inc	di
and	di,*-2.
push	di
push	*-36.(bp)
call	_getxfil
add	sp,*4.
mov	*-32.(bp),ax
test	ax,ax
bne	L9
cmpb	111.+_u,*0
bne	L9
mov	dx,di
neg	dx
add	dx,*-2.
mov	*-20.(bp),dx
mov	bx,*-14.(bp)
sal	bx,*1
mov	dx,*-20.(bp)
sub	dx,bx
add	dx,*-6.
mov	*-22.(bp),dx
mov	bx,322.+_u
add	dx,*-14.
mov	*16.(bx),dx
mov	dx,*-14.(bp)
sub	dx,*-16.(bp)
push	dx
push	*-22.(bp)
call	_suword
add	sp,*4.
sub	di,di
L26:add	*-22.(bp),*2.
cmp	*-28.(bp),*-1.
je	L28
inc	*-28.(bp)
L28:mov	dx,*-16.(bp)
cmp	*-14.(bp),dx
jne	L29
sub	dx,dx
push	dx
push	*-22.(bp)
call	_suword
add	sp,*4.
add	*-22.(bp),*2.
L29:dec	*-14.(bp)
jl	L27
push	*-20.(bp)
push	*-22.(bp)
call	_suword
add	sp,*4.
L32:test	di,#1023.
jne	L33
cmp	di,*0
jne	L34
mov	dx,*-10.(bp)
j	L20003
L34:mov	bx,*-8.(bp)
mov	dx,*6.(bx)
L20003:mov	*-8.(bp),dx
mov	bx,dx
mov	si,*14.(bx)
L33:movb	ax,(si)
cbw
inc	si
mov	*-24.(bp),ax
push	ax
push	*-20.(bp)
inc	*-20.(bp)
call	_subyte
add	sp,*4.
inc	di
test	*-24.(bp),#255.
jne	L32
j	L26
L27:sub	dx,dx
push	dx
push	*-22.(bp)
call	_suword
add	sp,*4.
sub	dx,dx
push	dx
push	*-20.(bp)
call	_suword
add	sp,*4.
call	_setregs
L9:push	*-36.(bp)
call	_iput
L20004:add	sp,*2.
mov	dx,*-10.(bp)
mov	*-8.(bp),dx
test	dx,dx
jne	L20006
mov	dx,_execnt
dec	_execnt
cmp	dx,*2.
jl	L38
mov	dx,#_execnt
push	dx
call	_wakeup
add	sp,*2.
L38:cmp	*-32.(bp),*-1.
jne	L39
mov	dx,*9.
push	dx
call	_exit
add	sp,*2.
L39:|NREG	3
L6:|RTYP	0
br	cret
L20006:mov	bx,*-8.(bp)
mov	dx,*6.(bx)
mov	*-10.(bp),dx
push	bx
call	_brelse
j	L20004
.globl	_getxfil
.even
_getxfil:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
|	_nargc=6.
sub	sp,*52.
|NREG	1
|	_ds=si
|	_ts=-6.
|	_ss=-8.
|	_sep=-10.
|	_overlay=-12.
|	_msdos=-14.
|	_lsize=-18.
|	_uhdr=-48.
|	_psp=-50.
|	_xp=-52.
|	_n=-54.
|	_b=-56.
mov	128.+_u,#340.+_u
mov	dx,*32.
mov	372.+_u,dx
mov	130.+_u,dx
sub	ax,ax
cwd
mov	134.+_u,ax
mov	132.+_u,dx
movb	110.+_u,*1.
push	di
call	_readi
add	sp,*2.
movb	110.+_u,*0.
cmpb	111.+_u,*0
bne	L43
cmp	130.+_u,*0
bne	L44
sub	dx,dx
mov	*-14.(bp),dx
mov	*-12.(bp),dx
mov	*-10.(bp),dx
mov	dx,*30.
push	dx
lea	dx,*-48.(bp)
push	dx
mov	dx,#340.+_u
push	dx
call	_copy
add	sp,*6.
mov	dx,340.+_u
and	dx,*-64.
cmp	dx,#320.
beq	L45
mov	372.+_u,*16.
mov	ax,*-46.(bp)
sub	dx,dx
mov	346.+_u,ax
mov	344.+_u,dx
mov	ax,*-44.(bp)
mov	350.+_u,ax
mov	348.+_u,dx
mov	ax,*-42.(bp)
mov	354.+_u,ax
mov	352.+_u,dx
mov	ax,*-40.(bp)
mov	358.+_u,ax
mov	356.+_u,dx
mov	dx,*-38.(bp)
mov	360.+_u,dx
mov	362.+_u,*0.
sub	ax,ax
cwd
mov	366.+_u,ax
mov	364.+_u,dx
movb	342.+_u,*0.
mov	dx,346.+_u
mov	400.+_u,dx
cmp	340.+_u,#263.
jne	L46
movb	343.+_u,*32.
j	L47
L20008:mov	dx,#321.
j	L10003
L46:cmp	340.+_u,#265.
je	L10001
cmp	340.+_u,#261.
bne	L44
L10001:movb	343.+_u,*38.
L47:cmp	340.+_u,#261.
jne	L20008
mov	dx,#322.
L10003:mov	340.+_u,dx
L45:cmp	340.+_u,#321.
je	L10004
cmp	340.+_u,#322.
bne	L44
L10004:movb	dx,343.+_u
and	dx,*24.
bgt	L44
cmp	340.+_u,#322.
jne	L10005
mov	dx,*1.
j	L10006
L10005:sub	dx,dx
L10006:mov	*-12.(bp),dx
movb	dx,343.+_u
and	dx,*4.
je	L10007
movb	dx,343.+_u
and	dx,*2.
beq	L44
L10007:movb	dx,343.+_u
and	dx,*4.
mov	*-10.(bp),dx
test	dx,dx
jne	L50
mov	ax,346.+_u
mov	dx,344.+_u
add	350.+_u,ax
adc	348.+_u,dx
mov	ax,350.+_u
sub	cx,cx
mov	dx,348.+_u
cmp	dx,cx
jne	L10008
cmp	ax,ax
je	L51
L10008:movb	111.+_u,*12.
br	L43
L51:sub	ax,ax
cwd
mov	346.+_u,ax
mov	344.+_u,dx
L50:cmp	344.+_u,*0
jne	L10009
cmp	346.+_u,*0.
je	L53
L10009:movb	dx,(di)
and	dx,*32.
jne	L53
cmpb	*1.(di),*1.
je	L53
movb	111.+_u,*26.
br	L43
L53:mov	ax,#2047.
cwd
mov	cx,ax
mov	bx,dx
mov	ax,346.+_u
mov	dx,344.+_u
add	ax,cx
adc	dx,bx
mov	cx,*11.
sar	dx,*1
rcr	ax,*1
loop	.-4
mov	*-6.(bp),ax
mov	ax,350.+_u
mov	dx,348.+_u
add	ax,354.+_u
adc	dx,352.+_u
mov	*-16.(bp),ax
mov	*-18.(bp),dx
sub	cx,cx
cmp	dx,cx
bne	L10008
cmp	ax,ax
bne	L10008
mov	si,ax
add	si,#2047.
mov	cx,*11.
shr	si,cl
cmp	si,*0
jne	L55
mov	ax,*-16.(bp)
mov	dx,*-18.(bp)
cmp	dx,*0
jne	L10011
cmp	ax,*0
je	L55
L10011:mov	si,*32.
L55:mov	dx,*6.(bp)
add	dx,#3071.
mov	cx,*11.
shr	dx,cl
mov	*-8.(bp),dx
cmp	*-12.(bp),*0
je	L56
cmp	*-10.(bp),*0
beq	L10008
cmpb	333.+_u,*0
beq	L10008
cmp	*6.(bp),*0
bne	L10008
mov	si,246.+_u
mov	dx,248.+_u
mov	*-8.(bp),dx
call	_xfree
push	di
call	_xalloc
add	sp,*2.
mov	bx,322.+_u
mov	dx,*16.(bx)
mov	*-50.(bp),dx
mov	dx,360.+_u
and	dx,*1.
push	dx
mov	dx,*-50.(bp)
add	dx,*8.
push	dx
call	_suword
add	sp,*4.
mov	bx,120.+_u
mov	bx,*26.(bx)
mov	*-52.(bp),bx
mov	dx,*2.(bx)
mov	cx,*7.
sal	dx,cl
add	dx,362.+_u
push	dx
mov	dx,*-50.(bp)
add	dx,*10.
push	dx
call	_suword
br	L20009
L56:mov	330.+_u,*0.
call	_xfree
mov	dx,si
add	dx,*-8.(bp)
inc	dx
push	dx
call	_expand
add	sp,*2.
mov	bx,120.+_u
mov	dx,si
inc	dx
mov	*22.(bx),dx
mov	246.+_u,si
mov	dx,*-8.(bp)
mov	248.+_u,dx
mov	dx,*-10.(bp)
movb	333.+_u,dx
movb	410.+_u,*0.
mov	*-54.(bp),si
mov	bx,120.+_u
mov	dx,*16.(bx)
inc	dx
mov	*-56.(bp),dx
j	L59
L20011:push	*-56.(bp)
inc	*-56.(bp)
call	_clearse
add	sp,*2.
L59:mov	dx,*-54.(bp)
dec	*-54.(bp)
test	dx,dx
jne	L20011
push	di
call	_xalloc
add	sp,*2.
mov	244.+_u,*0.
mov	246.+_u,si
mov	248.+_u,*0.
movb	333.+_u,*0.
mov	128.+_u,*0.
mov	ax,372.+_u
cwd
add	ax,346.+_u
adc	dx,344.+_u
mov	134.+_u,ax
mov	132.+_u,dx
mov	dx,350.+_u
mov	130.+_u,dx
push	di
call	_readi
add	sp,*2.
cmp	*-6.(bp),*0
jne	L61
sub	dx,dx
push	dx
call	_ucodech
add	sp,*2.
L61:cmpb	111.+_u,*0
jne	L43
mov	bx,120.+_u
movb	dx,*1.(bx)
and	dx,*16.
jne	L62
test	*6.(di),#2048.
je	L63
cmp	112.+_u,*0
je	L63
mov	dx,*10.(di)
mov	112.+_u,dx
mov	bx,120.+_u
mov	dx,*10.(di)
mov	*8.(bx),dx
L63:test	*6.(di),#1024.
je	L58
mov	dx,*12.(di)
mov	114.+_u,dx
j	L58
L62:mov	dx,*5.
push	dx
push	120.+_u
call	_psignal
L20009:add	sp,*4.
L58:mov	dx,*-6.(bp)
mov	244.+_u,dx
mov	246.+_u,si
mov	dx,*-8.(bp)
mov	248.+_u,dx
mov	dx,*-10.(bp)
movb	333.+_u,dx
j	L43
L44:movb	111.+_u,*8.
L43:mov	ax,*-12.(bp)
|RTYP	0
br	cret
.globl	_setregs
.even
_setregs:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*10.
|NREG	2
|	_rp=di
|NREG	1
|	_cp=si
|	_i=-6.
|	_psp=-8.
|	_pp=-10.
|	_xp=-12.
|	_pn=-14.
mov	bx,120.+_u
mov	*-10.(bp),bx
mov	dx,*26.(bx)
mov	*-12.(bp),dx
mov	di,#272.+_u
j	L70
L20013:test	(di),*1.
jne	L72
mov	(di),*0.
L72:add	di,*2.
L70:cmp	di,#306.+_u
jb	L20013
mov	si,#_regloc
j	L74
L20015:movb	ax,(si)
cbw
inc	si
sal	ax,*1
add	ax,322.+_u
mov	bx,ax
mov	(bx),*0.
L74:mov	dx,_reglocc
add	dx,#_regloc
cmp	dx,si
ja	L20015
mov	bx,322.+_u
mov	dx,*16.(bx)
mov	*-8.(bp),dx
mov	bx,*-10.(bp)
mov	dx,*16.(bx)
inc	dx
mov	cx,*7.
sal	dx,cl
mov	*-14.(bp),dx
mov	bx,322.+_u
mov	*10.(bx),dx
mov	bx,322.+_u
mov	dx,*-14.(bp)
mov	*14.(bx),dx
push	*-14.(bp)
mov	dx,*-8.(bp)
add	dx,*2.
push	dx
call	_suword
add	sp,*4.
cmpb	333.+_u,*0
je	L76
mov	bx,*-12.(bp)
mov	dx,*2.(bx)
mov	cx,*7.
sal	dx,cl
mov	*-14.(bp),dx
L76:mov	dx,362.+_u
add	dx,*-14.(bp)
push	dx
mov	dx,*-8.(bp)
add	dx,*10.
push	dx
call	_suword
add	sp,*4.
mov	dx,360.+_u
and	dx,*-2.
push	dx
mov	dx,*-8.(bp)
add	dx,*8.
push	dx
call	_suword
add	sp,*4.
mov	dx,#512.
push	dx
mov	dx,*-8.(bp)
add	dx,*12.
push	dx
call	_suword
add	sp,*4.
mov	12.+_u,*0.
mov	dx,*96.
push	dx
mov	dx,#14.+_u
push	dx
call	_zeromem
add	sp,*4.
mov	*-6.(bp),*0.
L20017:mov	bx,*-6.(bp)
movb	dx,#214.+_u(bx)
and	dx,*1.
je	L79
mov	bx,*-6.(bp)
sal	bx,*1
push	#174.+_u(bx)
call	_closef
add	sp,*2.
mov	bx,*-6.(bp)
sal	bx,*1
mov	174.+_u(bx),*0.
mov	bx,*-6.(bp)
andb	214.+_u(bx),*-2.
L79:inc	*-6.(bp)
cmp	*-6.(bp),*20.
jl	L20017
mov	dx,*14.
push	dx
mov	dx,#374.+_u
push	dx
mov	dx,#140.+_u
push	dx
call	_bcopy
add	sp,*6.
|NREG	3
|RTYP	0
br	cret
.globl	_rexit
.even
_rexit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_uap=di
mov	di,122.+_u
mov	si,(di)
and	si,#255.
mov	cx,*8.
sal	si,cl
push	si
call	_exit
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_exit
.even
_exit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_rv=4.
sub	sp,*4.
|NREG	2
|	_i=di
|NREG	1
|	_p=si
|	_q=-6.
|	_f=-8.
mov	si,120.+_u
andb	*1.(si),*-81.
mov	*30.(si),*0.
sub	di,di
L20019:mov	bx,di
sal	bx,*1
mov	272.+_u(bx),*1.
inc	di
cmp	di,*17.
jl	L20019
sub	di,di
L20021:mov	bx,di
sal	bx,*1
mov	dx,#174.+_u(bx)
mov	*-8.(bp),dx
mov	bx,di
sal	bx,*1
mov	174.+_u(bx),*0.
push	*-8.(bp)
call	_closef
add	sp,*2.
inc	di
cmp	di,*20.
jl	L20021
push	136.+_u
call	_plock
add	sp,*2.
push	136.+_u
call	_iput
add	sp,*2.
cmp	138.+_u,*0
je	L93
push	138.+_u
call	_plock
add	sp,*2.
push	138.+_u
call	_iput
add	sp,*2.
L93:call	_xfree
push	*16.(si)
mov	dx,*33.
push	dx
push	*18.(si)
call	_max
add	sp,*4.
push	ax
mov	dx,#_coremap
push	dx
call	_mfree
add	sp,*6.
movb	(si),*5.
mov	dx,*4.(bp)
mov	*16.(si),dx
mov	ax,316.+_u
mov	dx,314.+_u
add	ax,308.+_u
adc	dx,306.+_u
mov	*20.(si),ax
mov	*18.(si),dx
mov	ax,320.+_u
mov	dx,318.+_u
add	ax,312.+_u
adc	dx,310.+_u
mov	*24.(si),ax
mov	*22.(si),dx
sub	di,di
mov	*-6.(bp),#_proc
j	L94
L20024:mov	bx,*-6.(bp)
mov	dx,*14.(bx)
cmp	dx,*12.(si)
jne	L97
mov	*14.(bx),*1.
mov	bx,*-6.(bp)
cmpb	(bx),*5.
jne	L98
inc	di
j	L96
L98:mov	dx,*-6.(bp)
mov	bx,dx
cmpb	(bx),*6.
jne	L96
push	dx
call	_setrun
j	L20022
L97:mov	bx,*-6.(bp)
mov	dx,*12.(bx)
cmp	dx,*14.(si)
jne	L96
push	bx
call	_wakeup
L20022:add	sp,*2.
L96:add	*-6.(bp),*32.
L94:mov	dx,_Nproca
cmp	*-6.(bp),dx
jb	L20024
cmp	di,*0
je	L103
mov	dx,#32.+_proc
push	dx
call	_wakeup
add	sp,*2.
L103:call	_swtch
|NREG	3
|RTYP	0
br	cret
.globl	_wait
.even
_wait:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_f=di
|NREG	1
|	_p=si
sub	di,di
L107:mov	si,#_proc
br	L108
L20028:mov	bx,120.+_u
mov	dx,*12.(bx)
cmp	dx,*14.(si)
bne	L110
inc	di
cmpb	(si),*5.
bne	L112
mov	dx,*12.(si)
mov	124.+_u,dx
mov	dx,*16.(si)
mov	126.+_u,dx
mov	ax,*20.(si)
mov	dx,*18.(si)
add	316.+_u,ax
adc	314.+_u,dx
mov	ax,*24.(si)
mov	dx,*22.(si)
add	320.+_u,ax
adc	318.+_u,dx
mov	*12.(si),*0.
mov	*14.(si),*0.
mov	*10.(si),*0.
mov	*6.(si),*0.
movb	*1.(si),*0.
mov	*24.(si),*0.
call	_spl7
mov	di,ax
movb	(si),*0.
mov	dx,_Nproc
mov	cx,*5.
sal	dx,cl
add	dx,#-32.+_proc
cmp	dx,si
je	L114
j	L113
L20026:cmpb	(si),*0
jne	L115
dec	_Nproc
sub	si,*32.
L114:cmp	si,#_proc
ja	L20026
L115:mov	dx,_Nproc
mov	cx,*5.
sal	dx,cl
add	dx,#_proc
mov	_Nproca,dx
L113:push	di
call	_splx
add	sp,*2.
j	L106
L112:cmpb	(si),*6.
jne	L110
movb	dx,*1.(si)
and	dx,*32.
jne	L110
orb	*1.(si),*32.
mov	dx,*12.(si)
mov	124.+_u,dx
push	si
call	_fsig
add	sp,*2.
mov	cx,*8.
sal	ax,cl
or	ax,*127.
mov	126.+_u,ax
j	L106
L110:add	si,*32.
L108:cmp	si,_Nproca
blo	L20028
cmp	di,*0
je	L118
mov	dx,*30.
push	dx
push	120.+_u
call	_sleep
add	sp,*4.
br	L107
L118:movb	111.+_u,*10.
|NREG	3
L106:|RTYP	0
br	cret
.globl	_fork
.even
_fork:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*6.
|NREG	2
|	_p1=di
|NREG	1
|	_p2=si
|	_a=-6.
|	_psp=-8.
|	_pn=-10.
cmp	_Bigswap,*0
je	L122
mov	dx,#128.
push	dx
mov	dx,#_swapmap
push	dx
call	_malloc
add	sp,*4.
mov	*-6.(bp),ax
test	ax,ax
jne	L123
movb	111.+_u,*12.
br	L124
L123:push	*-6.(bp)
mov	dx,#128.
push	dx
mov	dx,#_swapmap
push	dx
call	_mfree
add	sp,*6.
L122:mov	*-6.(bp),*0.
sub	si,si
mov	di,#_proc
j	L125
L20030:cmpb	(di),*0
jne	L128
cmp	si,*0
jne	L128
mov	si,di
j	L127
L128:mov	dx,112.+_u
cmp	*8.(di),dx
jne	L127
cmpb	(di),*0
je	L127
inc	*-6.(bp)
L127:add	di,*32.
L125:cmp	di,_Nproca
jbe	L20030
cmp	si,*0
je	L10013
cmp	112.+_u,*0
je	L131
mov	dx,16.+_v
mov	cx,*5.
sal	dx,cl
add	dx,#-32.+_proc
cmp	dx,si
je	L10013
mov	dx,26.+_v
cmp	*-6.(bp),dx
jle	L131
L10013:movb	111.+_u,*11.
j	L121
L131:mov	di,120.+_u
call	_newproc
test	ax,ax
je	L132
mov	dx,*12.(di)
mov	124.+_u,dx
mov	126.+_u,*0.
mov	ax,2.+_time
mov	dx,_time
mov	390.+_u,ax
mov	388.+_u,dx
sub	ax,ax
cwd
mov	320.+_u,ax
mov	318.+_u,dx
cwd
mov	312.+_u,ax
mov	310.+_u,dx
cwd
mov	316.+_u,ax
mov	314.+_u,dx
cwd
mov	308.+_u,ax
mov	306.+_u,dx
call	_sureg
j	L121
L132:mov	dx,*12.(si)
mov	124.+_u,dx
L124:mov	126.+_u,*1.
|NREG	3
L121:|RTYP	0
br	cret
.data
