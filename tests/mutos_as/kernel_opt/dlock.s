.globl	_dl_tab
.data
_dl_tab:.word	/0,/0
.blkb	4.
.even
.blkb	112.
.globl	_dl_serv
.data
.data
_dl_serv:.word	/0,/0
.blkb	12.
.even
.globl	_dl_dev_
.data
.data
_dl_dev_:/0
.globl	_dl_busy
.data
.data
_dl_busy:/ffff
.globl	_dl_pend
.data
.data
_dl_pend:/0
.globl	_dl_slee
.text
.even
_dl_slee:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_wchan=di
call	_spl0
mov	si,*1.
push	si
push	di
call	_sleep
add	sp,*4.
cmp	_dl_dev_,*0
jne	L4
movb	111.+_u,*-4.
mov	si,*1.
L20000:mov	ax,si
|RTYP	0
br	cret
L4:call	_spl7
sub	si,si
j	L20000
.globl	_dl_open
.even
_dl_open:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
cmp	116.+_u,*0
jne	L7
cmp	_dl_dev_,*0
jne	L7
sub	di,di
mov	_dl_pend,di
mov	_dl_busy,di
mov	di,120.+_u
mov	di,*12.(di)
mov	_dl_dev_,di
L7:|RTYP	0
br	cret
.globl	_dl_abor
.even
_dl_abor:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_pid=di
sub	sp,*2.
|NREG	1
|	_dt=si
|	_ruid=-6.
mov	dx,116.+_u
mov	*-6.(bp),dx
cmp	dx,*9.
je	L10000
cmp	dx,*0
jne	L12
L10000:cmp	di,*0
je	L13
sub	dx,dx
push	dx
push	di
call	_dl_slot
add	sp,*4.
mov	si,ax
test	ax,ax
je	L12
orb	*7.(si),#128.
push	si
call	_dl_rm_a
add	sp,*2.
L12:call	_spl0
|NREG	2
|RTYP	0
br	cret
L13:sub	dx,dx
mov	_dl_pend,dx
mov	_dl_dev_,dx
mov	_dl_busy,*-1.
mov	dx,#_dl_serv
push	dx
call	_wakeup
add	sp,*2.
mov	dx,#_dl_pend
push	dx
call	_wakeup
add	sp,*2.
mov	si,#112.+_dl_tab
L15:cmp	si,#_dl_tab
jb	L12
push	si
call	_dl_rm_o
add	sp,*2.
push	si
call	_wakeup
add	sp,*2.
sub	si,*8.
j	L15
.globl	_dl_read
.even
_dl_read:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
|	_i=di
|NREG	1
|	_server=si
mov	di,130.+_u
cmp	di,*16.
je	L21
movb	111.+_u,*-3.
j	L20
L21:call	_spl7
cmp	_dl_busy,*0
jne	L22
mov	dx,#_dl_serv
push	dx
call	_dl_slee
add	sp,*2.
L22:cmp	_dl_busy,*0
jg	L23
call	_spl0
j	L20
L23:mov	si,#_dl_serv
j	L24
L20002:movb	ax,(si)
cbw
push	ax
inc	si
call	_passc
add	sp,*2.
dec	di
L24:cmp	di,*0
jne	L20002
mov	_dl_busy,*0.
cmp	_dl_pend,*0
je	L27
mov	dx,#_dl_pend
push	dx
call	_wakeup
add	sp,*2.
L27:call	_spl0
|NREG	3
L20:|RTYP	0
br	cret
.globl	_dl_requ
.even
_dl_requ:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_req=di
sub	sp,*2.
|NREG	1
|	_p=si
|	_i=-6.
inc	_dl_pend
L31:cmp	_dl_busy,*0
je	L32
mov	dx,#_dl_pend
push	dx
call	_dl_slee
add	sp,*2.
test	ax,ax
je	L31
call	_spl0
mov	dx,*1.
j	L20005
L32:mov	si,#_dl_serv
mov	*-6.(bp),*16.
L20004:movb	dx,(di)
movb	(si),dx
inc	si
inc	di
dec	*-6.(bp)
cmp	*-6.(bp),*0
jne	L20004
dec	_dl_pend
inc	_dl_busy
mov	dx,#_dl_serv
push	dx
call	_wakeup
add	sp,*2.
sub	dx,dx
L20005:mov	ax,dx
|RTYP	0
br	cret
.globl	_dl_writ
.even
_dl_writ:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*28.
|NREG	2
|	_i=di
|NREG	1
|	_server=si
|	_dt=-6.
|	_table=-8.
|	_p=-10.
|	_act=-12.
|	_req_buf=-28.
|	_res_buf=-32.
mov	di,130.+_u
mov	bx,120.+_u
mov	dx,*12.(bx)
mov	*-12.(bp),dx
cmp	di,*4.
jne	L40
cmp	_dl_dev_,dx
jne	L40
call	_spl7
movb	111.+_u,*0.
lea	dx,*-32.(bp)
mov	*-10.(bp),dx
j	L41
L20007:push	*-10.(bp)
call	_cpass
pop	bx
movb	(bx),ax
inc	*-10.(bp)
dec	di
L41:cmp	di,*0
jne	L20007
sub	dx,dx
push	dx
push	*-32.(bp)
call	_dl_slot
add	sp,*4.
mov	*-6.(bp),ax
test	ax,ax
je	L44
cmp	*-30.(bp),*0
je	L45
mov	bx,ax
orb	*7.(bx),*64.
L45:push	*-6.(bp)
call	_wakeup
add	sp,*2.
L44:call	_spl0
br	L39
L40:cmp	di,*14.
je	L46
movb	111.+_u,*-3.
br	L39
L46:call	_spl7
lea	si,*-28.(bp)
mov	*-10.(bp),si
j	L47
L20009:push	*-10.(bp)
call	_cpass
pop	bx
movb	(bx),ax
inc	*-10.(bp)
dec	di
L47:cmp	di,*0
jne	L20009
cmpb	*12.(si),*4.
jne	L50
push	*2.(si)
call	_dl_abor
j	L20012
L20015:push	si
call	_dl_requ
L20012:add	sp,*2.
br	L39
L50:cmpb	*12.(si),*5.
je	L20015
mov	dx,*-12.(bp)
mov	*14.(si),dx
cmp	_dl_dev_,*0
beq	L10001
push	si
push	*-12.(bp)
call	_dl_slot
add	sp,*4.
mov	*-6.(bp),ax
test	ax,ax
beq	L10001
movb	dx,*13.(si)
and	dx,*1.
jne	L20017
movb	ax,*12.(si)
cbw
mov	*-12.(bp),ax
movb	dx,*13.(si)
mov	di,dx
and	di,*24.
cmp	*-12.(bp),*2.
jne	L58
mov	bx,*-6.(bp)
movb	dx,*6.(bx)
testb	dx,dx
jne	L59
movb	111.+_u,*-2.
br	L53
L20017:mov	bx,*-6.(bp)
cmpb	*6.(bx),*8.
beq	L53
push	si
call	_dl_requ
add	sp,*2.
test	ax,ax
bne	L39
push	*-6.(bp)
call	_dl_slee
add	sp,*2.
test	ax,ax
bne	L39
mov	bx,*-6.(bp)
movb	dx,*7.(bx)
and	dx,*64.
je	L56
mov	bx,*-6.(bp)
andb	*7.(bx),*-65.
movb	111.+_u,*-1.
L56:mov	bx,*-6.(bp)
movb	dx,*7.(bx)
and	dx,#128.
je	L53
mov	bx,*-6.(bp)
andb	*7.(bx),#-129.
L10001:movb	111.+_u,*-4.
j	L53
L58:cmp	*-12.(bp),*3.
jne	L63
push	*-6.(bp)
call	_dl_rm_a
j	L20013
L20019:movb	111.+_u,*-1.
L59:push	*-6.(bp)
call	_dl_rm_o
L20013:add	sp,*2.
j	L53
L10002:mov	dx,*1.
push	dx
j	L10003
L20011:cmp	*-12.(bp),*1.
je	L20019
mov	bx,*-8.(bp)
orb	*7.(bx),*32.
push	*-8.(bp)
call	_dl_slee
add	sp,*2.
test	ax,ax
jne	L39
L63:cmp	di,*8.
je	L10002
sub	dx,dx
push	dx
L10003:push	*-6.(bp)
call	_dl_db_l
add	sp,*4.
mov	*-8.(bp),ax
test	ax,ax
jne	L20011
mov	bx,*-6.(bp)
mov	ax,di
movb	*6.(bx),ax
L53:call	_spl0
|NREG	3
L39:|RTYP	0
br	cret
.globl	_dl_rm_a
.even
_dl_rm_a:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dt=di
sub	sp,*16.
|NREG	1
|	_server=si
|	_req_buf=-20.
cmp	_dl_dev_,*0
jne	L70
movb	111.+_u,*-4.
j	L69
L70:cmpb	*6.(di),*8.
je	L71
call	_spl7
lea	si,*-20.(bp)
mov	bx,si
movb	*12.(bx),*3.
mov	dx,*4.(di)
mov	*14.(si),dx
push	si
call	_dl_requ
add	sp,*2.
test	ax,ax
jne	L69
call	_spl0
L71:push	di
call	_dl_rm_o
add	sp,*2.
|NREG	2
L69:|RTYP	0
br	cret
.globl	_dl_rm_o
.even
_dl_rm_o:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dt=di
sub	ax,ax
cwd
mov	si,dx
mov	dx,ax
mov	*2.(di),dx
mov	(di),si
mov	*4.(di),ax
movb	*6.(di),*0.
movb	dx,*7.(di)
and	dx,*32.
je	L75
push	di
call	_wakeup
add	sp,*2.
movb	*7.(di),*0.
L75:cmp	_dl_dev_,*0
jne	L74
movb	111.+_u,*-4.
L74:|RTYP	0
br	cret
.globl	_dl_slot
.even
_dl_slot:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_pid=di
|NREG	1
mov	si,*6.(bp)
|	_enter=si
sub	sp,*2.
|	_dt=-6.
mov	*-6.(bp),#112.+_dl_tab
j	L80
L20021:mov	bx,*-6.(bp)
cmp	*4.(bx),di
je	L83
sub	*-6.(bp),*8.
L80:cmp	*-6.(bp),#_dl_tab
jae	L20021
cmp	si,*0
je	L83
mov	*-6.(bp),#112.+_dl_tab
L85:cmp	*-6.(bp),#_dl_tab
jb	L83
mov	bx,*-6.(bp)
mov	dx,*4.(bx)
test	dx,dx
jne	L87
mov	ax,*2.(si)
mov	dx,(si)
mov	+2(bx),ax
mov	(bx),dx
mov	bx,*-6.(bp)
mov	*4.(bx),di
mov	bx,*-6.(bp)
movb	*6.(bx),*0.
mov	bx,*-6.(bp)
movb	*7.(bx),*0.
L83:cmp	*-6.(bp),#_dl_tab
jae	L89
mov	*-6.(bp),*0.
L89:mov	ax,*-6.(bp)
|RTYP	12
br	cret
L87:sub	*-6.(bp),*8.
j	L85
.globl	_dl_db_l
.even
_dl_db_l:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_dt=di
|NREG	1
mov	si,*6.(bp)
|	_excl_req=si
sub	sp,*2.
|	_t=-6.
mov	*-6.(bp),#112.+_dl_tab
j	L93
L20023:mov	bx,*-6.(bp)
mov	dx,*4.(bx)
test	dx,dx
je	L95
movb	dx,*6.(bx)
testb	dx,dx
je	L95
mov	bx,*-6.(bp)
mov	ax,*2(bx)
mov	dx,(bx)
cmp	dx,(di)
jne	L95
cmp	ax,*2.(di)
jne	L95
cmpb	*6.(bx),*8.
je	L10004
cmp	si,*0
je	L95
L10004:mov	dx,*-6.(bp)
L20024:mov	ax,dx
|RTYP	12
br	cret
L95:sub	*-6.(bp),*8.
L93:cmp	*-6.(bp),#_dl_tab
jae	L20023
sub	dx,dx
j	L20024
.data
