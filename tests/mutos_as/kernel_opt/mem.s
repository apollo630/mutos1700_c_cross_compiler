.globl	_mmread
.text
.even
_mmread:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*8.
|NREG	2
|	_y=di
|NREG	1
|	_x=si
|	_a=-6.
|	_b=-8.
|	_c=-10.
|	_ds=-12.
mov	di,*4.(bp)
and	di,#255.
cmp	di,*2.
beq	L3
cmp	di,*3.
jne	L4
mov	si,134.+_u
L5:push	si
call	_inb
add	sp,*2.
mov	di,ax
push	di
call	_passc
add	sp,*2.
test	ax,ax
jge	L5
j	L20003
L20005:mov	si,134.+_u
L11:push	si
call	_in
add	sp,*2.
mov	di,ax
mov	dx,di
mov	cx,*8.
sar	dx,cl
push	dx
call	_passc
add	sp,*2.
push	di
call	_passc
add	sp,*2.
test	ax,ax
jge	L11
L20003:mov	ax,si
cwd
mov	134.+_u,ax
mov	132.+_u,dx
br	L3
L4:cmp	di,*4.
je	L20005
mov	*-12.(bp),*0.
mov	ax,134.+_u
mov	dx,132.+_u
and	ax,*0.
and	dx,*-1.
test	dx,dx
jne	L10000
test	ax,ax
je	L15
L10000:mov	ax,#2048.
cwd
push	ax
push	dx
push	134.+_u
push	132.+_u
call	lrem
add	sp,*8.
mov	si,ax
mov	ax,#2048.
cwd
push	ax
push	dx
push	134.+_u
push	132.+_u
call	ldiv
add	sp,*8.
mov	*-12.(bp),ax
mov	dx,ax
mov	cx,*7.
sal	dx,cl
mov	*-12.(bp),dx
mov	ax,si
cwd
mov	134.+_u,ax
mov	132.+_u,dx
L15:cmp	*-12.(bp),*0
je	L16
call	_spl7
mov	*-10.(bp),ax
push	134.+_u
push	*-12.(bp)
call	_getbyte
add	sp,*4.
mov	di,ax
push	*-10.(bp)
call	_splx
add	sp,*2.
j	L17
L16:mov	bx,134.+_u
mov	*-6.(bp),bx
movb	ax,(bx)
cbw
mov	di,ax
L17:cmpb	111.+_u,*0
jne	L3
push	di
call	_passc
add	sp,*2.
test	ax,ax
jge	L15
|NREG	3
L3:|RTYP	0
br	cret
.globl	_mmwrite
.even
_mmwrite:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_dev=4.
sub	sp,*10.
|NREG	2
|	_y=di
|NREG	1
|	_x=si
|	_a=-6.
|	_b=-8.
|	_c=-10.
|	_d=-12.
|	_ds=-14.
mov	di,*4.(bp)
and	di,#255.
cmp	di,*2.
jne	L21
mov	130.+_u,*0.
br	L20
L21:cmp	di,*3.
jne	L22
mov	si,134.+_u
j	L23
L20007:cmpb	111.+_u,*0
jne	L24
push	di
push	si
call	_outb
add	sp,*4.
L23:call	_cpass
mov	di,ax
test	ax,ax
jge	L20007
j	L24
L20017:mov	si,134.+_u
j	L29
L20009:cmpb	111.+_u,*0
jne	L24
call	_cpass
mov	*-12.(bp),ax
test	ax,ax
jl	L24
cmpb	111.+_u,*0
jne	L24
subb	ah,ah
mov	dx,di
mov	cx,*8.
sal	dx,cl
or	dx,ax
push	dx
push	si
call	_out
add	sp,*4.
L29:call	_cpass
mov	di,ax
test	ax,ax
jge	L20009
L24:mov	ax,si
cwd
mov	134.+_u,ax
mov	132.+_u,dx
br	L20
L22:cmp	di,*4.
je	L20017
mov	*-14.(bp),*0.
mov	ax,134.+_u
mov	dx,132.+_u
and	ax,*0.
and	dx,*-1.
test	dx,dx
jne	L10001
test	ax,ax
beq	L38
L10001:mov	ax,#2048.
cwd
push	ax
push	dx
push	134.+_u
push	132.+_u
call	lrem
add	sp,*8.
mov	si,ax
mov	ax,#2048.
cwd
push	ax
push	dx
push	134.+_u
push	132.+_u
call	ldiv
add	sp,*8.
mov	*-14.(bp),ax
mov	dx,ax
mov	cx,*7.
sal	dx,cl
mov	*-14.(bp),dx
mov	ax,si
cwd
mov	134.+_u,ax
mov	132.+_u,dx
j	L38
L20010:call	_spl7
mov	*-10.(bp),ax
push	di
mov	dx,134.+_u
dec	dx
push	dx
push	*-14.(bp)
call	_setbyte
add	sp,*6.
push	*-10.(bp)
call	_splx
add	sp,*2.
j	L38
L20012:cmpb	111.+_u,*0
jne	L20
cmp	*-14.(bp),*0
jne	L20010
mov	bx,134.+_u
dec	bx
mov	*-6.(bp),bx
mov	ax,di
movb	(bx),ax
L38:call	_cpass
mov	di,ax
test	ax,ax
jge	L20012
|NREG	3
L20:|RTYP	0
br	cret
.data
