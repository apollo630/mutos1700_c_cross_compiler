.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_io_info,222
.comm	_cfreeli,2
.comm	_cbad,2
.globl	_getc
.text
.even
_getc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_p=di
sub	sp,*4.
|NREG	1
|	_bp=si
|	_c=-6.
|	_s=-8.
call	_spl6
mov	*-8.(bp),ax
cmp	(di),*0
jg	L4
mov	*-6.(bp),*-1.
mov	(di),*0.
sub	dx,dx
mov	*4.(di),dx
mov	*2.(di),dx
j	L5
L4:mov	bx,*2.(di)
movb	dx,(bx)
inc	*2.(di)
subb	dh,dh
mov	*-6.(bp),dx
dec	(di)
jg	L6
mov	si,*2.(di)
dec	si
and	si,*-8.
mov	*2.(di),*0.
mov	*4.(di),*0.
j	L20002
L6:test	*2.(di),*7.
jne	L5
mov	si,*2.(di)
sub	si,*8.
mov	dx,(si)
add	dx,*2.
mov	*2.(di),dx
L20002:mov	dx,_cfreeli
mov	(si),dx
mov	_cfreeli,si
L5:push	*-8.(bp)
call	_splx
add	sp,*2.
mov	ax,*-6.(bp)
|RTYP	0
br	cret
.globl	_putc
.even
_putc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_c=4.
|NREG	2
mov	di,*6.(bp)
|	_p=di
sub	sp,*4.
|NREG	1
|	_bp=si
|	_cp=-6.
|	_s=-8.
call	_spl6
mov	*-8.(bp),ax
mov	dx,*4.(di)
mov	*-6.(bp),dx
test	dx,dx
je	L10000
cmp	(di),*0
jge	L12
L10000:mov	dx,_cfreeli
mov	si,dx
test	dx,dx
jne	L13
push	*-8.(bp)
L20006:call	_splx
add	sp,*2.
mov	dx,*-1.
j	L20003
L13:mov	dx,(si)
mov	_cfreeli,dx
mov	(si),*0.
mov	dx,si
add	dx,*2.
mov	*-6.(bp),dx
mov	*2.(di),dx
j	L14
L12:test	*-6.(bp),*7.
jne	L14
mov	si,*-6.(bp)
sub	si,*8.
mov	dx,_cfreeli
mov	(si),dx
test	dx,dx
jne	L16
push	*-8.(bp)
j	L20006
L16:mov	si,(si)
mov	dx,(si)
mov	_cfreeli,dx
mov	(si),*0.
mov	dx,si
add	dx,*2.
mov	*-6.(bp),dx
L14:mov	bx,*-6.(bp)
mov	dx,*4.(bp)
movb	(bx),dx
inc	*-6.(bp)
inc	(di)
mov	dx,*-6.(bp)
mov	*4.(di),dx
push	*-8.(bp)
call	_splx
add	sp,*2.
sub	dx,dx
L20003:mov	ax,dx
|RTYP	0
br	cret
.globl	_cinit
.even
_cinit:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
sub	sp,*2.
|NREG	2
|	_ccp=di
|NREG	1
|	_cp=si
|	_cdp=-6.
mov	di,#_cfree
add	di,*7.
and	di,*-8.
mov	si,di
j	L20
L20008:mov	dx,_cfreeli
mov	(si),dx
mov	_cfreeli,si
add	si,*8.
L20:mov	dx,24.+_v
mov	cx,*3.
sal	dx,cl
add	dx,#-8.+_cfree
cmp	dx,si
jae	L20008
|NREG	3
|RTYP	0
br	cret
.data
