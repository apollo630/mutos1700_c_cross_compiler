.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.globl	_lblock
.data
_lblock:/0
.globl	_clock
.text
.even
_clock:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_pc=4.
|	_ps=6.
|	_vec=8.
sub	sp,*2.
|NREG	2
|	_p1=di
|NREG	1
|	_p2=si
|	_s=-6.
cmp	_clkflg,*0
bne	L3
call	_clkintr
cmp	4.+_callout,*0
beq	L4
mov	si,#_callout
j	L5
L20001:cmp	*4.(si),*0
je	L6
add	si,*6.
L5:cmp	(si),*0
jle	L20001
L6:dec	(si)
test	*6.(bp),#-256.
jne	L4
cmp	_lblock,*0
jne	L4
mov	dx,*1.
push	dx
call	_tasktim
add	sp,*2.
call	_spl5
mov	*-6.(bp),ax
cmp	_callout,*0
jg	L7
mov	di,#_callout
j	L8
L20003:cmp	(di),*0
jg	L9
push	*2.(di)
call	@*4.(di)
add	sp,*2.
add	di,*6.
L8:cmp	*4.(di),*0
jne	L20003
L9:mov	si,#_callout
j	L10
L20005:mov	dx,(di)
mov	(si),dx
mov	dx,*2.(di)
mov	*2.(si),dx
add	di,*6.
add	si,*6.
L10:mov	dx,*4.(di)
mov	*4.(si),dx
test	dx,dx
jne	L20005
L7:push	*-6.(bp)
call	_splx
add	sp,*2.
|NREG	3
L4:test	*8.(bp),#-32768.
jne	L12
add	308.+_u,*1.
adc	306.+_u,*0
cmp	330.+_u,*0
je	L14
mov	di,*1.
push	di
mov	di,#324.+_u
push	di
push	*4.(bp)
call	_addupc
add	sp,*6.
j	L14
L12:add	312.+_u,*1.
adc	310.+_u,*0
L14:|NREG	2
|	_pp=di
|NREG	1
|	_a=si
mov	di,120.+_u
incb	*4.(di)
jne	L15
decb	*4.(di)
L15:incb	*4.(di)
jne	L16
decb	*4.(di)
L16:inc	_lbolt
cmp	_lbolt,*20.
blt	L17
test	*6.(bp),#-256.
jne	L10000
cmp	_lblock,*0
je	L18
L10000:sub	dx,dx
L20008:mov	ax,dx
L3:|RTYP	0
br	cret
L18:inc	_lblock
sub	_lbolt,*20.
add	2.+_time,*1.
adc	_time,*0
mov	dx,*1.
push	dx
call	_tasktim
add	sp,*2.
incb	_runrun
mov	dx,#_lbolt
push	dx
call	_wakeup
add	sp,*2.
mov	di,#_proc
j	L19
L20007:cmpb	(di),*0
je	L21
cmpb	(di),*5.
jge	L21
cmpb	*3.(di),*127.
je	L23
incb	*3.(di)
L23:cmp	*30.(di),*0
je	L24
dec	*30.(di)
jne	L24
mov	dx,*14.
push	dx
push	di
call	_psignal
add	sp,*4.
L24:movb	ax,*5.(di)
cbw
mov	bx,ax
movb	ax,*4.(di)
subb	ah,ah
mov	cx,*3.
sal	ax,cl
cwd
mov	cx,*10.
idiv	cx
add	ax,bx
mov	si,ax
add	si,*-20.
cmp	si,*0
jge	L26
sub	si,si
L26:cmp	si,#255.
jle	L27
mov	si,#255.
L27:mov	ax,si
movb	*4.(di),ax
cmpb	*2.(di),*50.
jl	L21
push	di
call	_setpri
add	sp,*2.
L21:add	di,*32.
L19:cmp	di,_Nproca
blo	L20007
cmpb	_runin,*0
je	L29
movb	_runin,*0.
mov	dx,#_runin
push	dx
call	_wakeup
add	sp,*2.
L29:mov	_lblock,*0.
mov	dx,*1.
br	L20008
L17:|NREG	3
sub	di,di
mov	ax,di
br	L3
.globl	_timeout
.even
_timeout:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_fun=4.
|	_arg=6.
|	_tim=8.
sub	sp,*4.
|NREG	2
|	_p1=di
|NREG	1
|	_p2=si
|	_t=-6.
|	_s=-8.
mov	dx,*8.(bp)
mov	*-6.(bp),dx
mov	di,#_callout
call	_spl7
mov	*-8.(bp),ax
j	L33
L20010:mov	dx,*-6.(bp)
cmp	(di),dx
jg	L34
mov	dx,(di)
sub	*-6.(bp),dx
add	di,*6.
L33:cmp	*4.(di),*0
jne	L20010
L34:mov	ax,2.+_v
mov	cx,*6.
imul	cx
add	ax,#-6.+_callout
cmp	ax,di
ja	L35
mov	dx,#L36
push	dx
call	_panic
add	sp,*2.
L35:mov	dx,*-6.(bp)
sub	(di),dx
mov	si,di
j	L37
L20012:add	si,*6.
L37:cmp	*4.(si),*0
jne	L20012
j	L39
L20014:mov	dx,(si)
mov	*6.(si),dx
mov	dx,*4.(si)
mov	*10.(si),dx
mov	dx,*2.(si)
mov	*8.(si),dx
sub	si,*6.
L39:cmp	si,di
jae	L20014
mov	dx,*-6.(bp)
mov	(di),dx
mov	dx,*4.(bp)
mov	*4.(di),dx
mov	dx,*6.(bp)
mov	*2.(di),dx
push	*-8.(bp)
call	_splx
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.globl	_timecan
.even
_timecan:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_fun=4.
|	_arg=6.
sub	sp,*4.
|	_t=-6.
|NREG	2
|	_p1=di
|NREG	1
|	_p2=si
|	_s=-8.
mov	*-6.(bp),*0.
mov	di,#_callout
call	_spl7
mov	*-8.(bp),ax
j	L44
L20016:mov	dx,*4.(si)
mov	*4.(di),dx
mov	dx,*2.(si)
mov	*2.(di),dx
mov	dx,(si)
mov	(di),dx
L47:add	di,*6.
j	L44
L20018:mov	si,di
add	si,*6.
cmp	*-6.(bp),*0
jne	L20016
mov	dx,*4.(bp)
cmp	*4.(di),dx
jne	L47
mov	dx,*6.(bp)
cmp	*2.(di),dx
jne	L47
inc	*-6.(bp)
mov	dx,(di)
add	(si),dx
L44:cmp	*4.(di),*0
jne	L20018
push	*-8.(bp)
call	_splx
add	sp,*2.
|NREG	3
|RTYP	0
br	cret
.data
L36:.byte	/54,/69,/6d,/65,/6f,/75,/74,/20,/74
.byte	/61,/62,/6c,/65,/20
.byte	/6f,/76,/65,/72,/66,/6c,/6f,/77,/0
