.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.globl	_malloc
.text
.even
_malloc:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_mp=4.
|	_size=6.
|NREG	2
|	_a=di
|NREG	1
|	_bp=si
mov	si,*4.(bp)
j	L4
L20001:mov	dx,*6.(bp)
cmp	(si),dx
jl	L6
mov	di,*2.(si)
add	*2.(si),dx
mov	dx,*6.(bp)
sub	(si),dx
jne	L8
L11:add	si,*4.
mov	bx,si
mov	dx,*2.(si)
mov	*2.+-4.(bx),dx
mov	dx,(si)
mov	*-4.(si),dx
test	dx,dx
jne	L11
L8:mov	dx,di
j	L20002
L6:add	si,*4.
L4:cmp	(si),*0
jne	L20001
sub	dx,dx
L20002:mov	ax,dx
|RTYP	0
br	cret
.globl	_mfree
.even
_mfree:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_mp=4.
|	_size=6.
|NREG	2
mov	di,*8.(bp)
|	_a=di
sub	sp,*2.
|NREG	1
|	_bp=si
|	_t=-6.
mov	si,*4.(bp)
j	L15
L20004:cmp	(si),*0
je	L16
add	si,*4.
L15:cmp	*2.(si),di
jbe	L20004
L16:cmp	si,*4.(bp)
jbe	L18
mov	bx,si
add	bx,*-4.
mov	dx,*2.(bx)
add	dx,*-4.(si)
cmp	dx,di
jne	L18
mov	dx,*6.(bp)
add	*-4.(si),dx
mov	dx,di
add	dx,*6.(bp)
cmp	dx,*2.(si)
jne	L22
mov	dx,(si)
add	*-4.(si),dx
L20:cmp	(si),*0
je	L22
add	si,*4.
mov	dx,*2.(si)
mov	*2.+-4.(si),dx
mov	dx,(si)
mov	*-4.(si),dx
j	L20
L18:mov	dx,di
add	dx,*6.(bp)
cmp	dx,*2.(si)
jne	L23
cmp	(si),*0
je	L23
mov	dx,*6.(bp)
sub	*2.(si),dx
mov	dx,*6.(bp)
add	(si),dx
j	L22
L23:cmp	*6.(bp),*0
je	L22
L28:mov	dx,*2.(si)
mov	*-6.(bp),dx
mov	*2.(si),di
mov	di,*-6.(bp)
mov	dx,(si)
mov	*-6.(bp),dx
mov	dx,*6.(bp)
mov	(si),dx
add	si,*4.
mov	dx,*-6.(bp)
mov	*6.(bp),dx
test	dx,dx
jne	L28
L22:|NREG	2
|RTYP	0
br	cret
.data
