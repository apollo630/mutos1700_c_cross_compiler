.comm	_canonb,256
.comm	_rootdir,2
.comm	_runq,2
.comm	_Hogproc,2
.comm	_inode86,2
.comm	_dbreak,2
.comm	_brkseg,2
.comm	_brkoff,2
.comm	_cputype,2
.comm	_lbolt,2
.comm	_time,4
.comm	_nblkdev,2
.comm	_nchrdev,2
.comm	_mpid,2
.comm	_runin,2
.comm	_runout,2
.comm	_runrun,2
.comm	_runtxt,2
.comm	_curpri,2
.comm	_maxmem,2
.comm	_lks,2
.comm	_swplo,4
.comm	_nswap,2
.comm	_updlock,2
.comm	_rablock,4
.comm	_msgbuf,1024
.comm	_rootdev,2
.comm	_swapdev,2
.comm	_pipedev,2
.comm	_acctp,2
.comm	_Nproca,2
.comm	_Nproc,2
.comm	_Timezon,2
.comm	_Dstflag,2
.comm	_dk_busy,2
.comm	_dk_time,128
.comm	_dk_numb,12
.comm	_dk_wds,12
.comm	_tk_nin,4
.comm	_tk_nout,4
.comm	_version,2
.comm	_systype,2
.comm	_mpxip,2
.comm	_io_info,222
.globl	_readi
.text
.even
_readi:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
sub	sp,*20.
|	_bp=-6.
|	_dev=-8.
|	_lbn=-12.
|	_bn=-16.
|	_diff=-20.
|NREG	1
|	_on=si
|	_n=-22.
|	_type=-24.
cmp	130.+_u,*0
beq	L3
cmp	132.+_u,*0
jg	L4
jl	L10000
cmp	134.+_u,*0.
jae	L4
L10000:movb	111.+_u,*22.
br	L3
L4:orb	(di),*4.
mov	dx,*22.(di)
mov	*-8.(bp),dx
mov	dx,*6.(di)
and	dx,#-4096.
mov	*-24.(bp),dx
cmp	dx,#8192.
je	L10001
cmp	dx,#12288.
jne	L8
L10001:push	*-8.(bp)
mov	ax,*-8.(bp)
mov	cx,*8.
sar	ax,cl
mov	cx,*14.
imul	cx
mov	bx,ax
call	@4.+_cdevsw(bx)
add	sp,*2.
br	L3
L8:mov	ax,134.+_u
mov	dx,132.+_u
mov	cx,*10.
sar	dx,*1
rcr	ax,*1
loop	.-4
mov	*-14.(bp),ax
mov	*-16.(bp),dx
mov	*-10.(bp),ax
mov	*-12.(bp),dx
mov	si,134.+_u
and	si,#1023.
push	130.+_u
mov	dx,#1024.
sub	dx,si
push	dx
call	_min
add	sp,*4.
mov	*-22.(bp),ax
cmp	*-24.(bp),#24576.
je	L9
cmp	*-24.(bp),#28672.
je	L9
mov	ax,*16.(di)
mov	dx,*14.(di)
sub	ax,134.+_u
sbb	dx,132.+_u
mov	*-18.(bp),ax
mov	*-20.(bp),dx
cmp	dx,*0
blt	L3
jg	L10002
test	ax,ax
blos	L3
L10002:mov	ax,*-22.(bp)
cwd
cmp	dx,*-20.(bp)
jl	L10
jg	L10003
cmp	ax,*-18.(bp)
jbe	L10
L10003:mov	dx,*-18.(bp)
mov	*-22.(bp),dx
L10:mov	dx,*1.
push	dx
push	*-14.(bp)
push	*-16.(bp)
push	di
call	_bmap
add	sp,*8.
mov	*-14.(bp),ax
mov	*-16.(bp),dx
cmpb	111.+_u,*0
bne	L3
mov	dx,*2.(di)
mov	*-8.(bp),dx
j	L11
L9:mov	ax,*1.
cwd
mov	cx,ax
mov	bx,dx
mov	ax,*-14.(bp)
mov	dx,*-16.(bp)
add	ax,cx
adc	dx,bx
mov	2.+_rablock,ax
mov	_rablock,dx
L11:cmp	*-16.(bp),*0
jg	L12
jl	L10004
cmp	*-14.(bp),*0.
jae	L12
L10004:call	_geteblk
mov	*-6.(bp),ax
push	ax
call	_clrbuf
add	sp,*2.
j	L13
L12:mov	ax,*1.
cwd
mov	cx,ax
mov	bx,dx
mov	ax,*74.(di)
mov	dx,*72.(di)
add	ax,cx
adc	dx,bx
cmp	dx,*-12.(bp)
jne	L14
cmp	ax,*-10.(bp)
jne	L14
push	2.+_rablock
push	_rablock
push	*-14.(bp)
push	*-16.(bp)
push	*-8.(bp)
call	_breada
add	sp,*10.
j	L20000
L14:push	*-14.(bp)
push	*-16.(bp)
push	*-8.(bp)
call	_bread
add	sp,*6.
L20000:mov	*-6.(bp),ax
L13:mov	ax,*-10.(bp)
mov	dx,*-12.(bp)
mov	*74.(di),ax
mov	*72.(di),dx
mov	bx,*-6.(bp)
mov	dx,#1024.
sub	dx,*22.(bx)
push	dx
push	*-22.(bp)
call	_min
add	sp,*4.
mov	*-22.(bp),ax
cmp	ax,*0
je	L16
mov	dx,*1.
push	dx
push	ax
mov	bx,*-6.(bp)
mov	dx,*14.(bx)
add	dx,si
push	dx
call	_iomove
add	sp,*6.
L16:push	*-6.(bp)
call	_brelse
add	sp,*2.
cmpb	111.+_u,*0
jne	L7
cmp	130.+_u,*0
je	L7
cmp	*-22.(bp),*0
bgt	L8
L7:|NREG	2
L3:|RTYP	0
br	cret
.globl	_writei
.even
_writei:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_ip=di
sub	sp,*12.
|	_bp=-6.
|	_dev=-8.
|	_bn=-12.
|NREG	1
|	_n=si
|	_on=-14.
|	_type=-16.
cmp	132.+_u,*0
jg	L20
jl	L10006
cmp	134.+_u,*0.
jae	L20
L10006:movb	111.+_u,*22.
br	L19
L20:mov	dx,*22.(di)
mov	*-8.(bp),dx
mov	dx,*6.(di)
and	dx,#-4096.
mov	*-16.(bp),dx
cmp	dx,#8192.
je	L10007
cmp	dx,#12288.
jne	L21
L10007:orb	(di),*66.
push	*-8.(bp)
mov	ax,*-8.(bp)
mov	cx,*8.
sar	ax,cl
mov	cx,*14.
imul	cx
mov	bx,ax
call	@6.+_cdevsw(bx)
add	sp,*2.
br	L19
L21:cmp	130.+_u,*0
beq	L19
L24:mov	ax,134.+_u
mov	dx,132.+_u
mov	cx,*10.
sar	dx,*1
rcr	ax,*1
loop	.-4
mov	*-10.(bp),ax
mov	*-12.(bp),dx
mov	dx,134.+_u
and	dx,#1023.
mov	*-14.(bp),dx
push	130.+_u
mov	dx,#1024.
sub	dx,*-14.(bp)
push	dx
call	_min
add	sp,*4.
mov	si,ax
cmp	*-16.(bp),#24576.
je	L25
cmp	*-16.(bp),#28672.
je	L25
sub	dx,dx
push	dx
push	*-10.(bp)
push	*-12.(bp)
push	di
call	_bmap
add	sp,*8.
mov	*-10.(bp),ax
mov	*-12.(bp),dx
cmp	dx,*0
blt	L19
jg	L10008
test	ax,ax
blo	L19
L10008:mov	dx,*2.(di)
mov	*-8.(bp),dx
L25:cmp	si,#1024.
jne	L26
push	*-10.(bp)
push	*-12.(bp)
push	*-8.(bp)
call	_getblk
j	L20002
L26:push	*-10.(bp)
push	*-12.(bp)
push	*-8.(bp)
call	_bread
L20002:add	sp,*6.
mov	*-6.(bp),ax
sub	dx,dx
push	dx
push	si
mov	bx,ax
mov	dx,*14.(bx)
add	dx,*-14.(bp)
push	dx
call	_iomove
add	sp,*6.
cmpb	111.+_u,*0
je	L28
push	*-6.(bp)
call	_brelse
j	L20003
L28:push	*-6.(bp)
call	_bdwrite
L20003:add	sp,*2.
mov	ax,134.+_u
mov	dx,132.+_u
cmp	dx,*14.(di)
jl	L30
jg	L10009
cmp	ax,*16.(di)
jbe	L30
L10009:cmp	*-16.(bp),#16384.
je	L10010
cmp	*-16.(bp),#-32768.
jne	L30
L10010:mov	ax,134.+_u
mov	dx,132.+_u
mov	*16.(di),ax
mov	*14.(di),dx
L30:orb	(di),*66.
cmpb	111.+_u,*0
jne	L23
cmp	130.+_u,*0
bne	L24
L23:|NREG	2
L19:|RTYP	0
br	cret
.globl	_max
.even
_max:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_a=4.
|	_b=6.
mov	di,*6.(bp)
cmp	*4.(bp),di
jbe	L34
mov	di,*4.(bp)
L20004:mov	ax,di
|RTYP	0
br	cret
L34:mov	di,*6.(bp)
j	L20004
.globl	_min
.even
_min:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|	_a=4.
|	_b=6.
mov	di,*6.(bp)
cmp	*4.(bp),di
jae	L38
mov	di,*4.(bp)
L20005:mov	ax,di
|RTYP	0
br	cret
L38:mov	di,*6.(bp)
j	L20005
.globl	_iomove
.even
_iomove:
push	bp
mov	bp,sp
push	di
push	si
|NREG	3
|NREG	2
mov	di,*4.(bp)
|	_cp=di
|NREG	1
mov	si,*6.(bp)
|	_n=si
|	_flag=8.
sub	sp,*2.
|	_t=-6.
cmp	si,*0
beq	L41
cmpb	110.+_u,*1.
je	L42
test	si,*1.
jne	L42
test	di,*1.
jne	L42
test	128.+_u,*1.
jne	L42
cmp	*8.(bp),*0
jne	L43
cmpb	110.+_u,*0
jne	L44
push	si
push	di
push	128.+_u
call	_copyin
j	L20007
L44:push	si
push	di
push	128.+_u
call	_copyiin
j	L20007
L43:cmpb	110.+_u,*0
jne	L47
push	si
push	128.+_u
push	di
call	_copyout
j	L20007
L47:push	si
push	128.+_u
push	di
call	_copyiou
L20007:add	sp,*6.
mov	*-6.(bp),ax
cmp	ax,*0
je	L49
movb	111.+_u,*14.
j	L41
L49:add	128.+_u,si
mov	ax,si
cwd
add	134.+_u,ax
adc	132.+_u,dx
sub	130.+_u,si
j	L41
L42:cmp	*8.(bp),*0
jne	L57
L53:call	_cpass
mov	*-6.(bp),ax
test	ax,ax
jl	L41
movb	(di),ax
inc	di
dec	si
jne	L53
j	L41
L57:movb	ax,(di)
cbw
push	ax
inc	di
call	_passc
add	sp,*2.
test	ax,ax
jl	L41
dec	si
jne	L57
L41:|RTYP	0
br	cret
.data
