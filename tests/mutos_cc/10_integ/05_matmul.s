.globl	_main
.text
.even
_main:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L1
L2:| _a=-12.
| _b=-20.
| _c=-28.
| _i=-30.
| _j=-32.
| _k=-34.
| _sum=-36.
mov	*-12.(bp),*1.
mov	*-10.(bp),*2.
mov	*-8.(bp),*3.
mov	*-6.(bp),*4.
mov	*-20.(bp),*5.
mov	*-18.(bp),*6.
mov	*-16.(bp),*7.
mov	*-14.(bp),*8.
mov	*-30.(bp),*0.
L4:cmp	*-30.(bp),*2.
bge	L5
mov	*-32.(bp),*0.
L7:cmp	*-32.(bp),*2.
bge	L8
mov	*-36.(bp),*0.
mov	*-34.(bp),*0.
L10:cmp	*-34.(bp),*2.
bge	L11
lea	di,*-20.(bp)
mov	si,*-34.(bp)
sal	si,*1
add	si,*-32.(bp)
sal	si,*1
add	di,si
mov	di,(di)
push	di
lea	di,*-12.(bp)
mov	si,*-30.(bp)
sal	si,*1
add	si,*-34.(bp)
sal	si,*1
add	di,si
mov	di,(di)
mov	ax,di
pop	cx
imul	cx
add	ax,*-36.(bp)
mov	*-36.(bp),ax
L12:mov	di,*-34.(bp)
inc	di
mov	*-34.(bp),di
jmp	L10
L11:lea	di,*-28.(bp)
mov	si,*-30.(bp)
sal	si,*1
add	si,*-32.(bp)
sal	si,*1
add	di,si
push	di
mov	di,*-36.(bp)
pop	bx
mov	(bx),di
L9:mov	di,*-32.(bp)
inc	di
mov	*-32.(bp),di
jmp	L7
L8:L6:mov	di,*-30.(bp)
inc	di
mov	*-30.(bp),di
jmp	L4
L5:mov	di,*-28.(bp)
add	di,*-26.(bp)
add	di,*-24.(bp)
add	di,*-22.(bp)
mov	ax,di
jmp	L3
L3:|RTYP 0
jmp	cret
L1:sub	sp,*32.
jmp	L2
.data
