.globl	_main
.text
.even
_main:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L1
L2:| _a=-8.
| _b=-12.
| _c=-16.
mov	si,#-7616.
mov	di,*1.
mov	*-6.(bp),si
mov	*-8.(bp),di
mov	ax,*37.
cwd
mov	di,dx
mov	si,ax
mov	*-10.(bp),si
mov	*-12.(bp),di
mov	di,*-10.(bp)
push	di
mov	di,*-12.(bp)
push	di
mov	di,*-6.(bp)
push	di
mov	di,*-8.(bp)
push	di
call	lmul
add	sp,*8.
mov	di,dx
mov	si,ax
mov	*-14.(bp),si
mov	*-16.(bp),di
mov	di,*-10.(bp)
push	di
mov	di,*-12.(bp)
push	di
mov	di,*-6.(bp)
push	di
mov	di,*-8.(bp)
push	di
call	ldiv
add	sp,*8.
mov	di,dx
mov	si,ax
mov	*-14.(bp),si
mov	*-16.(bp),di
mov	di,*-10.(bp)
push	di
mov	di,*-12.(bp)
push	di
mov	di,*-6.(bp)
push	di
mov	di,*-8.(bp)
push	di
call	lrem
add	sp,*8.
mov	di,dx
mov	si,ax
mov	*-14.(bp),si
mov	*-16.(bp),di
mov	di,*-14.(bp)
mov	ax,di
jmp	L3
L3:|RTYP 0
jmp	cret
L1:sub	sp,*12.
jmp	L2
.data
