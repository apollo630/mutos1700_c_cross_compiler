.globl	_main
.text
.even
_main:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L1
L2:| _m=-28.
| _i=-30.
| _j=-32.
| _sum=-34.
mov	*-30.(bp),*0.
L4:cmp	*-30.(bp),*3.
bge	L5
mov	*-32.(bp),*0.
L7:cmp	*-32.(bp),*4.
bge	L8
lea	di,*-28.(bp)
mov	si,*-30.(bp)
sal	si,*1
sal	si,*1
add	si,*-32.(bp)
sal	si,*1
add	di,si
push	di
mov	ax,*-30.(bp)
mov	cx,*10.
imul	cx
add	ax,*-32.(bp)
pop	bx
mov	(bx),ax
L9:mov	di,*-32.(bp)
inc	di
mov	*-32.(bp),di
jmp	L7
L8:L6:mov	di,*-30.(bp)
inc	di
mov	*-30.(bp),di
jmp	L4
L5:mov	*-34.(bp),*0.
mov	*-30.(bp),*0.
L10:cmp	*-30.(bp),*3.
bge	L11
mov	*-32.(bp),*0.
L13:cmp	*-32.(bp),*4.
bge	L14
lea	di,*-28.(bp)
mov	si,*-30.(bp)
sal	si,*1
sal	si,*1
add	si,*-32.(bp)
sal	si,*1
add	di,si
mov	di,(di)
add	di,*-34.(bp)
mov	*-34.(bp),di
L15:mov	di,*-32.(bp)
inc	di
mov	*-32.(bp),di
jmp	L13
L14:L12:mov	di,*-30.(bp)
inc	di
mov	*-30.(bp),di
jmp	L10
L11:mov	di,*-34.(bp)
mov	ax,di
jmp	L3
L3:|RTYP 0
jmp	cret
L1:sub	sp,*30.
jmp	L2
.data
