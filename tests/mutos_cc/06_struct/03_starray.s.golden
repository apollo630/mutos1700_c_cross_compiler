.globl	_main
.text
.even
_main:
push	bp
mov	bp,sp
push	di
push	si
|NREG 3
jmp	L1
L2:| _pts=-16.
| _i=-18.
| _sum=-20.
mov	*-18.(bp),*0.
L4:cmp	*-18.(bp),*3.
bge	L5
lea	di,*-16.(bp)
mov	si,*-18.(bp)
sal	si,*1
sal	si,*1
add	di,si
push	di
mov	di,*-18.(bp)
pop	bx
mov	(bx),di
mov	di,*-18.(bp)
sal	di,*1
lea	si,*-16.(bp)
mov	dx,*-18.(bp)
sal	dx,*1
sal	dx,*1
add	si,dx
mov	*2.(si),di
L6:mov	di,*-18.(bp)
inc	di
mov	*-18.(bp),di
jmp	L4
L5:mov	*-20.(bp),*0.
mov	*-18.(bp),*0.
L7:cmp	*-18.(bp),*3.
bge	L8
lea	di,*-16.(bp)
mov	si,*-18.(bp)
sal	si,*1
sal	si,*1
add	di,si
mov	di,(di)
lea	si,*-16.(bp)
mov	dx,*-18.(bp)
sal	dx,*1
sal	dx,*1
add	si,dx
add	di,*2.(si)
add	di,*-20.(bp)
mov	*-20.(bp),di
L9:mov	di,*-18.(bp)
inc	di
mov	*-18.(bp),di
jmp	L7
L8:mov	di,*-20.(bp)
mov	ax,di
jmp	L3
L3:|RTYP 0
jmp	cret
L1:sub	sp,*16.
jmp	L2
.data
