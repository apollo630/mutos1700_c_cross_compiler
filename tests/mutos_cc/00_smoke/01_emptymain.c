/*
 * 00_smoke/01_emptymain.c
 *
 * The absolute minimum C program: an empty main().
 * Expected code generation: the fixed, unconditional MUTOS 1700
 * function prologue/epilogue (push bp / mov bp,sp / push di / push si
 * ... jmp cret) with no locals and no body code at all -- see
 * docs/MUTOS_C_ABI.md sect. 1.2.
 */
main()
{
}
